[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Efficient-Visualization-of-Large-Directed-Acyclic-Graphs"
version = "0.1.0"

[tool.setuptools]
packages = ["app", "research_scripts"]
