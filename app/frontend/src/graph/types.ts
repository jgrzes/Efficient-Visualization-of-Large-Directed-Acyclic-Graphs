export type GraphColors = {
  default: string;
  parent: string;
  child: string;
  selected: string;
  hover: string;
  search: string;
  background: string;
};

export type RGBA = [number, number, number, number];