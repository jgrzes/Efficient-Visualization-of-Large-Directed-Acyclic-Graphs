import "./chunk-BUSYA2B4.js";

// node_modules/@cosmograph/cosmos/dist/index.js
var Yo = "http://www.w3.org/1999/xhtml";
var Rf = {
  svg: "http://www.w3.org/2000/svg",
  xhtml: Yo,
  xlink: "http://www.w3.org/1999/xlink",
  xml: "http://www.w3.org/XML/1998/namespace",
  xmlns: "http://www.w3.org/2000/xmlns/"
};
function Vi(t3) {
  var e = t3 += "", n = e.indexOf(":");
  return n >= 0 && (e = t3.slice(0, n)) !== "xmlns" && (t3 = t3.slice(n + 1)), Rf.hasOwnProperty(e) ? { space: Rf[e], local: t3 } : t3;
}
function Sd(t3) {
  return function() {
    var e = this.ownerDocument, n = this.namespaceURI;
    return n === Yo && e.documentElement.namespaceURI === Yo ? e.createElement(t3) : e.createElementNS(n, t3);
  };
}
function wd(t3) {
  return function() {
    return this.ownerDocument.createElementNS(t3.space, t3.local);
  };
}
function hu(t3) {
  var e = Vi(t3);
  return (e.local ? wd : Sd)(e);
}
function Td() {
}
function ha(t3) {
  return t3 == null ? Td : function() {
    return this.querySelector(t3);
  };
}
function Ad(t3) {
  typeof t3 != "function" && (t3 = ha(t3));
  for (var e = this._groups, n = e.length, r = new Array(n), i = 0; i < n; ++i)
    for (var o = e[i], s = o.length, f = r[i] = new Array(s), u, l, p = 0; p < s; ++p)
      (u = o[p]) && (l = t3.call(u, u.__data__, p, o)) && ("__data__" in u && (l.__data__ = u.__data__), f[p] = l);
  return new Nt(r, this._parents);
}
function Ed(t3) {
  return t3 == null ? [] : Array.isArray(t3) ? t3 : Array.from(t3);
}
function Pd() {
  return [];
}
function du(t3) {
  return t3 == null ? Pd : function() {
    return this.querySelectorAll(t3);
  };
}
function Cd(t3) {
  return function() {
    return Ed(t3.apply(this, arguments));
  };
}
function kd(t3) {
  typeof t3 == "function" ? t3 = Cd(t3) : t3 = du(t3);
  for (var e = this._groups, n = e.length, r = [], i = [], o = 0; o < n; ++o)
    for (var s = e[o], f = s.length, u, l = 0; l < f; ++l)
      (u = s[l]) && (r.push(t3.call(u, u.__data__, l, s)), i.push(u));
  return new Nt(r, i);
}
function mu(t3) {
  return function() {
    return this.matches(t3);
  };
}
function pu(t3) {
  return function(e) {
    return e.matches(t3);
  };
}
var Fd = Array.prototype.find;
function zd(t3) {
  return function() {
    return Fd.call(this.children, t3);
  };
}
function Ld() {
  return this.firstElementChild;
}
function Id(t3) {
  return this.select(t3 == null ? Ld : zd(typeof t3 == "function" ? t3 : pu(t3)));
}
var Rd = Array.prototype.filter;
function Dd() {
  return Array.from(this.children);
}
function Md(t3) {
  return function() {
    return Rd.call(this.children, t3);
  };
}
function Od(t3) {
  return this.selectAll(t3 == null ? Dd : Md(typeof t3 == "function" ? t3 : pu(t3)));
}
function Nd(t3) {
  typeof t3 != "function" && (t3 = mu(t3));
  for (var e = this._groups, n = e.length, r = new Array(n), i = 0; i < n; ++i)
    for (var o = e[i], s = o.length, f = r[i] = [], u, l = 0; l < s; ++l)
      (u = o[l]) && t3.call(u, u.__data__, l, o) && f.push(u);
  return new Nt(r, this._parents);
}
function vu(t3) {
  return new Array(t3.length);
}
function Gd() {
  return new Nt(this._enter || this._groups.map(vu), this._parents);
}
function Li(t3, e) {
  this.ownerDocument = t3.ownerDocument, this.namespaceURI = t3.namespaceURI, this._next = null, this._parent = t3, this.__data__ = e;
}
Li.prototype = {
  constructor: Li,
  appendChild: function(t3) {
    return this._parent.insertBefore(t3, this._next);
  },
  insertBefore: function(t3, e) {
    return this._parent.insertBefore(t3, e);
  },
  querySelector: function(t3) {
    return this._parent.querySelector(t3);
  },
  querySelectorAll: function(t3) {
    return this._parent.querySelectorAll(t3);
  }
};
function Bd(t3) {
  return function() {
    return t3;
  };
}
function Vd(t3, e, n, r, i, o) {
  for (var s = 0, f, u = e.length, l = o.length; s < l; ++s)
    (f = e[s]) ? (f.__data__ = o[s], r[s] = f) : n[s] = new Li(t3, o[s]);
  for (; s < u; ++s)
    (f = e[s]) && (i[s] = f);
}
function $d(t3, e, n, r, i, o, s) {
  var f, u, l = /* @__PURE__ */ new Map(), p = e.length, y = o.length, S = new Array(p), D;
  for (f = 0; f < p; ++f)
    (u = e[f]) && (S[f] = D = s.call(u, u.__data__, f, e) + "", l.has(D) ? i[f] = u : l.set(D, u));
  for (f = 0; f < y; ++f)
    D = s.call(t3, o[f], f, o) + "", (u = l.get(D)) ? (r[f] = u, u.__data__ = o[f], l.delete(D)) : n[f] = new Li(t3, o[f]);
  for (f = 0; f < p; ++f)
    (u = e[f]) && l.get(S[f]) === u && (i[f] = u);
}
function jd(t3) {
  return t3.__data__;
}
function Ud(t3, e) {
  if (!arguments.length) return Array.from(this, jd);
  var n = e ? $d : Vd, r = this._parents, i = this._groups;
  typeof t3 != "function" && (t3 = Bd(t3));
  for (var o = i.length, s = new Array(o), f = new Array(o), u = new Array(o), l = 0; l < o; ++l) {
    var p = r[l], y = i[l], S = y.length, D = Xd(t3.call(p, p && p.__data__, l, r)), Z = D.length, we = f[l] = new Array(Z), Se = s[l] = new Array(Z), ae = u[l] = new Array(S);
    n(p, y, we, Se, ae, D, e);
    for (var Ne = 0, Ue = 0, q, he; Ne < Z; ++Ne)
      if (q = we[Ne]) {
        for (Ne >= Ue && (Ue = Ne + 1); !(he = Se[Ue]) && ++Ue < Z; ) ;
        q._next = he || null;
      }
  }
  return s = new Nt(s, r), s._enter = f, s._exit = u, s;
}
function Xd(t3) {
  return typeof t3 == "object" && "length" in t3 ? t3 : Array.from(t3);
}
function Hd() {
  return new Nt(this._exit || this._groups.map(vu), this._parents);
}
function Wd(t3, e, n) {
  var r = this.enter(), i = this, o = this.exit();
  return typeof t3 == "function" ? (r = t3(r), r && (r = r.selection())) : r = r.append(t3 + ""), e != null && (i = e(i), i && (i = i.selection())), n == null ? o.remove() : n(o), r && i ? r.merge(i).order() : i;
}
function qd(t3) {
  for (var e = t3.selection ? t3.selection() : t3, n = this._groups, r = e._groups, i = n.length, o = r.length, s = Math.min(i, o), f = new Array(i), u = 0; u < s; ++u)
    for (var l = n[u], p = r[u], y = l.length, S = f[u] = new Array(y), D, Z = 0; Z < y; ++Z)
      (D = l[Z] || p[Z]) && (S[Z] = D);
  for (; u < i; ++u)
    f[u] = n[u];
  return new Nt(f, this._parents);
}
function Yd() {
  for (var t3 = this._groups, e = -1, n = t3.length; ++e < n; )
    for (var r = t3[e], i = r.length - 1, o = r[i], s; --i >= 0; )
      (s = r[i]) && (o && s.compareDocumentPosition(o) ^ 4 && o.parentNode.insertBefore(s, o), o = s);
  return this;
}
function Zd(t3) {
  t3 || (t3 = Qd);
  function e(y, S) {
    return y && S ? t3(y.__data__, S.__data__) : !y - !S;
  }
  for (var n = this._groups, r = n.length, i = new Array(r), o = 0; o < r; ++o) {
    for (var s = n[o], f = s.length, u = i[o] = new Array(f), l, p = 0; p < f; ++p)
      (l = s[p]) && (u[p] = l);
    u.sort(e);
  }
  return new Nt(i, this._parents).order();
}
function Qd(t3, e) {
  return t3 < e ? -1 : t3 > e ? 1 : t3 >= e ? 0 : NaN;
}
function Kd() {
  var t3 = arguments[0];
  return arguments[0] = this, t3.apply(null, arguments), this;
}
function Jd() {
  return Array.from(this);
}
function e0() {
  for (var t3 = this._groups, e = 0, n = t3.length; e < n; ++e)
    for (var r = t3[e], i = 0, o = r.length; i < o; ++i) {
      var s = r[i];
      if (s) return s;
    }
  return null;
}
function t0() {
  let t3 = 0;
  for (const e of this) ++t3;
  return t3;
}
function n0() {
  return !this.node();
}
function r0(t3) {
  for (var e = this._groups, n = 0, r = e.length; n < r; ++n)
    for (var i = e[n], o = 0, s = i.length, f; o < s; ++o)
      (f = i[o]) && t3.call(f, f.__data__, o, i);
  return this;
}
function i0(t3) {
  return function() {
    this.removeAttribute(t3);
  };
}
function o0(t3) {
  return function() {
    this.removeAttributeNS(t3.space, t3.local);
  };
}
function a0(t3, e) {
  return function() {
    this.setAttribute(t3, e);
  };
}
function s0(t3, e) {
  return function() {
    this.setAttributeNS(t3.space, t3.local, e);
  };
}
function f0(t3, e) {
  return function() {
    var n = e.apply(this, arguments);
    n == null ? this.removeAttribute(t3) : this.setAttribute(t3, n);
  };
}
function u0(t3, e) {
  return function() {
    var n = e.apply(this, arguments);
    n == null ? this.removeAttributeNS(t3.space, t3.local) : this.setAttributeNS(t3.space, t3.local, n);
  };
}
function c0(t3, e) {
  var n = Vi(t3);
  if (arguments.length < 2) {
    var r = this.node();
    return n.local ? r.getAttributeNS(n.space, n.local) : r.getAttribute(n);
  }
  return this.each((e == null ? n.local ? o0 : i0 : typeof e == "function" ? n.local ? u0 : f0 : n.local ? s0 : a0)(n, e));
}
function gu(t3) {
  return t3.ownerDocument && t3.ownerDocument.defaultView || t3.document && t3 || t3.defaultView;
}
function l0(t3) {
  return function() {
    this.style.removeProperty(t3);
  };
}
function h0(t3, e, n) {
  return function() {
    this.style.setProperty(t3, e, n);
  };
}
function d0(t3, e, n) {
  return function() {
    var r = e.apply(this, arguments);
    r == null ? this.style.removeProperty(t3) : this.style.setProperty(t3, r, n);
  };
}
function m0(t3, e, n) {
  return arguments.length > 1 ? this.each((e == null ? l0 : typeof e == "function" ? d0 : h0)(t3, e, n ?? "")) : sr(this.node(), t3);
}
function sr(t3, e) {
  return t3.style.getPropertyValue(e) || gu(t3).getComputedStyle(t3, null).getPropertyValue(e);
}
function p0(t3) {
  return function() {
    delete this[t3];
  };
}
function v0(t3, e) {
  return function() {
    this[t3] = e;
  };
}
function g0(t3, e) {
  return function() {
    var n = e.apply(this, arguments);
    n == null ? delete this[t3] : this[t3] = n;
  };
}
function x0(t3, e) {
  return arguments.length > 1 ? this.each((e == null ? p0 : typeof e == "function" ? g0 : v0)(t3, e)) : this.node()[t3];
}
function xu(t3) {
  return t3.trim().split(/^|\s+/);
}
function da(t3) {
  return t3.classList || new yu(t3);
}
function yu(t3) {
  this._node = t3, this._names = xu(t3.getAttribute("class") || "");
}
yu.prototype = {
  add: function(t3) {
    var e = this._names.indexOf(t3);
    e < 0 && (this._names.push(t3), this._node.setAttribute("class", this._names.join(" ")));
  },
  remove: function(t3) {
    var e = this._names.indexOf(t3);
    e >= 0 && (this._names.splice(e, 1), this._node.setAttribute("class", this._names.join(" ")));
  },
  contains: function(t3) {
    return this._names.indexOf(t3) >= 0;
  }
};
function bu(t3, e) {
  for (var n = da(t3), r = -1, i = e.length; ++r < i; ) n.add(e[r]);
}
function _u(t3, e) {
  for (var n = da(t3), r = -1, i = e.length; ++r < i; ) n.remove(e[r]);
}
function y0(t3) {
  return function() {
    bu(this, t3);
  };
}
function b0(t3) {
  return function() {
    _u(this, t3);
  };
}
function _0(t3, e) {
  return function() {
    (e.apply(this, arguments) ? bu : _u)(this, t3);
  };
}
function S0(t3, e) {
  var n = xu(t3 + "");
  if (arguments.length < 2) {
    for (var r = da(this.node()), i = -1, o = n.length; ++i < o; ) if (!r.contains(n[i])) return false;
    return true;
  }
  return this.each((typeof e == "function" ? _0 : e ? y0 : b0)(n, e));
}
function w0() {
  this.textContent = "";
}
function T0(t3) {
  return function() {
    this.textContent = t3;
  };
}
function A0(t3) {
  return function() {
    var e = t3.apply(this, arguments);
    this.textContent = e ?? "";
  };
}
function E0(t3) {
  return arguments.length ? this.each(t3 == null ? w0 : (typeof t3 == "function" ? A0 : T0)(t3)) : this.node().textContent;
}
function P0() {
  this.innerHTML = "";
}
function C0(t3) {
  return function() {
    this.innerHTML = t3;
  };
}
function k0(t3) {
  return function() {
    var e = t3.apply(this, arguments);
    this.innerHTML = e ?? "";
  };
}
function F0(t3) {
  return arguments.length ? this.each(t3 == null ? P0 : (typeof t3 == "function" ? k0 : C0)(t3)) : this.node().innerHTML;
}
function z0() {
  this.nextSibling && this.parentNode.appendChild(this);
}
function L0() {
  return this.each(z0);
}
function I0() {
  this.previousSibling && this.parentNode.insertBefore(this, this.parentNode.firstChild);
}
function R0() {
  return this.each(I0);
}
function D0(t3) {
  var e = typeof t3 == "function" ? t3 : hu(t3);
  return this.select(function() {
    return this.appendChild(e.apply(this, arguments));
  });
}
function M0() {
  return null;
}
function O0(t3, e) {
  var n = typeof t3 == "function" ? t3 : hu(t3), r = e == null ? M0 : typeof e == "function" ? e : ha(e);
  return this.select(function() {
    return this.insertBefore(n.apply(this, arguments), r.apply(this, arguments) || null);
  });
}
function N0() {
  var t3 = this.parentNode;
  t3 && t3.removeChild(this);
}
function G0() {
  return this.each(N0);
}
function B0() {
  var t3 = this.cloneNode(false), e = this.parentNode;
  return e ? e.insertBefore(t3, this.nextSibling) : t3;
}
function V0() {
  var t3 = this.cloneNode(true), e = this.parentNode;
  return e ? e.insertBefore(t3, this.nextSibling) : t3;
}
function $0(t3) {
  return this.select(t3 ? V0 : B0);
}
function j0(t3) {
  return arguments.length ? this.property("__data__", t3) : this.node().__data__;
}
function U0(t3) {
  return function(e) {
    t3.call(this, e, this.__data__);
  };
}
function X0(t3) {
  return t3.trim().split(/^|\s+/).map(function(e) {
    var n = "", r = e.indexOf(".");
    return r >= 0 && (n = e.slice(r + 1), e = e.slice(0, r)), { type: e, name: n };
  });
}
function H0(t3) {
  return function() {
    var e = this.__on;
    if (e) {
      for (var n = 0, r = -1, i = e.length, o; n < i; ++n)
        o = e[n], (!t3.type || o.type === t3.type) && o.name === t3.name ? this.removeEventListener(o.type, o.listener, o.options) : e[++r] = o;
      ++r ? e.length = r : delete this.__on;
    }
  };
}
function W0(t3, e, n) {
  return function() {
    var r = this.__on, i, o = U0(e);
    if (r) {
      for (var s = 0, f = r.length; s < f; ++s)
        if ((i = r[s]).type === t3.type && i.name === t3.name) {
          this.removeEventListener(i.type, i.listener, i.options), this.addEventListener(i.type, i.listener = o, i.options = n), i.value = e;
          return;
        }
    }
    this.addEventListener(t3.type, o, n), i = { type: t3.type, name: t3.name, value: e, listener: o, options: n }, r ? r.push(i) : this.__on = [i];
  };
}
function q0(t3, e, n) {
  var r = X0(t3 + ""), i, o = r.length, s;
  if (arguments.length < 2) {
    var f = this.node().__on;
    if (f) {
      for (var u = 0, l = f.length, p; u < l; ++u)
        for (i = 0, p = f[u]; i < o; ++i)
          if ((s = r[i]).type === p.type && s.name === p.name)
            return p.value;
    }
    return;
  }
  for (f = e ? W0 : H0, i = 0; i < o; ++i) this.each(f(r[i], e, n));
  return this;
}
function Su(t3, e, n) {
  var r = gu(t3), i = r.CustomEvent;
  typeof i == "function" ? i = new i(e, n) : (i = r.document.createEvent("Event"), n ? (i.initEvent(e, n.bubbles, n.cancelable), i.detail = n.detail) : i.initEvent(e, false, false)), t3.dispatchEvent(i);
}
function Y0(t3, e) {
  return function() {
    return Su(this, t3, e);
  };
}
function Z0(t3, e) {
  return function() {
    return Su(this, t3, e.apply(this, arguments));
  };
}
function Q0(t3, e) {
  return this.each((typeof e == "function" ? Z0 : Y0)(t3, e));
}
function* K0() {
  for (var t3 = this._groups, e = 0, n = t3.length; e < n; ++e)
    for (var r = t3[e], i = 0, o = r.length, s; i < o; ++i)
      (s = r[i]) && (yield s);
}
var wu = [null];
function Nt(t3, e) {
  this._groups = t3, this._parents = e;
}
function Gr() {
  return new Nt([[document.documentElement]], wu);
}
function J0() {
  return this;
}
Nt.prototype = Gr.prototype = {
  constructor: Nt,
  select: Ad,
  selectAll: kd,
  selectChild: Id,
  selectChildren: Od,
  filter: Nd,
  data: Ud,
  enter: Gd,
  exit: Hd,
  join: Wd,
  merge: qd,
  selection: J0,
  order: Yd,
  sort: Zd,
  call: Kd,
  nodes: Jd,
  node: e0,
  size: t0,
  empty: n0,
  each: r0,
  attr: c0,
  style: m0,
  property: x0,
  classed: S0,
  text: E0,
  html: F0,
  raise: L0,
  lower: R0,
  append: D0,
  insert: O0,
  remove: G0,
  clone: $0,
  datum: j0,
  on: q0,
  dispatch: Q0,
  [Symbol.iterator]: K0
};
function Pt(t3) {
  return typeof t3 == "string" ? new Nt([[document.querySelector(t3)]], [document.documentElement]) : new Nt([[t3]], wu);
}
function em(t3) {
  let e;
  for (; e = t3.sourceEvent; ) t3 = e;
  return t3;
}
function un(t3, e) {
  if (t3 = em(t3), e === void 0 && (e = t3.currentTarget), e) {
    var n = e.ownerSVGElement || e;
    if (n.createSVGPoint) {
      var r = n.createSVGPoint();
      return r.x = t3.clientX, r.y = t3.clientY, r = r.matrixTransform(e.getScreenCTM().inverse()), [r.x, r.y];
    }
    if (e.getBoundingClientRect) {
      var i = e.getBoundingClientRect();
      return [t3.clientX - i.left - e.clientLeft, t3.clientY - i.top - e.clientTop];
    }
  }
  return [t3.pageX, t3.pageY];
}
var tm = { value: () => {
} };
function $i() {
  for (var t3 = 0, e = arguments.length, n = {}, r; t3 < e; ++t3) {
    if (!(r = arguments[t3] + "") || r in n || /[\s.]/.test(r)) throw new Error("illegal type: " + r);
    n[r] = [];
  }
  return new Pi(n);
}
function Pi(t3) {
  this._ = t3;
}
function nm(t3, e) {
  return t3.trim().split(/^|\s+/).map(function(n) {
    var r = "", i = n.indexOf(".");
    if (i >= 0 && (r = n.slice(i + 1), n = n.slice(0, i)), n && !e.hasOwnProperty(n)) throw new Error("unknown type: " + n);
    return { type: n, name: r };
  });
}
Pi.prototype = $i.prototype = {
  constructor: Pi,
  on: function(t3, e) {
    var n = this._, r = nm(t3 + "", n), i, o = -1, s = r.length;
    if (arguments.length < 2) {
      for (; ++o < s; ) if ((i = (t3 = r[o]).type) && (i = rm(n[i], t3.name))) return i;
      return;
    }
    if (e != null && typeof e != "function") throw new Error("invalid callback: " + e);
    for (; ++o < s; )
      if (i = (t3 = r[o]).type) n[i] = Df(n[i], t3.name, e);
      else if (e == null) for (i in n) n[i] = Df(n[i], t3.name, null);
    return this;
  },
  copy: function() {
    var t3 = {}, e = this._;
    for (var n in e) t3[n] = e[n].slice();
    return new Pi(t3);
  },
  call: function(t3, e) {
    if ((i = arguments.length - 2) > 0) for (var n = new Array(i), r = 0, i, o; r < i; ++r) n[r] = arguments[r + 2];
    if (!this._.hasOwnProperty(t3)) throw new Error("unknown type: " + t3);
    for (o = this._[t3], r = 0, i = o.length; r < i; ++r) o[r].value.apply(e, n);
  },
  apply: function(t3, e, n) {
    if (!this._.hasOwnProperty(t3)) throw new Error("unknown type: " + t3);
    for (var r = this._[t3], i = 0, o = r.length; i < o; ++i) r[i].value.apply(e, n);
  }
};
function rm(t3, e) {
  for (var n = 0, r = t3.length, i; n < r; ++n)
    if ((i = t3[n]).name === e)
      return i.value;
}
function Df(t3, e, n) {
  for (var r = 0, i = t3.length; r < i; ++r)
    if (t3[r].name === e) {
      t3[r] = tm, t3 = t3.slice(0, r).concat(t3.slice(r + 1));
      break;
    }
  return n != null && t3.push({ name: e, value: n }), t3;
}
var fr = 0;
var Lr = 0;
var Fr = 0;
var Tu = 1e3;
var Ii;
var Ir;
var Ri = 0;
var Vn = 0;
var ji = 0;
var Dr = typeof performance == "object" && performance.now ? performance : Date;
var Au = typeof window == "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(t3) {
  setTimeout(t3, 17);
};
function ma() {
  return Vn || (Au(im), Vn = Dr.now() + ji);
}
function im() {
  Vn = 0;
}
function Di() {
  this._call = this._time = this._next = null;
}
Di.prototype = Eu.prototype = {
  constructor: Di,
  restart: function(t3, e, n) {
    if (typeof t3 != "function") throw new TypeError("callback is not a function");
    n = (n == null ? ma() : +n) + (e == null ? 0 : +e), !this._next && Ir !== this && (Ir ? Ir._next = this : Ii = this, Ir = this), this._call = t3, this._time = n, Zo();
  },
  stop: function() {
    this._call && (this._call = null, this._time = 1 / 0, Zo());
  }
};
function Eu(t3, e, n) {
  var r = new Di();
  return r.restart(t3, e, n), r;
}
function om() {
  ma(), ++fr;
  for (var t3 = Ii, e; t3; )
    (e = Vn - t3._time) >= 0 && t3._call.call(null, e), t3 = t3._next;
  --fr;
}
function Mf() {
  Vn = (Ri = Dr.now()) + ji, fr = Lr = 0;
  try {
    om();
  } finally {
    fr = 0, sm(), Vn = 0;
  }
}
function am() {
  var t3 = Dr.now(), e = t3 - Ri;
  e > Tu && (ji -= e, Ri = t3);
}
function sm() {
  for (var t3, e = Ii, n, r = 1 / 0; e; )
    e._call ? (r > e._time && (r = e._time), t3 = e, e = e._next) : (n = e._next, e._next = null, e = t3 ? t3._next = n : Ii = n);
  Ir = t3, Zo(r);
}
function Zo(t3) {
  if (!fr) {
    Lr && (Lr = clearTimeout(Lr));
    var e = t3 - Vn;
    e > 24 ? (t3 < 1 / 0 && (Lr = setTimeout(Mf, t3 - Dr.now() - ji)), Fr && (Fr = clearInterval(Fr))) : (Fr || (Ri = Dr.now(), Fr = setInterval(am, Tu)), fr = 1, Au(Mf));
  }
}
function Of(t3, e, n) {
  var r = new Di();
  return e = e == null ? 0 : +e, r.restart((i) => {
    r.stop(), t3(i + e);
  }, e, n), r;
}
var fm = $i("start", "end", "cancel", "interrupt");
var um = [];
var Pu = 0;
var Nf = 1;
var Qo = 2;
var Ci = 3;
var Gf = 4;
var Ko = 5;
var ki = 6;
function Ui(t3, e, n, r, i, o) {
  var s = t3.__transition;
  if (!s) t3.__transition = {};
  else if (n in s) return;
  cm(t3, n, {
    name: e,
    index: r,
    // For context during callback.
    group: i,
    // For context during callback.
    on: fm,
    tween: um,
    time: o.time,
    delay: o.delay,
    duration: o.duration,
    ease: o.ease,
    timer: null,
    state: Pu
  });
}
function pa(t3, e) {
  var n = Yt(t3, e);
  if (n.state > Pu) throw new Error("too late; already scheduled");
  return n;
}
function rn(t3, e) {
  var n = Yt(t3, e);
  if (n.state > Ci) throw new Error("too late; already running");
  return n;
}
function Yt(t3, e) {
  var n = t3.__transition;
  if (!n || !(n = n[e])) throw new Error("transition not found");
  return n;
}
function cm(t3, e, n) {
  var r = t3.__transition, i;
  r[e] = n, n.timer = Eu(o, 0, n.time);
  function o(l) {
    n.state = Nf, n.timer.restart(s, n.delay, n.time), n.delay <= l && s(l - n.delay);
  }
  function s(l) {
    var p, y, S, D;
    if (n.state !== Nf) return u();
    for (p in r)
      if (D = r[p], D.name === n.name) {
        if (D.state === Ci) return Of(s);
        D.state === Gf ? (D.state = ki, D.timer.stop(), D.on.call("interrupt", t3, t3.__data__, D.index, D.group), delete r[p]) : +p < e && (D.state = ki, D.timer.stop(), D.on.call("cancel", t3, t3.__data__, D.index, D.group), delete r[p]);
      }
    if (Of(function() {
      n.state === Ci && (n.state = Gf, n.timer.restart(f, n.delay, n.time), f(l));
    }), n.state = Qo, n.on.call("start", t3, t3.__data__, n.index, n.group), n.state === Qo) {
      for (n.state = Ci, i = new Array(S = n.tween.length), p = 0, y = -1; p < S; ++p)
        (D = n.tween[p].value.call(t3, t3.__data__, n.index, n.group)) && (i[++y] = D);
      i.length = y + 1;
    }
  }
  function f(l) {
    for (var p = l < n.duration ? n.ease.call(null, l / n.duration) : (n.timer.restart(u), n.state = Ko, 1), y = -1, S = i.length; ++y < S; )
      i[y].call(t3, p);
    n.state === Ko && (n.on.call("end", t3, t3.__data__, n.index, n.group), u());
  }
  function u() {
    n.state = ki, n.timer.stop(), delete r[e];
    for (var l in r) return;
    delete t3.__transition;
  }
}
function Fi(t3, e) {
  var n = t3.__transition, r, i, o = true, s;
  if (n) {
    e = e == null ? null : e + "";
    for (s in n) {
      if ((r = n[s]).name !== e) {
        o = false;
        continue;
      }
      i = r.state > Qo && r.state < Ko, r.state = ki, r.timer.stop(), r.on.call(i ? "interrupt" : "cancel", t3, t3.__data__, r.index, r.group), delete n[s];
    }
    o && delete t3.__transition;
  }
}
function lm(t3) {
  return this.each(function() {
    Fi(this, t3);
  });
}
function va(t3, e, n) {
  t3.prototype = e.prototype = n, n.constructor = t3;
}
function Cu(t3, e) {
  var n = Object.create(t3.prototype);
  for (var r in e) n[r] = e[r];
  return n;
}
function Br() {
}
var Mr = 0.7;
var Mi = 1 / Mr;
var or = "\\s*([+-]?\\d+)\\s*";
var Or = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var nn = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var hm = /^#([0-9a-f]{3,8})$/;
var dm = new RegExp(`^rgb\\(${or},${or},${or}\\)$`);
var mm = new RegExp(`^rgb\\(${nn},${nn},${nn}\\)$`);
var pm = new RegExp(`^rgba\\(${or},${or},${or},${Or}\\)$`);
var vm = new RegExp(`^rgba\\(${nn},${nn},${nn},${Or}\\)$`);
var gm = new RegExp(`^hsl\\(${Or},${nn},${nn}\\)$`);
var xm = new RegExp(`^hsla\\(${Or},${nn},${nn},${Or}\\)$`);
var Bf = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
va(Br, Sn, {
  copy(t3) {
    return Object.assign(new this.constructor(), this, t3);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: Vf,
  // Deprecated! Use color.formatHex.
  formatHex: Vf,
  formatHex8: ym,
  formatHsl: bm,
  formatRgb: $f,
  toString: $f
});
function Vf() {
  return this.rgb().formatHex();
}
function ym() {
  return this.rgb().formatHex8();
}
function bm() {
  return ku(this).formatHsl();
}
function $f() {
  return this.rgb().formatRgb();
}
function Sn(t3) {
  var e, n;
  return t3 = (t3 + "").trim().toLowerCase(), (e = hm.exec(t3)) ? (n = e[1].length, e = parseInt(e[1], 16), n === 6 ? jf(e) : n === 3 ? new Ft(e >> 8 & 15 | e >> 4 & 240, e >> 4 & 15 | e & 240, (e & 15) << 4 | e & 15, 1) : n === 8 ? bi(e >> 24 & 255, e >> 16 & 255, e >> 8 & 255, (e & 255) / 255) : n === 4 ? bi(e >> 12 & 15 | e >> 8 & 240, e >> 8 & 15 | e >> 4 & 240, e >> 4 & 15 | e & 240, ((e & 15) << 4 | e & 15) / 255) : null) : (e = dm.exec(t3)) ? new Ft(e[1], e[2], e[3], 1) : (e = mm.exec(t3)) ? new Ft(e[1] * 255 / 100, e[2] * 255 / 100, e[3] * 255 / 100, 1) : (e = pm.exec(t3)) ? bi(e[1], e[2], e[3], e[4]) : (e = vm.exec(t3)) ? bi(e[1] * 255 / 100, e[2] * 255 / 100, e[3] * 255 / 100, e[4]) : (e = gm.exec(t3)) ? Hf(e[1], e[2] / 100, e[3] / 100, 1) : (e = xm.exec(t3)) ? Hf(e[1], e[2] / 100, e[3] / 100, e[4]) : Bf.hasOwnProperty(t3) ? jf(Bf[t3]) : t3 === "transparent" ? new Ft(NaN, NaN, NaN, 0) : null;
}
function jf(t3) {
  return new Ft(t3 >> 16 & 255, t3 >> 8 & 255, t3 & 255, 1);
}
function bi(t3, e, n, r) {
  return r <= 0 && (t3 = e = n = NaN), new Ft(t3, e, n, r);
}
function _m(t3) {
  return t3 instanceof Br || (t3 = Sn(t3)), t3 ? (t3 = t3.rgb(), new Ft(t3.r, t3.g, t3.b, t3.opacity)) : new Ft();
}
function Jo(t3, e, n, r) {
  return arguments.length === 1 ? _m(t3) : new Ft(t3, e, n, r ?? 1);
}
function Ft(t3, e, n, r) {
  this.r = +t3, this.g = +e, this.b = +n, this.opacity = +r;
}
va(Ft, Jo, Cu(Br, {
  brighter(t3) {
    return t3 = t3 == null ? Mi : Math.pow(Mi, t3), new Ft(this.r * t3, this.g * t3, this.b * t3, this.opacity);
  },
  darker(t3) {
    return t3 = t3 == null ? Mr : Math.pow(Mr, t3), new Ft(this.r * t3, this.g * t3, this.b * t3, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Ft(Gn(this.r), Gn(this.g), Gn(this.b), Oi(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && -0.5 <= this.g && this.g < 255.5 && -0.5 <= this.b && this.b < 255.5 && 0 <= this.opacity && this.opacity <= 1;
  },
  hex: Uf,
  // Deprecated! Use color.formatHex.
  formatHex: Uf,
  formatHex8: Sm,
  formatRgb: Xf,
  toString: Xf
}));
function Uf() {
  return `#${Nn(this.r)}${Nn(this.g)}${Nn(this.b)}`;
}
function Sm() {
  return `#${Nn(this.r)}${Nn(this.g)}${Nn(this.b)}${Nn((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function Xf() {
  const t3 = Oi(this.opacity);
  return `${t3 === 1 ? "rgb(" : "rgba("}${Gn(this.r)}, ${Gn(this.g)}, ${Gn(this.b)}${t3 === 1 ? ")" : `, ${t3})`}`;
}
function Oi(t3) {
  return isNaN(t3) ? 1 : Math.max(0, Math.min(1, t3));
}
function Gn(t3) {
  return Math.max(0, Math.min(255, Math.round(t3) || 0));
}
function Nn(t3) {
  return t3 = Gn(t3), (t3 < 16 ? "0" : "") + t3.toString(16);
}
function Hf(t3, e, n, r) {
  return r <= 0 ? t3 = e = n = NaN : n <= 0 || n >= 1 ? t3 = e = NaN : e <= 0 && (t3 = NaN), new Wt(t3, e, n, r);
}
function ku(t3) {
  if (t3 instanceof Wt) return new Wt(t3.h, t3.s, t3.l, t3.opacity);
  if (t3 instanceof Br || (t3 = Sn(t3)), !t3) return new Wt();
  if (t3 instanceof Wt) return t3;
  t3 = t3.rgb();
  var e = t3.r / 255, n = t3.g / 255, r = t3.b / 255, i = Math.min(e, n, r), o = Math.max(e, n, r), s = NaN, f = o - i, u = (o + i) / 2;
  return f ? (e === o ? s = (n - r) / f + (n < r) * 6 : n === o ? s = (r - e) / f + 2 : s = (e - n) / f + 4, f /= u < 0.5 ? o + i : 2 - o - i, s *= 60) : f = u > 0 && u < 1 ? 0 : s, new Wt(s, f, u, t3.opacity);
}
function wm(t3, e, n, r) {
  return arguments.length === 1 ? ku(t3) : new Wt(t3, e, n, r ?? 1);
}
function Wt(t3, e, n, r) {
  this.h = +t3, this.s = +e, this.l = +n, this.opacity = +r;
}
va(Wt, wm, Cu(Br, {
  brighter(t3) {
    return t3 = t3 == null ? Mi : Math.pow(Mi, t3), new Wt(this.h, this.s, this.l * t3, this.opacity);
  },
  darker(t3) {
    return t3 = t3 == null ? Mr : Math.pow(Mr, t3), new Wt(this.h, this.s, this.l * t3, this.opacity);
  },
  rgb() {
    var t3 = this.h % 360 + (this.h < 0) * 360, e = isNaN(t3) || isNaN(this.s) ? 0 : this.s, n = this.l, r = n + (n < 0.5 ? n : 1 - n) * e, i = 2 * n - r;
    return new Ft(
      Uo(t3 >= 240 ? t3 - 240 : t3 + 120, i, r),
      Uo(t3, i, r),
      Uo(t3 < 120 ? t3 + 240 : t3 - 120, i, r),
      this.opacity
    );
  },
  clamp() {
    return new Wt(Wf(this.h), _i(this.s), _i(this.l), Oi(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && 0 <= this.l && this.l <= 1 && 0 <= this.opacity && this.opacity <= 1;
  },
  formatHsl() {
    const t3 = Oi(this.opacity);
    return `${t3 === 1 ? "hsl(" : "hsla("}${Wf(this.h)}, ${_i(this.s) * 100}%, ${_i(this.l) * 100}%${t3 === 1 ? ")" : `, ${t3})`}`;
  }
}));
function Wf(t3) {
  return t3 = (t3 || 0) % 360, t3 < 0 ? t3 + 360 : t3;
}
function _i(t3) {
  return Math.max(0, Math.min(1, t3 || 0));
}
function Uo(t3, e, n) {
  return (t3 < 60 ? e + (n - e) * t3 / 60 : t3 < 180 ? n : t3 < 240 ? e + (n - e) * (240 - t3) / 60 : e) * 255;
}
var ga = (t3) => () => t3;
function Tm(t3, e) {
  return function(n) {
    return t3 + n * e;
  };
}
function Am(t3, e, n) {
  return t3 = Math.pow(t3, n), e = Math.pow(e, n) - t3, n = 1 / n, function(r) {
    return Math.pow(t3 + r * e, n);
  };
}
function Em(t3) {
  return (t3 = +t3) == 1 ? Fu : function(e, n) {
    return n - e ? Am(e, n, t3) : ga(isNaN(e) ? n : e);
  };
}
function Fu(t3, e) {
  var n = e - t3;
  return n ? Tm(t3, n) : ga(isNaN(t3) ? e : t3);
}
var Ni = function t(e) {
  var n = Em(e);
  function r(i, o) {
    var s = n((i = Jo(i)).r, (o = Jo(o)).r), f = n(i.g, o.g), u = n(i.b, o.b), l = Fu(i.opacity, o.opacity);
    return function(p) {
      return i.r = s(p), i.g = f(p), i.b = u(p), i.opacity = l(p), i + "";
    };
  }
  return r.gamma = t, r;
}(1);
function Pm(t3, e) {
  e || (e = []);
  var n = t3 ? Math.min(e.length, t3.length) : 0, r = e.slice(), i;
  return function(o) {
    for (i = 0; i < n; ++i) r[i] = t3[i] * (1 - o) + e[i] * o;
    return r;
  };
}
function Cm(t3) {
  return ArrayBuffer.isView(t3) && !(t3 instanceof DataView);
}
function km(t3, e) {
  var n = e ? e.length : 0, r = t3 ? Math.min(n, t3.length) : 0, i = new Array(r), o = new Array(n), s;
  for (s = 0; s < r; ++s) i[s] = xa(t3[s], e[s]);
  for (; s < n; ++s) o[s] = e[s];
  return function(f) {
    for (s = 0; s < r; ++s) o[s] = i[s](f);
    return o;
  };
}
function Fm(t3, e) {
  var n = /* @__PURE__ */ new Date();
  return t3 = +t3, e = +e, function(r) {
    return n.setTime(t3 * (1 - r) + e * r), n;
  };
}
function Ht(t3, e) {
  return t3 = +t3, e = +e, function(n) {
    return t3 * (1 - n) + e * n;
  };
}
function zm(t3, e) {
  var n = {}, r = {}, i;
  (t3 === null || typeof t3 != "object") && (t3 = {}), (e === null || typeof e != "object") && (e = {});
  for (i in e)
    i in t3 ? n[i] = xa(t3[i], e[i]) : r[i] = e[i];
  return function(o) {
    for (i in n) r[i] = n[i](o);
    return r;
  };
}
var ea = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var Xo = new RegExp(ea.source, "g");
function Lm(t3) {
  return function() {
    return t3;
  };
}
function Im(t3) {
  return function(e) {
    return t3(e) + "";
  };
}
function zu(t3, e) {
  var n = ea.lastIndex = Xo.lastIndex = 0, r, i, o, s = -1, f = [], u = [];
  for (t3 = t3 + "", e = e + ""; (r = ea.exec(t3)) && (i = Xo.exec(e)); )
    (o = i.index) > n && (o = e.slice(n, o), f[s] ? f[s] += o : f[++s] = o), (r = r[0]) === (i = i[0]) ? f[s] ? f[s] += i : f[++s] = i : (f[++s] = null, u.push({ i: s, x: Ht(r, i) })), n = Xo.lastIndex;
  return n < e.length && (o = e.slice(n), f[s] ? f[s] += o : f[++s] = o), f.length < 2 ? u[0] ? Im(u[0].x) : Lm(e) : (e = u.length, function(l) {
    for (var p = 0, y; p < e; ++p) f[(y = u[p]).i] = y.x(l);
    return f.join("");
  });
}
function xa(t3, e) {
  var n = typeof e, r;
  return e == null || n === "boolean" ? ga(e) : (n === "number" ? Ht : n === "string" ? (r = Sn(e)) ? (e = r, Ni) : zu : e instanceof Sn ? Ni : e instanceof Date ? Fm : Cm(e) ? Pm : Array.isArray(e) ? km : typeof e.valueOf != "function" && typeof e.toString != "function" || isNaN(e) ? zm : Ht)(t3, e);
}
function Rm(t3, e) {
  return t3 = +t3, e = +e, function(n) {
    return Math.round(t3 * (1 - n) + e * n);
  };
}
var qf = 180 / Math.PI;
var ta = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function Lu(t3, e, n, r, i, o) {
  var s, f, u;
  return (s = Math.sqrt(t3 * t3 + e * e)) && (t3 /= s, e /= s), (u = t3 * n + e * r) && (n -= t3 * u, r -= e * u), (f = Math.sqrt(n * n + r * r)) && (n /= f, r /= f, u /= f), t3 * r < e * n && (t3 = -t3, e = -e, u = -u, s = -s), {
    translateX: i,
    translateY: o,
    rotate: Math.atan2(e, t3) * qf,
    skewX: Math.atan(u) * qf,
    scaleX: s,
    scaleY: f
  };
}
var Si;
function Dm(t3) {
  const e = new (typeof DOMMatrix == "function" ? DOMMatrix : WebKitCSSMatrix)(t3 + "");
  return e.isIdentity ? ta : Lu(e.a, e.b, e.c, e.d, e.e, e.f);
}
function Mm(t3) {
  return t3 == null || (Si || (Si = document.createElementNS("http://www.w3.org/2000/svg", "g")), Si.setAttribute("transform", t3), !(t3 = Si.transform.baseVal.consolidate())) ? ta : (t3 = t3.matrix, Lu(t3.a, t3.b, t3.c, t3.d, t3.e, t3.f));
}
function Iu(t3, e, n, r) {
  function i(l) {
    return l.length ? l.pop() + " " : "";
  }
  function o(l, p, y, S, D, Z) {
    if (l !== y || p !== S) {
      var we = D.push("translate(", null, e, null, n);
      Z.push({ i: we - 4, x: Ht(l, y) }, { i: we - 2, x: Ht(p, S) });
    } else (y || S) && D.push("translate(" + y + e + S + n);
  }
  function s(l, p, y, S) {
    l !== p ? (l - p > 180 ? p += 360 : p - l > 180 && (l += 360), S.push({ i: y.push(i(y) + "rotate(", null, r) - 2, x: Ht(l, p) })) : p && y.push(i(y) + "rotate(" + p + r);
  }
  function f(l, p, y, S) {
    l !== p ? S.push({ i: y.push(i(y) + "skewX(", null, r) - 2, x: Ht(l, p) }) : p && y.push(i(y) + "skewX(" + p + r);
  }
  function u(l, p, y, S, D, Z) {
    if (l !== y || p !== S) {
      var we = D.push(i(D) + "scale(", null, ",", null, ")");
      Z.push({ i: we - 4, x: Ht(l, y) }, { i: we - 2, x: Ht(p, S) });
    } else (y !== 1 || S !== 1) && D.push(i(D) + "scale(" + y + "," + S + ")");
  }
  return function(l, p) {
    var y = [], S = [];
    return l = t3(l), p = t3(p), o(l.translateX, l.translateY, p.translateX, p.translateY, y, S), s(l.rotate, p.rotate, y, S), f(l.skewX, p.skewX, y, S), u(l.scaleX, l.scaleY, p.scaleX, p.scaleY, y, S), l = p = null, function(D) {
      for (var Z = -1, we = S.length, Se; ++Z < we; ) y[(Se = S[Z]).i] = Se.x(D);
      return y.join("");
    };
  };
}
var Om = Iu(Dm, "px, ", "px)", "deg)");
var Nm = Iu(Mm, ", ", ")", ")");
var Gm = 1e-12;
function Yf(t3) {
  return ((t3 = Math.exp(t3)) + 1 / t3) / 2;
}
function Bm(t3) {
  return ((t3 = Math.exp(t3)) - 1 / t3) / 2;
}
function Vm(t3) {
  return ((t3 = Math.exp(2 * t3)) - 1) / (t3 + 1);
}
var $m = function t2(e, n, r) {
  function i(o, s) {
    var f = o[0], u = o[1], l = o[2], p = s[0], y = s[1], S = s[2], D = p - f, Z = y - u, we = D * D + Z * Z, Se, ae;
    if (we < Gm)
      ae = Math.log(S / l) / e, Se = function(Ge) {
        return [
          f + Ge * D,
          u + Ge * Z,
          l * Math.exp(e * Ge * ae)
        ];
      };
    else {
      var Ne = Math.sqrt(we), Ue = (S * S - l * l + r * we) / (2 * l * n * Ne), q = (S * S - l * l - r * we) / (2 * S * n * Ne), he = Math.log(Math.sqrt(Ue * Ue + 1) - Ue), Re = Math.log(Math.sqrt(q * q + 1) - q);
      ae = (Re - he) / e, Se = function(Ge) {
        var qe = Ge * ae, et = Yf(he), rt = l / (n * Ne) * (et * Vm(e * qe + he) - Bm(he));
        return [
          f + rt * D,
          u + rt * Z,
          l * et / Yf(e * qe + he)
        ];
      };
    }
    return Se.duration = ae * 1e3 * e / Math.SQRT2, Se;
  }
  return i.rho = function(o) {
    var s = Math.max(1e-3, +o), f = s * s, u = f * f;
    return t2(s, f, u);
  }, i;
}(Math.SQRT2, 2, 4);
function jm(t3, e) {
  var n, r;
  return function() {
    var i = rn(this, t3), o = i.tween;
    if (o !== n) {
      r = n = o;
      for (var s = 0, f = r.length; s < f; ++s)
        if (r[s].name === e) {
          r = r.slice(), r.splice(s, 1);
          break;
        }
    }
    i.tween = r;
  };
}
function Um(t3, e, n) {
  var r, i;
  if (typeof n != "function") throw new Error();
  return function() {
    var o = rn(this, t3), s = o.tween;
    if (s !== r) {
      i = (r = s).slice();
      for (var f = { name: e, value: n }, u = 0, l = i.length; u < l; ++u)
        if (i[u].name === e) {
          i[u] = f;
          break;
        }
      u === l && i.push(f);
    }
    o.tween = i;
  };
}
function Xm(t3, e) {
  var n = this._id;
  if (t3 += "", arguments.length < 2) {
    for (var r = Yt(this.node(), n).tween, i = 0, o = r.length, s; i < o; ++i)
      if ((s = r[i]).name === t3)
        return s.value;
    return null;
  }
  return this.each((e == null ? jm : Um)(n, t3, e));
}
function ya(t3, e, n) {
  var r = t3._id;
  return t3.each(function() {
    var i = rn(this, r);
    (i.value || (i.value = {}))[e] = n.apply(this, arguments);
  }), function(i) {
    return Yt(i, r).value[e];
  };
}
function Ru(t3, e) {
  var n;
  return (typeof e == "number" ? Ht : e instanceof Sn ? Ni : (n = Sn(e)) ? (e = n, Ni) : zu)(t3, e);
}
function Hm(t3) {
  return function() {
    this.removeAttribute(t3);
  };
}
function Wm(t3) {
  return function() {
    this.removeAttributeNS(t3.space, t3.local);
  };
}
function qm(t3, e, n) {
  var r, i = n + "", o;
  return function() {
    var s = this.getAttribute(t3);
    return s === i ? null : s === r ? o : o = e(r = s, n);
  };
}
function Ym(t3, e, n) {
  var r, i = n + "", o;
  return function() {
    var s = this.getAttributeNS(t3.space, t3.local);
    return s === i ? null : s === r ? o : o = e(r = s, n);
  };
}
function Zm(t3, e, n) {
  var r, i, o;
  return function() {
    var s, f = n(this), u;
    return f == null ? void this.removeAttribute(t3) : (s = this.getAttribute(t3), u = f + "", s === u ? null : s === r && u === i ? o : (i = u, o = e(r = s, f)));
  };
}
function Qm(t3, e, n) {
  var r, i, o;
  return function() {
    var s, f = n(this), u;
    return f == null ? void this.removeAttributeNS(t3.space, t3.local) : (s = this.getAttributeNS(t3.space, t3.local), u = f + "", s === u ? null : s === r && u === i ? o : (i = u, o = e(r = s, f)));
  };
}
function Km(t3, e) {
  var n = Vi(t3), r = n === "transform" ? Nm : Ru;
  return this.attrTween(t3, typeof e == "function" ? (n.local ? Qm : Zm)(n, r, ya(this, "attr." + t3, e)) : e == null ? (n.local ? Wm : Hm)(n) : (n.local ? Ym : qm)(n, r, e));
}
function Jm(t3, e) {
  return function(n) {
    this.setAttribute(t3, e.call(this, n));
  };
}
function ep(t3, e) {
  return function(n) {
    this.setAttributeNS(t3.space, t3.local, e.call(this, n));
  };
}
function tp(t3, e) {
  var n, r;
  function i() {
    var o = e.apply(this, arguments);
    return o !== r && (n = (r = o) && ep(t3, o)), n;
  }
  return i._value = e, i;
}
function np(t3, e) {
  var n, r;
  function i() {
    var o = e.apply(this, arguments);
    return o !== r && (n = (r = o) && Jm(t3, o)), n;
  }
  return i._value = e, i;
}
function rp(t3, e) {
  var n = "attr." + t3;
  if (arguments.length < 2) return (n = this.tween(n)) && n._value;
  if (e == null) return this.tween(n, null);
  if (typeof e != "function") throw new Error();
  var r = Vi(t3);
  return this.tween(n, (r.local ? tp : np)(r, e));
}
function ip(t3, e) {
  return function() {
    pa(this, t3).delay = +e.apply(this, arguments);
  };
}
function op(t3, e) {
  return e = +e, function() {
    pa(this, t3).delay = e;
  };
}
function ap(t3) {
  var e = this._id;
  return arguments.length ? this.each((typeof t3 == "function" ? ip : op)(e, t3)) : Yt(this.node(), e).delay;
}
function sp(t3, e) {
  return function() {
    rn(this, t3).duration = +e.apply(this, arguments);
  };
}
function fp(t3, e) {
  return e = +e, function() {
    rn(this, t3).duration = e;
  };
}
function up(t3) {
  var e = this._id;
  return arguments.length ? this.each((typeof t3 == "function" ? sp : fp)(e, t3)) : Yt(this.node(), e).duration;
}
function cp(t3, e) {
  if (typeof e != "function") throw new Error();
  return function() {
    rn(this, t3).ease = e;
  };
}
function lp(t3) {
  var e = this._id;
  return arguments.length ? this.each(cp(e, t3)) : Yt(this.node(), e).ease;
}
function hp(t3, e) {
  return function() {
    var n = e.apply(this, arguments);
    if (typeof n != "function") throw new Error();
    rn(this, t3).ease = n;
  };
}
function dp(t3) {
  if (typeof t3 != "function") throw new Error();
  return this.each(hp(this._id, t3));
}
function mp(t3) {
  typeof t3 != "function" && (t3 = mu(t3));
  for (var e = this._groups, n = e.length, r = new Array(n), i = 0; i < n; ++i)
    for (var o = e[i], s = o.length, f = r[i] = [], u, l = 0; l < s; ++l)
      (u = o[l]) && t3.call(u, u.__data__, l, o) && f.push(u);
  return new ln(r, this._parents, this._name, this._id);
}
function pp(t3) {
  if (t3._id !== this._id) throw new Error();
  for (var e = this._groups, n = t3._groups, r = e.length, i = n.length, o = Math.min(r, i), s = new Array(r), f = 0; f < o; ++f)
    for (var u = e[f], l = n[f], p = u.length, y = s[f] = new Array(p), S, D = 0; D < p; ++D)
      (S = u[D] || l[D]) && (y[D] = S);
  for (; f < r; ++f)
    s[f] = e[f];
  return new ln(s, this._parents, this._name, this._id);
}
function vp(t3) {
  return (t3 + "").trim().split(/^|\s+/).every(function(e) {
    var n = e.indexOf(".");
    return n >= 0 && (e = e.slice(0, n)), !e || e === "start";
  });
}
function gp(t3, e, n) {
  var r, i, o = vp(e) ? pa : rn;
  return function() {
    var s = o(this, t3), f = s.on;
    f !== r && (i = (r = f).copy()).on(e, n), s.on = i;
  };
}
function xp(t3, e) {
  var n = this._id;
  return arguments.length < 2 ? Yt(this.node(), n).on.on(t3) : this.each(gp(n, t3, e));
}
function yp(t3) {
  return function() {
    var e = this.parentNode;
    for (var n in this.__transition) if (+n !== t3) return;
    e && e.removeChild(this);
  };
}
function bp() {
  return this.on("end.remove", yp(this._id));
}
function _p(t3) {
  var e = this._name, n = this._id;
  typeof t3 != "function" && (t3 = ha(t3));
  for (var r = this._groups, i = r.length, o = new Array(i), s = 0; s < i; ++s)
    for (var f = r[s], u = f.length, l = o[s] = new Array(u), p, y, S = 0; S < u; ++S)
      (p = f[S]) && (y = t3.call(p, p.__data__, S, f)) && ("__data__" in p && (y.__data__ = p.__data__), l[S] = y, Ui(l[S], e, n, S, l, Yt(p, n)));
  return new ln(o, this._parents, e, n);
}
function Sp(t3) {
  var e = this._name, n = this._id;
  typeof t3 != "function" && (t3 = du(t3));
  for (var r = this._groups, i = r.length, o = [], s = [], f = 0; f < i; ++f)
    for (var u = r[f], l = u.length, p, y = 0; y < l; ++y)
      if (p = u[y]) {
        for (var S = t3.call(p, p.__data__, y, u), D, Z = Yt(p, n), we = 0, Se = S.length; we < Se; ++we)
          (D = S[we]) && Ui(D, e, n, we, S, Z);
        o.push(S), s.push(p);
      }
  return new ln(o, s, e, n);
}
var wp = Gr.prototype.constructor;
function Tp() {
  return new wp(this._groups, this._parents);
}
function Ap(t3, e) {
  var n, r, i;
  return function() {
    var o = sr(this, t3), s = (this.style.removeProperty(t3), sr(this, t3));
    return o === s ? null : o === n && s === r ? i : i = e(n = o, r = s);
  };
}
function Du(t3) {
  return function() {
    this.style.removeProperty(t3);
  };
}
function Ep(t3, e, n) {
  var r, i = n + "", o;
  return function() {
    var s = sr(this, t3);
    return s === i ? null : s === r ? o : o = e(r = s, n);
  };
}
function Pp(t3, e, n) {
  var r, i, o;
  return function() {
    var s = sr(this, t3), f = n(this), u = f + "";
    return f == null && (u = f = (this.style.removeProperty(t3), sr(this, t3))), s === u ? null : s === r && u === i ? o : (i = u, o = e(r = s, f));
  };
}
function Cp(t3, e) {
  var n, r, i, o = "style." + e, s = "end." + o, f;
  return function() {
    var u = rn(this, t3), l = u.on, p = u.value[o] == null ? f || (f = Du(e)) : void 0;
    (l !== n || i !== p) && (r = (n = l).copy()).on(s, i = p), u.on = r;
  };
}
function kp(t3, e, n) {
  var r = (t3 += "") == "transform" ? Om : Ru;
  return e == null ? this.styleTween(t3, Ap(t3, r)).on("end.style." + t3, Du(t3)) : typeof e == "function" ? this.styleTween(t3, Pp(t3, r, ya(this, "style." + t3, e))).each(Cp(this._id, t3)) : this.styleTween(t3, Ep(t3, r, e), n).on("end.style." + t3, null);
}
function Fp(t3, e, n) {
  return function(r) {
    this.style.setProperty(t3, e.call(this, r), n);
  };
}
function zp(t3, e, n) {
  var r, i;
  function o() {
    var s = e.apply(this, arguments);
    return s !== i && (r = (i = s) && Fp(t3, s, n)), r;
  }
  return o._value = e, o;
}
function Lp(t3, e, n) {
  var r = "style." + (t3 += "");
  if (arguments.length < 2) return (r = this.tween(r)) && r._value;
  if (e == null) return this.tween(r, null);
  if (typeof e != "function") throw new Error();
  return this.tween(r, zp(t3, e, n ?? ""));
}
function Ip(t3) {
  return function() {
    this.textContent = t3;
  };
}
function Rp(t3) {
  return function() {
    var e = t3(this);
    this.textContent = e ?? "";
  };
}
function Dp(t3) {
  return this.tween("text", typeof t3 == "function" ? Rp(ya(this, "text", t3)) : Ip(t3 == null ? "" : t3 + ""));
}
function Mp(t3) {
  return function(e) {
    this.textContent = t3.call(this, e);
  };
}
function Op(t3) {
  var e, n;
  function r() {
    var i = t3.apply(this, arguments);
    return i !== n && (e = (n = i) && Mp(i)), e;
  }
  return r._value = t3, r;
}
function Np(t3) {
  var e = "text";
  if (arguments.length < 1) return (e = this.tween(e)) && e._value;
  if (t3 == null) return this.tween(e, null);
  if (typeof t3 != "function") throw new Error();
  return this.tween(e, Op(t3));
}
function Gp() {
  for (var t3 = this._name, e = this._id, n = Mu(), r = this._groups, i = r.length, o = 0; o < i; ++o)
    for (var s = r[o], f = s.length, u, l = 0; l < f; ++l)
      if (u = s[l]) {
        var p = Yt(u, e);
        Ui(u, t3, n, l, s, {
          time: p.time + p.delay + p.duration,
          delay: 0,
          duration: p.duration,
          ease: p.ease
        });
      }
  return new ln(r, this._parents, t3, n);
}
function Bp() {
  var t3, e, n = this, r = n._id, i = n.size();
  return new Promise(function(o, s) {
    var f = { value: s }, u = { value: function() {
      --i === 0 && o();
    } };
    n.each(function() {
      var l = rn(this, r), p = l.on;
      p !== t3 && (e = (t3 = p).copy(), e._.cancel.push(f), e._.interrupt.push(f), e._.end.push(u)), l.on = e;
    }), i === 0 && o();
  });
}
var Vp = 0;
function ln(t3, e, n, r) {
  this._groups = t3, this._parents = e, this._name = n, this._id = r;
}
function Mu() {
  return ++Vp;
}
var fn = Gr.prototype;
ln.prototype = {
  constructor: ln,
  select: _p,
  selectAll: Sp,
  selectChild: fn.selectChild,
  selectChildren: fn.selectChildren,
  filter: mp,
  merge: pp,
  selection: Tp,
  transition: Gp,
  call: fn.call,
  nodes: fn.nodes,
  node: fn.node,
  size: fn.size,
  empty: fn.empty,
  each: fn.each,
  on: xp,
  attr: Km,
  attrTween: rp,
  style: kp,
  styleTween: Lp,
  text: Dp,
  textTween: Np,
  remove: bp,
  tween: Xm,
  delay: ap,
  duration: up,
  ease: lp,
  easeVarying: dp,
  end: Bp,
  [Symbol.iterator]: fn[Symbol.iterator]
};
function $p(t3) {
  return t3 * t3;
}
function jp(t3) {
  return t3 * (2 - t3);
}
function Up(t3) {
  return ((t3 *= 2) <= 1 ? t3 * t3 : --t3 * (2 - t3) + 1) / 2;
}
function Xp(t3) {
  return ((t3 *= 2) <= 1 ? t3 * t3 * t3 : (t3 -= 2) * t3 * t3 + 2) / 2;
}
var Hp = {
  time: null,
  // Set on use.
  delay: 0,
  duration: 250,
  ease: Xp
};
function Wp(t3, e) {
  for (var n; !(n = t3.__transition) || !(n = n[e]); )
    if (!(t3 = t3.parentNode))
      throw new Error(`transition ${e} not found`);
  return n;
}
function qp(t3) {
  var e, n;
  t3 instanceof ln ? (e = t3._id, t3 = t3._name) : (e = Mu(), (n = Hp).time = ma(), t3 = t3 == null ? null : t3 + "");
  for (var r = this._groups, i = r.length, o = 0; o < i; ++o)
    for (var s = r[o], f = s.length, u, l = 0; l < f; ++l)
      (u = s[l]) && Ui(u, t3, e, l, s, n || Wp(u, e));
  return new ln(r, this._parents, t3, e);
}
Gr.prototype.interrupt = lm;
Gr.prototype.transition = qp;
var hn = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function ba(t3) {
  return t3 && t3.__esModule && Object.prototype.hasOwnProperty.call(t3, "default") ? t3.default : t3;
}
function Yp(t3) {
  if (t3.__esModule) return t3;
  var e = t3.default;
  if (typeof e == "function") {
    var n = function r() {
      return this instanceof r ? Reflect.construct(e, arguments, this.constructor) : e.apply(this, arguments);
    };
    n.prototype = e.prototype;
  } else n = {};
  return Object.defineProperty(n, "__esModule", { value: true }), Object.keys(t3).forEach(function(r) {
    var i = Object.getOwnPropertyDescriptor(t3, r);
    Object.defineProperty(n, r, i.get ? i : {
      enumerable: true,
      get: function() {
        return t3[r];
      }
    });
  }), n;
}
var Ou = { exports: {} };
(function(t3, e) {
  (function(n, r) {
    t3.exports = r();
  })(hn, function() {
    var n = function(a) {
      return a instanceof Uint8Array || a instanceof Uint16Array || a instanceof Uint32Array || a instanceof Int8Array || a instanceof Int16Array || a instanceof Int32Array || a instanceof Float32Array || a instanceof Float64Array || a instanceof Uint8ClampedArray;
    }, r = function(a, h) {
      for (var _ = Object.keys(h), R = 0; R < _.length; ++R)
        a[_[R]] = h[_[R]];
      return a;
    }, i = `
`;
    function o(a) {
      return typeof atob < "u" ? atob(a) : "base64:" + a;
    }
    function s(a) {
      var h = new Error("(regl) " + a);
      throw console.error(h), h;
    }
    function f(a, h) {
      a || s(h);
    }
    function u(a) {
      return a ? ": " + a : "";
    }
    function l(a, h, _) {
      a in h || s("unknown parameter (" + a + ")" + u(_) + ". possible values: " + Object.keys(h).join());
    }
    function p(a, h) {
      n(a) || s(
        "invalid parameter type" + u(h) + ". must be a typed array"
      );
    }
    function y(a, h) {
      switch (h) {
        case "number":
          return typeof a == "number";
        case "object":
          return typeof a == "object";
        case "string":
          return typeof a == "string";
        case "boolean":
          return typeof a == "boolean";
        case "function":
          return typeof a == "function";
        case "undefined":
          return typeof a > "u";
        case "symbol":
          return typeof a == "symbol";
      }
    }
    function S(a, h, _) {
      y(a, h) || s(
        "invalid parameter type" + u(_) + ". expected " + h + ", got " + typeof a
      );
    }
    function D(a, h) {
      a >= 0 && (a | 0) === a || s("invalid parameter type, (" + a + ")" + u(h) + ". must be a nonnegative integer");
    }
    function Z(a, h, _) {
      h.indexOf(a) < 0 && s("invalid value" + u(_) + ". must be one of: " + h);
    }
    var we = [
      "gl",
      "canvas",
      "container",
      "attributes",
      "pixelRatio",
      "extensions",
      "optionalExtensions",
      "profile",
      "onDone"
    ];
    function Se(a) {
      Object.keys(a).forEach(function(h) {
        we.indexOf(h) < 0 && s('invalid regl constructor argument "' + h + '". must be one of ' + we);
      });
    }
    function ae(a, h) {
      for (a = a + ""; a.length < h; )
        a = " " + a;
      return a;
    }
    function Ne() {
      this.name = "unknown", this.lines = [], this.index = {}, this.hasErrors = false;
    }
    function Ue(a, h) {
      this.number = a, this.line = h, this.errors = [];
    }
    function q(a, h, _) {
      this.file = a, this.line = h, this.message = _;
    }
    function he() {
      var a = new Error(), h = (a.stack || a).toString(), _ = /compileProcedure.*\n\s*at.*\((.*)\)/.exec(h);
      if (_)
        return _[1];
      var R = /compileProcedure.*\n\s*at\s+(.*)(\n|$)/.exec(h);
      return R ? R[1] : "unknown";
    }
    function Re() {
      var a = new Error(), h = (a.stack || a).toString(), _ = /at REGLCommand.*\n\s+at.*\((.*)\)/.exec(h);
      if (_)
        return _[1];
      var R = /at REGLCommand.*\n\s+at\s+(.*)\n/.exec(h);
      return R ? R[1] : "unknown";
    }
    function Ge(a, h) {
      var _ = a.split(`
`), R = 1, j = 0, I = {
        unknown: new Ne(),
        0: new Ne()
      };
      I.unknown.name = I[0].name = h || he(), I.unknown.lines.push(new Ue(0, ""));
      for (var G = 0; G < _.length; ++G) {
        var Q = _[G], Y = /^\s*#\s*(\w+)\s+(.+)\s*$/.exec(Q);
        if (Y)
          switch (Y[1]) {
            case "line":
              var ne = /(\d+)(\s+\d+)?/.exec(Y[2]);
              ne && (R = ne[1] | 0, ne[2] && (j = ne[2] | 0, j in I || (I[j] = new Ne())));
              break;
            case "define":
              var ie = /SHADER_NAME(_B64)?\s+(.*)$/.exec(Y[2]);
              ie && (I[j].name = ie[1] ? o(ie[2]) : ie[2]);
              break;
          }
        I[j].lines.push(new Ue(R++, Q));
      }
      return Object.keys(I).forEach(function(re) {
        var fe = I[re];
        fe.lines.forEach(function(K) {
          fe.index[K.number] = K;
        });
      }), I;
    }
    function qe(a) {
      var h = [];
      return a.split(`
`).forEach(function(_) {
        if (!(_.length < 5)) {
          var R = /^ERROR:\s+(\d+):(\d+):\s*(.*)$/.exec(_);
          R ? h.push(new q(
            R[1] | 0,
            R[2] | 0,
            R[3].trim()
          )) : _.length > 0 && h.push(new q("unknown", 0, _));
        }
      }), h;
    }
    function et(a, h) {
      h.forEach(function(_) {
        var R = a[_.file];
        if (R) {
          var j = R.index[_.line];
          if (j) {
            j.errors.push(_), R.hasErrors = true;
            return;
          }
        }
        a.unknown.hasErrors = true, a.unknown.lines[0].errors.push(_);
      });
    }
    function rt(a, h, _, R, j) {
      if (!a.getShaderParameter(h, a.COMPILE_STATUS)) {
        var I = a.getShaderInfoLog(h), G = R === a.FRAGMENT_SHADER ? "fragment" : "vertex";
        Ce(_, "string", G + " shader source must be a string", j);
        var Q = Ge(_, j), Y = qe(I);
        et(Q, Y), Object.keys(Q).forEach(function(ne) {
          var ie = Q[ne];
          if (!ie.hasErrors)
            return;
          var re = [""], fe = [""];
          function K(te, C) {
            re.push(te), fe.push(C || "");
          }
          K("file number " + ne + ": " + ie.name + `
`, "color:red;text-decoration:underline;font-weight:bold"), ie.lines.forEach(function(te) {
            if (te.errors.length > 0) {
              K(ae(te.number, 4) + "|  ", "background-color:yellow; font-weight:bold"), K(te.line + i, "color:red; background-color:yellow; font-weight:bold");
              var C = 0;
              te.errors.forEach(function(O) {
                var ee = O.message, ge = /^\s*'(.*)'\s*:\s*(.*)$/.exec(ee);
                if (ge) {
                  var W = ge[1];
                  switch (ee = ge[2], W) {
                    case "assign":
                      W = "=";
                      break;
                  }
                  C = Math.max(te.line.indexOf(W, C), 0);
                } else
                  C = 0;
                K(ae("| ", 6)), K(ae("^^^", C + 3) + i, "font-weight:bold"), K(ae("| ", 6)), K(ee + i, "font-weight:bold");
              }), K(ae("| ", 6) + i);
            } else
              K(ae(te.number, 4) + "|  "), K(te.line + i, "color:red");
          }), typeof document < "u" && !window.chrome ? (fe[0] = re.join("%c"), console.log.apply(console, fe)) : console.log(re.join(""));
        }), f.raise("Error compiling " + G + " shader, " + Q[0].name);
      }
    }
    function Le(a, h, _, R, j) {
      if (!a.getProgramParameter(h, a.LINK_STATUS)) {
        var I = a.getProgramInfoLog(h), G = Ge(_, j), Q = Ge(R, j), Y = 'Error linking program with vertex shader, "' + Q[0].name + '", and fragment shader "' + G[0].name + '"';
        typeof document < "u" ? console.log(
          "%c" + Y + i + "%c" + I,
          "color:red;text-decoration:underline;font-weight:bold",
          "color:red"
        ) : console.log(Y + i + I), f.raise(Y);
      }
    }
    function st(a) {
      a._commandRef = he();
    }
    function tt(a, h, _, R) {
      st(a);
      function j(Y) {
        return Y ? R.id(Y) : 0;
      }
      a._fragId = j(a.static.frag), a._vertId = j(a.static.vert);
      function I(Y, ne) {
        Object.keys(ne).forEach(function(ie) {
          Y[R.id(ie)] = true;
        });
      }
      var G = a._uniformSet = {};
      I(G, h.static), I(G, h.dynamic);
      var Q = a._attributeSet = {};
      I(Q, _.static), I(Q, _.dynamic), a._hasCount = "count" in a.static || "count" in a.dynamic || "elements" in a.static || "elements" in a.dynamic;
    }
    function N(a, h) {
      var _ = Re();
      s(a + " in command " + (h || he()) + (_ === "unknown" ? "" : " called from " + _));
    }
    function Ae(a, h, _) {
      a || N(h, _ || he());
    }
    function oe(a, h, _, R) {
      a in h || N(
        "unknown parameter (" + a + ")" + u(_) + ". possible values: " + Object.keys(h).join(),
        R || he()
      );
    }
    function Ce(a, h, _, R) {
      y(a, h) || N(
        "invalid parameter type" + u(_) + ". expected " + h + ", got " + typeof a,
        R || he()
      );
    }
    function $e(a) {
      a();
    }
    function Be(a, h, _) {
      a.texture ? Z(
        a.texture._texture.internalformat,
        h,
        "unsupported texture format for attachment"
      ) : Z(
        a.renderbuffer._renderbuffer.format,
        _,
        "unsupported renderbuffer format for attachment"
      );
    }
    var Ye = 33071, nt = 9728, ut = 9984, Gt = 9985, bt = 9986, zt = 9987, $t = 5120, Lt = 5121, jt = 5122, lr = 5123, rc = 5124, ic = 5125, ka = 5126, Fa = 32819, za = 32820, La = 33635, Ia = 34042, oc = 36193, It = {};
    It[$t] = It[Lt] = 1, It[jt] = It[lr] = It[oc] = It[La] = It[Fa] = It[za] = 2, It[rc] = It[ic] = It[ka] = It[Ia] = 4;
    function Ra(a, h) {
      return a === za || a === Fa || a === La ? 2 : a === Ia ? 4 : It[a] * h;
    }
    function Vr(a) {
      return !(a & a - 1) && !!a;
    }
    function ac(a, h, _) {
      var R, j = h.width, I = h.height, G = h.channels;
      f(
        j > 0 && j <= _.maxTextureSize && I > 0 && I <= _.maxTextureSize,
        "invalid texture shape"
      ), (a.wrapS !== Ye || a.wrapT !== Ye) && f(
        Vr(j) && Vr(I),
        "incompatible wrap mode for texture, both width and height must be power of 2"
      ), h.mipmask === 1 ? j !== 1 && I !== 1 && f(
        a.minFilter !== ut && a.minFilter !== bt && a.minFilter !== Gt && a.minFilter !== zt,
        "min filter requires mipmap"
      ) : (f(
        Vr(j) && Vr(I),
        "texture must be a square power of 2 to support mipmapping"
      ), f(
        h.mipmask === (j << 1) - 1,
        "missing or incomplete mipmap data"
      )), h.type === ka && (_.extensions.indexOf("oes_texture_float_linear") < 0 && f(
        a.minFilter === nt && a.magFilter === nt,
        "filter not supported, must enable oes_texture_float_linear"
      ), f(
        !a.genMipmaps,
        "mipmap generation not supported with float textures"
      ));
      var Q = h.images;
      for (R = 0; R < 16; ++R)
        if (Q[R]) {
          var Y = j >> R, ne = I >> R;
          f(h.mipmask & 1 << R, "missing mipmap data");
          var ie = Q[R];
          if (f(
            ie.width === Y && ie.height === ne,
            "invalid shape for mip images"
          ), f(
            ie.format === h.format && ie.internalformat === h.internalformat && ie.type === h.type,
            "incompatible type for mip image"
          ), !ie.compressed) if (ie.data) {
            var re = Math.ceil(Ra(ie.type, G) * Y / ie.unpackAlignment) * ie.unpackAlignment;
            f(
              ie.data.byteLength === re * ne,
              "invalid data for image, buffer size is inconsistent with image format"
            );
          } else ie.element || ie.copy;
        } else a.genMipmaps || f((h.mipmask & 1 << R) === 0, "extra mipmap data");
      h.compressed && f(
        !a.genMipmaps,
        "mipmap generation for compressed images not supported"
      );
    }
    function sc(a, h, _, R) {
      var j = a.width, I = a.height, G = a.channels;
      f(
        j > 0 && j <= R.maxTextureSize && I > 0 && I <= R.maxTextureSize,
        "invalid texture shape"
      ), f(
        j === I,
        "cube map must be square"
      ), f(
        h.wrapS === Ye && h.wrapT === Ye,
        "wrap mode not supported by cube map"
      );
      for (var Q = 0; Q < _.length; ++Q) {
        var Y = _[Q];
        f(
          Y.width === j && Y.height === I,
          "inconsistent cube map face shape"
        ), h.genMipmaps && (f(
          !Y.compressed,
          "can not generate mipmap for compressed textures"
        ), f(
          Y.mipmask === 1,
          "can not specify mipmaps and generate mipmaps"
        ));
        for (var ne = Y.images, ie = 0; ie < 16; ++ie) {
          var re = ne[ie];
          if (re) {
            var fe = j >> ie, K = I >> ie;
            f(Y.mipmask & 1 << ie, "missing mipmap data"), f(
              re.width === fe && re.height === K,
              "invalid shape for mip images"
            ), f(
              re.format === a.format && re.internalformat === a.internalformat && re.type === a.type,
              "incompatible type for mip image"
            ), re.compressed || (re.data ? f(
              re.data.byteLength === fe * K * Math.max(Ra(re.type, G), re.unpackAlignment),
              "invalid data for image, buffer size is inconsistent with image format"
            ) : re.element || re.copy);
          }
        }
      }
    }
    var d = r(f, {
      optional: $e,
      raise: s,
      commandRaise: N,
      command: Ae,
      parameter: l,
      commandParameter: oe,
      constructor: Se,
      type: S,
      commandType: Ce,
      isTypedArray: p,
      nni: D,
      oneOf: Z,
      shaderError: rt,
      linkError: Le,
      callSite: Re,
      saveCommandRef: st,
      saveDrawInfo: tt,
      framebufferFormat: Be,
      guessCommand: he,
      texture2D: ac,
      textureCube: sc
    }), fc = 0, uc = 0, cc = 5, lc = 6;
    function wn(a, h) {
      this.id = fc++, this.type = a, this.data = h;
    }
    function Da(a) {
      return a.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
    }
    function hr(a) {
      if (a.length === 0)
        return [];
      var h = a.charAt(0), _ = a.charAt(a.length - 1);
      if (a.length > 1 && h === _ && (h === '"' || h === "'"))
        return ['"' + Da(a.substr(1, a.length - 2)) + '"'];
      var R = /\[(false|true|null|\d+|'[^']*'|"[^"]*")\]/.exec(a);
      if (R)
        return hr(a.substr(0, R.index)).concat(hr(R[1])).concat(hr(a.substr(R.index + R[0].length)));
      var j = a.split(".");
      if (j.length === 1)
        return ['"' + Da(a) + '"'];
      for (var I = [], G = 0; G < j.length; ++G)
        I = I.concat(hr(j[G]));
      return I;
    }
    function Ma(a) {
      return "[" + hr(a).join("][") + "]";
    }
    function hc(a, h) {
      return new wn(a, Ma(h + ""));
    }
    function dc(a) {
      return typeof a == "function" && !a._reglType || a instanceof wn;
    }
    function Oa(a, h) {
      if (typeof a == "function")
        return new wn(uc, a);
      if (typeof a == "number" || typeof a == "boolean")
        return new wn(cc, a);
      if (Array.isArray(a))
        return new wn(lc, a.map(function(_, R) {
          return Oa(_, h + "[" + R + "]");
        }));
      if (a instanceof wn)
        return a;
      d(false, "invalid option type in uniform " + h);
    }
    var Rt = {
      DynamicVariable: wn,
      define: hc,
      isDynamic: dc,
      unbox: Oa,
      accessor: Ma
    }, Hi = {
      next: typeof requestAnimationFrame == "function" ? function(a) {
        return requestAnimationFrame(a);
      } : function(a) {
        return setTimeout(a, 16);
      },
      cancel: typeof cancelAnimationFrame == "function" ? function(a) {
        return cancelAnimationFrame(a);
      } : clearTimeout
    }, Na = typeof performance < "u" && performance.now ? function() {
      return performance.now();
    } : function() {
      return +/* @__PURE__ */ new Date();
    };
    function mc() {
      var a = { "": 0 }, h = [""];
      return {
        id: function(_) {
          var R = a[_];
          return R || (R = a[_] = h.length, h.push(_), R);
        },
        str: function(_) {
          return h[_];
        }
      };
    }
    function pc(a, h, _) {
      var R = document.createElement("canvas");
      r(R.style, {
        border: 0,
        margin: 0,
        padding: 0,
        top: 0,
        left: 0,
        width: "100%",
        height: "100%"
      }), a.appendChild(R), a === document.body && (R.style.position = "absolute", r(a.style, {
        margin: 0,
        padding: 0
      }));
      function j() {
        var Q = window.innerWidth, Y = window.innerHeight;
        if (a !== document.body) {
          var ne = R.getBoundingClientRect();
          Q = ne.right - ne.left, Y = ne.bottom - ne.top;
        }
        R.width = _ * Q, R.height = _ * Y;
      }
      var I;
      a !== document.body && typeof ResizeObserver == "function" ? (I = new ResizeObserver(function() {
        setTimeout(j);
      }), I.observe(a)) : window.addEventListener("resize", j, false);
      function G() {
        I ? I.disconnect() : window.removeEventListener("resize", j), a.removeChild(R);
      }
      return j(), {
        canvas: R,
        onDestroy: G
      };
    }
    function vc(a, h) {
      function _(R) {
        try {
          return a.getContext(R, h);
        } catch {
          return null;
        }
      }
      return _("webgl") || _("experimental-webgl") || _("webgl-experimental");
    }
    function gc(a) {
      return typeof a.nodeName == "string" && typeof a.appendChild == "function" && typeof a.getBoundingClientRect == "function";
    }
    function xc(a) {
      return typeof a.drawArrays == "function" || typeof a.drawElements == "function";
    }
    function Ga(a) {
      return typeof a == "string" ? a.split() : (d(Array.isArray(a), "invalid extension array"), a);
    }
    function Ba(a) {
      return typeof a == "string" ? (d(typeof document < "u", "not supported outside of DOM"), document.querySelector(a)) : a;
    }
    function yc(a) {
      var h = a || {}, _, R, j, I, G = {}, Q = [], Y = [], ne = typeof window > "u" ? 1 : window.devicePixelRatio, ie = false, re = function(te) {
        te && d.raise(te);
      }, fe = function() {
      };
      if (typeof h == "string" ? (d(
        typeof document < "u",
        "selector queries only supported in DOM enviroments"
      ), _ = document.querySelector(h), d(_, "invalid query string for element")) : typeof h == "object" ? gc(h) ? _ = h : xc(h) ? (I = h, j = I.canvas) : (d.constructor(h), "gl" in h ? I = h.gl : "canvas" in h ? j = Ba(h.canvas) : "container" in h && (R = Ba(h.container)), "attributes" in h && (G = h.attributes, d.type(G, "object", "invalid context attributes")), "extensions" in h && (Q = Ga(h.extensions)), "optionalExtensions" in h && (Y = Ga(h.optionalExtensions)), "onDone" in h && (d.type(
        h.onDone,
        "function",
        "invalid or missing onDone callback"
      ), re = h.onDone), "profile" in h && (ie = !!h.profile), "pixelRatio" in h && (ne = +h.pixelRatio, d(ne > 0, "invalid pixel ratio"))) : d.raise("invalid arguments to regl"), _ && (_.nodeName.toLowerCase() === "canvas" ? j = _ : R = _), !I) {
        if (!j) {
          d(
            typeof document < "u",
            "must manually specify webgl context outside of DOM environments"
          );
          var K = pc(R || document.body, re, ne);
          if (!K)
            return null;
          j = K.canvas, fe = K.onDestroy;
        }
        G.premultipliedAlpha === void 0 && (G.premultipliedAlpha = true), I = vc(j, G);
      }
      return I ? {
        gl: I,
        canvas: j,
        container: R,
        extensions: Q,
        optionalExtensions: Y,
        pixelRatio: ne,
        profile: ie,
        onDone: re,
        onDestroy: fe
      } : (fe(), re("webgl not supported, try upgrading your browser or graphics drivers http://get.webgl.org"), null);
    }
    function bc(a, h) {
      var _ = {};
      function R(G) {
        d.type(G, "string", "extension name must be string");
        var Q = G.toLowerCase(), Y;
        try {
          Y = _[Q] = a.getExtension(Q);
        } catch {
        }
        return !!Y;
      }
      for (var j = 0; j < h.extensions.length; ++j) {
        var I = h.extensions[j];
        if (!R(I))
          return h.onDestroy(), h.onDone('"' + I + '" extension is not supported by the current WebGL context, try upgrading your system or a different browser'), null;
      }
      return h.optionalExtensions.forEach(R), {
        extensions: _,
        restore: function() {
          Object.keys(_).forEach(function(G) {
            if (_[G] && !R(G))
              throw new Error("(regl): error restoring extension " + G);
          });
        }
      };
    }
    function Dt(a, h) {
      for (var _ = Array(a), R = 0; R < a; ++R)
        _[R] = h(R);
      return _;
    }
    var _c = 5120, Sc = 5121, wc = 5122, Tc = 5123, Ac = 5124, Ec = 5125, Pc = 5126;
    function Cc(a) {
      for (var h = 16; h <= 1 << 28; h *= 16)
        if (a <= h)
          return h;
      return 0;
    }
    function Va(a) {
      var h, _;
      return h = (a > 65535) << 4, a >>>= h, _ = (a > 255) << 3, a >>>= _, h |= _, _ = (a > 15) << 2, a >>>= _, h |= _, _ = (a > 3) << 1, a >>>= _, h |= _, h | a >> 1;
    }
    function $a() {
      var a = Dt(8, function() {
        return [];
      });
      function h(I) {
        var G = Cc(I), Q = a[Va(G) >> 2];
        return Q.length > 0 ? Q.pop() : new ArrayBuffer(G);
      }
      function _(I) {
        a[Va(I.byteLength) >> 2].push(I);
      }
      function R(I, G) {
        var Q = null;
        switch (I) {
          case _c:
            Q = new Int8Array(h(G), 0, G);
            break;
          case Sc:
            Q = new Uint8Array(h(G), 0, G);
            break;
          case wc:
            Q = new Int16Array(h(2 * G), 0, G);
            break;
          case Tc:
            Q = new Uint16Array(h(2 * G), 0, G);
            break;
          case Ac:
            Q = new Int32Array(h(4 * G), 0, G);
            break;
          case Ec:
            Q = new Uint32Array(h(4 * G), 0, G);
            break;
          case Pc:
            Q = new Float32Array(h(4 * G), 0, G);
            break;
          default:
            return null;
        }
        return Q.length !== G ? Q.subarray(0, G) : Q;
      }
      function j(I) {
        _(I.buffer);
      }
      return {
        alloc: h,
        free: _,
        allocType: R,
        freeType: j
      };
    }
    var ht = $a();
    ht.zero = $a();
    var kc = 3408, Fc = 3410, zc = 3411, Lc = 3412, Ic = 3413, Rc = 3414, Dc = 3415, Mc = 33901, Oc = 33902, Nc = 3379, Gc = 3386, Bc = 34921, Vc = 36347, $c = 36348, jc = 35661, Uc = 35660, Xc = 34930, Hc = 36349, Wc = 34076, qc = 34024, Yc = 7936, Zc = 7937, Qc = 7938, Kc = 35724, Jc = 34047, el = 36063, tl = 34852, $r = 3553, ja = 34067, nl = 34069, rl = 33984, dr = 6408, Wi = 5126, Ua = 5121, qi = 36160, il = 36053, ol = 36064, al = 16384, sl = function(a, h) {
      var _ = 1;
      h.ext_texture_filter_anisotropic && (_ = a.getParameter(Jc));
      var R = 1, j = 1;
      h.webgl_draw_buffers && (R = a.getParameter(tl), j = a.getParameter(el));
      var I = !!h.oes_texture_float;
      if (I) {
        var G = a.createTexture();
        a.bindTexture($r, G), a.texImage2D($r, 0, dr, 1, 1, 0, dr, Wi, null);
        var Q = a.createFramebuffer();
        if (a.bindFramebuffer(qi, Q), a.framebufferTexture2D(qi, ol, $r, G, 0), a.bindTexture($r, null), a.checkFramebufferStatus(qi) !== il) I = false;
        else {
          a.viewport(0, 0, 1, 1), a.clearColor(1, 0, 0, 1), a.clear(al);
          var Y = ht.allocType(Wi, 4);
          a.readPixels(0, 0, 1, 1, dr, Wi, Y), a.getError() ? I = false : (a.deleteFramebuffer(Q), a.deleteTexture(G), I = Y[0] === 1), ht.freeType(Y);
        }
      }
      var ne = typeof navigator < "u" && (/MSIE/.test(navigator.userAgent) || /Trident\//.test(navigator.appVersion) || /Edge/.test(navigator.userAgent)), ie = true;
      if (!ne) {
        var re = a.createTexture(), fe = ht.allocType(Ua, 36);
        a.activeTexture(rl), a.bindTexture(ja, re), a.texImage2D(nl, 0, dr, 3, 3, 0, dr, Ua, fe), ht.freeType(fe), a.bindTexture(ja, null), a.deleteTexture(re), ie = !a.getError();
      }
      return {
        // drawing buffer bit depth
        colorBits: [
          a.getParameter(Fc),
          a.getParameter(zc),
          a.getParameter(Lc),
          a.getParameter(Ic)
        ],
        depthBits: a.getParameter(Rc),
        stencilBits: a.getParameter(Dc),
        subpixelBits: a.getParameter(kc),
        // supported extensions
        extensions: Object.keys(h).filter(function(K) {
          return !!h[K];
        }),
        // max aniso samples
        maxAnisotropic: _,
        // max draw buffers
        maxDrawbuffers: R,
        maxColorAttachments: j,
        // point and line size ranges
        pointSizeDims: a.getParameter(Mc),
        lineWidthDims: a.getParameter(Oc),
        maxViewportDims: a.getParameter(Gc),
        maxCombinedTextureUnits: a.getParameter(jc),
        maxCubeMapSize: a.getParameter(Wc),
        maxRenderbufferSize: a.getParameter(qc),
        maxTextureUnits: a.getParameter(Xc),
        maxTextureSize: a.getParameter(Nc),
        maxAttributes: a.getParameter(Bc),
        maxVertexUniforms: a.getParameter(Vc),
        maxVertexTextureUnits: a.getParameter(Uc),
        maxVaryingVectors: a.getParameter($c),
        maxFragmentUniforms: a.getParameter(Hc),
        // vendor info
        glsl: a.getParameter(Kc),
        renderer: a.getParameter(Zc),
        vendor: a.getParameter(Yc),
        version: a.getParameter(Qc),
        // quirks
        readFloat: I,
        npotTextureCube: ie
      };
    };
    function Ut(a) {
      return !!a && typeof a == "object" && Array.isArray(a.shape) && Array.isArray(a.stride) && typeof a.offset == "number" && a.shape.length === a.stride.length && (Array.isArray(a.data) || n(a.data));
    }
    var Mt = function(a) {
      return Object.keys(a).map(function(h) {
        return a[h];
      });
    }, jr = {
      shape: ll,
      flatten: cl
    };
    function fl(a, h, _) {
      for (var R = 0; R < h; ++R)
        _[R] = a[R];
    }
    function ul(a, h, _, R) {
      for (var j = 0, I = 0; I < h; ++I)
        for (var G = a[I], Q = 0; Q < _; ++Q)
          R[j++] = G[Q];
    }
    function Xa(a, h, _, R, j, I) {
      for (var G = I, Q = 0; Q < h; ++Q)
        for (var Y = a[Q], ne = 0; ne < _; ++ne)
          for (var ie = Y[ne], re = 0; re < R; ++re)
            j[G++] = ie[re];
    }
    function Ha(a, h, _, R, j) {
      for (var I = 1, G = _ + 1; G < h.length; ++G)
        I *= h[G];
      var Q = h[_];
      if (h.length - _ === 4) {
        var Y = h[_ + 1], ne = h[_ + 2], ie = h[_ + 3];
        for (G = 0; G < Q; ++G)
          Xa(a[G], Y, ne, ie, R, j), j += I;
      } else
        for (G = 0; G < Q; ++G)
          Ha(a[G], h, _ + 1, R, j), j += I;
    }
    function cl(a, h, _, R) {
      var j = 1;
      if (h.length)
        for (var I = 0; I < h.length; ++I)
          j *= h[I];
      else
        j = 0;
      var G = R || ht.allocType(_, j);
      switch (h.length) {
        case 0:
          break;
        case 1:
          fl(a, h[0], G);
          break;
        case 2:
          ul(a, h[0], h[1], G);
          break;
        case 3:
          Xa(a, h[0], h[1], h[2], G, 0);
          break;
        default:
          Ha(a, h, 0, G, 0);
      }
      return G;
    }
    function ll(a) {
      for (var h = [], _ = a; _.length; _ = _[0])
        h.push(_.length);
      return h;
    }
    var Yi = {
      "[object Int8Array]": 5120,
      "[object Int16Array]": 5122,
      "[object Int32Array]": 5124,
      "[object Uint8Array]": 5121,
      "[object Uint8ClampedArray]": 5121,
      "[object Uint16Array]": 5123,
      "[object Uint32Array]": 5125,
      "[object Float32Array]": 5126,
      "[object Float64Array]": 5121,
      "[object ArrayBuffer]": 5121
    }, hl = 5120, dl = 5122, ml = 5124, pl = 5121, vl = 5123, gl = 5125, xl = 5126, yl = 5126, Tn = {
      int8: hl,
      int16: dl,
      int32: ml,
      uint8: pl,
      uint16: vl,
      uint32: gl,
      float: xl,
      float32: yl
    }, bl = 35048, _l = 35040, Ur = {
      dynamic: bl,
      stream: _l,
      static: 35044
    }, Zi = jr.flatten, Wa = jr.shape, qa = 35044, Sl = 35040, Qi = 5121, Ki = 5126, mn = [];
    mn[5120] = 1, mn[5122] = 2, mn[5124] = 4, mn[5121] = 1, mn[5123] = 2, mn[5125] = 4, mn[5126] = 4;
    function Xr(a) {
      return Yi[Object.prototype.toString.call(a)] | 0;
    }
    function Ya(a, h) {
      for (var _ = 0; _ < h.length; ++_)
        a[_] = h[_];
    }
    function Za(a, h, _, R, j, I, G) {
      for (var Q = 0, Y = 0; Y < _; ++Y)
        for (var ne = 0; ne < R; ++ne)
          a[Q++] = h[j * Y + I * ne + G];
    }
    function wl(a, h, _, R) {
      var j = 0, I = {};
      function G(C) {
        this.id = j++, this.buffer = a.createBuffer(), this.type = C, this.usage = qa, this.byteLength = 0, this.dimension = 1, this.dtype = Qi, this.persistentData = null, _.profile && (this.stats = { size: 0 });
      }
      G.prototype.bind = function() {
        a.bindBuffer(this.type, this.buffer);
      }, G.prototype.destroy = function() {
        fe(this);
      };
      var Q = [];
      function Y(C, O) {
        var ee = Q.pop();
        return ee || (ee = new G(C)), ee.bind(), re(ee, O, Sl, 0, 1, false), ee;
      }
      function ne(C) {
        Q.push(C);
      }
      function ie(C, O, ee) {
        C.byteLength = O.byteLength, a.bufferData(C.type, O, ee);
      }
      function re(C, O, ee, ge, W, pe) {
        var de;
        if (C.usage = ee, Array.isArray(O)) {
          if (C.dtype = ge || Ki, O.length > 0) {
            var Pe;
            if (Array.isArray(O[0])) {
              de = Wa(O);
              for (var U = 1, $ = 1; $ < de.length; ++$)
                U *= de[$];
              C.dimension = U, Pe = Zi(O, de, C.dtype), ie(C, Pe, ee), pe ? C.persistentData = Pe : ht.freeType(Pe);
            } else if (typeof O[0] == "number") {
              C.dimension = W;
              var be = ht.allocType(C.dtype, O.length);
              Ya(be, O), ie(C, be, ee), pe ? C.persistentData = be : ht.freeType(be);
            } else n(O[0]) ? (C.dimension = O[0].length, C.dtype = ge || Xr(O[0]) || Ki, Pe = Zi(
              O,
              [O.length, O[0].length],
              C.dtype
            ), ie(C, Pe, ee), pe ? C.persistentData = Pe : ht.freeType(Pe)) : d.raise("invalid buffer data");
          }
        } else if (n(O))
          C.dtype = ge || Xr(O), C.dimension = W, ie(C, O, ee), pe && (C.persistentData = new Uint8Array(new Uint8Array(O.buffer)));
        else if (Ut(O)) {
          de = O.shape;
          var ue = O.stride, J = O.offset, ce = 0, le = 0, Me = 0, De = 0;
          de.length === 1 ? (ce = de[0], le = 1, Me = ue[0], De = 0) : de.length === 2 ? (ce = de[0], le = de[1], Me = ue[0], De = ue[1]) : d.raise("invalid shape"), C.dtype = ge || Xr(O.data) || Ki, C.dimension = le;
          var me = ht.allocType(C.dtype, ce * le);
          Za(
            me,
            O.data,
            ce,
            le,
            Me,
            De,
            J
          ), ie(C, me, ee), pe ? C.persistentData = me : ht.freeType(me);
        } else O instanceof ArrayBuffer ? (C.dtype = Qi, C.dimension = W, ie(C, O, ee), pe && (C.persistentData = new Uint8Array(new Uint8Array(O)))) : d.raise("invalid buffer data");
      }
      function fe(C) {
        h.bufferCount--, R(C);
        var O = C.buffer;
        d(O, "buffer must not be deleted already"), a.deleteBuffer(O), C.buffer = null, delete I[C.id];
      }
      function K(C, O, ee, ge) {
        h.bufferCount++;
        var W = new G(O);
        I[W.id] = W;
        function pe(U) {
          var $ = qa, be = null, ue = 0, J = 0, ce = 1;
          return Array.isArray(U) || n(U) || Ut(U) || U instanceof ArrayBuffer ? be = U : typeof U == "number" ? ue = U | 0 : U && (d.type(
            U,
            "object",
            "buffer arguments must be an object, a number or an array"
          ), "data" in U && (d(
            be === null || Array.isArray(be) || n(be) || Ut(be),
            "invalid data for buffer"
          ), be = U.data), "usage" in U && (d.parameter(U.usage, Ur, "invalid buffer usage"), $ = Ur[U.usage]), "type" in U && (d.parameter(U.type, Tn, "invalid buffer type"), J = Tn[U.type]), "dimension" in U && (d.type(U.dimension, "number", "invalid dimension"), ce = U.dimension | 0), "length" in U && (d.nni(ue, "buffer length must be a nonnegative integer"), ue = U.length | 0)), W.bind(), be ? re(W, be, $, J, ce, ge) : (ue && a.bufferData(W.type, ue, $), W.dtype = J || Qi, W.usage = $, W.dimension = ce, W.byteLength = ue), _.profile && (W.stats.size = W.byteLength * mn[W.dtype]), pe;
        }
        function de(U, $) {
          d(
            $ + U.byteLength <= W.byteLength,
            "invalid buffer subdata call, buffer is too small.  Can't write data of size " + U.byteLength + " starting from offset " + $ + " to a buffer of size " + W.byteLength
          ), a.bufferSubData(W.type, $, U);
        }
        function Pe(U, $) {
          var be = ($ || 0) | 0, ue;
          if (W.bind(), n(U) || U instanceof ArrayBuffer)
            de(U, be);
          else if (Array.isArray(U)) {
            if (U.length > 0)
              if (typeof U[0] == "number") {
                var J = ht.allocType(W.dtype, U.length);
                Ya(J, U), de(J, be), ht.freeType(J);
              } else if (Array.isArray(U[0]) || n(U[0])) {
                ue = Wa(U);
                var ce = Zi(U, ue, W.dtype);
                de(ce, be), ht.freeType(ce);
              } else
                d.raise("invalid buffer data");
          } else if (Ut(U)) {
            ue = U.shape;
            var le = U.stride, Me = 0, De = 0, me = 0, ve = 0;
            ue.length === 1 ? (Me = ue[0], De = 1, me = le[0], ve = 0) : ue.length === 2 ? (Me = ue[0], De = ue[1], me = le[0], ve = le[1]) : d.raise("invalid shape");
            var Fe = Array.isArray(U.data) ? W.dtype : Xr(U.data), Oe = ht.allocType(Fe, Me * De);
            Za(
              Oe,
              U.data,
              Me,
              De,
              me,
              ve,
              U.offset
            ), de(Oe, be), ht.freeType(Oe);
          } else
            d.raise("invalid data for buffer subdata");
          return pe;
        }
        return ee || pe(C), pe._reglType = "buffer", pe._buffer = W, pe.subdata = Pe, _.profile && (pe.stats = W.stats), pe.destroy = function() {
          fe(W);
        }, pe;
      }
      function te() {
        Mt(I).forEach(function(C) {
          C.buffer = a.createBuffer(), a.bindBuffer(C.type, C.buffer), a.bufferData(
            C.type,
            C.persistentData || C.byteLength,
            C.usage
          );
        });
      }
      return _.profile && (h.getTotalBufferSize = function() {
        var C = 0;
        return Object.keys(I).forEach(function(O) {
          C += I[O].stats.size;
        }), C;
      }), {
        create: K,
        createStream: Y,
        destroyStream: ne,
        clear: function() {
          Mt(I).forEach(fe), Q.forEach(fe);
        },
        getBuffer: function(C) {
          return C && C._buffer instanceof G ? C._buffer : null;
        },
        restore: te,
        _initBuffer: re
      };
    }
    var Tl = 0, Al = 0, El = 1, Pl = 1, Cl = 4, kl = 4, pn = {
      points: Tl,
      point: Al,
      lines: El,
      line: Pl,
      triangles: Cl,
      triangle: kl,
      "line loop": 2,
      "line strip": 3,
      "triangle strip": 5,
      "triangle fan": 6
    }, Fl = 0, zl = 1, mr = 4, Ll = 5120, Un = 5121, Qa = 5122, Xn = 5123, Ka = 5124, An = 5125, Ji = 34963, Il = 35040, Rl = 35044;
    function Dl(a, h, _, R) {
      var j = {}, I = 0, G = {
        uint8: Un,
        uint16: Xn
      };
      h.oes_element_index_uint && (G.uint32 = An);
      function Q(te) {
        this.id = I++, j[this.id] = this, this.buffer = te, this.primType = mr, this.vertCount = 0, this.type = 0;
      }
      Q.prototype.bind = function() {
        this.buffer.bind();
      };
      var Y = [];
      function ne(te) {
        var C = Y.pop();
        return C || (C = new Q(_.create(
          null,
          Ji,
          true,
          false
        )._buffer)), re(C, te, Il, -1, -1, 0, 0), C;
      }
      function ie(te) {
        Y.push(te);
      }
      function re(te, C, O, ee, ge, W, pe) {
        te.buffer.bind();
        var de;
        if (C) {
          var Pe = pe;
          !pe && (!n(C) || Ut(C) && !n(C.data)) && (Pe = h.oes_element_index_uint ? An : Xn), _._initBuffer(
            te.buffer,
            C,
            O,
            Pe,
            3
          );
        } else
          a.bufferData(Ji, W, O), te.buffer.dtype = de || Un, te.buffer.usage = O, te.buffer.dimension = 3, te.buffer.byteLength = W;
        if (de = pe, !pe) {
          switch (te.buffer.dtype) {
            case Un:
            case Ll:
              de = Un;
              break;
            case Xn:
            case Qa:
              de = Xn;
              break;
            case An:
            case Ka:
              de = An;
              break;
            default:
              d.raise("unsupported type for element array");
          }
          te.buffer.dtype = de;
        }
        te.type = de, d(
          de !== An || !!h.oes_element_index_uint,
          "32 bit element buffers not supported, enable oes_element_index_uint first"
        );
        var U = ge;
        U < 0 && (U = te.buffer.byteLength, de === Xn ? U >>= 1 : de === An && (U >>= 2)), te.vertCount = U;
        var $ = ee;
        if (ee < 0) {
          $ = mr;
          var be = te.buffer.dimension;
          be === 1 && ($ = Fl), be === 2 && ($ = zl), be === 3 && ($ = mr);
        }
        te.primType = $;
      }
      function fe(te) {
        R.elementsCount--, d(te.buffer !== null, "must not double destroy elements"), delete j[te.id], te.buffer.destroy(), te.buffer = null;
      }
      function K(te, C) {
        var O = _.create(null, Ji, true), ee = new Q(O._buffer);
        R.elementsCount++;
        function ge(W) {
          if (!W)
            O(), ee.primType = mr, ee.vertCount = 0, ee.type = Un;
          else if (typeof W == "number")
            O(W), ee.primType = mr, ee.vertCount = W | 0, ee.type = Un;
          else {
            var pe = null, de = Rl, Pe = -1, U = -1, $ = 0, be = 0;
            Array.isArray(W) || n(W) || Ut(W) ? pe = W : (d.type(W, "object", "invalid arguments for elements"), "data" in W && (pe = W.data, d(
              Array.isArray(pe) || n(pe) || Ut(pe),
              "invalid data for element buffer"
            )), "usage" in W && (d.parameter(
              W.usage,
              Ur,
              "invalid element buffer usage"
            ), de = Ur[W.usage]), "primitive" in W && (d.parameter(
              W.primitive,
              pn,
              "invalid element buffer primitive"
            ), Pe = pn[W.primitive]), "count" in W && (d(
              typeof W.count == "number" && W.count >= 0,
              "invalid vertex count for elements"
            ), U = W.count | 0), "type" in W && (d.parameter(
              W.type,
              G,
              "invalid buffer type"
            ), be = G[W.type]), "length" in W ? $ = W.length | 0 : ($ = U, be === Xn || be === Qa ? $ *= 2 : (be === An || be === Ka) && ($ *= 4))), re(
              ee,
              pe,
              de,
              Pe,
              U,
              $,
              be
            );
          }
          return ge;
        }
        return ge(te), ge._reglType = "elements", ge._elements = ee, ge.subdata = function(W, pe) {
          return O.subdata(W, pe), ge;
        }, ge.destroy = function() {
          fe(ee);
        }, ge;
      }
      return {
        create: K,
        createStream: ne,
        destroyStream: ie,
        getElements: function(te) {
          return typeof te == "function" && te._elements instanceof Q ? te._elements : null;
        },
        clear: function() {
          Mt(j).forEach(fe);
        }
      };
    }
    var Ja = new Float32Array(1), Ml = new Uint32Array(Ja.buffer), Ol = 5123;
    function es(a) {
      for (var h = ht.allocType(Ol, a.length), _ = 0; _ < a.length; ++_)
        if (isNaN(a[_]))
          h[_] = 65535;
        else if (a[_] === 1 / 0)
          h[_] = 31744;
        else if (a[_] === -1 / 0)
          h[_] = 64512;
        else {
          Ja[0] = a[_];
          var R = Ml[0], j = R >>> 31 << 15, I = (R << 1 >>> 24) - 127, G = R >> 13 & 1023;
          if (I < -24)
            h[_] = j;
          else if (I < -14) {
            var Q = -14 - I;
            h[_] = j + (G + 1024 >> Q);
          } else I > 15 ? h[_] = j + 31744 : h[_] = j + (I + 15 << 10) + G;
        }
      return h;
    }
    function ot(a) {
      return Array.isArray(a) || n(a);
    }
    var ts = function(a) {
      return !(a & a - 1) && !!a;
    }, Nl = 34467, Zt = 3553, eo = 34067, Hr = 34069, En = 6408, to = 6406, Wr = 6407, pr = 6409, qr = 6410, ns = 32854, no = 32855, rs = 36194, Gl = 32819, Bl = 32820, Vl = 33635, $l = 34042, ro = 6402, Yr = 34041, io = 35904, oo = 35906, Hn = 36193, ao = 33776, so = 33777, fo = 33778, uo = 33779, is = 35986, os = 35987, as = 34798, ss = 35840, fs = 35841, us = 35842, cs = 35843, ls = 36196, Wn = 5121, co = 5123, lo = 5125, vr = 5126, jl = 10242, Ul = 10243, Xl = 10497, ho = 33071, Hl = 33648, Wl = 10240, ql = 10241, mo = 9728, Yl = 9729, po = 9984, hs = 9985, ds = 9986, vo = 9987, Zl = 33170, Zr = 4352, Ql = 4353, Kl = 4354, Jl = 34046, eh = 3317, th = 37440, nh = 37441, rh = 37443, ms = 37444, gr = 33984, ih = [
      po,
      ds,
      hs,
      vo
    ], Qr = [
      0,
      pr,
      qr,
      Wr,
      En
    ], Bt = {};
    Bt[pr] = Bt[to] = Bt[ro] = 1, Bt[Yr] = Bt[qr] = 2, Bt[Wr] = Bt[io] = 3, Bt[En] = Bt[oo] = 4;
    function qn(a) {
      return "[object " + a + "]";
    }
    var ps = qn("HTMLCanvasElement"), vs = qn("OffscreenCanvas"), gs = qn("CanvasRenderingContext2D"), xs = qn("ImageBitmap"), ys = qn("HTMLImageElement"), bs = qn("HTMLVideoElement"), oh = Object.keys(Yi).concat([
      ps,
      vs,
      gs,
      xs,
      ys,
      bs
    ]), Yn = [];
    Yn[Wn] = 1, Yn[vr] = 4, Yn[Hn] = 2, Yn[co] = 2, Yn[lo] = 4;
    var wt = [];
    wt[ns] = 2, wt[no] = 2, wt[rs] = 2, wt[Yr] = 4, wt[ao] = 0.5, wt[so] = 0.5, wt[fo] = 1, wt[uo] = 1, wt[is] = 0.5, wt[os] = 1, wt[as] = 1, wt[ss] = 0.5, wt[fs] = 0.25, wt[us] = 0.5, wt[cs] = 0.25, wt[ls] = 0.5;
    function _s(a) {
      return Array.isArray(a) && (a.length === 0 || typeof a[0] == "number");
    }
    function Ss(a) {
      if (!Array.isArray(a))
        return false;
      var h = a.length;
      return !(h === 0 || !ot(a[0]));
    }
    function Pn(a) {
      return Object.prototype.toString.call(a);
    }
    function ws(a) {
      return Pn(a) === ps;
    }
    function Ts(a) {
      return Pn(a) === vs;
    }
    function ah(a) {
      return Pn(a) === gs;
    }
    function sh(a) {
      return Pn(a) === xs;
    }
    function fh(a) {
      return Pn(a) === ys;
    }
    function uh(a) {
      return Pn(a) === bs;
    }
    function go(a) {
      if (!a)
        return false;
      var h = Pn(a);
      return oh.indexOf(h) >= 0 ? true : _s(a) || Ss(a) || Ut(a);
    }
    function As(a) {
      return Yi[Object.prototype.toString.call(a)] | 0;
    }
    function ch(a, h) {
      var _ = h.length;
      switch (a.type) {
        case Wn:
        case co:
        case lo:
        case vr:
          var R = ht.allocType(a.type, _);
          R.set(h), a.data = R;
          break;
        case Hn:
          a.data = es(h);
          break;
        default:
          d.raise("unsupported texture type, must specify a typed array");
      }
    }
    function Es(a, h) {
      return ht.allocType(
        a.type === Hn ? vr : a.type,
        h
      );
    }
    function Ps(a, h) {
      a.type === Hn ? (a.data = es(h), ht.freeType(h)) : a.data = h;
    }
    function lh(a, h, _, R, j, I) {
      for (var G = a.width, Q = a.height, Y = a.channels, ne = G * Q * Y, ie = Es(a, ne), re = 0, fe = 0; fe < Q; ++fe)
        for (var K = 0; K < G; ++K)
          for (var te = 0; te < Y; ++te)
            ie[re++] = h[_ * K + R * fe + j * te + I];
      Ps(a, ie);
    }
    function Kr(a, h, _, R, j, I) {
      var G;
      if (typeof wt[a] < "u" ? G = wt[a] : G = Bt[a] * Yn[h], I && (G *= 6), j) {
        for (var Q = 0, Y = _; Y >= 1; )
          Q += G * Y * Y, Y /= 2;
        return Q;
      } else
        return G * _ * R;
    }
    function hh(a, h, _, R, j, I, G) {
      var Q = {
        "don't care": Zr,
        "dont care": Zr,
        nice: Kl,
        fast: Ql
      }, Y = {
        repeat: Xl,
        clamp: ho,
        mirror: Hl
      }, ne = {
        nearest: mo,
        linear: Yl
      }, ie = r({
        mipmap: vo,
        "nearest mipmap nearest": po,
        "linear mipmap nearest": hs,
        "nearest mipmap linear": ds,
        "linear mipmap linear": vo
      }, ne), re = {
        none: 0,
        browser: ms
      }, fe = {
        uint8: Wn,
        rgba4: Gl,
        rgb565: Vl,
        "rgb5 a1": Bl
      }, K = {
        alpha: to,
        luminance: pr,
        "luminance alpha": qr,
        rgb: Wr,
        rgba: En,
        rgba4: ns,
        "rgb5 a1": no,
        rgb565: rs
      }, te = {};
      h.ext_srgb && (K.srgb = io, K.srgba = oo), h.oes_texture_float && (fe.float32 = fe.float = vr), h.oes_texture_half_float && (fe.float16 = fe["half float"] = Hn), h.webgl_depth_texture && (r(K, {
        depth: ro,
        "depth stencil": Yr
      }), r(fe, {
        uint16: co,
        uint32: lo,
        "depth stencil": $l
      })), h.webgl_compressed_texture_s3tc && r(te, {
        "rgb s3tc dxt1": ao,
        "rgba s3tc dxt1": so,
        "rgba s3tc dxt3": fo,
        "rgba s3tc dxt5": uo
      }), h.webgl_compressed_texture_atc && r(te, {
        "rgb atc": is,
        "rgba atc explicit alpha": os,
        "rgba atc interpolated alpha": as
      }), h.webgl_compressed_texture_pvrtc && r(te, {
        "rgb pvrtc 4bppv1": ss,
        "rgb pvrtc 2bppv1": fs,
        "rgba pvrtc 4bppv1": us,
        "rgba pvrtc 2bppv1": cs
      }), h.webgl_compressed_texture_etc1 && (te["rgb etc1"] = ls);
      var C = Array.prototype.slice.call(
        a.getParameter(Nl)
      );
      Object.keys(te).forEach(function(x) {
        var M = te[x];
        C.indexOf(M) >= 0 && (K[x] = M);
      });
      var O = Object.keys(K);
      _.textureFormats = O;
      var ee = [];
      Object.keys(K).forEach(function(x) {
        var M = K[x];
        ee[M] = x;
      });
      var ge = [];
      Object.keys(fe).forEach(function(x) {
        var M = fe[x];
        ge[M] = x;
      });
      var W = [];
      Object.keys(ne).forEach(function(x) {
        var M = ne[x];
        W[M] = x;
      });
      var pe = [];
      Object.keys(ie).forEach(function(x) {
        var M = ie[x];
        pe[M] = x;
      });
      var de = [];
      Object.keys(Y).forEach(function(x) {
        var M = Y[x];
        de[M] = x;
      });
      var Pe = O.reduce(function(x, M) {
        var L = K[M];
        return L === pr || L === to || L === pr || L === qr || L === ro || L === Yr || h.ext_srgb && (L === io || L === oo) ? x[L] = L : L === no || M.indexOf("rgba") >= 0 ? x[L] = En : x[L] = Wr, x;
      }, {});
      function U() {
        this.internalformat = En, this.format = En, this.type = Wn, this.compressed = false, this.premultiplyAlpha = false, this.flipY = false, this.unpackAlignment = 1, this.colorSpace = ms, this.width = 0, this.height = 0, this.channels = 0;
      }
      function $(x, M) {
        x.internalformat = M.internalformat, x.format = M.format, x.type = M.type, x.compressed = M.compressed, x.premultiplyAlpha = M.premultiplyAlpha, x.flipY = M.flipY, x.unpackAlignment = M.unpackAlignment, x.colorSpace = M.colorSpace, x.width = M.width, x.height = M.height, x.channels = M.channels;
      }
      function be(x, M) {
        if (!(typeof M != "object" || !M)) {
          if ("premultiplyAlpha" in M && (d.type(
            M.premultiplyAlpha,
            "boolean",
            "invalid premultiplyAlpha"
          ), x.premultiplyAlpha = M.premultiplyAlpha), "flipY" in M && (d.type(
            M.flipY,
            "boolean",
            "invalid texture flip"
          ), x.flipY = M.flipY), "alignment" in M && (d.oneOf(
            M.alignment,
            [1, 2, 4, 8],
            "invalid texture unpack alignment"
          ), x.unpackAlignment = M.alignment), "colorSpace" in M && (d.parameter(
            M.colorSpace,
            re,
            "invalid colorSpace"
          ), x.colorSpace = re[M.colorSpace]), "type" in M) {
            var L = M.type;
            d(
              h.oes_texture_float || !(L === "float" || L === "float32"),
              "you must enable the OES_texture_float extension in order to use floating point textures."
            ), d(
              h.oes_texture_half_float || !(L === "half float" || L === "float16"),
              "you must enable the OES_texture_half_float extension in order to use 16-bit floating point textures."
            ), d(
              h.webgl_depth_texture || !(L === "uint16" || L === "uint32" || L === "depth stencil"),
              "you must enable the WEBGL_depth_texture extension in order to use depth/stencil textures."
            ), d.parameter(
              L,
              fe,
              "invalid texture type"
            ), x.type = fe[L];
          }
          var ye = x.width, Xe = x.height, m = x.channels, c = false;
          "shape" in M ? (d(
            Array.isArray(M.shape) && M.shape.length >= 2,
            "shape must be an array"
          ), ye = M.shape[0], Xe = M.shape[1], M.shape.length === 3 && (m = M.shape[2], d(m > 0 && m <= 4, "invalid number of channels"), c = true), d(ye >= 0 && ye <= _.maxTextureSize, "invalid width"), d(Xe >= 0 && Xe <= _.maxTextureSize, "invalid height")) : ("radius" in M && (ye = Xe = M.radius, d(ye >= 0 && ye <= _.maxTextureSize, "invalid radius")), "width" in M && (ye = M.width, d(ye >= 0 && ye <= _.maxTextureSize, "invalid width")), "height" in M && (Xe = M.height, d(Xe >= 0 && Xe <= _.maxTextureSize, "invalid height")), "channels" in M && (m = M.channels, d(m > 0 && m <= 4, "invalid number of channels"), c = true)), x.width = ye | 0, x.height = Xe | 0, x.channels = m | 0;
          var w = false;
          if ("format" in M) {
            var P = M.format;
            d(
              h.webgl_depth_texture || !(P === "depth" || P === "depth stencil"),
              "you must enable the WEBGL_depth_texture extension in order to use depth/stencil textures."
            ), d.parameter(
              P,
              K,
              "invalid texture format"
            );
            var k = x.internalformat = K[P];
            x.format = Pe[k], P in fe && ("type" in M || (x.type = fe[P])), P in te && (x.compressed = true), w = true;
          }
          !c && w ? x.channels = Bt[x.format] : c && !w ? x.channels !== Qr[x.format] && (x.format = x.internalformat = Qr[x.channels]) : w && c && d(
            x.channels === Bt[x.format],
            "number of channels inconsistent with specified format"
          );
        }
      }
      function ue(x) {
        a.pixelStorei(th, x.flipY), a.pixelStorei(nh, x.premultiplyAlpha), a.pixelStorei(rh, x.colorSpace), a.pixelStorei(eh, x.unpackAlignment);
      }
      function J() {
        U.call(this), this.xOffset = 0, this.yOffset = 0, this.data = null, this.needsFree = false, this.element = null, this.needsCopy = false;
      }
      function ce(x, M) {
        var L = null;
        if (go(M) ? L = M : M && (d.type(M, "object", "invalid pixel data type"), be(x, M), "x" in M && (x.xOffset = M.x | 0), "y" in M && (x.yOffset = M.y | 0), go(M.data) && (L = M.data)), d(
          !x.compressed || L instanceof Uint8Array,
          "compressed texture data must be stored in a uint8array"
        ), M.copy) {
          d(!L, "can not specify copy and data field for the same texture");
          var ye = j.viewportWidth, Xe = j.viewportHeight;
          x.width = x.width || ye - x.xOffset, x.height = x.height || Xe - x.yOffset, x.needsCopy = true, d(
            x.xOffset >= 0 && x.xOffset < ye && x.yOffset >= 0 && x.yOffset < Xe && x.width > 0 && x.width <= ye && x.height > 0 && x.height <= Xe,
            "copy texture read out of bounds"
          );
        } else if (!L)
          x.width = x.width || 1, x.height = x.height || 1, x.channels = x.channels || 4;
        else if (n(L))
          x.channels = x.channels || 4, x.data = L, !("type" in M) && x.type === Wn && (x.type = As(L));
        else if (_s(L))
          x.channels = x.channels || 4, ch(x, L), x.alignment = 1, x.needsFree = true;
        else if (Ut(L)) {
          var m = L.data;
          !Array.isArray(m) && x.type === Wn && (x.type = As(m));
          var c = L.shape, w = L.stride, P, k, A, T, E, v;
          c.length === 3 ? (A = c[2], v = w[2]) : (d(c.length === 2, "invalid ndarray pixel data, must be 2 or 3D"), A = 1, v = 1), P = c[0], k = c[1], T = w[0], E = w[1], x.alignment = 1, x.width = P, x.height = k, x.channels = A, x.format = x.internalformat = Qr[A], x.needsFree = true, lh(x, m, T, E, v, L.offset);
        } else if (ws(L) || Ts(L) || ah(L))
          ws(L) || Ts(L) ? x.element = L : x.element = L.canvas, x.width = x.element.width, x.height = x.element.height, x.channels = 4;
        else if (sh(L))
          x.element = L, x.width = L.width, x.height = L.height, x.channels = 4;
        else if (fh(L))
          x.element = L, x.width = L.naturalWidth, x.height = L.naturalHeight, x.channels = 4;
        else if (uh(L))
          x.element = L, x.width = L.videoWidth, x.height = L.videoHeight, x.channels = 4;
        else if (Ss(L)) {
          var b = x.width || L[0].length, g = x.height || L.length, z = x.channels;
          ot(L[0][0]) ? z = z || L[0][0].length : z = z || 1;
          for (var F = jr.shape(L), V = 1, X = 0; X < F.length; ++X)
            V *= F[X];
          var se = Es(x, V);
          jr.flatten(L, F, "", se), Ps(x, se), x.alignment = 1, x.width = b, x.height = g, x.channels = z, x.format = x.internalformat = Qr[z], x.needsFree = true;
        }
        x.type === vr ? d(
          _.extensions.indexOf("oes_texture_float") >= 0,
          "oes_texture_float extension not enabled"
        ) : x.type === Hn && d(
          _.extensions.indexOf("oes_texture_half_float") >= 0,
          "oes_texture_half_float extension not enabled"
        );
      }
      function le(x, M, L) {
        var ye = x.element, Xe = x.data, m = x.internalformat, c = x.format, w = x.type, P = x.width, k = x.height;
        ue(x), ye ? a.texImage2D(M, L, c, c, w, ye) : x.compressed ? a.compressedTexImage2D(M, L, m, P, k, 0, Xe) : x.needsCopy ? (R(), a.copyTexImage2D(
          M,
          L,
          c,
          x.xOffset,
          x.yOffset,
          P,
          k,
          0
        )) : a.texImage2D(M, L, c, P, k, 0, c, w, Xe || null);
      }
      function Me(x, M, L, ye, Xe) {
        var m = x.element, c = x.data, w = x.internalformat, P = x.format, k = x.type, A = x.width, T = x.height;
        ue(x), m ? a.texSubImage2D(
          M,
          Xe,
          L,
          ye,
          P,
          k,
          m
        ) : x.compressed ? a.compressedTexSubImage2D(
          M,
          Xe,
          L,
          ye,
          w,
          A,
          T,
          c
        ) : x.needsCopy ? (R(), a.copyTexSubImage2D(
          M,
          Xe,
          L,
          ye,
          x.xOffset,
          x.yOffset,
          A,
          T
        )) : a.texSubImage2D(
          M,
          Xe,
          L,
          ye,
          A,
          T,
          P,
          k,
          c
        );
      }
      var De = [];
      function me() {
        return De.pop() || new J();
      }
      function ve(x) {
        x.needsFree && ht.freeType(x.data), J.call(x), De.push(x);
      }
      function Fe() {
        U.call(this), this.genMipmaps = false, this.mipmapHint = Zr, this.mipmask = 0, this.images = Array(16);
      }
      function Oe(x, M, L) {
        var ye = x.images[0] = me();
        x.mipmask = 1, ye.width = x.width = M, ye.height = x.height = L, ye.channels = x.channels = 4;
      }
      function He(x, M) {
        var L = null;
        if (go(M))
          L = x.images[0] = me(), $(L, x), ce(L, M), x.mipmask = 1;
        else if (be(x, M), Array.isArray(M.mipmap))
          for (var ye = M.mipmap, Xe = 0; Xe < ye.length; ++Xe)
            L = x.images[Xe] = me(), $(L, x), L.width >>= Xe, L.height >>= Xe, ce(L, ye[Xe]), x.mipmask |= 1 << Xe;
        else
          L = x.images[0] = me(), $(L, x), ce(L, M), x.mipmask = 1;
        $(x, x.images[0]), x.compressed && (x.internalformat === ao || x.internalformat === so || x.internalformat === fo || x.internalformat === uo) && d(
          x.width % 4 === 0 && x.height % 4 === 0,
          "for compressed texture formats, mipmap level 0 must have width and height that are a multiple of 4"
        );
      }
      function at(x, M) {
        for (var L = x.images, ye = 0; ye < L.length; ++ye) {
          if (!L[ye])
            return;
          le(L[ye], M, ye);
        }
      }
      var ft = [];
      function Ze() {
        var x = ft.pop() || new Fe();
        U.call(x), x.mipmask = 0;
        for (var M = 0; M < 16; ++M)
          x.images[M] = null;
        return x;
      }
      function pt(x) {
        for (var M = x.images, L = 0; L < M.length; ++L)
          M[L] && ve(M[L]), M[L] = null;
        ft.push(x);
      }
      function it() {
        this.minFilter = mo, this.magFilter = mo, this.wrapS = ho, this.wrapT = ho, this.anisotropic = 1, this.genMipmaps = false, this.mipmapHint = Zr;
      }
      function mt(x, M) {
        if ("min" in M) {
          var L = M.min;
          d.parameter(L, ie), x.minFilter = ie[L], ih.indexOf(x.minFilter) >= 0 && !("faces" in M) && (x.genMipmaps = true);
        }
        if ("mag" in M) {
          var ye = M.mag;
          d.parameter(ye, ne), x.magFilter = ne[ye];
        }
        var Xe = x.wrapS, m = x.wrapT;
        if ("wrap" in M) {
          var c = M.wrap;
          typeof c == "string" ? (d.parameter(c, Y), Xe = m = Y[c]) : Array.isArray(c) && (d.parameter(c[0], Y), d.parameter(c[1], Y), Xe = Y[c[0]], m = Y[c[1]]);
        } else {
          if ("wrapS" in M) {
            var w = M.wrapS;
            d.parameter(w, Y), Xe = Y[w];
          }
          if ("wrapT" in M) {
            var P = M.wrapT;
            d.parameter(P, Y), m = Y[P];
          }
        }
        if (x.wrapS = Xe, x.wrapT = m, "anisotropic" in M) {
          var k = M.anisotropic;
          d(
            typeof k == "number" && k >= 1 && k <= _.maxAnisotropic,
            "aniso samples must be between 1 and "
          ), x.anisotropic = M.anisotropic;
        }
        if ("mipmap" in M) {
          var A = false;
          switch (typeof M.mipmap) {
            case "string":
              d.parameter(
                M.mipmap,
                Q,
                "invalid mipmap hint"
              ), x.mipmapHint = Q[M.mipmap], x.genMipmaps = true, A = true;
              break;
            case "boolean":
              A = x.genMipmaps = M.mipmap;
              break;
            case "object":
              d(Array.isArray(M.mipmap), "invalid mipmap type"), x.genMipmaps = false, A = true;
              break;
            default:
              d.raise("invalid mipmap type");
          }
          A && !("min" in M) && (x.minFilter = po);
        }
      }
      function vt(x, M) {
        a.texParameteri(M, ql, x.minFilter), a.texParameteri(M, Wl, x.magFilter), a.texParameteri(M, jl, x.wrapS), a.texParameteri(M, Ul, x.wrapT), h.ext_texture_filter_anisotropic && a.texParameteri(M, Jl, x.anisotropic), x.genMipmaps && (a.hint(Zl, x.mipmapHint), a.generateMipmap(M));
      }
      var gt = 0, _t = {}, Tt = _.maxTextureUnits, ct = Array(Tt).map(function() {
        return null;
      });
      function Ve(x) {
        U.call(this), this.mipmask = 0, this.internalformat = En, this.id = gt++, this.refCount = 1, this.target = x, this.texture = a.createTexture(), this.unit = -1, this.bindCount = 0, this.texInfo = new it(), G.profile && (this.stats = { size: 0 });
      }
      function At(x) {
        a.activeTexture(gr), a.bindTexture(x.target, x.texture);
      }
      function Ke() {
        var x = ct[0];
        x ? a.bindTexture(x.target, x.texture) : a.bindTexture(Zt, null);
      }
      function ke(x) {
        var M = x.texture;
        d(M, "must not double destroy texture");
        var L = x.unit, ye = x.target;
        L >= 0 && (a.activeTexture(gr + L), a.bindTexture(ye, null), ct[L] = null), a.deleteTexture(M), x.texture = null, x.params = null, x.pixels = null, x.refCount = 0, delete _t[x.id], I.textureCount--;
      }
      r(Ve.prototype, {
        bind: function() {
          var x = this;
          x.bindCount += 1;
          var M = x.unit;
          if (M < 0) {
            for (var L = 0; L < Tt; ++L) {
              var ye = ct[L];
              if (ye) {
                if (ye.bindCount > 0)
                  continue;
                ye.unit = -1;
              }
              ct[L] = x, M = L;
              break;
            }
            M >= Tt && d.raise("insufficient number of texture units"), G.profile && I.maxTextureUnits < M + 1 && (I.maxTextureUnits = M + 1), x.unit = M, a.activeTexture(gr + M), a.bindTexture(x.target, x.texture);
          }
          return M;
        },
        unbind: function() {
          this.bindCount -= 1;
        },
        decRef: function() {
          --this.refCount <= 0 && ke(this);
        }
      });
      function We(x, M) {
        var L = new Ve(Zt);
        _t[L.id] = L, I.textureCount++;
        function ye(c, w) {
          var P = L.texInfo;
          it.call(P);
          var k = Ze();
          return typeof c == "number" ? typeof w == "number" ? Oe(k, c | 0, w | 0) : Oe(k, c | 0, c | 0) : c ? (d.type(c, "object", "invalid arguments to regl.texture"), mt(P, c), He(k, c)) : Oe(k, 1, 1), P.genMipmaps && (k.mipmask = (k.width << 1) - 1), L.mipmask = k.mipmask, $(L, k), d.texture2D(P, k, _), L.internalformat = k.internalformat, ye.width = k.width, ye.height = k.height, At(L), at(k, Zt), vt(P, Zt), Ke(), pt(k), G.profile && (L.stats.size = Kr(
            L.internalformat,
            L.type,
            k.width,
            k.height,
            P.genMipmaps,
            false
          )), ye.format = ee[L.internalformat], ye.type = ge[L.type], ye.mag = W[P.magFilter], ye.min = pe[P.minFilter], ye.wrapS = de[P.wrapS], ye.wrapT = de[P.wrapT], ye;
        }
        function Xe(c, w, P, k) {
          d(!!c, "must specify image data");
          var A = w | 0, T = P | 0, E = k | 0, v = me();
          return $(v, L), v.width = 0, v.height = 0, ce(v, c), v.width = v.width || (L.width >> E) - A, v.height = v.height || (L.height >> E) - T, d(
            L.type === v.type && L.format === v.format && L.internalformat === v.internalformat,
            "incompatible format for texture.subimage"
          ), d(
            A >= 0 && T >= 0 && A + v.width <= L.width && T + v.height <= L.height,
            "texture.subimage write out of bounds"
          ), d(
            L.mipmask & 1 << E,
            "missing mipmap data"
          ), d(
            v.data || v.element || v.needsCopy,
            "missing image data"
          ), At(L), Me(v, Zt, A, T, E), Ke(), ve(v), ye;
        }
        function m(c, w) {
          var P = c | 0, k = w | 0 || P;
          if (P === L.width && k === L.height)
            return ye;
          ye.width = L.width = P, ye.height = L.height = k, At(L);
          for (var A = 0; L.mipmask >> A; ++A) {
            var T = P >> A, E = k >> A;
            if (!T || !E) break;
            a.texImage2D(
              Zt,
              A,
              L.format,
              T,
              E,
              0,
              L.format,
              L.type,
              null
            );
          }
          return Ke(), G.profile && (L.stats.size = Kr(
            L.internalformat,
            L.type,
            P,
            k,
            false,
            false
          )), ye;
        }
        return ye(x, M), ye.subimage = Xe, ye.resize = m, ye._reglType = "texture2d", ye._texture = L, G.profile && (ye.stats = L.stats), ye.destroy = function() {
          L.decRef();
        }, ye;
      }
      function Qe(x, M, L, ye, Xe, m) {
        var c = new Ve(eo);
        _t[c.id] = c, I.cubeCount++;
        var w = new Array(6);
        function P(T, E, v, b, g, z) {
          var F, V = c.texInfo;
          for (it.call(V), F = 0; F < 6; ++F)
            w[F] = Ze();
          if (typeof T == "number" || !T) {
            var X = T | 0 || 1;
            for (F = 0; F < 6; ++F)
              Oe(w[F], X, X);
          } else if (typeof T == "object")
            if (E)
              He(w[0], T), He(w[1], E), He(w[2], v), He(w[3], b), He(w[4], g), He(w[5], z);
            else if (mt(V, T), be(c, T), "faces" in T) {
              var se = T.faces;
              for (d(
                Array.isArray(se) && se.length === 6,
                "cube faces must be a length 6 array"
              ), F = 0; F < 6; ++F)
                d(
                  typeof se[F] == "object" && !!se[F],
                  "invalid input for cube map face"
                ), $(w[F], c), He(w[F], se[F]);
            } else
              for (F = 0; F < 6; ++F)
                He(w[F], T);
          else
            d.raise("invalid arguments to cube map");
          for ($(c, w[0]), d.optional(function() {
            _.npotTextureCube || d(ts(c.width) && ts(c.height), "your browser does not support non power or two texture dimensions");
          }), V.genMipmaps ? c.mipmask = (w[0].width << 1) - 1 : c.mipmask = w[0].mipmask, d.textureCube(c, V, w, _), c.internalformat = w[0].internalformat, P.width = w[0].width, P.height = w[0].height, At(c), F = 0; F < 6; ++F)
            at(w[F], Hr + F);
          for (vt(V, eo), Ke(), G.profile && (c.stats.size = Kr(
            c.internalformat,
            c.type,
            P.width,
            P.height,
            V.genMipmaps,
            true
          )), P.format = ee[c.internalformat], P.type = ge[c.type], P.mag = W[V.magFilter], P.min = pe[V.minFilter], P.wrapS = de[V.wrapS], P.wrapT = de[V.wrapT], F = 0; F < 6; ++F)
            pt(w[F]);
          return P;
        }
        function k(T, E, v, b, g) {
          d(!!E, "must specify image data"), d(typeof T == "number" && T === (T | 0) && T >= 0 && T < 6, "invalid face");
          var z = v | 0, F = b | 0, V = g | 0, X = me();
          return $(X, c), X.width = 0, X.height = 0, ce(X, E), X.width = X.width || (c.width >> V) - z, X.height = X.height || (c.height >> V) - F, d(
            c.type === X.type && c.format === X.format && c.internalformat === X.internalformat,
            "incompatible format for texture.subimage"
          ), d(
            z >= 0 && F >= 0 && z + X.width <= c.width && F + X.height <= c.height,
            "texture.subimage write out of bounds"
          ), d(
            c.mipmask & 1 << V,
            "missing mipmap data"
          ), d(
            X.data || X.element || X.needsCopy,
            "missing image data"
          ), At(c), Me(X, Hr + T, z, F, V), Ke(), ve(X), P;
        }
        function A(T) {
          var E = T | 0;
          if (E !== c.width) {
            P.width = c.width = E, P.height = c.height = E, At(c);
            for (var v = 0; v < 6; ++v)
              for (var b = 0; c.mipmask >> b; ++b)
                a.texImage2D(
                  Hr + v,
                  b,
                  c.format,
                  E >> b,
                  E >> b,
                  0,
                  c.format,
                  c.type,
                  null
                );
            return Ke(), G.profile && (c.stats.size = Kr(
              c.internalformat,
              c.type,
              P.width,
              P.height,
              false,
              true
            )), P;
          }
        }
        return P(x, M, L, ye, Xe, m), P.subimage = k, P.resize = A, P._reglType = "textureCube", P._texture = c, G.profile && (P.stats = c.stats), P.destroy = function() {
          c.decRef();
        }, P;
      }
      function lt() {
        for (var x = 0; x < Tt; ++x)
          a.activeTexture(gr + x), a.bindTexture(Zt, null), ct[x] = null;
        Mt(_t).forEach(ke), I.cubeCount = 0, I.textureCount = 0;
      }
      G.profile && (I.getTotalTextureSize = function() {
        var x = 0;
        return Object.keys(_t).forEach(function(M) {
          x += _t[M].stats.size;
        }), x;
      });
      function Kt() {
        for (var x = 0; x < Tt; ++x) {
          var M = ct[x];
          M && (M.bindCount = 0, M.unit = -1, ct[x] = null);
        }
        Mt(_t).forEach(function(L) {
          L.texture = a.createTexture(), a.bindTexture(L.target, L.texture);
          for (var ye = 0; ye < 32; ++ye)
            if (L.mipmask & 1 << ye)
              if (L.target === Zt)
                a.texImage2D(
                  Zt,
                  ye,
                  L.internalformat,
                  L.width >> ye,
                  L.height >> ye,
                  0,
                  L.internalformat,
                  L.type,
                  null
                );
              else
                for (var Xe = 0; Xe < 6; ++Xe)
                  a.texImage2D(
                    Hr + Xe,
                    ye,
                    L.internalformat,
                    L.width >> ye,
                    L.height >> ye,
                    0,
                    L.internalformat,
                    L.type,
                    null
                  );
          vt(L.texInfo, L.target);
        });
      }
      function Rn() {
        for (var x = 0; x < Tt; ++x) {
          var M = ct[x];
          M && (M.bindCount = 0, M.unit = -1, ct[x] = null), a.activeTexture(gr + x), a.bindTexture(Zt, null), a.bindTexture(eo, null);
        }
      }
      return {
        create2D: We,
        createCube: Qe,
        clear: lt,
        getTexture: function(x) {
          return null;
        },
        restore: Kt,
        refresh: Rn
      };
    }
    var vn = 36161, Jr = 32854, Cs = 32855, ks = 36194, Fs = 33189, zs = 36168, Ls = 34041, Is = 35907, Rs = 34836, Ds = 34842, Ms = 34843, Xt = [];
    Xt[Jr] = 2, Xt[Cs] = 2, Xt[ks] = 2, Xt[Fs] = 2, Xt[zs] = 1, Xt[Ls] = 4, Xt[Is] = 4, Xt[Rs] = 16, Xt[Ds] = 8, Xt[Ms] = 6;
    function Os(a, h, _) {
      return Xt[a] * h * _;
    }
    var dh = function(a, h, _, R, j) {
      var I = {
        rgba4: Jr,
        rgb565: ks,
        "rgb5 a1": Cs,
        depth: Fs,
        stencil: zs,
        "depth stencil": Ls
      };
      h.ext_srgb && (I.srgba = Is), h.ext_color_buffer_half_float && (I.rgba16f = Ds, I.rgb16f = Ms), h.webgl_color_buffer_float && (I.rgba32f = Rs);
      var G = [];
      Object.keys(I).forEach(function(K) {
        var te = I[K];
        G[te] = K;
      });
      var Q = 0, Y = {};
      function ne(K) {
        this.id = Q++, this.refCount = 1, this.renderbuffer = K, this.format = Jr, this.width = 0, this.height = 0, j.profile && (this.stats = { size: 0 });
      }
      ne.prototype.decRef = function() {
        --this.refCount <= 0 && ie(this);
      };
      function ie(K) {
        var te = K.renderbuffer;
        d(te, "must not double destroy renderbuffer"), a.bindRenderbuffer(vn, null), a.deleteRenderbuffer(te), K.renderbuffer = null, K.refCount = 0, delete Y[K.id], R.renderbufferCount--;
      }
      function re(K, te) {
        var C = new ne(a.createRenderbuffer());
        Y[C.id] = C, R.renderbufferCount++;
        function O(ge, W) {
          var pe = 0, de = 0, Pe = Jr;
          if (typeof ge == "object" && ge) {
            var U = ge;
            if ("shape" in U) {
              var $ = U.shape;
              d(
                Array.isArray($) && $.length >= 2,
                "invalid renderbuffer shape"
              ), pe = $[0] | 0, de = $[1] | 0;
            } else
              "radius" in U && (pe = de = U.radius | 0), "width" in U && (pe = U.width | 0), "height" in U && (de = U.height | 0);
            "format" in U && (d.parameter(
              U.format,
              I,
              "invalid renderbuffer format"
            ), Pe = I[U.format]);
          } else typeof ge == "number" ? (pe = ge | 0, typeof W == "number" ? de = W | 0 : de = pe) : ge ? d.raise("invalid arguments to renderbuffer constructor") : pe = de = 1;
          if (d(
            pe > 0 && de > 0 && pe <= _.maxRenderbufferSize && de <= _.maxRenderbufferSize,
            "invalid renderbuffer size"
          ), !(pe === C.width && de === C.height && Pe === C.format))
            return O.width = C.width = pe, O.height = C.height = de, C.format = Pe, a.bindRenderbuffer(vn, C.renderbuffer), a.renderbufferStorage(vn, Pe, pe, de), d(
              a.getError() === 0,
              "invalid render buffer format"
            ), j.profile && (C.stats.size = Os(C.format, C.width, C.height)), O.format = G[C.format], O;
        }
        function ee(ge, W) {
          var pe = ge | 0, de = W | 0 || pe;
          return pe === C.width && de === C.height || (d(
            pe > 0 && de > 0 && pe <= _.maxRenderbufferSize && de <= _.maxRenderbufferSize,
            "invalid renderbuffer size"
          ), O.width = C.width = pe, O.height = C.height = de, a.bindRenderbuffer(vn, C.renderbuffer), a.renderbufferStorage(vn, C.format, pe, de), d(
            a.getError() === 0,
            "invalid render buffer format"
          ), j.profile && (C.stats.size = Os(
            C.format,
            C.width,
            C.height
          ))), O;
        }
        return O(K, te), O.resize = ee, O._reglType = "renderbuffer", O._renderbuffer = C, j.profile && (O.stats = C.stats), O.destroy = function() {
          C.decRef();
        }, O;
      }
      j.profile && (R.getTotalRenderbufferSize = function() {
        var K = 0;
        return Object.keys(Y).forEach(function(te) {
          K += Y[te].stats.size;
        }), K;
      });
      function fe() {
        Mt(Y).forEach(function(K) {
          K.renderbuffer = a.createRenderbuffer(), a.bindRenderbuffer(vn, K.renderbuffer), a.renderbufferStorage(vn, K.format, K.width, K.height);
        }), a.bindRenderbuffer(vn, null);
      }
      return {
        create: re,
        clear: function() {
          Mt(Y).forEach(ie);
        },
        restore: fe
      };
    }, on = 36160, xo = 36161, Cn = 3553, ei = 34069, Ns = 36064, Gs = 36096, Bs = 36128, Vs = 33306, $s = 36053, mh = 36054, ph = 36055, vh = 36057, gh = 36061, xh = 36193, yh = 5121, bh = 5126, js = 6407, Us = 6408, _h = 6402, Sh = [
      js,
      Us
    ], yo = [];
    yo[Us] = 4, yo[js] = 3;
    var ti = [];
    ti[yh] = 1, ti[bh] = 4, ti[xh] = 2;
    var wh = 32854, Th = 32855, Ah = 36194, Eh = 33189, Ph = 36168, Xs = 34041, Ch = 35907, kh = 34836, Fh = 34842, zh = 34843, Lh = [
      wh,
      Th,
      Ah,
      Ch,
      Fh,
      zh,
      kh
    ], Zn = {};
    Zn[$s] = "complete", Zn[mh] = "incomplete attachment", Zn[vh] = "incomplete dimensions", Zn[ph] = "incomplete, missing attachment", Zn[gh] = "unsupported";
    function Ih(a, h, _, R, j, I) {
      var G = {
        cur: null,
        next: null,
        dirty: false,
        setFBO: null
      }, Q = ["rgba"], Y = ["rgba4", "rgb565", "rgb5 a1"];
      h.ext_srgb && Y.push("srgba"), h.ext_color_buffer_half_float && Y.push("rgba16f", "rgb16f"), h.webgl_color_buffer_float && Y.push("rgba32f");
      var ne = ["uint8"];
      h.oes_texture_half_float && ne.push("half float", "float16"), h.oes_texture_float && ne.push("float", "float32");
      function ie(J, ce, le) {
        this.target = J, this.texture = ce, this.renderbuffer = le;
        var Me = 0, De = 0;
        ce ? (Me = ce.width, De = ce.height) : le && (Me = le.width, De = le.height), this.width = Me, this.height = De;
      }
      function re(J) {
        J && (J.texture && J.texture._texture.decRef(), J.renderbuffer && J.renderbuffer._renderbuffer.decRef());
      }
      function fe(J, ce, le) {
        if (J)
          if (J.texture) {
            var Me = J.texture._texture, De = Math.max(1, Me.width), me = Math.max(1, Me.height);
            d(
              De === ce && me === le,
              "inconsistent width/height for supplied texture"
            ), Me.refCount += 1;
          } else {
            var ve = J.renderbuffer._renderbuffer;
            d(
              ve.width === ce && ve.height === le,
              "inconsistent width/height for renderbuffer"
            ), ve.refCount += 1;
          }
      }
      function K(J, ce) {
        ce && (ce.texture ? a.framebufferTexture2D(
          on,
          J,
          ce.target,
          ce.texture._texture.texture,
          0
        ) : a.framebufferRenderbuffer(
          on,
          J,
          xo,
          ce.renderbuffer._renderbuffer.renderbuffer
        ));
      }
      function te(J) {
        var ce = Cn, le = null, Me = null, De = J;
        typeof J == "object" && (De = J.data, "target" in J && (ce = J.target | 0)), d.type(De, "function", "invalid attachment data");
        var me = De._reglType;
        return me === "texture2d" ? (le = De, d(ce === Cn)) : me === "textureCube" ? (le = De, d(
          ce >= ei && ce < ei + 6,
          "invalid cube map target"
        )) : me === "renderbuffer" ? (Me = De, ce = xo) : d.raise("invalid regl object for attachment"), new ie(ce, le, Me);
      }
      function C(J, ce, le, Me, De) {
        if (le) {
          var me = R.create2D({
            width: J,
            height: ce,
            format: Me,
            type: De
          });
          return me._texture.refCount = 0, new ie(Cn, me, null);
        } else {
          var ve = j.create({
            width: J,
            height: ce,
            format: Me
          });
          return ve._renderbuffer.refCount = 0, new ie(xo, null, ve);
        }
      }
      function O(J) {
        return J && (J.texture || J.renderbuffer);
      }
      function ee(J, ce, le) {
        J && (J.texture ? J.texture.resize(ce, le) : J.renderbuffer && J.renderbuffer.resize(ce, le), J.width = ce, J.height = le);
      }
      var ge = 0, W = {};
      function pe() {
        this.id = ge++, W[this.id] = this, this.framebuffer = a.createFramebuffer(), this.width = 0, this.height = 0, this.colorAttachments = [], this.depthAttachment = null, this.stencilAttachment = null, this.depthStencilAttachment = null;
      }
      function de(J) {
        J.colorAttachments.forEach(re), re(J.depthAttachment), re(J.stencilAttachment), re(J.depthStencilAttachment);
      }
      function Pe(J) {
        var ce = J.framebuffer;
        d(ce, "must not double destroy framebuffer"), a.deleteFramebuffer(ce), J.framebuffer = null, I.framebufferCount--, delete W[J.id];
      }
      function U(J) {
        var ce;
        a.bindFramebuffer(on, J.framebuffer);
        var le = J.colorAttachments;
        for (ce = 0; ce < le.length; ++ce)
          K(Ns + ce, le[ce]);
        for (ce = le.length; ce < _.maxColorAttachments; ++ce)
          a.framebufferTexture2D(
            on,
            Ns + ce,
            Cn,
            null,
            0
          );
        a.framebufferTexture2D(
          on,
          Vs,
          Cn,
          null,
          0
        ), a.framebufferTexture2D(
          on,
          Gs,
          Cn,
          null,
          0
        ), a.framebufferTexture2D(
          on,
          Bs,
          Cn,
          null,
          0
        ), K(Gs, J.depthAttachment), K(Bs, J.stencilAttachment), K(Vs, J.depthStencilAttachment);
        var Me = a.checkFramebufferStatus(on);
        !a.isContextLost() && Me !== $s && d.raise("framebuffer configuration not supported, status = " + Zn[Me]), a.bindFramebuffer(on, G.next ? G.next.framebuffer : null), G.cur = G.next, a.getError();
      }
      function $(J, ce) {
        var le = new pe();
        I.framebufferCount++;
        function Me(me, ve) {
          var Fe;
          d(
            G.next !== le,
            "can not update framebuffer which is currently in use"
          );
          var Oe = 0, He = 0, at = true, ft = true, Ze = null, pt = true, it = "rgba", mt = "uint8", vt = 1, gt = null, _t = null, Tt = null, ct = false;
          if (typeof me == "number")
            Oe = me | 0, He = ve | 0 || Oe;
          else if (!me)
            Oe = He = 1;
          else {
            d.type(me, "object", "invalid arguments for framebuffer");
            var Ve = me;
            if ("shape" in Ve) {
              var At = Ve.shape;
              d(
                Array.isArray(At) && At.length >= 2,
                "invalid shape for framebuffer"
              ), Oe = At[0], He = At[1];
            } else
              "radius" in Ve && (Oe = He = Ve.radius), "width" in Ve && (Oe = Ve.width), "height" in Ve && (He = Ve.height);
            ("color" in Ve || "colors" in Ve) && (Ze = Ve.color || Ve.colors, Array.isArray(Ze) && d(
              Ze.length === 1 || h.webgl_draw_buffers,
              "multiple render targets not supported"
            )), Ze || ("colorCount" in Ve && (vt = Ve.colorCount | 0, d(vt > 0, "invalid color buffer count")), "colorTexture" in Ve && (pt = !!Ve.colorTexture, it = "rgba4"), "colorType" in Ve && (mt = Ve.colorType, pt ? (d(
              h.oes_texture_float || !(mt === "float" || mt === "float32"),
              "you must enable OES_texture_float in order to use floating point framebuffer objects"
            ), d(
              h.oes_texture_half_float || !(mt === "half float" || mt === "float16"),
              "you must enable OES_texture_half_float in order to use 16-bit floating point framebuffer objects"
            )) : mt === "half float" || mt === "float16" ? (d(
              h.ext_color_buffer_half_float,
              "you must enable EXT_color_buffer_half_float to use 16-bit render buffers"
            ), it = "rgba16f") : (mt === "float" || mt === "float32") && (d(
              h.webgl_color_buffer_float,
              "you must enable WEBGL_color_buffer_float in order to use 32-bit floating point renderbuffers"
            ), it = "rgba32f"), d.oneOf(mt, ne, "invalid color type")), "colorFormat" in Ve && (it = Ve.colorFormat, Q.indexOf(it) >= 0 ? pt = true : Y.indexOf(it) >= 0 ? pt = false : d.optional(function() {
              pt ? d.oneOf(
                Ve.colorFormat,
                Q,
                "invalid color format for texture"
              ) : d.oneOf(
                Ve.colorFormat,
                Y,
                "invalid color format for renderbuffer"
              );
            }))), ("depthTexture" in Ve || "depthStencilTexture" in Ve) && (ct = !!(Ve.depthTexture || Ve.depthStencilTexture), d(
              !ct || h.webgl_depth_texture,
              "webgl_depth_texture extension not supported"
            )), "depth" in Ve && (typeof Ve.depth == "boolean" ? at = Ve.depth : (gt = Ve.depth, ft = false)), "stencil" in Ve && (typeof Ve.stencil == "boolean" ? ft = Ve.stencil : (_t = Ve.stencil, at = false)), "depthStencil" in Ve && (typeof Ve.depthStencil == "boolean" ? at = ft = Ve.depthStencil : (Tt = Ve.depthStencil, at = false, ft = false));
          }
          var Ke = null, ke = null, We = null, Qe = null;
          if (Array.isArray(Ze))
            Ke = Ze.map(te);
          else if (Ze)
            Ke = [te(Ze)];
          else
            for (Ke = new Array(vt), Fe = 0; Fe < vt; ++Fe)
              Ke[Fe] = C(
                Oe,
                He,
                pt,
                it,
                mt
              );
          d(
            h.webgl_draw_buffers || Ke.length <= 1,
            "you must enable the WEBGL_draw_buffers extension in order to use multiple color buffers."
          ), d(
            Ke.length <= _.maxColorAttachments,
            "too many color attachments, not supported"
          ), Oe = Oe || Ke[0].width, He = He || Ke[0].height, gt ? ke = te(gt) : at && !ft && (ke = C(
            Oe,
            He,
            ct,
            "depth",
            "uint32"
          )), _t ? We = te(_t) : ft && !at && (We = C(
            Oe,
            He,
            false,
            "stencil",
            "uint8"
          )), Tt ? Qe = te(Tt) : !gt && !_t && ft && at && (Qe = C(
            Oe,
            He,
            ct,
            "depth stencil",
            "depth stencil"
          )), d(
            !!gt + !!_t + !!Tt <= 1,
            "invalid framebuffer configuration, can specify exactly one depth/stencil attachment"
          );
          var lt = null;
          for (Fe = 0; Fe < Ke.length; ++Fe)
            if (fe(Ke[Fe], Oe, He), d(
              !Ke[Fe] || Ke[Fe].texture && Sh.indexOf(Ke[Fe].texture._texture.format) >= 0 || Ke[Fe].renderbuffer && Lh.indexOf(Ke[Fe].renderbuffer._renderbuffer.format) >= 0,
              "framebuffer color attachment " + Fe + " is invalid"
            ), Ke[Fe] && Ke[Fe].texture) {
              var Kt = yo[Ke[Fe].texture._texture.format] * ti[Ke[Fe].texture._texture.type];
              lt === null ? lt = Kt : d(
                lt === Kt,
                "all color attachments much have the same number of bits per pixel."
              );
            }
          return fe(ke, Oe, He), d(
            !ke || ke.texture && ke.texture._texture.format === _h || ke.renderbuffer && ke.renderbuffer._renderbuffer.format === Eh,
            "invalid depth attachment for framebuffer object"
          ), fe(We, Oe, He), d(
            !We || We.renderbuffer && We.renderbuffer._renderbuffer.format === Ph,
            "invalid stencil attachment for framebuffer object"
          ), fe(Qe, Oe, He), d(
            !Qe || Qe.texture && Qe.texture._texture.format === Xs || Qe.renderbuffer && Qe.renderbuffer._renderbuffer.format === Xs,
            "invalid depth-stencil attachment for framebuffer object"
          ), de(le), le.width = Oe, le.height = He, le.colorAttachments = Ke, le.depthAttachment = ke, le.stencilAttachment = We, le.depthStencilAttachment = Qe, Me.color = Ke.map(O), Me.depth = O(ke), Me.stencil = O(We), Me.depthStencil = O(Qe), Me.width = le.width, Me.height = le.height, U(le), Me;
        }
        function De(me, ve) {
          d(
            G.next !== le,
            "can not resize a framebuffer which is currently in use"
          );
          var Fe = Math.max(me | 0, 1), Oe = Math.max(ve | 0 || Fe, 1);
          if (Fe === le.width && Oe === le.height)
            return Me;
          for (var He = le.colorAttachments, at = 0; at < He.length; ++at)
            ee(He[at], Fe, Oe);
          return ee(le.depthAttachment, Fe, Oe), ee(le.stencilAttachment, Fe, Oe), ee(le.depthStencilAttachment, Fe, Oe), le.width = Me.width = Fe, le.height = Me.height = Oe, U(le), Me;
        }
        return Me(J, ce), r(Me, {
          resize: De,
          _reglType: "framebuffer",
          _framebuffer: le,
          destroy: function() {
            Pe(le), de(le);
          },
          use: function(me) {
            G.setFBO({
              framebuffer: Me
            }, me);
          }
        });
      }
      function be(J) {
        var ce = Array(6);
        function le(De) {
          var me;
          d(
            ce.indexOf(G.next) < 0,
            "can not update framebuffer which is currently in use"
          );
          var ve = {
            color: null
          }, Fe = 0, Oe = null, He = "rgba", at = "uint8", ft = 1;
          if (typeof De == "number")
            Fe = De | 0;
          else if (!De)
            Fe = 1;
          else {
            d.type(De, "object", "invalid arguments for framebuffer");
            var Ze = De;
            if ("shape" in Ze) {
              var pt = Ze.shape;
              d(
                Array.isArray(pt) && pt.length >= 2,
                "invalid shape for framebuffer"
              ), d(
                pt[0] === pt[1],
                "cube framebuffer must be square"
              ), Fe = pt[0];
            } else
              "radius" in Ze && (Fe = Ze.radius | 0), "width" in Ze ? (Fe = Ze.width | 0, "height" in Ze && d(Ze.height === Fe, "must be square")) : "height" in Ze && (Fe = Ze.height | 0);
            ("color" in Ze || "colors" in Ze) && (Oe = Ze.color || Ze.colors, Array.isArray(Oe) && d(
              Oe.length === 1 || h.webgl_draw_buffers,
              "multiple render targets not supported"
            )), Oe || ("colorCount" in Ze && (ft = Ze.colorCount | 0, d(ft > 0, "invalid color buffer count")), "colorType" in Ze && (d.oneOf(
              Ze.colorType,
              ne,
              "invalid color type"
            ), at = Ze.colorType), "colorFormat" in Ze && (He = Ze.colorFormat, d.oneOf(
              Ze.colorFormat,
              Q,
              "invalid color format for texture"
            ))), "depth" in Ze && (ve.depth = Ze.depth), "stencil" in Ze && (ve.stencil = Ze.stencil), "depthStencil" in Ze && (ve.depthStencil = Ze.depthStencil);
          }
          var it;
          if (Oe)
            if (Array.isArray(Oe))
              for (it = [], me = 0; me < Oe.length; ++me)
                it[me] = Oe[me];
            else
              it = [Oe];
          else {
            it = Array(ft);
            var mt = {
              radius: Fe,
              format: He,
              type: at
            };
            for (me = 0; me < ft; ++me)
              it[me] = R.createCube(mt);
          }
          for (ve.color = Array(it.length), me = 0; me < it.length; ++me) {
            var vt = it[me];
            d(
              typeof vt == "function" && vt._reglType === "textureCube",
              "invalid cube map"
            ), Fe = Fe || vt.width, d(
              vt.width === Fe && vt.height === Fe,
              "invalid cube map shape"
            ), ve.color[me] = {
              target: ei,
              data: it[me]
            };
          }
          for (me = 0; me < 6; ++me) {
            for (var gt = 0; gt < it.length; ++gt)
              ve.color[gt].target = ei + me;
            me > 0 && (ve.depth = ce[0].depth, ve.stencil = ce[0].stencil, ve.depthStencil = ce[0].depthStencil), ce[me] ? ce[me](ve) : ce[me] = $(ve);
          }
          return r(le, {
            width: Fe,
            height: Fe,
            color: it
          });
        }
        function Me(De) {
          var me, ve = De | 0;
          if (d(
            ve > 0 && ve <= _.maxCubeMapSize,
            "invalid radius for cube fbo"
          ), ve === le.width)
            return le;
          var Fe = le.color;
          for (me = 0; me < Fe.length; ++me)
            Fe[me].resize(ve);
          for (me = 0; me < 6; ++me)
            ce[me].resize(ve);
          return le.width = le.height = ve, le;
        }
        return le(J), r(le, {
          faces: ce,
          resize: Me,
          _reglType: "framebufferCube",
          destroy: function() {
            ce.forEach(function(De) {
              De.destroy();
            });
          }
        });
      }
      function ue() {
        G.cur = null, G.next = null, G.dirty = true, Mt(W).forEach(function(J) {
          J.framebuffer = a.createFramebuffer(), U(J);
        });
      }
      return r(G, {
        getFramebuffer: function(J) {
          if (typeof J == "function" && J._reglType === "framebuffer") {
            var ce = J._framebuffer;
            if (ce instanceof pe)
              return ce;
          }
          return null;
        },
        create: $,
        createCube: be,
        clear: function() {
          Mt(W).forEach(Pe);
        },
        restore: ue
      });
    }
    var Rh = 5126, Hs = 34962, ni = 34963, Ws = [
      "attributes",
      "elements",
      "offset",
      "count",
      "primitive",
      "instances"
    ];
    function bo() {
      this.state = 0, this.x = 0, this.y = 0, this.z = 0, this.w = 0, this.buffer = null, this.size = 0, this.normalized = false, this.type = Rh, this.offset = 0, this.stride = 0, this.divisor = 0;
    }
    function Dh(a, h, _, R, j, I, G) {
      for (var Q = _.maxAttributes, Y = new Array(Q), ne = 0; ne < Q; ++ne)
        Y[ne] = new bo();
      var ie = 0, re = {}, fe = {
        Record: bo,
        scope: {},
        state: Y,
        currentVAO: null,
        targetVAO: null,
        restore: te() ? de : function() {
        },
        createVAO: Pe,
        getVAO: O,
        destroyBuffer: K,
        setVAO: te() ? ee : ge,
        clear: te() ? W : function() {
        }
      };
      function K(U) {
        for (var $ = 0; $ < Y.length; ++$) {
          var be = Y[$];
          be.buffer === U && (a.disableVertexAttribArray($), be.buffer = null);
        }
      }
      function te() {
        return h.oes_vertex_array_object;
      }
      function C() {
        return h.angle_instanced_arrays;
      }
      function O(U) {
        return typeof U == "function" && U._vao ? U._vao : null;
      }
      function ee(U) {
        if (U !== fe.currentVAO) {
          var $ = te();
          U ? $.bindVertexArrayOES(U.vao) : $.bindVertexArrayOES(null), fe.currentVAO = U;
        }
      }
      function ge(U) {
        if (U !== fe.currentVAO) {
          if (U)
            U.bindAttrs();
          else {
            for (var $ = C(), be = 0; be < Y.length; ++be) {
              var ue = Y[be];
              ue.buffer ? (a.enableVertexAttribArray(be), ue.buffer.bind(), a.vertexAttribPointer(be, ue.size, ue.type, ue.normalized, ue.stride, ue.offfset), $ && ue.divisor && $.vertexAttribDivisorANGLE(be, ue.divisor)) : (a.disableVertexAttribArray(be), a.vertexAttrib4f(be, ue.x, ue.y, ue.z, ue.w));
            }
            G.elements ? a.bindBuffer(ni, G.elements.buffer.buffer) : a.bindBuffer(ni, null);
          }
          fe.currentVAO = U;
        }
      }
      function W() {
        Mt(re).forEach(function(U) {
          U.destroy();
        });
      }
      function pe() {
        this.id = ++ie, this.attributes = [], this.elements = null, this.ownsElements = false, this.count = 0, this.offset = 0, this.instances = -1, this.primitive = 4;
        var U = te();
        U ? this.vao = U.createVertexArrayOES() : this.vao = null, re[this.id] = this, this.buffers = [];
      }
      pe.prototype.bindAttrs = function() {
        for (var U = C(), $ = this.attributes, be = 0; be < $.length; ++be) {
          var ue = $[be];
          ue.buffer ? (a.enableVertexAttribArray(be), a.bindBuffer(Hs, ue.buffer.buffer), a.vertexAttribPointer(be, ue.size, ue.type, ue.normalized, ue.stride, ue.offset), U && ue.divisor && U.vertexAttribDivisorANGLE(be, ue.divisor)) : (a.disableVertexAttribArray(be), a.vertexAttrib4f(be, ue.x, ue.y, ue.z, ue.w));
        }
        for (var J = $.length; J < Q; ++J)
          a.disableVertexAttribArray(J);
        var ce = I.getElements(this.elements);
        ce ? a.bindBuffer(ni, ce.buffer.buffer) : a.bindBuffer(ni, null);
      }, pe.prototype.refresh = function() {
        var U = te();
        U && (U.bindVertexArrayOES(this.vao), this.bindAttrs(), fe.currentVAO = null, U.bindVertexArrayOES(null));
      }, pe.prototype.destroy = function() {
        if (this.vao) {
          var U = te();
          this === fe.currentVAO && (fe.currentVAO = null, U.bindVertexArrayOES(null)), U.deleteVertexArrayOES(this.vao), this.vao = null;
        }
        this.ownsElements && (this.elements.destroy(), this.elements = null, this.ownsElements = false), re[this.id] && (delete re[this.id], R.vaoCount -= 1);
      };
      function de() {
        var U = te();
        U && Mt(re).forEach(function($) {
          $.refresh();
        });
      }
      function Pe(U) {
        var $ = new pe();
        R.vaoCount += 1;
        function be(ue) {
          var J;
          if (Array.isArray(ue))
            J = ue, $.elements && $.ownsElements && $.elements.destroy(), $.elements = null, $.ownsElements = false, $.offset = 0, $.count = 0, $.instances = -1, $.primitive = 4;
          else {
            if (d(typeof ue == "object", "invalid arguments for create vao"), d("attributes" in ue, "must specify attributes for vao"), ue.elements) {
              var ce = ue.elements;
              $.ownsElements ? typeof ce == "function" && ce._reglType === "elements" ? ($.elements.destroy(), $.ownsElements = false) : ($.elements(ce), $.ownsElements = false) : I.getElements(ue.elements) ? ($.elements = ue.elements, $.ownsElements = false) : ($.elements = I.create(ue.elements), $.ownsElements = true);
            } else
              $.elements = null, $.ownsElements = false;
            J = ue.attributes, $.offset = 0, $.count = -1, $.instances = -1, $.primitive = 4, $.elements && ($.count = $.elements._elements.vertCount, $.primitive = $.elements._elements.primType), "offset" in ue && ($.offset = ue.offset | 0), "count" in ue && ($.count = ue.count | 0), "instances" in ue && ($.instances = ue.instances | 0), "primitive" in ue && (d(ue.primitive in pn, "bad primitive type: " + ue.primitive), $.primitive = pn[ue.primitive]), d.optional(() => {
              for (var at = Object.keys(ue), ft = 0; ft < at.length; ++ft)
                d(Ws.indexOf(at[ft]) >= 0, 'invalid option for vao: "' + at[ft] + '" valid options are ' + Ws);
            }), d(Array.isArray(J), "attributes must be an array");
          }
          d(J.length < Q, "too many attributes"), d(J.length > 0, "must specify at least one attribute");
          var le = {}, Me = $.attributes;
          Me.length = J.length;
          for (var De = 0; De < J.length; ++De) {
            var me = J[De], ve = Me[De] = new bo(), Fe = me.data || me;
            if (Array.isArray(Fe) || n(Fe) || Ut(Fe)) {
              var Oe;
              $.buffers[De] && (Oe = $.buffers[De], n(Fe) && Oe._buffer.byteLength >= Fe.byteLength ? Oe.subdata(Fe) : (Oe.destroy(), $.buffers[De] = null)), $.buffers[De] || (Oe = $.buffers[De] = j.create(me, Hs, false, true)), ve.buffer = j.getBuffer(Oe), ve.size = ve.buffer.dimension | 0, ve.normalized = false, ve.type = ve.buffer.dtype, ve.offset = 0, ve.stride = 0, ve.divisor = 0, ve.state = 1, le[De] = 1;
            } else j.getBuffer(me) ? (ve.buffer = j.getBuffer(me), ve.size = ve.buffer.dimension | 0, ve.normalized = false, ve.type = ve.buffer.dtype, ve.offset = 0, ve.stride = 0, ve.divisor = 0, ve.state = 1) : j.getBuffer(me.buffer) ? (ve.buffer = j.getBuffer(me.buffer), ve.size = (+me.size || ve.buffer.dimension) | 0, ve.normalized = !!me.normalized || false, "type" in me ? (d.parameter(me.type, Tn, "invalid buffer type"), ve.type = Tn[me.type]) : ve.type = ve.buffer.dtype, ve.offset = (me.offset || 0) | 0, ve.stride = (me.stride || 0) | 0, ve.divisor = (me.divisor || 0) | 0, ve.state = 1, d(ve.size >= 1 && ve.size <= 4, "size must be between 1 and 4"), d(ve.offset >= 0, "invalid offset"), d(ve.stride >= 0 && ve.stride <= 255, "stride must be between 0 and 255"), d(ve.divisor >= 0, "divisor must be positive"), d(!ve.divisor || !!h.angle_instanced_arrays, "ANGLE_instanced_arrays must be enabled to use divisor")) : "x" in me ? (d(De > 0, "first attribute must not be a constant"), ve.x = +me.x || 0, ve.y = +me.y || 0, ve.z = +me.z || 0, ve.w = +me.w || 0, ve.state = 2) : d(false, "invalid attribute spec for location " + De);
          }
          for (var He = 0; He < $.buffers.length; ++He)
            !le[He] && $.buffers[He] && ($.buffers[He].destroy(), $.buffers[He] = null);
          return $.refresh(), be;
        }
        return be.destroy = function() {
          for (var ue = 0; ue < $.buffers.length; ++ue)
            $.buffers[ue] && $.buffers[ue].destroy();
          $.buffers.length = 0, $.ownsElements && ($.elements.destroy(), $.elements = null, $.ownsElements = false), $.destroy();
        }, be._vao = $, be._reglType = "vao", be(U);
      }
      return fe;
    }
    var qs = 35632, Mh = 35633, Oh = 35718, Nh = 35721;
    function Gh(a, h, _, R) {
      var j = {}, I = {};
      function G(C, O, ee, ge) {
        this.name = C, this.id = O, this.location = ee, this.info = ge;
      }
      function Q(C, O) {
        for (var ee = 0; ee < C.length; ++ee)
          if (C[ee].id === O.id) {
            C[ee].location = O.location;
            return;
          }
        C.push(O);
      }
      function Y(C, O, ee) {
        var ge = C === qs ? j : I, W = ge[O];
        if (!W) {
          var pe = h.str(O);
          W = a.createShader(C), a.shaderSource(W, pe), a.compileShader(W), d.shaderError(a, W, pe, C, ee), ge[O] = W;
        }
        return W;
      }
      var ne = {}, ie = [], re = 0;
      function fe(C, O) {
        this.id = re++, this.fragId = C, this.vertId = O, this.program = null, this.uniforms = [], this.attributes = [], this.refCount = 1, R.profile && (this.stats = {
          uniformsCount: 0,
          attributesCount: 0
        });
      }
      function K(C, O, ee) {
        var ge, W, pe = Y(qs, C.fragId), de = Y(Mh, C.vertId), Pe = C.program = a.createProgram();
        if (a.attachShader(Pe, pe), a.attachShader(Pe, de), ee)
          for (ge = 0; ge < ee.length; ++ge) {
            var U = ee[ge];
            a.bindAttribLocation(Pe, U[0], U[1]);
          }
        a.linkProgram(Pe), d.linkError(
          a,
          Pe,
          h.str(C.fragId),
          h.str(C.vertId),
          O
        );
        var $ = a.getProgramParameter(Pe, Oh);
        R.profile && (C.stats.uniformsCount = $);
        var be = C.uniforms;
        for (ge = 0; ge < $; ++ge)
          if (W = a.getActiveUniform(Pe, ge), W) {
            if (W.size > 1)
              for (var ue = 0; ue < W.size; ++ue) {
                var J = W.name.replace("[0]", "[" + ue + "]");
                Q(be, new G(
                  J,
                  h.id(J),
                  a.getUniformLocation(Pe, J),
                  W
                ));
              }
            var ce = W.name;
            W.size > 1 && (ce = ce.replace("[0]", "")), Q(be, new G(
              ce,
              h.id(ce),
              a.getUniformLocation(Pe, ce),
              W
            ));
          }
        var le = a.getProgramParameter(Pe, Nh);
        R.profile && (C.stats.attributesCount = le);
        var Me = C.attributes;
        for (ge = 0; ge < le; ++ge)
          W = a.getActiveAttrib(Pe, ge), W && Q(Me, new G(
            W.name,
            h.id(W.name),
            a.getAttribLocation(Pe, W.name),
            W
          ));
      }
      R.profile && (_.getMaxUniformsCount = function() {
        var C = 0;
        return ie.forEach(function(O) {
          O.stats.uniformsCount > C && (C = O.stats.uniformsCount);
        }), C;
      }, _.getMaxAttributesCount = function() {
        var C = 0;
        return ie.forEach(function(O) {
          O.stats.attributesCount > C && (C = O.stats.attributesCount);
        }), C;
      });
      function te() {
        j = {}, I = {};
        for (var C = 0; C < ie.length; ++C)
          K(ie[C], null, ie[C].attributes.map(function(O) {
            return [O.location, O.name];
          }));
      }
      return {
        clear: function() {
          var C = a.deleteShader.bind(a);
          Mt(j).forEach(C), j = {}, Mt(I).forEach(C), I = {}, ie.forEach(function(O) {
            a.deleteProgram(O.program);
          }), ie.length = 0, ne = {}, _.shaderCount = 0;
        },
        program: function(C, O, ee, ge) {
          d.command(C >= 0, "missing vertex shader", ee), d.command(O >= 0, "missing fragment shader", ee);
          var W = ne[O];
          W || (W = ne[O] = {});
          var pe = W[C];
          if (pe && (pe.refCount++, !ge))
            return pe;
          var de = new fe(O, C);
          return _.shaderCount++, K(de, ee, ge), pe || (W[C] = de), ie.push(de), r(de, {
            destroy: function() {
              if (de.refCount--, de.refCount <= 0) {
                a.deleteProgram(de.program);
                var Pe = ie.indexOf(de);
                ie.splice(Pe, 1), _.shaderCount--;
              }
              W[de.vertId].refCount <= 0 && (a.deleteShader(I[de.vertId]), delete I[de.vertId], delete ne[de.fragId][de.vertId]), Object.keys(ne[de.fragId]).length || (a.deleteShader(j[de.fragId]), delete j[de.fragId], delete ne[de.fragId]);
            }
          });
        },
        restore: te,
        shader: Y,
        frag: -1,
        vert: -1
      };
    }
    var Bh = 6408, xr = 5121, Vh = 3333, ri = 5126;
    function $h(a, h, _, R, j, I, G) {
      function Q(ie) {
        var re;
        h.next === null ? (d(
          j.preserveDrawingBuffer,
          'you must create a webgl context with "preserveDrawingBuffer":true in order to read pixels from the drawing buffer'
        ), re = xr) : (d(
          h.next.colorAttachments[0].texture !== null,
          "You cannot read from a renderbuffer"
        ), re = h.next.colorAttachments[0].texture._texture.type, d.optional(function() {
          I.oes_texture_float ? (d(
            re === xr || re === ri,
            "Reading from a framebuffer is only allowed for the types 'uint8' and 'float'"
          ), re === ri && d(G.readFloat, "Reading 'float' values is not permitted in your browser. For a fallback, please see: https://www.npmjs.com/package/glsl-read-float")) : d(
            re === xr,
            "Reading from a framebuffer is only allowed for the type 'uint8'"
          );
        }));
        var fe = 0, K = 0, te = R.framebufferWidth, C = R.framebufferHeight, O = null;
        n(ie) ? O = ie : ie && (d.type(ie, "object", "invalid arguments to regl.read()"), fe = ie.x | 0, K = ie.y | 0, d(
          fe >= 0 && fe < R.framebufferWidth,
          "invalid x offset for regl.read"
        ), d(
          K >= 0 && K < R.framebufferHeight,
          "invalid y offset for regl.read"
        ), te = (ie.width || R.framebufferWidth - fe) | 0, C = (ie.height || R.framebufferHeight - K) | 0, O = ie.data || null), O && (re === xr ? d(
          O instanceof Uint8Array,
          "buffer must be 'Uint8Array' when reading from a framebuffer of type 'uint8'"
        ) : re === ri && d(
          O instanceof Float32Array,
          "buffer must be 'Float32Array' when reading from a framebuffer of type 'float'"
        )), d(
          te > 0 && te + fe <= R.framebufferWidth,
          "invalid width for read pixels"
        ), d(
          C > 0 && C + K <= R.framebufferHeight,
          "invalid height for read pixels"
        ), _();
        var ee = te * C * 4;
        return O || (re === xr ? O = new Uint8Array(ee) : re === ri && (O = O || new Float32Array(ee))), d.isTypedArray(O, "data buffer for regl.read() must be a typedarray"), d(O.byteLength >= ee, "data buffer for regl.read() too small"), a.pixelStorei(Vh, 4), a.readPixels(
          fe,
          K,
          te,
          C,
          Bh,
          re,
          O
        ), O;
      }
      function Y(ie) {
        var re;
        return h.setFBO({
          framebuffer: ie.framebuffer
        }, function() {
          re = Q(ie);
        }), re;
      }
      function ne(ie) {
        return !ie || !("framebuffer" in ie) ? Q(ie) : Y(ie);
      }
      return ne;
    }
    function Qn(a) {
      return Array.prototype.slice.call(a);
    }
    function Kn(a) {
      return Qn(a).join("");
    }
    function jh() {
      var a = 0, h = [], _ = [];
      function R(re) {
        for (var fe = 0; fe < _.length; ++fe)
          if (_[fe] === re)
            return h[fe];
        var K = "g" + a++;
        return h.push(K), _.push(re), K;
      }
      function j() {
        var re = [];
        function fe() {
          re.push.apply(re, Qn(arguments));
        }
        var K = [];
        function te() {
          var C = "v" + a++;
          return K.push(C), arguments.length > 0 && (re.push(C, "="), re.push.apply(re, Qn(arguments)), re.push(";")), C;
        }
        return r(fe, {
          def: te,
          toString: function() {
            return Kn([
              K.length > 0 ? "var " + K.join(",") + ";" : "",
              Kn(re)
            ]);
          }
        });
      }
      function I() {
        var re = j(), fe = j(), K = re.toString, te = fe.toString;
        function C(O, ee) {
          fe(O, ee, "=", re.def(O, ee), ";");
        }
        return r(function() {
          re.apply(re, Qn(arguments));
        }, {
          def: re.def,
          entry: re,
          exit: fe,
          save: C,
          set: function(O, ee, ge) {
            C(O, ee), re(O, ee, "=", ge, ";");
          },
          toString: function() {
            return K() + te();
          }
        });
      }
      function G() {
        var re = Kn(arguments), fe = I(), K = I(), te = fe.toString, C = K.toString;
        return r(fe, {
          then: function() {
            return fe.apply(fe, Qn(arguments)), this;
          },
          else: function() {
            return K.apply(K, Qn(arguments)), this;
          },
          toString: function() {
            var O = C();
            return O && (O = "else{" + O + "}"), Kn([
              "if(",
              re,
              "){",
              te(),
              "}",
              O
            ]);
          }
        });
      }
      var Q = j(), Y = {};
      function ne(re, fe) {
        var K = [];
        function te() {
          var W = "a" + K.length;
          return K.push(W), W;
        }
        fe = fe || 0;
        for (var C = 0; C < fe; ++C)
          te();
        var O = I(), ee = O.toString, ge = Y[re] = r(O, {
          arg: te,
          toString: function() {
            return Kn([
              "function(",
              K.join(),
              "){",
              ee(),
              "}"
            ]);
          }
        });
        return ge;
      }
      function ie() {
        var re = [
          '"use strict";',
          Q,
          "return {"
        ];
        Object.keys(Y).forEach(function(te) {
          re.push('"', te, '":', Y[te].toString(), ",");
        }), re.push("}");
        var fe = Kn(re).replace(/;/g, `;
`).replace(/}/g, `}
`).replace(/{/g, `{
`), K = Function.apply(null, h.concat(fe));
        return K.apply(null, _);
      }
      return {
        global: Q,
        link: R,
        block: j,
        proc: ne,
        scope: I,
        cond: G,
        compile: ie
      };
    }
    var Jn = "xyzw".split(""), Ys = 5121, er = 1, _o = 2, So = 0, wo = 1, To = 2, Ao = 3, ii = 4, Zs = 5, Qs = 6, Ks = "dither", Js = "blend.enable", ef = "blend.color", Eo = "blend.equation", Po = "blend.func", tf = "depth.enable", nf = "depth.func", rf = "depth.range", of = "depth.mask", Co = "colorMask", af = "cull.enable", sf = "cull.face", ko = "frontFace", Fo = "lineWidth", ff = "polygonOffset.enable", zo = "polygonOffset.offset", uf = "sample.alpha", cf = "sample.enable", Lo = "sample.coverage", lf = "stencil.enable", hf = "stencil.mask", Io = "stencil.func", Ro = "stencil.opFront", yr = "stencil.opBack", df = "scissor.enable", oi = "scissor.box", an = "viewport", br = "profile", kn = "framebuffer", _r = "vert", Sr = "frag", Fn = "elements", zn = "primitive", Ln = "count", ai = "offset", si = "instances", wr = "vao", Do = "Width", Mo = "Height", tr = kn + Do, nr = kn + Mo, Uh = an + Do, Xh = an + Mo, mf = "drawingBuffer", pf = mf + Do, vf = mf + Mo, Hh = [
      Po,
      Eo,
      Io,
      Ro,
      yr,
      Lo,
      an,
      oi,
      zo
    ], rr = 34962, Oo = 34963, Wh = 35632, qh = 35633, gf = 3553, Yh = 34067, Zh = 2884, Qh = 3042, Kh = 3024, Jh = 2960, ed = 2929, td = 3089, nd = 32823, rd = 32926, id = 32928, No = 5126, fi = 35664, ui = 35665, ci = 35666, Go = 5124, li = 35667, hi = 35668, di = 35669, Bo = 35670, mi = 35671, pi = 35672, vi = 35673, Tr = 35674, Ar = 35675, Er = 35676, Pr = 35678, Cr = 35680, Vo = 4, kr = 1028, In = 1029, xf = 2304, $o = 2305, od = 32775, ad = 32776, sd = 519, gn = 7680, yf = 0, bf = 1, _f = 32774, fd = 513, Sf = 36160, ud = 36064, Qt = {
      0: 0,
      1: 1,
      zero: 0,
      one: 1,
      "src color": 768,
      "one minus src color": 769,
      "src alpha": 770,
      "one minus src alpha": 771,
      "dst color": 774,
      "one minus dst color": 775,
      "dst alpha": 772,
      "one minus dst alpha": 773,
      "constant color": 32769,
      "one minus constant color": 32770,
      "constant alpha": 32771,
      "one minus constant alpha": 32772,
      "src alpha saturate": 776
    }, wf = [
      "constant color, constant alpha",
      "one minus constant color, constant alpha",
      "constant color, one minus constant alpha",
      "one minus constant color, one minus constant alpha",
      "constant alpha, constant color",
      "constant alpha, one minus constant color",
      "one minus constant alpha, constant color",
      "one minus constant alpha, one minus constant color"
    ], ir = {
      never: 512,
      less: 513,
      "<": 513,
      equal: 514,
      "=": 514,
      "==": 514,
      "===": 514,
      lequal: 515,
      "<=": 515,
      greater: 516,
      ">": 516,
      notequal: 517,
      "!=": 517,
      "!==": 517,
      gequal: 518,
      ">=": 518,
      always: 519
    }, xn = {
      0: 0,
      zero: 0,
      keep: 7680,
      replace: 7681,
      increment: 7682,
      decrement: 7683,
      "increment wrap": 34055,
      "decrement wrap": 34056,
      invert: 5386
    }, Tf = {
      frag: Wh,
      vert: qh
    }, jo = {
      cw: xf,
      ccw: $o
    };
    function gi(a) {
      return Array.isArray(a) || n(a) || Ut(a);
    }
    function Af(a) {
      return a.sort(function(h, _) {
        return h === an ? -1 : _ === an ? 1 : h < _ ? -1 : 1;
      });
    }
    function xt(a, h, _, R) {
      this.thisDep = a, this.contextDep = h, this.propDep = _, this.append = R;
    }
    function yn(a) {
      return a && !(a.thisDep || a.contextDep || a.propDep);
    }
    function dt(a) {
      return new xt(false, false, false, a);
    }
    function Ct(a, h) {
      var _ = a.type;
      if (_ === So) {
        var R = a.data.length;
        return new xt(
          true,
          R >= 1,
          R >= 2,
          h
        );
      } else if (_ === ii) {
        var j = a.data;
        return new xt(
          j.thisDep,
          j.contextDep,
          j.propDep,
          h
        );
      } else {
        if (_ === Zs)
          return new xt(
            false,
            false,
            false,
            h
          );
        if (_ === Qs) {
          for (var I = false, G = false, Q = false, Y = 0; Y < a.data.length; ++Y) {
            var ne = a.data[Y];
            if (ne.type === wo)
              Q = true;
            else if (ne.type === To)
              G = true;
            else if (ne.type === Ao)
              I = true;
            else if (ne.type === So) {
              I = true;
              var ie = ne.data;
              ie >= 1 && (G = true), ie >= 2 && (Q = true);
            } else ne.type === ii && (I = I || ne.data.thisDep, G = G || ne.data.contextDep, Q = Q || ne.data.propDep);
          }
          return new xt(
            I,
            G,
            Q,
            h
          );
        } else
          return new xt(
            _ === Ao,
            _ === To,
            _ === wo,
            h
          );
      }
    }
    var Ef = new xt(false, false, false, function() {
    });
    function cd(a, h, _, R, j, I, G, Q, Y, ne, ie, re, fe, K, te) {
      var C = ne.Record, O = {
        add: 32774,
        subtract: 32778,
        "reverse subtract": 32779
      };
      _.ext_blend_minmax && (O.min = od, O.max = ad);
      var ee = _.angle_instanced_arrays, ge = _.webgl_draw_buffers, W = _.oes_vertex_array_object, pe = {
        dirty: true,
        profile: te.profile
      }, de = {}, Pe = [], U = {}, $ = {};
      function be(m) {
        return m.replace(".", "_");
      }
      function ue(m, c, w) {
        var P = be(m);
        Pe.push(m), de[P] = pe[P] = !!w, U[P] = c;
      }
      function J(m, c, w) {
        var P = be(m);
        Pe.push(m), Array.isArray(w) ? (pe[P] = w.slice(), de[P] = w.slice()) : pe[P] = de[P] = w, $[P] = c;
      }
      ue(Ks, Kh), ue(Js, Qh), J(ef, "blendColor", [0, 0, 0, 0]), J(
        Eo,
        "blendEquationSeparate",
        [_f, _f]
      ), J(
        Po,
        "blendFuncSeparate",
        [bf, yf, bf, yf]
      ), ue(tf, ed, true), J(nf, "depthFunc", fd), J(rf, "depthRange", [0, 1]), J(of, "depthMask", true), J(Co, Co, [true, true, true, true]), ue(af, Zh), J(sf, "cullFace", In), J(ko, ko, $o), J(Fo, Fo, 1), ue(ff, nd), J(zo, "polygonOffset", [0, 0]), ue(uf, rd), ue(cf, id), J(Lo, "sampleCoverage", [1, false]), ue(lf, Jh), J(hf, "stencilMask", -1), J(Io, "stencilFunc", [sd, 0, -1]), J(
        Ro,
        "stencilOpSeparate",
        [kr, gn, gn, gn]
      ), J(
        yr,
        "stencilOpSeparate",
        [In, gn, gn, gn]
      ), ue(df, td), J(
        oi,
        "scissor",
        [0, 0, a.drawingBufferWidth, a.drawingBufferHeight]
      ), J(
        an,
        an,
        [0, 0, a.drawingBufferWidth, a.drawingBufferHeight]
      );
      var ce = {
        gl: a,
        context: fe,
        strings: h,
        next: de,
        current: pe,
        draw: re,
        elements: I,
        buffer: j,
        shader: ie,
        attributes: ne.state,
        vao: ne,
        uniforms: Y,
        framebuffer: Q,
        extensions: _,
        timer: K,
        isBufferArgs: gi
      }, le = {
        primTypes: pn,
        compareFuncs: ir,
        blendFuncs: Qt,
        blendEquations: O,
        stencilOps: xn,
        glTypes: Tn,
        orientationType: jo
      };
      d.optional(function() {
        ce.isArrayLike = ot;
      }), ge && (le.backBuffer = [In], le.drawBuffer = Dt(R.maxDrawbuffers, function(m) {
        return m === 0 ? [0] : Dt(m, function(c) {
          return ud + c;
        });
      }));
      var Me = 0;
      function De() {
        var m = jh(), c = m.link, w = m.global;
        m.id = Me++, m.batchId = "0";
        var P = c(ce), k = m.shared = {
          props: "a0"
        };
        Object.keys(ce).forEach(function(b) {
          k[b] = w.def(P, ".", b);
        }), d.optional(function() {
          m.CHECK = c(d), m.commandStr = d.guessCommand(), m.command = c(m.commandStr), m.assert = function(b, g, z) {
            b(
              "if(!(",
              g,
              "))",
              this.CHECK,
              ".commandRaise(",
              c(z),
              ",",
              this.command,
              ");"
            );
          }, le.invalidBlendCombinations = wf;
        });
        var A = m.next = {}, T = m.current = {};
        Object.keys($).forEach(function(b) {
          Array.isArray(pe[b]) && (A[b] = w.def(k.next, ".", b), T[b] = w.def(k.current, ".", b));
        });
        var E = m.constants = {};
        Object.keys(le).forEach(function(b) {
          E[b] = w.def(JSON.stringify(le[b]));
        }), m.invoke = function(b, g) {
          switch (g.type) {
            case So:
              var z = [
                "this",
                k.context,
                k.props,
                m.batchId
              ];
              return b.def(
                c(g.data),
                ".call(",
                z.slice(0, Math.max(g.data.length + 1, 4)),
                ")"
              );
            case wo:
              return b.def(k.props, g.data);
            case To:
              return b.def(k.context, g.data);
            case Ao:
              return b.def("this", g.data);
            case ii:
              return g.data.append(m, b), g.data.ref;
            case Zs:
              return g.data.toString();
            case Qs:
              return g.data.map(function(F) {
                return m.invoke(b, F);
              });
          }
        }, m.attribCache = {};
        var v = {};
        return m.scopeAttrib = function(b) {
          var g = h.id(b);
          if (g in v)
            return v[g];
          var z = ne.scope[g];
          z || (z = ne.scope[g] = new C());
          var F = v[g] = c(z);
          return F;
        }, m;
      }
      function me(m) {
        var c = m.static, w = m.dynamic, P;
        if (br in c) {
          var k = !!c[br];
          P = dt(function(T, E) {
            return k;
          }), P.enable = k;
        } else if (br in w) {
          var A = w[br];
          P = Ct(A, function(T, E) {
            return T.invoke(E, A);
          });
        }
        return P;
      }
      function ve(m, c) {
        var w = m.static, P = m.dynamic;
        if (kn in w) {
          var k = w[kn];
          return k ? (k = Q.getFramebuffer(k), d.command(k, "invalid framebuffer object"), dt(function(T, E) {
            var v = T.link(k), b = T.shared;
            E.set(
              b.framebuffer,
              ".next",
              v
            );
            var g = b.context;
            return E.set(
              g,
              "." + tr,
              v + ".width"
            ), E.set(
              g,
              "." + nr,
              v + ".height"
            ), v;
          })) : dt(function(T, E) {
            var v = T.shared;
            E.set(
              v.framebuffer,
              ".next",
              "null"
            );
            var b = v.context;
            return E.set(
              b,
              "." + tr,
              b + "." + pf
            ), E.set(
              b,
              "." + nr,
              b + "." + vf
            ), "null";
          });
        } else if (kn in P) {
          var A = P[kn];
          return Ct(A, function(T, E) {
            var v = T.invoke(E, A), b = T.shared, g = b.framebuffer, z = E.def(
              g,
              ".getFramebuffer(",
              v,
              ")"
            );
            d.optional(function() {
              T.assert(
                E,
                "!" + v + "||" + z,
                "invalid framebuffer object"
              );
            }), E.set(
              g,
              ".next",
              z
            );
            var F = b.context;
            return E.set(
              F,
              "." + tr,
              z + "?" + z + ".width:" + F + "." + pf
            ), E.set(
              F,
              "." + nr,
              z + "?" + z + ".height:" + F + "." + vf
            ), z;
          });
        } else
          return null;
      }
      function Fe(m, c, w) {
        var P = m.static, k = m.dynamic;
        function A(v) {
          if (v in P) {
            var b = P[v];
            d.commandType(b, "object", "invalid " + v, w.commandStr);
            var g = true, z = b.x | 0, F = b.y | 0, V, X;
            return "width" in b ? (V = b.width | 0, d.command(V >= 0, "invalid " + v, w.commandStr)) : g = false, "height" in b ? (X = b.height | 0, d.command(X >= 0, "invalid " + v, w.commandStr)) : g = false, new xt(
              !g && c && c.thisDep,
              !g && c && c.contextDep,
              !g && c && c.propDep,
              function(Ee, xe) {
                var H = Ee.shared.context, B = V;
                "width" in b || (B = xe.def(H, ".", tr, "-", z));
                var Te = X;
                return "height" in b || (Te = xe.def(H, ".", nr, "-", F)), [z, F, B, Te];
              }
            );
          } else if (v in k) {
            var se = k[v], Ie = Ct(se, function(Ee, xe) {
              var H = Ee.invoke(xe, se);
              d.optional(function() {
                Ee.assert(
                  xe,
                  H + "&&typeof " + H + '==="object"',
                  "invalid " + v
                );
              });
              var B = Ee.shared.context, Te = xe.def(H, ".x|0"), _e = xe.def(H, ".y|0"), ze = xe.def(
                '"width" in ',
                H,
                "?",
                H,
                ".width|0:",
                "(",
                B,
                ".",
                tr,
                "-",
                Te,
                ")"
              ), Je = xe.def(
                '"height" in ',
                H,
                "?",
                H,
                ".height|0:",
                "(",
                B,
                ".",
                nr,
                "-",
                _e,
                ")"
              );
              return d.optional(function() {
                Ee.assert(
                  xe,
                  ze + ">=0&&" + Je + ">=0",
                  "invalid " + v
                );
              }), [Te, _e, ze, Je];
            });
            return c && (Ie.thisDep = Ie.thisDep || c.thisDep, Ie.contextDep = Ie.contextDep || c.contextDep, Ie.propDep = Ie.propDep || c.propDep), Ie;
          } else return c ? new xt(
            c.thisDep,
            c.contextDep,
            c.propDep,
            function(Ee, xe) {
              var H = Ee.shared.context;
              return [
                0,
                0,
                xe.def(H, ".", tr),
                xe.def(H, ".", nr)
              ];
            }
          ) : null;
        }
        var T = A(an);
        if (T) {
          var E = T;
          T = new xt(
            T.thisDep,
            T.contextDep,
            T.propDep,
            function(v, b) {
              var g = E.append(v, b), z = v.shared.context;
              return b.set(
                z,
                "." + Uh,
                g[2]
              ), b.set(
                z,
                "." + Xh,
                g[3]
              ), g;
            }
          );
        }
        return {
          viewport: T,
          scissor_box: A(oi)
        };
      }
      function Oe(m, c) {
        var w = m.static, P = typeof w[Sr] == "string" && typeof w[_r] == "string";
        if (P) {
          if (Object.keys(c.dynamic).length > 0)
            return null;
          var k = c.static, A = Object.keys(k);
          if (A.length > 0 && typeof k[A[0]] == "number") {
            for (var T = [], E = 0; E < A.length; ++E)
              d(typeof k[A[E]] == "number", "must specify all vertex attribute locations when using vaos"), T.push([k[A[E]] | 0, A[E]]);
            return T;
          }
        }
        return null;
      }
      function He(m, c, w) {
        var P = m.static, k = m.dynamic;
        function A(g) {
          if (g in P) {
            var z = h.id(P[g]);
            d.optional(function() {
              ie.shader(Tf[g], z, d.guessCommand());
            });
            var F = dt(function() {
              return z;
            });
            return F.id = z, F;
          } else if (g in k) {
            var V = k[g];
            return Ct(V, function(X, se) {
              var Ie = X.invoke(se, V), Ee = se.def(X.shared.strings, ".id(", Ie, ")");
              return d.optional(function() {
                se(
                  X.shared.shader,
                  ".shader(",
                  Tf[g],
                  ",",
                  Ee,
                  ",",
                  X.command,
                  ");"
                );
              }), Ee;
            });
          }
          return null;
        }
        var T = A(Sr), E = A(_r), v = null, b;
        return yn(T) && yn(E) ? (v = ie.program(E.id, T.id, null, w), b = dt(function(g, z) {
          return g.link(v);
        })) : b = new xt(
          T && T.thisDep || E && E.thisDep,
          T && T.contextDep || E && E.contextDep,
          T && T.propDep || E && E.propDep,
          function(g, z) {
            var F = g.shared.shader, V;
            T ? V = T.append(g, z) : V = z.def(F, ".", Sr);
            var X;
            E ? X = E.append(g, z) : X = z.def(F, ".", _r);
            var se = F + ".program(" + X + "," + V;
            return d.optional(function() {
              se += "," + g.command;
            }), z.def(se + ")");
          }
        ), {
          frag: T,
          vert: E,
          progVar: b,
          program: v
        };
      }
      function at(m, c) {
        var w = m.static, P = m.dynamic, k = {}, A = false;
        function T() {
          if (wr in w) {
            var xe = w[wr];
            return xe !== null && ne.getVAO(xe) === null && (xe = ne.createVAO(xe)), A = true, k.vao = xe, dt(function(B) {
              var Te = ne.getVAO(xe);
              return Te ? B.link(Te) : "null";
            });
          } else if (wr in P) {
            A = true;
            var H = P[wr];
            return Ct(H, function(B, Te) {
              var _e = B.invoke(Te, H);
              return Te.def(B.shared.vao + ".getVAO(" + _e + ")");
            });
          }
          return null;
        }
        var E = T(), v = false;
        function b() {
          if (Fn in w) {
            var xe = w[Fn];
            if (k.elements = xe, gi(xe)) {
              var H = k.elements = I.create(xe, true);
              xe = I.getElements(H), v = true;
            } else xe && (xe = I.getElements(xe), v = true, d.command(xe, "invalid elements", c.commandStr));
            var B = dt(function(_e, ze) {
              if (xe) {
                var Je = _e.link(xe);
                return _e.ELEMENTS = Je, Je;
              }
              return _e.ELEMENTS = null, null;
            });
            return B.value = xe, B;
          } else if (Fn in P) {
            v = true;
            var Te = P[Fn];
            return Ct(Te, function(_e, ze) {
              var Je = _e.shared, Et = Je.isBufferArgs, Dn = Je.elements, sn = _e.invoke(ze, Te), Jt = ze.def("null"), bn = ze.def(Et, "(", sn, ")"), Mn = _e.cond(bn).then(Jt, "=", Dn, ".createStream(", sn, ");").else(Jt, "=", Dn, ".getElements(", sn, ");");
              return d.optional(function() {
                _e.assert(
                  Mn.else,
                  "!" + sn + "||" + Jt,
                  "invalid elements"
                );
              }), ze.entry(Mn), ze.exit(
                _e.cond(bn).then(Dn, ".destroyStream(", Jt, ");")
              ), _e.ELEMENTS = Jt, Jt;
            });
          } else if (A)
            return new xt(
              E.thisDep,
              E.contextDep,
              E.propDep,
              function(_e, ze) {
                return ze.def(_e.shared.vao + ".currentVAO?" + _e.shared.elements + ".getElements(" + _e.shared.vao + ".currentVAO.elements):null");
              }
            );
          return null;
        }
        var g = b();
        function z() {
          if (zn in w) {
            var xe = w[zn];
            return k.primitive = xe, d.commandParameter(xe, pn, "invalid primitve", c.commandStr), dt(function(B, Te) {
              return pn[xe];
            });
          } else if (zn in P) {
            var H = P[zn];
            return Ct(H, function(B, Te) {
              var _e = B.constants.primTypes, ze = B.invoke(Te, H);
              return d.optional(function() {
                B.assert(
                  Te,
                  ze + " in " + _e,
                  "invalid primitive, must be one of " + Object.keys(pn)
                );
              }), Te.def(_e, "[", ze, "]");
            });
          } else {
            if (v)
              return yn(g) ? g.value ? dt(function(B, Te) {
                return Te.def(B.ELEMENTS, ".primType");
              }) : dt(function() {
                return Vo;
              }) : new xt(
                g.thisDep,
                g.contextDep,
                g.propDep,
                function(B, Te) {
                  var _e = B.ELEMENTS;
                  return Te.def(_e, "?", _e, ".primType:", Vo);
                }
              );
            if (A)
              return new xt(
                E.thisDep,
                E.contextDep,
                E.propDep,
                function(B, Te) {
                  return Te.def(B.shared.vao + ".currentVAO?" + B.shared.vao + ".currentVAO.primitive:" + Vo);
                }
              );
          }
          return null;
        }
        function F(xe, H) {
          if (xe in w) {
            var B = w[xe] | 0;
            return H ? k.offset = B : k.instances = B, d.command(!H || B >= 0, "invalid " + xe, c.commandStr), dt(function(_e, ze) {
              return H && (_e.OFFSET = B), B;
            });
          } else if (xe in P) {
            var Te = P[xe];
            return Ct(Te, function(_e, ze) {
              var Je = _e.invoke(ze, Te);
              return H && (_e.OFFSET = Je, d.optional(function() {
                _e.assert(
                  ze,
                  Je + ">=0",
                  "invalid " + xe
                );
              })), Je;
            });
          } else if (H) {
            if (v)
              return dt(function(_e, ze) {
                return _e.OFFSET = 0, 0;
              });
            if (A)
              return new xt(
                E.thisDep,
                E.contextDep,
                E.propDep,
                function(_e, ze) {
                  return ze.def(_e.shared.vao + ".currentVAO?" + _e.shared.vao + ".currentVAO.offset:0");
                }
              );
          } else if (A)
            return new xt(
              E.thisDep,
              E.contextDep,
              E.propDep,
              function(_e, ze) {
                return ze.def(_e.shared.vao + ".currentVAO?" + _e.shared.vao + ".currentVAO.instances:-1");
              }
            );
          return null;
        }
        var V = F(ai, true);
        function X() {
          if (Ln in w) {
            var xe = w[Ln] | 0;
            return k.count = xe, d.command(
              typeof xe == "number" && xe >= 0,
              "invalid vertex count",
              c.commandStr
            ), dt(function() {
              return xe;
            });
          } else if (Ln in P) {
            var H = P[Ln];
            return Ct(H, function(ze, Je) {
              var Et = ze.invoke(Je, H);
              return d.optional(function() {
                ze.assert(
                  Je,
                  "typeof " + Et + '==="number"&&' + Et + ">=0&&" + Et + "===(" + Et + "|0)",
                  "invalid vertex count"
                );
              }), Et;
            });
          } else if (v)
            if (yn(g)) {
              if (g)
                return V ? new xt(
                  V.thisDep,
                  V.contextDep,
                  V.propDep,
                  function(ze, Je) {
                    var Et = Je.def(
                      ze.ELEMENTS,
                      ".vertCount-",
                      ze.OFFSET
                    );
                    return d.optional(function() {
                      ze.assert(
                        Je,
                        Et + ">=0",
                        "invalid vertex offset/element buffer too small"
                      );
                    }), Et;
                  }
                ) : dt(function(ze, Je) {
                  return Je.def(ze.ELEMENTS, ".vertCount");
                });
              var B = dt(function() {
                return -1;
              });
              return d.optional(function() {
                B.MISSING = true;
              }), B;
            } else {
              var Te = new xt(
                g.thisDep || V.thisDep,
                g.contextDep || V.contextDep,
                g.propDep || V.propDep,
                function(ze, Je) {
                  var Et = ze.ELEMENTS;
                  return ze.OFFSET ? Je.def(
                    Et,
                    "?",
                    Et,
                    ".vertCount-",
                    ze.OFFSET,
                    ":-1"
                  ) : Je.def(Et, "?", Et, ".vertCount:-1");
                }
              );
              return d.optional(function() {
                Te.DYNAMIC = true;
              }), Te;
            }
          else if (A) {
            var _e = new xt(
              E.thisDep,
              E.contextDep,
              E.propDep,
              function(ze, Je) {
                return Je.def(ze.shared.vao, ".currentVAO?", ze.shared.vao, ".currentVAO.count:-1");
              }
            );
            return _e;
          }
          return null;
        }
        var se = z(), Ie = X(), Ee = F(si, false);
        return {
          elements: g,
          primitive: se,
          count: Ie,
          instances: Ee,
          offset: V,
          vao: E,
          vaoActive: A,
          elementsActive: v,
          // static draw props
          static: k
        };
      }
      function ft(m, c) {
        var w = m.static, P = m.dynamic, k = {};
        return Pe.forEach(function(A) {
          var T = be(A);
          function E(v, b) {
            if (A in w) {
              var g = v(w[A]);
              k[T] = dt(function() {
                return g;
              });
            } else if (A in P) {
              var z = P[A];
              k[T] = Ct(z, function(F, V) {
                return b(F, V, F.invoke(V, z));
              });
            }
          }
          switch (A) {
            case af:
            case Js:
            case Ks:
            case lf:
            case tf:
            case df:
            case ff:
            case uf:
            case cf:
            case of:
              return E(
                function(v) {
                  return d.commandType(v, "boolean", A, c.commandStr), v;
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      "typeof " + g + '==="boolean"',
                      "invalid flag " + A,
                      v.commandStr
                    );
                  }), g;
                }
              );
            case nf:
              return E(
                function(v) {
                  return d.commandParameter(v, ir, "invalid " + A, c.commandStr), ir[v];
                },
                function(v, b, g) {
                  var z = v.constants.compareFuncs;
                  return d.optional(function() {
                    v.assert(
                      b,
                      g + " in " + z,
                      "invalid " + A + ", must be one of " + Object.keys(ir)
                    );
                  }), b.def(z, "[", g, "]");
                }
              );
            case rf:
              return E(
                function(v) {
                  return d.command(
                    ot(v) && v.length === 2 && typeof v[0] == "number" && typeof v[1] == "number" && v[0] <= v[1],
                    "depth range is 2d array",
                    c.commandStr
                  ), v;
                },
                function(v, b, g) {
                  d.optional(function() {
                    v.assert(
                      b,
                      v.shared.isArrayLike + "(" + g + ")&&" + g + ".length===2&&typeof " + g + '[0]==="number"&&typeof ' + g + '[1]==="number"&&' + g + "[0]<=" + g + "[1]",
                      "depth range must be a 2d array"
                    );
                  });
                  var z = b.def("+", g, "[0]"), F = b.def("+", g, "[1]");
                  return [z, F];
                }
              );
            case Po:
              return E(
                function(v) {
                  d.commandType(v, "object", "blend.func", c.commandStr);
                  var b = "srcRGB" in v ? v.srcRGB : v.src, g = "srcAlpha" in v ? v.srcAlpha : v.src, z = "dstRGB" in v ? v.dstRGB : v.dst, F = "dstAlpha" in v ? v.dstAlpha : v.dst;
                  return d.commandParameter(b, Qt, T + ".srcRGB", c.commandStr), d.commandParameter(g, Qt, T + ".srcAlpha", c.commandStr), d.commandParameter(z, Qt, T + ".dstRGB", c.commandStr), d.commandParameter(F, Qt, T + ".dstAlpha", c.commandStr), d.command(
                    wf.indexOf(b + ", " + z) === -1,
                    "unallowed blending combination (srcRGB, dstRGB) = (" + b + ", " + z + ")",
                    c.commandStr
                  ), [
                    Qt[b],
                    Qt[z],
                    Qt[g],
                    Qt[F]
                  ];
                },
                function(v, b, g) {
                  var z = v.constants.blendFuncs;
                  d.optional(function() {
                    v.assert(
                      b,
                      g + "&&typeof " + g + '==="object"',
                      "invalid blend func, must be an object"
                    );
                  });
                  function F(H, B) {
                    var Te = b.def(
                      '"',
                      H,
                      B,
                      '" in ',
                      g,
                      "?",
                      g,
                      ".",
                      H,
                      B,
                      ":",
                      g,
                      ".",
                      H
                    );
                    return d.optional(function() {
                      v.assert(
                        b,
                        Te + " in " + z,
                        "invalid " + A + "." + H + B + ", must be one of " + Object.keys(Qt)
                      );
                    }), Te;
                  }
                  var V = F("src", "RGB"), X = F("dst", "RGB");
                  d.optional(function() {
                    var H = v.constants.invalidBlendCombinations;
                    v.assert(
                      b,
                      H + ".indexOf(" + V + '+", "+' + X + ") === -1 ",
                      "unallowed blending combination for (srcRGB, dstRGB)"
                    );
                  });
                  var se = b.def(z, "[", V, "]"), Ie = b.def(z, "[", F("src", "Alpha"), "]"), Ee = b.def(z, "[", X, "]"), xe = b.def(z, "[", F("dst", "Alpha"), "]");
                  return [se, Ee, Ie, xe];
                }
              );
            case Eo:
              return E(
                function(v) {
                  if (typeof v == "string")
                    return d.commandParameter(v, O, "invalid " + A, c.commandStr), [
                      O[v],
                      O[v]
                    ];
                  if (typeof v == "object")
                    return d.commandParameter(
                      v.rgb,
                      O,
                      A + ".rgb",
                      c.commandStr
                    ), d.commandParameter(
                      v.alpha,
                      O,
                      A + ".alpha",
                      c.commandStr
                    ), [
                      O[v.rgb],
                      O[v.alpha]
                    ];
                  d.commandRaise("invalid blend.equation", c.commandStr);
                },
                function(v, b, g) {
                  var z = v.constants.blendEquations, F = b.def(), V = b.def(), X = v.cond("typeof ", g, '==="string"');
                  return d.optional(function() {
                    function se(Ie, Ee, xe) {
                      v.assert(
                        Ie,
                        xe + " in " + z,
                        "invalid " + Ee + ", must be one of " + Object.keys(O)
                      );
                    }
                    se(X.then, A, g), v.assert(
                      X.else,
                      g + "&&typeof " + g + '==="object"',
                      "invalid " + A
                    ), se(X.else, A + ".rgb", g + ".rgb"), se(X.else, A + ".alpha", g + ".alpha");
                  }), X.then(
                    F,
                    "=",
                    V,
                    "=",
                    z,
                    "[",
                    g,
                    "];"
                  ), X.else(
                    F,
                    "=",
                    z,
                    "[",
                    g,
                    ".rgb];",
                    V,
                    "=",
                    z,
                    "[",
                    g,
                    ".alpha];"
                  ), b(X), [F, V];
                }
              );
            case ef:
              return E(
                function(v) {
                  return d.command(
                    ot(v) && v.length === 4,
                    "blend.color must be a 4d array",
                    c.commandStr
                  ), Dt(4, function(b) {
                    return +v[b];
                  });
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      v.shared.isArrayLike + "(" + g + ")&&" + g + ".length===4",
                      "blend.color must be a 4d array"
                    );
                  }), Dt(4, function(z) {
                    return b.def("+", g, "[", z, "]");
                  });
                }
              );
            case hf:
              return E(
                function(v) {
                  return d.commandType(v, "number", T, c.commandStr), v | 0;
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      "typeof " + g + '==="number"',
                      "invalid stencil.mask"
                    );
                  }), b.def(g, "|0");
                }
              );
            case Io:
              return E(
                function(v) {
                  d.commandType(v, "object", T, c.commandStr);
                  var b = v.cmp || "keep", g = v.ref || 0, z = "mask" in v ? v.mask : -1;
                  return d.commandParameter(b, ir, A + ".cmp", c.commandStr), d.commandType(g, "number", A + ".ref", c.commandStr), d.commandType(z, "number", A + ".mask", c.commandStr), [
                    ir[b],
                    g,
                    z
                  ];
                },
                function(v, b, g) {
                  var z = v.constants.compareFuncs;
                  d.optional(function() {
                    function se() {
                      v.assert(
                        b,
                        Array.prototype.join.call(arguments, ""),
                        "invalid stencil.func"
                      );
                    }
                    se(g + "&&typeof ", g, '==="object"'), se(
                      '!("cmp" in ',
                      g,
                      ")||(",
                      g,
                      ".cmp in ",
                      z,
                      ")"
                    );
                  });
                  var F = b.def(
                    '"cmp" in ',
                    g,
                    "?",
                    z,
                    "[",
                    g,
                    ".cmp]",
                    ":",
                    gn
                  ), V = b.def(g, ".ref|0"), X = b.def(
                    '"mask" in ',
                    g,
                    "?",
                    g,
                    ".mask|0:-1"
                  );
                  return [F, V, X];
                }
              );
            case Ro:
            case yr:
              return E(
                function(v) {
                  d.commandType(v, "object", T, c.commandStr);
                  var b = v.fail || "keep", g = v.zfail || "keep", z = v.zpass || "keep";
                  return d.commandParameter(b, xn, A + ".fail", c.commandStr), d.commandParameter(g, xn, A + ".zfail", c.commandStr), d.commandParameter(z, xn, A + ".zpass", c.commandStr), [
                    A === yr ? In : kr,
                    xn[b],
                    xn[g],
                    xn[z]
                  ];
                },
                function(v, b, g) {
                  var z = v.constants.stencilOps;
                  d.optional(function() {
                    v.assert(
                      b,
                      g + "&&typeof " + g + '==="object"',
                      "invalid " + A
                    );
                  });
                  function F(V) {
                    return d.optional(function() {
                      v.assert(
                        b,
                        '!("' + V + '" in ' + g + ")||(" + g + "." + V + " in " + z + ")",
                        "invalid " + A + "." + V + ", must be one of " + Object.keys(xn)
                      );
                    }), b.def(
                      '"',
                      V,
                      '" in ',
                      g,
                      "?",
                      z,
                      "[",
                      g,
                      ".",
                      V,
                      "]:",
                      gn
                    );
                  }
                  return [
                    A === yr ? In : kr,
                    F("fail"),
                    F("zfail"),
                    F("zpass")
                  ];
                }
              );
            case zo:
              return E(
                function(v) {
                  d.commandType(v, "object", T, c.commandStr);
                  var b = v.factor | 0, g = v.units | 0;
                  return d.commandType(b, "number", T + ".factor", c.commandStr), d.commandType(g, "number", T + ".units", c.commandStr), [b, g];
                },
                function(v, b, g) {
                  d.optional(function() {
                    v.assert(
                      b,
                      g + "&&typeof " + g + '==="object"',
                      "invalid " + A
                    );
                  });
                  var z = b.def(g, ".factor|0"), F = b.def(g, ".units|0");
                  return [z, F];
                }
              );
            case sf:
              return E(
                function(v) {
                  var b = 0;
                  return v === "front" ? b = kr : v === "back" && (b = In), d.command(!!b, T, c.commandStr), b;
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      g + '==="front"||' + g + '==="back"',
                      "invalid cull.face"
                    );
                  }), b.def(g, '==="front"?', kr, ":", In);
                }
              );
            case Fo:
              return E(
                function(v) {
                  return d.command(
                    typeof v == "number" && v >= R.lineWidthDims[0] && v <= R.lineWidthDims[1],
                    "invalid line width, must be a positive number between " + R.lineWidthDims[0] + " and " + R.lineWidthDims[1],
                    c.commandStr
                  ), v;
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      "typeof " + g + '==="number"&&' + g + ">=" + R.lineWidthDims[0] + "&&" + g + "<=" + R.lineWidthDims[1],
                      "invalid line width"
                    );
                  }), g;
                }
              );
            case ko:
              return E(
                function(v) {
                  return d.commandParameter(v, jo, T, c.commandStr), jo[v];
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      g + '==="cw"||' + g + '==="ccw"',
                      "invalid frontFace, must be one of cw,ccw"
                    );
                  }), b.def(g + '==="cw"?' + xf + ":" + $o);
                }
              );
            case Co:
              return E(
                function(v) {
                  return d.command(
                    ot(v) && v.length === 4,
                    "color.mask must be length 4 array",
                    c.commandStr
                  ), v.map(function(b) {
                    return !!b;
                  });
                },
                function(v, b, g) {
                  return d.optional(function() {
                    v.assert(
                      b,
                      v.shared.isArrayLike + "(" + g + ")&&" + g + ".length===4",
                      "invalid color.mask"
                    );
                  }), Dt(4, function(z) {
                    return "!!" + g + "[" + z + "]";
                  });
                }
              );
            case Lo:
              return E(
                function(v) {
                  d.command(typeof v == "object" && v, T, c.commandStr);
                  var b = "value" in v ? v.value : 1, g = !!v.invert;
                  return d.command(
                    typeof b == "number" && b >= 0 && b <= 1,
                    "sample.coverage.value must be a number between 0 and 1",
                    c.commandStr
                  ), [b, g];
                },
                function(v, b, g) {
                  d.optional(function() {
                    v.assert(
                      b,
                      g + "&&typeof " + g + '==="object"',
                      "invalid sample.coverage"
                    );
                  });
                  var z = b.def(
                    '"value" in ',
                    g,
                    "?+",
                    g,
                    ".value:1"
                  ), F = b.def("!!", g, ".invert");
                  return [z, F];
                }
              );
          }
        }), k;
      }
      function Ze(m, c) {
        var w = m.static, P = m.dynamic, k = {};
        return Object.keys(w).forEach(function(A) {
          var T = w[A], E;
          if (typeof T == "number" || typeof T == "boolean")
            E = dt(function() {
              return T;
            });
          else if (typeof T == "function") {
            var v = T._reglType;
            v === "texture2d" || v === "textureCube" ? E = dt(function(b) {
              return b.link(T);
            }) : v === "framebuffer" || v === "framebufferCube" ? (d.command(
              T.color.length > 0,
              'missing color attachment for framebuffer sent to uniform "' + A + '"',
              c.commandStr
            ), E = dt(function(b) {
              return b.link(T.color[0]);
            })) : d.commandRaise('invalid data for uniform "' + A + '"', c.commandStr);
          } else ot(T) ? E = dt(function(b) {
            var g = b.global.def(
              "[",
              Dt(T.length, function(z) {
                return d.command(
                  typeof T[z] == "number" || typeof T[z] == "boolean",
                  "invalid uniform " + A,
                  b.commandStr
                ), T[z];
              }),
              "]"
            );
            return g;
          }) : d.commandRaise('invalid or missing data for uniform "' + A + '"', c.commandStr);
          E.value = T, k[A] = E;
        }), Object.keys(P).forEach(function(A) {
          var T = P[A];
          k[A] = Ct(T, function(E, v) {
            return E.invoke(v, T);
          });
        }), k;
      }
      function pt(m, c) {
        var w = m.static, P = m.dynamic, k = {};
        return Object.keys(w).forEach(function(A) {
          var T = w[A], E = h.id(A), v = new C();
          if (gi(T))
            v.state = er, v.buffer = j.getBuffer(
              j.create(T, rr, false, true)
            ), v.type = 0;
          else {
            var b = j.getBuffer(T);
            if (b)
              v.state = er, v.buffer = b, v.type = 0;
            else if (d.command(
              typeof T == "object" && T,
              "invalid data for attribute " + A,
              c.commandStr
            ), "constant" in T) {
              var g = T.constant;
              v.buffer = "null", v.state = _o, typeof g == "number" ? v.x = g : (d.command(
                ot(g) && g.length > 0 && g.length <= 4,
                "invalid constant for attribute " + A,
                c.commandStr
              ), Jn.forEach(function(Ee, xe) {
                xe < g.length && (v[Ee] = g[xe]);
              }));
            } else {
              gi(T.buffer) ? b = j.getBuffer(
                j.create(T.buffer, rr, false, true)
              ) : b = j.getBuffer(T.buffer), d.command(!!b, 'missing buffer for attribute "' + A + '"', c.commandStr);
              var z = T.offset | 0;
              d.command(
                z >= 0,
                'invalid offset for attribute "' + A + '"',
                c.commandStr
              );
              var F = T.stride | 0;
              d.command(
                F >= 0 && F < 256,
                'invalid stride for attribute "' + A + '", must be integer betweeen [0, 255]',
                c.commandStr
              );
              var V = T.size | 0;
              d.command(
                !("size" in T) || V > 0 && V <= 4,
                'invalid size for attribute "' + A + '", must be 1,2,3,4',
                c.commandStr
              );
              var X = !!T.normalized, se = 0;
              "type" in T && (d.commandParameter(
                T.type,
                Tn,
                "invalid type for attribute " + A,
                c.commandStr
              ), se = Tn[T.type]);
              var Ie = T.divisor | 0;
              d.optional(function() {
                "divisor" in T && (d.command(
                  Ie === 0 || ee,
                  'cannot specify divisor for attribute "' + A + '", instancing not supported',
                  c.commandStr
                ), d.command(
                  Ie >= 0,
                  'invalid divisor for attribute "' + A + '"',
                  c.commandStr
                ));
                var Ee = c.commandStr, xe = [
                  "buffer",
                  "offset",
                  "divisor",
                  "normalized",
                  "type",
                  "size",
                  "stride"
                ];
                Object.keys(T).forEach(function(H) {
                  d.command(
                    xe.indexOf(H) >= 0,
                    'unknown parameter "' + H + '" for attribute pointer "' + A + '" (valid parameters are ' + xe + ")",
                    Ee
                  );
                });
              }), v.buffer = b, v.state = er, v.size = V, v.normalized = X, v.type = se || b.dtype, v.offset = z, v.stride = F, v.divisor = Ie;
            }
          }
          k[A] = dt(function(Ee, xe) {
            var H = Ee.attribCache;
            if (E in H)
              return H[E];
            var B = {
              isStream: false
            };
            return Object.keys(v).forEach(function(Te) {
              B[Te] = v[Te];
            }), v.buffer && (B.buffer = Ee.link(v.buffer), B.type = B.type || B.buffer + ".dtype"), H[E] = B, B;
          });
        }), Object.keys(P).forEach(function(A) {
          var T = P[A];
          function E(v, b) {
            var g = v.invoke(b, T), z = v.shared, F = v.constants, V = z.isBufferArgs, X = z.buffer;
            d.optional(function() {
              v.assert(
                b,
                g + "&&(typeof " + g + '==="object"||typeof ' + g + '==="function")&&(' + V + "(" + g + ")||" + X + ".getBuffer(" + g + ")||" + X + ".getBuffer(" + g + ".buffer)||" + V + "(" + g + '.buffer)||("constant" in ' + g + "&&(typeof " + g + '.constant==="number"||' + z.isArrayLike + "(" + g + ".constant))))",
                'invalid dynamic attribute "' + A + '"'
              );
            });
            var se = {
              isStream: b.def(false)
            }, Ie = new C();
            Ie.state = er, Object.keys(Ie).forEach(function(B) {
              se[B] = b.def("" + Ie[B]);
            });
            var Ee = se.buffer, xe = se.type;
            b(
              "if(",
              V,
              "(",
              g,
              ")){",
              se.isStream,
              "=true;",
              Ee,
              "=",
              X,
              ".createStream(",
              rr,
              ",",
              g,
              ");",
              xe,
              "=",
              Ee,
              ".dtype;",
              "}else{",
              Ee,
              "=",
              X,
              ".getBuffer(",
              g,
              ");",
              "if(",
              Ee,
              "){",
              xe,
              "=",
              Ee,
              ".dtype;",
              '}else if("constant" in ',
              g,
              "){",
              se.state,
              "=",
              _o,
              ";",
              "if(typeof " + g + '.constant === "number"){',
              se[Jn[0]],
              "=",
              g,
              ".constant;",
              Jn.slice(1).map(function(B) {
                return se[B];
              }).join("="),
              "=0;",
              "}else{",
              Jn.map(function(B, Te) {
                return se[B] + "=" + g + ".constant.length>" + Te + "?" + g + ".constant[" + Te + "]:0;";
              }).join(""),
              "}}else{",
              "if(",
              V,
              "(",
              g,
              ".buffer)){",
              Ee,
              "=",
              X,
              ".createStream(",
              rr,
              ",",
              g,
              ".buffer);",
              "}else{",
              Ee,
              "=",
              X,
              ".getBuffer(",
              g,
              ".buffer);",
              "}",
              xe,
              '="type" in ',
              g,
              "?",
              F.glTypes,
              "[",
              g,
              ".type]:",
              Ee,
              ".dtype;",
              se.normalized,
              "=!!",
              g,
              ".normalized;"
            );
            function H(B) {
              b(se[B], "=", g, ".", B, "|0;");
            }
            return H("size"), H("offset"), H("stride"), H("divisor"), b("}}"), b.exit(
              "if(",
              se.isStream,
              "){",
              X,
              ".destroyStream(",
              Ee,
              ");",
              "}"
            ), se;
          }
          k[A] = Ct(T, E);
        }), k;
      }
      function it(m) {
        var c = m.static, w = m.dynamic, P = {};
        return Object.keys(c).forEach(function(k) {
          var A = c[k];
          P[k] = dt(function(T, E) {
            return typeof A == "number" || typeof A == "boolean" ? "" + A : T.link(A);
          });
        }), Object.keys(w).forEach(function(k) {
          var A = w[k];
          P[k] = Ct(A, function(T, E) {
            return T.invoke(E, A);
          });
        }), P;
      }
      function mt(m, c, w, P, k) {
        var A = m.static, T = m.dynamic;
        d.optional(function() {
          var H = [
            kn,
            _r,
            Sr,
            Fn,
            zn,
            ai,
            Ln,
            si,
            br,
            wr
          ].concat(Pe);
          function B(Te) {
            Object.keys(Te).forEach(function(_e) {
              d.command(
                H.indexOf(_e) >= 0,
                'unknown parameter "' + _e + '"',
                k.commandStr
              );
            });
          }
          B(A), B(T);
        });
        var E = Oe(m, c), v = ve(m), b = Fe(m, v, k), g = at(m, k), z = ft(m, k), F = He(m, k, E);
        function V(H) {
          var B = b[H];
          B && (z[H] = B);
        }
        V(an), V(be(oi));
        var X = Object.keys(z).length > 0, se = {
          framebuffer: v,
          draw: g,
          shader: F,
          state: z,
          dirty: X,
          scopeVAO: null,
          drawVAO: null,
          useVAO: false,
          attributes: {}
        };
        if (se.profile = me(m), se.uniforms = Ze(w, k), se.drawVAO = se.scopeVAO = g.vao, !se.drawVAO && F.program && !E && _.angle_instanced_arrays && g.static.elements) {
          var Ie = true, Ee = F.program.attributes.map(function(H) {
            var B = c.static[H];
            return Ie = Ie && !!B, B;
          });
          if (Ie && Ee.length > 0) {
            var xe = ne.getVAO(ne.createVAO({
              attributes: Ee,
              elements: g.static.elements
            }));
            se.drawVAO = new xt(null, null, null, function(H, B) {
              return H.link(xe);
            }), se.useVAO = true;
          }
        }
        return E ? se.useVAO = true : se.attributes = pt(c, k), se.context = it(P), se;
      }
      function vt(m, c, w) {
        var P = m.shared, k = P.context, A = m.scope();
        Object.keys(w).forEach(function(T) {
          c.save(k, "." + T);
          var E = w[T], v = E.append(m, c);
          Array.isArray(v) ? A(k, ".", T, "=[", v.join(), "];") : A(k, ".", T, "=", v, ";");
        }), c(A);
      }
      function gt(m, c, w, P) {
        var k = m.shared, A = k.gl, T = k.framebuffer, E;
        ge && (E = c.def(k.extensions, ".webgl_draw_buffers"));
        var v = m.constants, b = v.drawBuffer, g = v.backBuffer, z;
        w ? z = w.append(m, c) : z = c.def(T, ".next"), P || c("if(", z, "!==", T, ".cur){"), c(
          "if(",
          z,
          "){",
          A,
          ".bindFramebuffer(",
          Sf,
          ",",
          z,
          ".framebuffer);"
        ), ge && c(
          E,
          ".drawBuffersWEBGL(",
          b,
          "[",
          z,
          ".colorAttachments.length]);"
        ), c(
          "}else{",
          A,
          ".bindFramebuffer(",
          Sf,
          ",null);"
        ), ge && c(E, ".drawBuffersWEBGL(", g, ");"), c(
          "}",
          T,
          ".cur=",
          z,
          ";"
        ), P || c("}");
      }
      function _t(m, c, w) {
        var P = m.shared, k = P.gl, A = m.current, T = m.next, E = P.current, v = P.next, b = m.cond(E, ".dirty");
        Pe.forEach(function(g) {
          var z = be(g);
          if (!(z in w.state)) {
            var F, V;
            if (z in T) {
              F = T[z], V = A[z];
              var X = Dt(pe[z].length, function(Ie) {
                return b.def(F, "[", Ie, "]");
              });
              b(m.cond(X.map(function(Ie, Ee) {
                return Ie + "!==" + V + "[" + Ee + "]";
              }).join("||")).then(
                k,
                ".",
                $[z],
                "(",
                X,
                ");",
                X.map(function(Ie, Ee) {
                  return V + "[" + Ee + "]=" + Ie;
                }).join(";"),
                ";"
              ));
            } else {
              F = b.def(v, ".", z);
              var se = m.cond(F, "!==", E, ".", z);
              b(se), z in U ? se(
                m.cond(F).then(k, ".enable(", U[z], ");").else(k, ".disable(", U[z], ");"),
                E,
                ".",
                z,
                "=",
                F,
                ";"
              ) : se(
                k,
                ".",
                $[z],
                "(",
                F,
                ");",
                E,
                ".",
                z,
                "=",
                F,
                ";"
              );
            }
          }
        }), Object.keys(w.state).length === 0 && b(E, ".dirty=false;"), c(b);
      }
      function Tt(m, c, w, P) {
        var k = m.shared, A = m.current, T = k.current, E = k.gl;
        Af(Object.keys(w)).forEach(function(v) {
          var b = w[v];
          if (!(P && !P(b))) {
            var g = b.append(m, c);
            if (U[v]) {
              var z = U[v];
              yn(b) ? g ? c(E, ".enable(", z, ");") : c(E, ".disable(", z, ");") : c(m.cond(g).then(E, ".enable(", z, ");").else(E, ".disable(", z, ");")), c(T, ".", v, "=", g, ";");
            } else if (ot(g)) {
              var F = A[v];
              c(
                E,
                ".",
                $[v],
                "(",
                g,
                ");",
                g.map(function(V, X) {
                  return F + "[" + X + "]=" + V;
                }).join(";"),
                ";"
              );
            } else
              c(
                E,
                ".",
                $[v],
                "(",
                g,
                ");",
                T,
                ".",
                v,
                "=",
                g,
                ";"
              );
          }
        });
      }
      function ct(m, c) {
        ee && (m.instancing = c.def(
          m.shared.extensions,
          ".angle_instanced_arrays"
        ));
      }
      function Ve(m, c, w, P, k) {
        var A = m.shared, T = m.stats, E = A.current, v = A.timer, b = w.profile;
        function g() {
          return typeof performance > "u" ? "Date.now()" : "performance.now()";
        }
        var z, F;
        function V(H) {
          z = c.def(), H(z, "=", g(), ";"), typeof k == "string" ? H(T, ".count+=", k, ";") : H(T, ".count++;"), K && (P ? (F = c.def(), H(F, "=", v, ".getNumPendingQueries();")) : H(v, ".beginQuery(", T, ");"));
        }
        function X(H) {
          H(T, ".cpuTime+=", g(), "-", z, ";"), K && (P ? H(
            v,
            ".pushScopeStats(",
            F,
            ",",
            v,
            ".getNumPendingQueries(),",
            T,
            ");"
          ) : H(v, ".endQuery();"));
        }
        function se(H) {
          var B = c.def(E, ".profile");
          c(E, ".profile=", H, ";"), c.exit(E, ".profile=", B, ";");
        }
        var Ie;
        if (b) {
          if (yn(b)) {
            b.enable ? (V(c), X(c.exit), se("true")) : se("false");
            return;
          }
          Ie = b.append(m, c), se(Ie);
        } else
          Ie = c.def(E, ".profile");
        var Ee = m.block();
        V(Ee), c("if(", Ie, "){", Ee, "}");
        var xe = m.block();
        X(xe), c.exit("if(", Ie, "){", xe, "}");
      }
      function At(m, c, w, P, k) {
        var A = m.shared;
        function T(v) {
          switch (v) {
            case fi:
            case li:
            case mi:
              return 2;
            case ui:
            case hi:
            case pi:
              return 3;
            case ci:
            case di:
            case vi:
              return 4;
            default:
              return 1;
          }
        }
        function E(v, b, g) {
          var z = A.gl, F = c.def(v, ".location"), V = c.def(A.attributes, "[", F, "]"), X = g.state, se = g.buffer, Ie = [
            g.x,
            g.y,
            g.z,
            g.w
          ], Ee = [
            "buffer",
            "normalized",
            "offset",
            "stride"
          ];
          function xe() {
            c(
              "if(!",
              V,
              ".buffer){",
              z,
              ".enableVertexAttribArray(",
              F,
              ");}"
            );
            var B = g.type, Te;
            if (g.size ? Te = c.def(g.size, "||", b) : Te = b, c(
              "if(",
              V,
              ".type!==",
              B,
              "||",
              V,
              ".size!==",
              Te,
              "||",
              Ee.map(function(ze) {
                return V + "." + ze + "!==" + g[ze];
              }).join("||"),
              "){",
              z,
              ".bindBuffer(",
              rr,
              ",",
              se,
              ".buffer);",
              z,
              ".vertexAttribPointer(",
              [
                F,
                Te,
                B,
                g.normalized,
                g.stride,
                g.offset
              ],
              ");",
              V,
              ".type=",
              B,
              ";",
              V,
              ".size=",
              Te,
              ";",
              Ee.map(function(ze) {
                return V + "." + ze + "=" + g[ze] + ";";
              }).join(""),
              "}"
            ), ee) {
              var _e = g.divisor;
              c(
                "if(",
                V,
                ".divisor!==",
                _e,
                "){",
                m.instancing,
                ".vertexAttribDivisorANGLE(",
                [F, _e],
                ");",
                V,
                ".divisor=",
                _e,
                ";}"
              );
            }
          }
          function H() {
            c(
              "if(",
              V,
              ".buffer){",
              z,
              ".disableVertexAttribArray(",
              F,
              ");",
              V,
              ".buffer=null;",
              "}if(",
              Jn.map(function(B, Te) {
                return V + "." + B + "!==" + Ie[Te];
              }).join("||"),
              "){",
              z,
              ".vertexAttrib4f(",
              F,
              ",",
              Ie,
              ");",
              Jn.map(function(B, Te) {
                return V + "." + B + "=" + Ie[Te] + ";";
              }).join(""),
              "}"
            );
          }
          X === er ? xe() : X === _o ? H() : (c("if(", X, "===", er, "){"), xe(), c("}else{"), H(), c("}"));
        }
        P.forEach(function(v) {
          var b = v.name, g = w.attributes[b], z;
          if (g) {
            if (!k(g))
              return;
            z = g.append(m, c);
          } else {
            if (!k(Ef))
              return;
            var F = m.scopeAttrib(b);
            d.optional(function() {
              m.assert(
                c,
                F + ".state",
                "missing attribute " + b
              );
            }), z = {}, Object.keys(new C()).forEach(function(V) {
              z[V] = c.def(F, ".", V);
            });
          }
          E(
            m.link(v),
            T(v.info.type),
            z
          );
        });
      }
      function Ke(m, c, w, P, k, A) {
        for (var T = m.shared, E = T.gl, v = {}, b, g = 0; g < P.length; ++g) {
          var z = P[g], F = z.name, V = z.info.type, X = z.info.size, se = w.uniforms[F];
          if (X > 1) {
            if (!se)
              continue;
            var Ie = F.replace("[0]", "");
            if (v[Ie])
              continue;
            v[Ie] = 1;
          }
          var Ee = m.link(z), xe = Ee + ".location", H;
          if (se) {
            if (!k(se))
              continue;
            if (yn(se)) {
              var B = se.value;
              if (d.command(
                B !== null && typeof B < "u",
                'missing uniform "' + F + '"',
                m.commandStr
              ), V === Pr || V === Cr) {
                d.command(
                  typeof B == "function" && (V === Pr && (B._reglType === "texture2d" || B._reglType === "framebuffer") || V === Cr && (B._reglType === "textureCube" || B._reglType === "framebufferCube")),
                  "invalid texture for uniform " + F,
                  m.commandStr
                );
                var Te = m.link(B._texture || B.color[0]._texture);
                c(E, ".uniform1i(", xe, ",", Te + ".bind());"), c.exit(Te, ".unbind();");
              } else if (V === Tr || V === Ar || V === Er) {
                d.optional(function() {
                  d.command(
                    ot(B),
                    "invalid matrix for uniform " + F,
                    m.commandStr
                  ), d.command(
                    V === Tr && B.length === 4 || V === Ar && B.length === 9 || V === Er && B.length === 16,
                    "invalid length for matrix uniform " + F,
                    m.commandStr
                  );
                });
                var _e = m.global.def("new Float32Array([" + Array.prototype.slice.call(B) + "])"), ze = 2;
                V === Ar ? ze = 3 : V === Er && (ze = 4), c(
                  E,
                  ".uniformMatrix",
                  ze,
                  "fv(",
                  xe,
                  ",false,",
                  _e,
                  ");"
                );
              } else {
                switch (V) {
                  case No:
                    X === 1 ? d.commandType(B, "number", "uniform " + F, m.commandStr) : d.command(
                      ot(B) && B.length === X,
                      "uniform " + F,
                      m.commandStr
                    ), b = "1f";
                    break;
                  case fi:
                    d.command(
                      ot(B) && B.length && B.length % 2 === 0 && B.length <= X * 2,
                      "uniform " + F,
                      m.commandStr
                    ), b = "2f";
                    break;
                  case ui:
                    d.command(
                      ot(B) && B.length && B.length % 3 === 0 && B.length <= X * 3,
                      "uniform " + F,
                      m.commandStr
                    ), b = "3f";
                    break;
                  case ci:
                    d.command(
                      ot(B) && B.length && B.length % 4 === 0 && B.length <= X * 4,
                      "uniform " + F,
                      m.commandStr
                    ), b = "4f";
                    break;
                  case Bo:
                    X === 1 ? d.commandType(B, "boolean", "uniform " + F, m.commandStr) : d.command(
                      ot(B) && B.length === X,
                      "uniform " + F,
                      m.commandStr
                    ), b = "1i";
                    break;
                  case Go:
                    X === 1 ? d.commandType(B, "number", "uniform " + F, m.commandStr) : d.command(
                      ot(B) && B.length === X,
                      "uniform " + F,
                      m.commandStr
                    ), b = "1i";
                    break;
                  case mi:
                    d.command(
                      ot(B) && B.length && B.length % 2 === 0 && B.length <= X * 2,
                      "uniform " + F,
                      m.commandStr
                    ), b = "2i";
                    break;
                  case li:
                    d.command(
                      ot(B) && B.length && B.length % 2 === 0 && B.length <= X * 2,
                      "uniform " + F,
                      m.commandStr
                    ), b = "2i";
                    break;
                  case pi:
                    d.command(
                      ot(B) && B.length && B.length % 3 === 0 && B.length <= X * 3,
                      "uniform " + F,
                      m.commandStr
                    ), b = "3i";
                    break;
                  case hi:
                    d.command(
                      ot(B) && B.length && B.length % 3 === 0 && B.length <= X * 3,
                      "uniform " + F,
                      m.commandStr
                    ), b = "3i";
                    break;
                  case vi:
                    d.command(
                      ot(B) && B.length && B.length % 4 === 0 && B.length <= X * 4,
                      "uniform " + F,
                      m.commandStr
                    ), b = "4i";
                    break;
                  case di:
                    d.command(
                      ot(B) && B.length && B.length % 4 === 0 && B.length <= X * 4,
                      "uniform " + F,
                      m.commandStr
                    ), b = "4i";
                    break;
                }
                X > 1 ? (b += "v", B = m.global.def("[" + Array.prototype.slice.call(B) + "]")) : B = ot(B) ? Array.prototype.slice.call(B) : B, c(
                  E,
                  ".uniform",
                  b,
                  "(",
                  xe,
                  ",",
                  B,
                  ");"
                );
              }
              continue;
            } else
              H = se.append(m, c);
          } else {
            if (!k(Ef))
              continue;
            H = c.def(T.uniforms, "[", h.id(F), "]");
          }
          V === Pr ? (d(!Array.isArray(H), "must specify a scalar prop for textures"), c(
            "if(",
            H,
            "&&",
            H,
            '._reglType==="framebuffer"){',
            H,
            "=",
            H,
            ".color[0];",
            "}"
          )) : V === Cr && (d(!Array.isArray(H), "must specify a scalar prop for cube maps"), c(
            "if(",
            H,
            "&&",
            H,
            '._reglType==="framebufferCube"){',
            H,
            "=",
            H,
            ".color[0];",
            "}"
          )), d.optional(function() {
            function Ot(kt, xi) {
              m.assert(
                c,
                kt,
                'bad data or missing for uniform "' + F + '".  ' + xi
              );
            }
            function On(kt, xi) {
              xi === 1 && d(!Array.isArray(H), "must not specify an array type for uniform"), Ot(
                "Array.isArray(" + H + ") && typeof " + H + '[0]===" ' + kt + '" || typeof ' + H + '==="' + kt + '"',
                "invalid type, expected " + kt
              );
            }
            function Vt(kt, xi, yi) {
              Array.isArray(H) ? d(H.length && H.length % kt === 0 && H.length <= kt * yi, "must have length of " + (yi === 1 ? "" : "n * ") + kt) : Ot(
                T.isArrayLike + "(" + H + ")&&" + H + ".length && " + H + ".length % " + kt + " === 0 && " + H + ".length<=" + kt * yi,
                "invalid vector, should have length of " + (yi === 1 ? "" : "n * ") + kt,
                m.commandStr
              );
            }
            function If(kt) {
              d(!Array.isArray(H), "must not specify a value type"), Ot(
                "typeof " + H + '==="function"&&' + H + '._reglType==="texture' + (kt === gf ? "2d" : "Cube") + '"',
                "invalid texture type",
                m.commandStr
              );
            }
            switch (V) {
              case Go:
                On("number", X);
                break;
              case li:
                Vt(2, "number", X);
                break;
              case hi:
                Vt(3, "number", X);
                break;
              case di:
                Vt(4, "number", X);
                break;
              case No:
                On("number", X);
                break;
              case fi:
                Vt(2, "number", X);
                break;
              case ui:
                Vt(3, "number", X);
                break;
              case ci:
                Vt(4, "number", X);
                break;
              case Bo:
                On("boolean", X);
                break;
              case mi:
                Vt(2, "boolean", X);
                break;
              case pi:
                Vt(3, "boolean", X);
                break;
              case vi:
                Vt(4, "boolean", X);
                break;
              case Tr:
                Vt(4, "number", X);
                break;
              case Ar:
                Vt(9, "number", X);
                break;
              case Er:
                Vt(16, "number", X);
                break;
              case Pr:
                If(gf);
                break;
              case Cr:
                If(Yh);
                break;
            }
          });
          var Je = 1;
          switch (V) {
            case Pr:
            case Cr:
              var Et = c.def(H, "._texture");
              c(E, ".uniform1i(", xe, ",", Et, ".bind());"), c.exit(Et, ".unbind();");
              continue;
            case Go:
            case Bo:
              b = "1i";
              break;
            case li:
            case mi:
              b = "2i", Je = 2;
              break;
            case hi:
            case pi:
              b = "3i", Je = 3;
              break;
            case di:
            case vi:
              b = "4i", Je = 4;
              break;
            case No:
              b = "1f";
              break;
            case fi:
              b = "2f", Je = 2;
              break;
            case ui:
              b = "3f", Je = 3;
              break;
            case ci:
              b = "4f", Je = 4;
              break;
            case Tr:
              b = "Matrix2fv";
              break;
            case Ar:
              b = "Matrix3fv";
              break;
            case Er:
              b = "Matrix4fv";
              break;
          }
          if (b.indexOf("Matrix") === -1 && X > 1 && (b += "v", Je = 1), b.charAt(0) === "M") {
            c(E, ".uniform", b, "(", xe, ",");
            var Dn = Math.pow(V - Tr + 2, 2), sn = m.global.def("new Float32Array(", Dn, ")");
            Array.isArray(H) ? c(
              "false,(",
              Dt(Dn, function(Ot) {
                return sn + "[" + Ot + "]=" + H[Ot];
              }),
              ",",
              sn,
              ")"
            ) : c(
              "false,(Array.isArray(",
              H,
              ")||",
              H,
              " instanceof Float32Array)?",
              H,
              ":(",
              Dt(Dn, function(Ot) {
                return sn + "[" + Ot + "]=" + H + "[" + Ot + "]";
              }),
              ",",
              sn,
              ")"
            ), c(");");
          } else if (Je > 1) {
            for (var Jt = [], bn = [], Mn = 0; Mn < Je; ++Mn)
              Array.isArray(H) ? bn.push(H[Mn]) : bn.push(c.def(H + "[" + Mn + "]")), A && Jt.push(c.def());
            A && c("if(!", m.batchId, "||", Jt.map(function(Ot, On) {
              return Ot + "!==" + bn[On];
            }).join("||"), "){", Jt.map(function(Ot, On) {
              return Ot + "=" + bn[On] + ";";
            }).join("")), c(E, ".uniform", b, "(", xe, ",", bn.join(","), ");"), A && c("}");
          } else {
            if (d(!Array.isArray(H), "uniform value must not be an array"), A) {
              var Lf = c.def();
              c(
                "if(!",
                m.batchId,
                "||",
                Lf,
                "!==",
                H,
                "){",
                Lf,
                "=",
                H,
                ";"
              );
            }
            c(E, ".uniform", b, "(", xe, ",", H, ");"), A && c("}");
          }
        }
      }
      function ke(m, c, w, P) {
        var k = m.shared, A = k.gl, T = k.draw, E = P.draw;
        function v() {
          var Te = E.elements, _e, ze = c;
          return Te ? ((Te.contextDep && P.contextDynamic || Te.propDep) && (ze = w), _e = Te.append(m, ze), E.elementsActive && ze(
            "if(" + _e + ")" + A + ".bindBuffer(" + Oo + "," + _e + ".buffer.buffer);"
          )) : (_e = ze.def(), ze(
            _e,
            "=",
            T,
            ".",
            Fn,
            ";",
            "if(",
            _e,
            "){",
            A,
            ".bindBuffer(",
            Oo,
            ",",
            _e,
            ".buffer.buffer);}",
            "else if(",
            k.vao,
            ".currentVAO){",
            _e,
            "=",
            m.shared.elements + ".getElements(" + k.vao,
            ".currentVAO.elements);",
            W ? "" : "if(" + _e + ")" + A + ".bindBuffer(" + Oo + "," + _e + ".buffer.buffer);",
            "}"
          )), _e;
        }
        function b() {
          var Te = E.count, _e, ze = c;
          return Te ? ((Te.contextDep && P.contextDynamic || Te.propDep) && (ze = w), _e = Te.append(m, ze), d.optional(function() {
            Te.MISSING && m.assert(c, "false", "missing vertex count"), Te.DYNAMIC && m.assert(ze, _e + ">=0", "missing vertex count");
          })) : (_e = ze.def(T, ".", Ln), d.optional(function() {
            m.assert(ze, _e + ">=0", "missing vertex count");
          })), _e;
        }
        var g = v();
        function z(Te) {
          var _e = E[Te];
          return _e ? _e.contextDep && P.contextDynamic || _e.propDep ? _e.append(m, w) : _e.append(m, c) : c.def(T, ".", Te);
        }
        var F = z(zn), V = z(ai), X = b();
        if (typeof X == "number") {
          if (X === 0)
            return;
        } else
          w("if(", X, "){"), w.exit("}");
        var se, Ie;
        ee && (se = z(si), Ie = m.instancing);
        var Ee = g + ".type", xe = E.elements && yn(E.elements) && !E.vaoActive;
        function H() {
          function Te() {
            w(Ie, ".drawElementsInstancedANGLE(", [
              F,
              X,
              Ee,
              V + "<<((" + Ee + "-" + Ys + ")>>1)",
              se
            ], ");");
          }
          function _e() {
            w(
              Ie,
              ".drawArraysInstancedANGLE(",
              [F, V, X, se],
              ");"
            );
          }
          g && g !== "null" ? xe ? Te() : (w("if(", g, "){"), Te(), w("}else{"), _e(), w("}")) : _e();
        }
        function B() {
          function Te() {
            w(A + ".drawElements(" + [
              F,
              X,
              Ee,
              V + "<<((" + Ee + "-" + Ys + ")>>1)"
            ] + ");");
          }
          function _e() {
            w(A + ".drawArrays(" + [F, V, X] + ");");
          }
          g && g !== "null" ? xe ? Te() : (w("if(", g, "){"), Te(), w("}else{"), _e(), w("}")) : _e();
        }
        ee && (typeof se != "number" || se >= 0) ? typeof se == "string" ? (w("if(", se, ">0){"), H(), w("}else if(", se, "<0){"), B(), w("}")) : H() : B();
      }
      function We(m, c, w, P, k) {
        var A = De(), T = A.proc("body", k);
        return d.optional(function() {
          A.commandStr = c.commandStr, A.command = A.link(c.commandStr);
        }), ee && (A.instancing = T.def(
          A.shared.extensions,
          ".angle_instanced_arrays"
        )), m(A, T, w, P), A.compile().body;
      }
      function Qe(m, c, w, P) {
        ct(m, c), w.useVAO ? w.drawVAO ? c(m.shared.vao, ".setVAO(", w.drawVAO.append(m, c), ");") : c(m.shared.vao, ".setVAO(", m.shared.vao, ".targetVAO);") : (c(m.shared.vao, ".setVAO(null);"), At(m, c, w, P.attributes, function() {
          return true;
        })), Ke(m, c, w, P.uniforms, function() {
          return true;
        }, false), ke(m, c, c, w);
      }
      function lt(m, c) {
        var w = m.proc("draw", 1);
        ct(m, w), vt(m, w, c.context), gt(m, w, c.framebuffer), _t(m, w, c), Tt(m, w, c.state), Ve(m, w, c, false, true);
        var P = c.shader.progVar.append(m, w);
        if (w(m.shared.gl, ".useProgram(", P, ".program);"), c.shader.program)
          Qe(m, w, c, c.shader.program);
        else {
          w(m.shared.vao, ".setVAO(null);");
          var k = m.global.def("{}"), A = w.def(P, ".id"), T = w.def(k, "[", A, "]");
          w(
            m.cond(T).then(T, ".call(this,a0);").else(
              T,
              "=",
              k,
              "[",
              A,
              "]=",
              m.link(function(E) {
                return We(Qe, m, c, E, 1);
              }),
              "(",
              P,
              ");",
              T,
              ".call(this,a0);"
            )
          );
        }
        Object.keys(c.state).length > 0 && w(m.shared.current, ".dirty=true;"), m.shared.vao && w(m.shared.vao, ".setVAO(null);");
      }
      function Kt(m, c, w, P) {
        m.batchId = "a1", ct(m, c);
        function k() {
          return true;
        }
        At(m, c, w, P.attributes, k), Ke(m, c, w, P.uniforms, k, false), ke(m, c, c, w);
      }
      function Rn(m, c, w, P) {
        ct(m, c);
        var k = w.contextDep, A = c.def(), T = "a0", E = "a1", v = c.def();
        m.shared.props = v, m.batchId = A;
        var b = m.scope(), g = m.scope();
        c(
          b.entry,
          "for(",
          A,
          "=0;",
          A,
          "<",
          E,
          ";++",
          A,
          "){",
          v,
          "=",
          T,
          "[",
          A,
          "];",
          g,
          "}",
          b.exit
        );
        function z(Ee) {
          return Ee.contextDep && k || Ee.propDep;
        }
        function F(Ee) {
          return !z(Ee);
        }
        if (w.needsContext && vt(m, g, w.context), w.needsFramebuffer && gt(m, g, w.framebuffer), Tt(m, g, w.state, z), w.profile && z(w.profile) && Ve(m, g, w, false, true), P)
          w.useVAO ? w.drawVAO ? z(w.drawVAO) ? g(m.shared.vao, ".setVAO(", w.drawVAO.append(m, g), ");") : b(m.shared.vao, ".setVAO(", w.drawVAO.append(m, b), ");") : b(m.shared.vao, ".setVAO(", m.shared.vao, ".targetVAO);") : (b(m.shared.vao, ".setVAO(null);"), At(m, b, w, P.attributes, F), At(m, g, w, P.attributes, z)), Ke(m, b, w, P.uniforms, F, false), Ke(m, g, w, P.uniforms, z, true), ke(m, b, g, w);
        else {
          var V = m.global.def("{}"), X = w.shader.progVar.append(m, g), se = g.def(X, ".id"), Ie = g.def(V, "[", se, "]");
          g(
            m.shared.gl,
            ".useProgram(",
            X,
            ".program);",
            "if(!",
            Ie,
            "){",
            Ie,
            "=",
            V,
            "[",
            se,
            "]=",
            m.link(function(Ee) {
              return We(
                Kt,
                m,
                w,
                Ee,
                2
              );
            }),
            "(",
            X,
            ");}",
            Ie,
            ".call(this,a0[",
            A,
            "],",
            A,
            ");"
          );
        }
      }
      function x(m, c) {
        var w = m.proc("batch", 2);
        m.batchId = "0", ct(m, w);
        var P = false, k = true;
        Object.keys(c.context).forEach(function(V) {
          P = P || c.context[V].propDep;
        }), P || (vt(m, w, c.context), k = false);
        var A = c.framebuffer, T = false;
        A ? (A.propDep ? P = T = true : A.contextDep && P && (T = true), T || gt(m, w, A)) : gt(m, w, null), c.state.viewport && c.state.viewport.propDep && (P = true);
        function E(V) {
          return V.contextDep && P || V.propDep;
        }
        _t(m, w, c), Tt(m, w, c.state, function(V) {
          return !E(V);
        }), (!c.profile || !E(c.profile)) && Ve(m, w, c, false, "a1"), c.contextDep = P, c.needsContext = k, c.needsFramebuffer = T;
        var v = c.shader.progVar;
        if (v.contextDep && P || v.propDep)
          Rn(
            m,
            w,
            c,
            null
          );
        else {
          var b = v.append(m, w);
          if (w(m.shared.gl, ".useProgram(", b, ".program);"), c.shader.program)
            Rn(
              m,
              w,
              c,
              c.shader.program
            );
          else {
            w(m.shared.vao, ".setVAO(null);");
            var g = m.global.def("{}"), z = w.def(b, ".id"), F = w.def(g, "[", z, "]");
            w(
              m.cond(F).then(F, ".call(this,a0,a1);").else(
                F,
                "=",
                g,
                "[",
                z,
                "]=",
                m.link(function(V) {
                  return We(Rn, m, c, V, 2);
                }),
                "(",
                b,
                ");",
                F,
                ".call(this,a0,a1);"
              )
            );
          }
        }
        Object.keys(c.state).length > 0 && w(m.shared.current, ".dirty=true;"), m.shared.vao && w(m.shared.vao, ".setVAO(null);");
      }
      function M(m, c) {
        var w = m.proc("scope", 3);
        m.batchId = "a2";
        var P = m.shared, k = P.current;
        vt(m, w, c.context), c.framebuffer && c.framebuffer.append(m, w), Af(Object.keys(c.state)).forEach(function(T) {
          var E = c.state[T], v = E.append(m, w);
          ot(v) ? v.forEach(function(b, g) {
            w.set(m.next[T], "[" + g + "]", b);
          }) : w.set(P.next, "." + T, v);
        }), Ve(m, w, c, true, true), [Fn, ai, Ln, si, zn].forEach(
          function(T) {
            var E = c.draw[T];
            E && w.set(P.draw, "." + T, "" + E.append(m, w));
          }
        ), Object.keys(c.uniforms).forEach(function(T) {
          var E = c.uniforms[T].append(m, w);
          Array.isArray(E) && (E = "[" + E.join() + "]"), w.set(
            P.uniforms,
            "[" + h.id(T) + "]",
            E
          );
        }), Object.keys(c.attributes).forEach(function(T) {
          var E = c.attributes[T].append(m, w), v = m.scopeAttrib(T);
          Object.keys(new C()).forEach(function(b) {
            w.set(v, "." + b, E[b]);
          });
        }), c.scopeVAO && w.set(P.vao, ".targetVAO", c.scopeVAO.append(m, w));
        function A(T) {
          var E = c.shader[T];
          E && w.set(P.shader, "." + T, E.append(m, w));
        }
        A(_r), A(Sr), Object.keys(c.state).length > 0 && (w(k, ".dirty=true;"), w.exit(k, ".dirty=true;")), w("a1(", m.shared.context, ",a0,", m.batchId, ");");
      }
      function L(m) {
        if (!(typeof m != "object" || ot(m))) {
          for (var c = Object.keys(m), w = 0; w < c.length; ++w)
            if (Rt.isDynamic(m[c[w]]))
              return true;
          return false;
        }
      }
      function ye(m, c, w) {
        var P = c.static[w];
        if (!P || !L(P))
          return;
        var k = m.global, A = Object.keys(P), T = false, E = false, v = false, b = m.global.def("{}");
        A.forEach(function(z) {
          var F = P[z];
          if (Rt.isDynamic(F)) {
            typeof F == "function" && (F = P[z] = Rt.unbox(F));
            var V = Ct(F, null);
            T = T || V.thisDep, v = v || V.propDep, E = E || V.contextDep;
          } else {
            switch (k(b, ".", z, "="), typeof F) {
              case "number":
                k(F);
                break;
              case "string":
                k('"', F, '"');
                break;
              case "object":
                Array.isArray(F) && k("[", F.join(), "]");
                break;
              default:
                k(m.link(F));
                break;
            }
            k(";");
          }
        });
        function g(z, F) {
          A.forEach(function(V) {
            var X = P[V];
            if (Rt.isDynamic(X)) {
              var se = z.invoke(F, X);
              F(b, ".", V, "=", se, ";");
            }
          });
        }
        c.dynamic[w] = new Rt.DynamicVariable(ii, {
          thisDep: T,
          contextDep: E,
          propDep: v,
          ref: b,
          append: g
        }), delete c.static[w];
      }
      function Xe(m, c, w, P, k) {
        var A = De();
        A.stats = A.link(k), Object.keys(c.static).forEach(function(E) {
          ye(A, c, E);
        }), Hh.forEach(function(E) {
          ye(A, m, E);
        });
        var T = mt(m, c, w, P, A);
        return lt(A, T), M(A, T), x(A, T), r(A.compile(), {
          destroy: function() {
            T.shader.program.destroy();
          }
        });
      }
      return {
        next: de,
        current: pe,
        procs: function() {
          var m = De(), c = m.proc("poll"), w = m.proc("refresh"), P = m.block();
          c(P), w(P);
          var k = m.shared, A = k.gl, T = k.next, E = k.current;
          P(E, ".dirty=false;"), gt(m, c), gt(m, w, null, true);
          var v;
          ee && (v = m.link(ee)), _.oes_vertex_array_object && w(m.link(_.oes_vertex_array_object), ".bindVertexArrayOES(null);");
          for (var b = 0; b < R.maxAttributes; ++b) {
            var g = w.def(k.attributes, "[", b, "]"), z = m.cond(g, ".buffer");
            z.then(
              A,
              ".enableVertexAttribArray(",
              b,
              ");",
              A,
              ".bindBuffer(",
              rr,
              ",",
              g,
              ".buffer.buffer);",
              A,
              ".vertexAttribPointer(",
              b,
              ",",
              g,
              ".size,",
              g,
              ".type,",
              g,
              ".normalized,",
              g,
              ".stride,",
              g,
              ".offset);"
            ).else(
              A,
              ".disableVertexAttribArray(",
              b,
              ");",
              A,
              ".vertexAttrib4f(",
              b,
              ",",
              g,
              ".x,",
              g,
              ".y,",
              g,
              ".z,",
              g,
              ".w);",
              g,
              ".buffer=null;"
            ), w(z), ee && w(
              v,
              ".vertexAttribDivisorANGLE(",
              b,
              ",",
              g,
              ".divisor);"
            );
          }
          return w(
            m.shared.vao,
            ".currentVAO=null;",
            m.shared.vao,
            ".setVAO(",
            m.shared.vao,
            ".targetVAO);"
          ), Object.keys(U).forEach(function(F) {
            var V = U[F], X = P.def(T, ".", F), se = m.block();
            se(
              "if(",
              X,
              "){",
              A,
              ".enable(",
              V,
              ")}else{",
              A,
              ".disable(",
              V,
              ")}",
              E,
              ".",
              F,
              "=",
              X,
              ";"
            ), w(se), c(
              "if(",
              X,
              "!==",
              E,
              ".",
              F,
              "){",
              se,
              "}"
            );
          }), Object.keys($).forEach(function(F) {
            var V = $[F], X = pe[F], se, Ie, Ee = m.block();
            if (Ee(A, ".", V, "("), ot(X)) {
              var xe = X.length;
              se = m.global.def(T, ".", F), Ie = m.global.def(E, ".", F), Ee(
                Dt(xe, function(H) {
                  return se + "[" + H + "]";
                }),
                ");",
                Dt(xe, function(H) {
                  return Ie + "[" + H + "]=" + se + "[" + H + "];";
                }).join("")
              ), c(
                "if(",
                Dt(xe, function(H) {
                  return se + "[" + H + "]!==" + Ie + "[" + H + "]";
                }).join("||"),
                "){",
                Ee,
                "}"
              );
            } else
              se = P.def(T, ".", F), Ie = P.def(E, ".", F), Ee(
                se,
                ");",
                E,
                ".",
                F,
                "=",
                se,
                ";"
              ), c(
                "if(",
                se,
                "!==",
                Ie,
                "){",
                Ee,
                "}"
              );
            w(Ee);
          }), m.compile();
        }(),
        compile: Xe
      };
    }
    function ld() {
      return {
        vaoCount: 0,
        bufferCount: 0,
        elementsCount: 0,
        framebufferCount: 0,
        shaderCount: 0,
        textureCount: 0,
        cubeCount: 0,
        renderbufferCount: 0,
        maxTextureUnits: 0
      };
    }
    var hd = 34918, dd = 34919, Pf = 35007, md = function(a, h) {
      if (!h.ext_disjoint_timer_query)
        return null;
      var _ = [];
      function R() {
        return _.pop() || h.ext_disjoint_timer_query.createQueryEXT();
      }
      function j(ee) {
        _.push(ee);
      }
      var I = [];
      function G(ee) {
        var ge = R();
        h.ext_disjoint_timer_query.beginQueryEXT(Pf, ge), I.push(ge), K(I.length - 1, I.length, ee);
      }
      function Q() {
        h.ext_disjoint_timer_query.endQueryEXT(Pf);
      }
      function Y() {
        this.startQueryIndex = -1, this.endQueryIndex = -1, this.sum = 0, this.stats = null;
      }
      var ne = [];
      function ie() {
        return ne.pop() || new Y();
      }
      function re(ee) {
        ne.push(ee);
      }
      var fe = [];
      function K(ee, ge, W) {
        var pe = ie();
        pe.startQueryIndex = ee, pe.endQueryIndex = ge, pe.sum = 0, pe.stats = W, fe.push(pe);
      }
      var te = [], C = [];
      function O() {
        var ee, ge, W = I.length;
        if (W !== 0) {
          C.length = Math.max(C.length, W + 1), te.length = Math.max(te.length, W + 1), te[0] = 0, C[0] = 0;
          var pe = 0;
          for (ee = 0, ge = 0; ge < I.length; ++ge) {
            var de = I[ge];
            h.ext_disjoint_timer_query.getQueryObjectEXT(de, dd) ? (pe += h.ext_disjoint_timer_query.getQueryObjectEXT(de, hd), j(de)) : I[ee++] = de, te[ge + 1] = pe, C[ge + 1] = ee;
          }
          for (I.length = ee, ee = 0, ge = 0; ge < fe.length; ++ge) {
            var Pe = fe[ge], U = Pe.startQueryIndex, $ = Pe.endQueryIndex;
            Pe.sum += te[$] - te[U];
            var be = C[U], ue = C[$];
            ue === be ? (Pe.stats.gpuTime += Pe.sum / 1e6, re(Pe)) : (Pe.startQueryIndex = be, Pe.endQueryIndex = ue, fe[ee++] = Pe);
          }
          fe.length = ee;
        }
      }
      return {
        beginQuery: G,
        endQuery: Q,
        pushScopeStats: K,
        update: O,
        getNumPendingQueries: function() {
          return I.length;
        },
        clear: function() {
          _.push.apply(_, I);
          for (var ee = 0; ee < _.length; ee++)
            h.ext_disjoint_timer_query.deleteQueryEXT(_[ee]);
          I.length = 0, _.length = 0;
        },
        restore: function() {
          I.length = 0, _.length = 0;
        }
      };
    }, pd = 16384, vd = 256, gd = 1024, xd = 34962, Cf = "webglcontextlost", kf = "webglcontextrestored", Ff = 1, yd = 2, bd = 3;
    function zf(a, h) {
      for (var _ = 0; _ < a.length; ++_)
        if (a[_] === h)
          return _;
      return -1;
    }
    function _d(a) {
      var h = yc(a);
      if (!h)
        return null;
      var _ = h.gl, R = _.getContextAttributes(), j = _.isContextLost(), I = bc(_, h);
      if (!I)
        return null;
      var G = mc(), Q = ld(), Y = I.extensions, ne = md(_, Y), ie = Na(), re = _.drawingBufferWidth, fe = _.drawingBufferHeight, K = {
        tick: 0,
        time: 0,
        viewportWidth: re,
        viewportHeight: fe,
        framebufferWidth: re,
        framebufferHeight: fe,
        drawingBufferWidth: re,
        drawingBufferHeight: fe,
        pixelRatio: h.pixelRatio
      }, te = {}, C = {
        elements: null,
        primitive: 4,
        // GL_TRIANGLES
        count: -1,
        offset: 0,
        instances: -1
      }, O = sl(_, Y), ee = wl(
        _,
        Q,
        h,
        pe
      ), ge = Dl(_, Y, ee, Q), W = Dh(
        _,
        Y,
        O,
        Q,
        ee,
        ge,
        C
      );
      function pe(ke) {
        return W.destroyBuffer(ke);
      }
      var de = Gh(_, G, Q, h), Pe = hh(
        _,
        Y,
        O,
        function() {
          be.procs.poll();
        },
        K,
        Q,
        h
      ), U = dh(_, Y, O, Q, h), $ = Ih(
        _,
        Y,
        O,
        Pe,
        U,
        Q
      ), be = cd(
        _,
        G,
        Y,
        O,
        ee,
        ge,
        Pe,
        $,
        te,
        W,
        de,
        C,
        K,
        ne,
        h
      ), ue = $h(
        _,
        $,
        be.procs.poll,
        K,
        R,
        Y,
        O
      ), J = be.next, ce = _.canvas, le = [], Me = [], De = [], me = [h.onDestroy], ve = null;
      function Fe() {
        if (le.length === 0) {
          ne && ne.update(), ve = null;
          return;
        }
        ve = Hi.next(Fe), Tt();
        for (var ke = le.length - 1; ke >= 0; --ke) {
          var We = le[ke];
          We && We(K, null, 0);
        }
        _.flush(), ne && ne.update();
      }
      function Oe() {
        !ve && le.length > 0 && (ve = Hi.next(Fe));
      }
      function He() {
        ve && (Hi.cancel(Fe), ve = null);
      }
      function at(ke) {
        ke.preventDefault(), j = true, He(), Me.forEach(function(We) {
          We();
        });
      }
      function ft(ke) {
        _.getError(), j = false, I.restore(), de.restore(), ee.restore(), Pe.restore(), U.restore(), $.restore(), W.restore(), ne && ne.restore(), be.procs.refresh(), Oe(), De.forEach(function(We) {
          We();
        });
      }
      ce && (ce.addEventListener(Cf, at, false), ce.addEventListener(kf, ft, false));
      function Ze() {
        le.length = 0, He(), ce && (ce.removeEventListener(Cf, at), ce.removeEventListener(kf, ft)), de.clear(), $.clear(), U.clear(), W.clear(), Pe.clear(), ge.clear(), ee.clear(), ne && ne.clear(), me.forEach(function(ke) {
          ke();
        });
      }
      function pt(ke) {
        d(!!ke, "invalid args to regl({...})"), d.type(ke, "object", "invalid args to regl({...})");
        function We(k) {
          var A = r({}, k);
          delete A.uniforms, delete A.attributes, delete A.context, delete A.vao, "stencil" in A && A.stencil.op && (A.stencil.opBack = A.stencil.opFront = A.stencil.op, delete A.stencil.op);
          function T(E) {
            if (E in A) {
              var v = A[E];
              delete A[E], Object.keys(v).forEach(function(b) {
                A[E + "." + b] = v[b];
              });
            }
          }
          return T("blend"), T("depth"), T("cull"), T("stencil"), T("polygonOffset"), T("scissor"), T("sample"), "vao" in k && (A.vao = k.vao), A;
        }
        function Qe(k, A) {
          var T = {}, E = {};
          return Object.keys(k).forEach(function(v) {
            var b = k[v];
            if (Rt.isDynamic(b)) {
              E[v] = Rt.unbox(b, v);
              return;
            } else if (A && Array.isArray(b)) {
              for (var g = 0; g < b.length; ++g)
                if (Rt.isDynamic(b[g])) {
                  E[v] = Rt.unbox(b, v);
                  return;
                }
            }
            T[v] = b;
          }), {
            dynamic: E,
            static: T
          };
        }
        var lt = Qe(ke.context || {}, true), Kt = Qe(ke.uniforms || {}, true), Rn = Qe(ke.attributes || {}, false), x = Qe(We(ke), false), M = {
          gpuTime: 0,
          cpuTime: 0,
          count: 0
        }, L = be.compile(x, Rn, Kt, lt, M), ye = L.draw, Xe = L.batch, m = L.scope, c = [];
        function w(k) {
          for (; c.length < k; )
            c.push(null);
          return c;
        }
        function P(k, A) {
          var T;
          if (j && d.raise("context lost"), typeof k == "function")
            return m.call(this, null, k, 0);
          if (typeof A == "function")
            if (typeof k == "number")
              for (T = 0; T < k; ++T)
                m.call(this, null, A, T);
            else if (Array.isArray(k))
              for (T = 0; T < k.length; ++T)
                m.call(this, k[T], A, T);
            else
              return m.call(this, k, A, 0);
          else if (typeof k == "number") {
            if (k > 0)
              return Xe.call(this, w(k | 0), k | 0);
          } else if (Array.isArray(k)) {
            if (k.length)
              return Xe.call(this, k, k.length);
          } else
            return ye.call(this, k);
        }
        return r(P, {
          stats: M,
          destroy: function() {
            L.destroy();
          }
        });
      }
      var it = $.setFBO = pt({
        framebuffer: Rt.define.call(null, Ff, "framebuffer")
      });
      function mt(ke, We) {
        var Qe = 0;
        be.procs.poll();
        var lt = We.color;
        lt && (_.clearColor(+lt[0] || 0, +lt[1] || 0, +lt[2] || 0, +lt[3] || 0), Qe |= pd), "depth" in We && (_.clearDepth(+We.depth), Qe |= vd), "stencil" in We && (_.clearStencil(We.stencil | 0), Qe |= gd), d(!!Qe, "called regl.clear with no buffer specified"), _.clear(Qe);
      }
      function vt(ke) {
        if (d(
          typeof ke == "object" && ke,
          "regl.clear() takes an object as input"
        ), "framebuffer" in ke)
          if (ke.framebuffer && ke.framebuffer_reglType === "framebufferCube")
            for (var We = 0; We < 6; ++We)
              it(r({
                framebuffer: ke.framebuffer.faces[We]
              }, ke), mt);
          else
            it(ke, mt);
        else
          mt(null, ke);
      }
      function gt(ke) {
        d.type(ke, "function", "regl.frame() callback must be a function"), le.push(ke);
        function We() {
          var Qe = zf(le, ke);
          d(Qe >= 0, "cannot cancel a frame twice");
          function lt() {
            var Kt = zf(le, lt);
            le[Kt] = le[le.length - 1], le.length -= 1, le.length <= 0 && He();
          }
          le[Qe] = lt;
        }
        return Oe(), {
          cancel: We
        };
      }
      function _t() {
        var ke = J.viewport, We = J.scissor_box;
        ke[0] = ke[1] = We[0] = We[1] = 0, K.viewportWidth = K.framebufferWidth = K.drawingBufferWidth = ke[2] = We[2] = _.drawingBufferWidth, K.viewportHeight = K.framebufferHeight = K.drawingBufferHeight = ke[3] = We[3] = _.drawingBufferHeight;
      }
      function Tt() {
        K.tick += 1, K.time = Ve(), _t(), be.procs.poll();
      }
      function ct() {
        Pe.refresh(), _t(), be.procs.refresh(), ne && ne.update();
      }
      function Ve() {
        return (Na() - ie) / 1e3;
      }
      ct();
      function At(ke, We) {
        d.type(We, "function", "listener callback must be a function");
        var Qe;
        switch (ke) {
          case "frame":
            return gt(We);
          case "lost":
            Qe = Me;
            break;
          case "restore":
            Qe = De;
            break;
          case "destroy":
            Qe = me;
            break;
          default:
            d.raise("invalid event, must be one of frame,lost,restore,destroy");
        }
        return Qe.push(We), {
          cancel: function() {
            for (var lt = 0; lt < Qe.length; ++lt)
              if (Qe[lt] === We) {
                Qe[lt] = Qe[Qe.length - 1], Qe.pop();
                return;
              }
          }
        };
      }
      var Ke = r(pt, {
        // Clear current FBO
        clear: vt,
        // Short cuts for dynamic variables
        prop: Rt.define.bind(null, Ff),
        context: Rt.define.bind(null, yd),
        this: Rt.define.bind(null, bd),
        // executes an empty draw command
        draw: pt({}),
        // Resources
        buffer: function(ke) {
          return ee.create(ke, xd, false, false);
        },
        elements: function(ke) {
          return ge.create(ke, false);
        },
        texture: Pe.create2D,
        cube: Pe.createCube,
        renderbuffer: U.create,
        framebuffer: $.create,
        framebufferCube: $.createCube,
        vao: W.createVAO,
        // Expose context attributes
        attributes: R,
        // Frame rendering
        frame: gt,
        on: At,
        // System limits
        limits: O,
        hasExtension: function(ke) {
          return O.extensions.indexOf(ke.toLowerCase()) >= 0;
        },
        // Read pixels
        read: ue,
        // Destroy regl and all associated resources
        destroy: Ze,
        // Direct GL state manipulation
        _gl: _,
        _refresh: ct,
        poll: function() {
          Tt(), ne && ne.update();
        },
        // Current time
        now: Ve,
        // regl Statistics Information
        stats: Q
      });
      return h.onDone(null, Ke), Ke;
    }
    return _d;
  });
})(Ou);
var Zp = Ou.exports;
var Qp = ba(Zp);
var Kp = "#b3b3b3";
var Jp = 0.1;
var ev = 4;
var tv = "#666666";
var nv = 0.1;
var rv = 1;
var iv = "#222222";
var je = {
  disableSimulation: false,
  spaceSize: 8192,
  pointSizeScale: 1,
  linkWidthScale: 1,
  arrowSizeScale: 1,
  renderLinks: true,
  curvedLinks: false,
  curvedLinkSegments: 19,
  curvedLinkWeight: 0.8,
  curvedLinkControlPointDistance: 0.5,
  arrowLinks: false,
  linkVisibilityDistanceRange: [50, 150],
  linkVisibilityMinTransparency: 0.25,
  hoveredPointCursor: "auto",
  renderHoveredPointRing: false,
  hoveredPointRingColor: "white",
  focusedPointRingColor: "white",
  focusedPointIndex: void 0,
  useQuadtree: false,
  simulation: {
    decay: 5e3,
    gravity: 0.25,
    center: 0,
    repulsion: 1,
    repulsionTheta: 1.15,
    repulsionQuadtreeLevels: 12,
    linkSpring: 1,
    linkDistance: 10,
    linkDistRandomVariationRange: [1, 1.2],
    repulsionFromMouse: 2,
    friction: 0.85,
    cluster: 0.1
  },
  showFPSMonitor: false,
  pixelRatio: 2,
  scalePointsOnZoom: true,
  disableZoom: false,
  enableSimulationDuringZoom: false,
  enableDrag: false,
  fitViewOnInit: true,
  fitViewDelay: 250,
  fitViewPadding: 0.1,
  fitViewDuration: 250,
  pointSamplingDistance: 150,
  disableAttribution: false,
  disableRescalePositions: void 0
};
var ov = 0.7;
var av = 0.95;
var sv = 3;
var fv = (t3) => typeof t3 == "function";
var Nu = (t3) => Array.isArray(t3);
var uv = (t3) => t3 instanceof Object;
var cv = (t3) => t3 instanceof Object ? t3.constructor.name !== "Function" && t3.constructor.name !== "Object" : false;
var Zf = (t3) => uv(t3) && !Nu(t3) && !fv(t3) && !cv(t3);
function ur(t3) {
  let e;
  if (Nu(t3))
    e = t3;
  else {
    const n = Sn(t3), r = n == null ? void 0 : n.rgb();
    e = [(r == null ? void 0 : r.r) || 0, (r == null ? void 0 : r.g) || 0, (r == null ? void 0 : r.b) || 0, (n == null ? void 0 : n.opacity) ?? 1];
  }
  return [
    e[0] / 255,
    e[1] / 255,
    e[2] / 255,
    e[3]
  ];
}
function lv(t3, e, n) {
  return 0.2126 * t3 + 0.7152 * e + 0.0722 * n;
}
function _n(t3, e) {
  let n = new Float32Array();
  return t3({ framebuffer: e })(() => {
    n = t3.read();
  }), n;
}
function hv(t3, e, n) {
  return Math.min(Math.max(t3, e), n);
}
function en(t3) {
  return t3 != null && !Number.isNaN(t3);
}
var dv = class {
  constructor() {
    this.disableSimulation = je.disableSimulation, this.backgroundColor = iv, this.spaceSize = je.spaceSize, this.pointColor = Kp, this.pointGreyoutOpacity = Jp, this.pointSize = ev, this.pointSizeScale = je.pointSizeScale, this.hoveredPointCursor = je.hoveredPointCursor, this.renderHoveredPointRing = je.renderHoveredPointRing, this.hoveredPointRingColor = je.hoveredPointRingColor, this.focusedPointRingColor = je.focusedPointRingColor, this.focusedPointIndex = je.focusedPointIndex, this.linkColor = tv, this.linkGreyoutOpacity = nv, this.linkWidth = rv, this.linkWidthScale = je.linkWidthScale, this.renderLinks = je.renderLinks, this.curvedLinks = je.curvedLinks, this.curvedLinkSegments = je.curvedLinkSegments, this.curvedLinkWeight = je.curvedLinkWeight, this.curvedLinkControlPointDistance = je.curvedLinkControlPointDistance, this.linkArrows = je.arrowLinks, this.linkArrowsSizeScale = je.arrowSizeScale, this.linkVisibilityDistanceRange = je.linkVisibilityDistanceRange, this.linkVisibilityMinTransparency = je.linkVisibilityMinTransparency, this.useQuadtree = je.useQuadtree, this.simulationDecay = je.simulation.decay, this.simulationGravity = je.simulation.gravity, this.simulationCenter = je.simulation.center, this.simulationRepulsion = je.simulation.repulsion, this.simulationRepulsionTheta = je.simulation.repulsionTheta, this.simulationRepulsionQuadtreeLevels = je.simulation.repulsionQuadtreeLevels, this.simulationLinkSpring = je.simulation.linkSpring, this.simulationLinkDistance = je.simulation.linkDistance, this.simulationLinkDistRandomVariationRange = je.simulation.linkDistRandomVariationRange, this.simulationRepulsionFromMouse = je.simulation.repulsionFromMouse, this.simulationFriction = je.simulation.friction, this.simulationCluster = je.simulation.cluster, this.onSimulationStart = void 0, this.onSimulationTick = void 0, this.onSimulationEnd = void 0, this.onSimulationPause = void 0, this.onSimulationRestart = void 0, this.onClick = void 0, this.onMouseMove = void 0, this.onPointMouseOver = void 0, this.onPointMouseOut = void 0, this.onZoomStart = void 0, this.onZoom = void 0, this.onZoomEnd = void 0, this.onDragStart = void 0, this.onDrag = void 0, this.onDragEnd = void 0, this.showFPSMonitor = je.showFPSMonitor, this.pixelRatio = je.pixelRatio, this.scalePointsOnZoom = je.scalePointsOnZoom, this.initialZoomLevel = void 0, this.disableZoom = je.disableZoom, this.enableSimulationDuringZoom = je.enableSimulationDuringZoom, this.enableDrag = je.enableDrag, this.fitViewOnInit = je.fitViewOnInit, this.fitViewDelay = je.fitViewDelay, this.fitViewPadding = je.fitViewPadding, this.fitViewDuration = je.fitViewDuration, this.fitViewByPointsInRect = void 0, this.randomSeed = void 0, this.pointSamplingDistance = je.pointSamplingDistance, this.disableAttribution = je.disableAttribution, this.disableRescalePositions = je.disableRescalePositions;
  }
  init(e) {
    Object.keys(e).forEach((n) => {
      this.deepMergeConfig(this.getConfig(), e, n);
    });
  }
  deepMergeConfig(e, n, r) {
    Zf(e[r]) && Zf(n[r]) ? Object.keys(n[r]).forEach((i) => {
      this.deepMergeConfig(e[r], n[r], i);
    }) : e[r] = n[r];
  }
  getConfig() {
    return this;
  }
};
var dn = class {
  constructor(e, n, r, i, o) {
    this.reglInstance = e, this.config = n, this.store = r, this.data = i, o && (this.points = o);
  }
};
var mv = `#ifdef GL_ES
precision highp float;
#endif

varying vec4 rgba;

void main() {
  gl_FragColor = rgba;
}`;
var pv = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform float pointsTextureSize;

attribute vec2 pointIndices;

varying vec4 rgba;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, pointIndices / pointsTextureSize);
  rgba = vec4(pointPosition.xy, 1.0, 0.0);

  gl_Position = vec4(0.0, 0.0, 0.0, 1.0);
  gl_PointSize = 1.0;
}`;
var vv = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D centermassTexture;
uniform float centerForce;
uniform float alpha;

varying vec2 textureCoords;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 velocity = vec4(0.0);
  vec4 centermassValues = texture2D(centermassTexture, vec2(0.0));
  vec2 centermassPosition = centermassValues.xy / centermassValues.b;
  vec2 distVector = centermassPosition - pointPosition.xy;
  float dist = sqrt(dot(distVector, distVector));
  if (dist > 0.0) {
    float angle = atan(distVector.y, distVector.x);
    float addV = alpha * centerForce * dist * 0.01;
    velocity.rg += addV * vec2(cos(angle), sin(angle));
  }

  gl_FragColor = velocity;
}`;
function yt(t3) {
  return {
    buffer: t3.buffer(new Float32Array([-1, -1, 1, -1, -1, 1, 1, 1])),
    size: 2
  };
}
function Bn(t3) {
  const e = new Float32Array(t3 * t3 * 2);
  for (let n = 0; n < t3; n++)
    for (let r = 0; r < t3; r++) {
      const i = n * t3 * 2 + r * 2;
      e[i + 0] = r, e[i + 1] = n;
    }
  return e;
}
function wi(t3) {
  var e;
  t3 && (e = t3 == null ? void 0 : t3._framebuffer) != null && e.framebuffer && t3.destroy();
}
var $n = `#ifdef GL_ES
precision highp float;
#endif

void main() {
  gl_FragColor = vec4(0.0);
}`;
var St = `#ifdef GL_ES
precision highp float;
#endif

attribute vec2 vertexCoord; 
varying vec2 textureCoords; 

void main() {
    
    textureCoords = (vertexCoord + 1.0) / 2.0;
    gl_Position = vec4(vertexCoord, 0, 1);
}`;
var gv = class extends dn {
  create() {
    const { reglInstance: e, store: n } = this;
    this.centermassTexture || (this.centermassTexture = e.texture()), this.centermassTexture({
      data: new Float32Array(4).fill(0),
      shape: [1, 1, 4],
      type: "float"
    }), this.centermassFbo || (this.centermassFbo = e.framebuffer({
      color: this.centermassTexture,
      depth: false,
      stencil: false
    })), this.pointIndices || (this.pointIndices = e.buffer(0)), this.pointIndices(Bn(n.pointsTextureSize));
  }
  initPrograms() {
    const { reglInstance: e, config: n, store: r, data: i, points: o } = this;
    this.clearCentermassCommand || (this.clearCentermassCommand = e({
      frag: $n,
      vert: St,
      framebuffer: () => this.centermassFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    })), this.calculateCentermassCommand || (this.calculateCentermassCommand = e({
      frag: mv,
      vert: pv,
      framebuffer: () => this.centermassFbo,
      primitive: "points",
      count: () => i.pointsNumber ?? 0,
      attributes: {
        pointIndices: {
          buffer: this.pointIndices,
          size: 2
        }
      },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        pointsTextureSize: () => r.pointsTextureSize
      },
      blend: {
        enable: true,
        func: {
          src: "one",
          dst: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: { enable: false, mask: false },
      stencil: { enable: false }
    })), this.runCommand || (this.runCommand = e({
      frag: vv,
      vert: St,
      framebuffer: () => o == null ? void 0 : o.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        centermassTexture: () => this.centermassFbo,
        centerForce: () => n.simulationCenter,
        alpha: () => r.alpha
      }
    }));
  }
  run() {
    var e, n, r;
    (e = this.clearCentermassCommand) == null || e.call(this), (n = this.calculateCentermassCommand) == null || n.call(this), (r = this.runCommand) == null || r.call(this);
  }
};
var xv = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform float gravity;
uniform float spaceSize;
uniform float alpha;

varying vec2 textureCoords;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);

  vec4 velocity = vec4(0.0);

  vec2 centerPosition = vec2(spaceSize / 2.0);
  vec2 distVector = centerPosition - pointPosition.rg;
  float dist = sqrt(dot(distVector, distVector));
  if (dist > 0.0) {
    float angle = atan(distVector.y, distVector.x);
    float additionalVelocity = alpha * gravity * dist * 0.1;
    velocity.rg += additionalVelocity * vec2(cos(angle), sin(angle));
  }

  gl_FragColor = velocity;
}`;
var yv = class extends dn {
  initPrograms() {
    const { reglInstance: e, config: n, store: r, points: i } = this;
    this.runCommand || (this.runCommand = e({
      frag: xv,
      vert: St,
      framebuffer: () => i == null ? void 0 : i.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => i == null ? void 0 : i.previousPositionFbo,
        gravity: () => n.simulationGravity,
        spaceSize: () => r.adjustedSpaceSize,
        alpha: () => r.alpha
      }
    }));
  }
  run() {
    var e;
    (e = this.runCommand) == null || e.call(this);
  }
};
function bv(t3) {
  return `
#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform float linkSpring;
uniform float linkDistance;
uniform vec2 linkDistRandomVariationRange;

uniform sampler2D linkInfoTexture; // Texture storing first link indices and amount
uniform sampler2D linkIndicesTexture;
uniform sampler2D linkPropertiesTexture; // Texture storing link bias and strength
uniform sampler2D linkRandomDistanceTexture;

uniform float pointsTextureSize;
uniform float linksTextureSize;
uniform float alpha;

varying vec2 textureCoords;

const float MAX_LINKS = ${t3}.0;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 velocity = vec4(0.0);

  vec4 linkInfo = texture2D(linkInfoTexture, textureCoords);
  float iCount = linkInfo.r;
  float jCount = linkInfo.g;
  float linkAmount = linkInfo.b;
  if (linkAmount > 0.0) {
    for (float i = 0.0; i < MAX_LINKS; i += 1.0) {
      if (i < linkAmount) {
        if (iCount >= linksTextureSize) {
          iCount = 0.0;
          jCount += 1.0;
        }
        vec2 linkTextureIndex = (vec2(iCount, jCount) + 0.5) / linksTextureSize;
        vec4 connectedPointIndex = texture2D(linkIndicesTexture, linkTextureIndex);
        vec4 biasAndStrength = texture2D(linkPropertiesTexture, linkTextureIndex);
        vec4 randomMinDistance = texture2D(linkRandomDistanceTexture, linkTextureIndex);
        float bias = biasAndStrength.r;
        float strength = biasAndStrength.g;
        float randomMinLinkDist = randomMinDistance.r * (linkDistRandomVariationRange.g - linkDistRandomVariationRange.r) + linkDistRandomVariationRange.r;
        randomMinLinkDist *= linkDistance;

        iCount += 1.0;

        vec4 connectedPointPosition = texture2D(positionsTexture, (connectedPointIndex.rg + 0.5) / pointsTextureSize);
        float x = connectedPointPosition.x - (pointPosition.x + velocity.x);
        float y = connectedPointPosition.y - (pointPosition.y + velocity.y);
        float l = sqrt(x * x + y * y);

        // Apply the link force
        l = max(l, randomMinLinkDist * 0.99);
        l = (l - randomMinLinkDist) / l;
        l *= linkSpring * alpha;
        l *= strength;
        l *= bias;
        x *= l;
        y *= l;
        velocity.x += x;
        velocity.y += y;
      }
    }
  }

  gl_FragColor = vec4(velocity.rg, 0.0, 0.0);
}
  `;
}
var na = ((t3) => (t3.OUTGOING = "outgoing", t3.INCOMING = "incoming", t3))(na || {});
var Qf = class extends dn {
  constructor() {
    super(...arguments), this.linkFirstIndicesAndAmount = new Float32Array(), this.indices = new Float32Array(), this.maxPointDegree = 0;
  }
  create(e) {
    const { reglInstance: n, store: { pointsTextureSize: r, linksTextureSize: i }, data: o } = this;
    if (!r || !i) return;
    this.linkFirstIndicesAndAmount = new Float32Array(r * r * 4), this.indices = new Float32Array(i * i * 4);
    const s = new Float32Array(i * i * 4), f = new Float32Array(i * i * 4), u = e === "incoming" ? o.sourceIndexToTargetIndices : o.targetIndexToSourceIndices;
    this.maxPointDegree = 0;
    let l = 0;
    u == null || u.forEach((p, y) => {
      p && (this.linkFirstIndicesAndAmount[y * 4 + 0] = l % i, this.linkFirstIndicesAndAmount[y * 4 + 1] = Math.floor(l / i), this.linkFirstIndicesAndAmount[y * 4 + 2] = p.length ?? 0, p.forEach(([S, D]) => {
        var q, he, Re;
        this.indices[l * 4 + 0] = S % r, this.indices[l * 4 + 1] = Math.floor(S / r);
        const Z = ((q = o.degree) == null ? void 0 : q[S]) ?? 0, we = ((he = o.degree) == null ? void 0 : he[y]) ?? 0, Se = Z + we, ae = Se !== 0 ? Z / Se : 0.5, Ne = Math.min(Z, we);
        let Ue = ((Re = o.linkStrength) == null ? void 0 : Re[D]) ?? 1 / Math.max(Ne, 1);
        Ue = Math.sqrt(Ue), s[l * 4 + 0] = ae, s[l * 4 + 1] = Ue, f[l * 4] = this.store.getRandomFloat(0, 1), l += 1;
      }), this.maxPointDegree = Math.max(this.maxPointDegree, p.length ?? 0));
    }), this.linkFirstIndicesAndAmountTexture || (this.linkFirstIndicesAndAmountTexture = n.texture()), this.linkFirstIndicesAndAmountTexture({
      data: this.linkFirstIndicesAndAmount,
      shape: [r, r, 4],
      type: "float"
    }), this.linkFirstIndicesAndAmountFbo || (this.linkFirstIndicesAndAmountFbo = n.framebuffer({
      color: this.linkFirstIndicesAndAmountTexture,
      depth: false,
      stencil: false
    })), this.indicesTexture || (this.indicesTexture = n.texture()), this.indicesTexture({
      data: this.indices,
      shape: [i, i, 4],
      type: "float"
    }), this.indicesFbo || (this.indicesFbo = n.framebuffer({
      color: this.indicesTexture,
      depth: false,
      stencil: false
    })), this.biasAndStrengthTexture || (this.biasAndStrengthTexture = n.texture()), this.biasAndStrengthTexture({
      data: s,
      shape: [i, i, 4],
      type: "float"
    }), this.biasAndStrengthFbo || (this.biasAndStrengthFbo = n.framebuffer({
      color: this.biasAndStrengthTexture,
      depth: false,
      stencil: false
    })), this.randomDistanceTexture || (this.randomDistanceTexture = n.texture()), this.randomDistanceTexture({
      data: f,
      shape: [i, i, 4],
      type: "float"
    }), this.randomDistanceFbo || (this.randomDistanceFbo = n.framebuffer({
      color: this.randomDistanceTexture,
      depth: false,
      stencil: false
    }));
  }
  initPrograms() {
    const { reglInstance: e, config: n, store: r, points: i } = this;
    this.runCommand || (this.runCommand = e({
      frag: () => bv(this.maxPointDegree),
      vert: St,
      framebuffer: () => i == null ? void 0 : i.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => i == null ? void 0 : i.previousPositionFbo,
        linkSpring: () => n.simulationLinkSpring,
        linkDistance: () => n.simulationLinkDistance,
        linkDistRandomVariationRange: () => n.simulationLinkDistRandomVariationRange,
        linkInfoTexture: () => this.linkFirstIndicesAndAmountFbo,
        linkIndicesTexture: () => this.indicesFbo,
        linkPropertiesTexture: () => this.biasAndStrengthFbo,
        linkRandomDistanceTexture: () => this.randomDistanceFbo,
        pointsTextureSize: () => r.pointsTextureSize,
        linksTextureSize: () => r.linksTextureSize,
        alpha: () => r.alpha
      }
    }));
  }
  run() {
    var e;
    (e = this.runCommand) == null || e.call(this);
  }
};
var Gu = `#ifdef GL_ES
precision highp float;
#endif

varying vec4 rgba;

void main() {
  gl_FragColor = rgba;
}`;
var Bu = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform float pointsTextureSize;
uniform float levelTextureSize;
uniform float cellSize;

attribute vec2 pointIndices;

varying vec4 rgba;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, pointIndices / pointsTextureSize);
  rgba = vec4(pointPosition.rg, 1.0, 0.0);

  float n = floor(pointPosition.x / cellSize);
  float m = floor(pointPosition.y / cellSize);
  
  vec2 levelPosition = 2.0 * (vec2(n, m) + 0.5) / levelTextureSize - 1.0;

  gl_Position = vec4(levelPosition, 0.0, 1.0);
  gl_PointSize = 1.0;
}`;
var _v = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D levelFbo;

uniform float level;
uniform float levels;
uniform float levelTextureSize;
uniform float repulsion;
uniform float alpha;
uniform float spaceSize;
uniform float theta;

varying vec2 textureCoords;

const float MAX_LEVELS_NUM = 14.0;

vec2 calculateAdditionalVelocity (vec2 ij, vec2 pp) {
  vec2 add = vec2(0.0);
  vec4 centermass = texture2D(levelFbo, ij);
  if (centermass.r > 0.0 && centermass.g > 0.0 && centermass.b > 0.0) {
    vec2 centermassPosition = vec2(centermass.rg / centermass.b);
    vec2 distVector = pp - centermassPosition;
    float l = dot(distVector, distVector);
    float dist = sqrt(l);
    if (l > 0.0) {
      float c = alpha * repulsion * centermass.b;

      float distanceMin2 = 1.0;
      if (l < distanceMin2) l = sqrt(distanceMin2 * l);
      float addV = c / sqrt(l);
      add = addV * normalize(distVector);
    }
  }
  return add;
}

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  float x = pointPosition.x;
  float y = pointPosition.y;

  float left = 0.0;
  float top = 0.0;
  float right = spaceSize;
  float bottom = spaceSize;

  float n_left = 0.0;
  float n_top = 0.0;
  float n_right = 0.0;
  float n_bottom = 0.0;

  float cellSize = 0.0;

  
  for (float i = 0.0; i < MAX_LEVELS_NUM; i += 1.0) {
    if (i <= level) {
      left += cellSize * n_left;
      top += cellSize * n_top;
      right -= cellSize * n_right;
      bottom -= cellSize * n_bottom;

      cellSize = pow(2.0 , levels - i - 1.0);

      float dist_left = x - left;
      n_left = max(0.0, floor(dist_left / cellSize - theta));

      float dist_top = y - top;
      n_top = max(0.0, floor(dist_top / cellSize - theta));
      
      float dist_right = right - x;
      n_right = max(0.0, floor(dist_right / cellSize - theta));

      float dist_bottom = bottom - y;
      n_bottom = max(0.0, floor(dist_bottom / cellSize - theta));

    }
  }

  vec4 velocity = vec4(vec2(0.0), 1.0, 0.0);

  
  for (float i = 0.0; i < 12.0; i += 1.0) {
    for (float j = 0.0; j < 4.0; j += 1.0) {
      float n = left + cellSize * j;
      float m = top + cellSize * n_top + cellSize * i;

      if (n < (left + n_left * cellSize) && m < bottom) {
        velocity.xy += calculateAdditionalVelocity(vec2(n / cellSize, m / cellSize) / levelTextureSize, pointPosition.xy);
      }

      n = left + cellSize * i;
      m = top + cellSize * j;

      if (n < (right - n_right * cellSize) && m < (top + n_top * cellSize)) {
        velocity.xy += calculateAdditionalVelocity(vec2(n / cellSize, m / cellSize) / levelTextureSize, pointPosition.xy);
      }

      n = right - n_right * cellSize + cellSize * j;
      m = top + cellSize * i;

      if (n < right && m < (bottom - n_bottom * cellSize)) {
        velocity.xy += calculateAdditionalVelocity(vec2(n / cellSize, m / cellSize) / levelTextureSize, pointPosition.xy);
      }

      n = left + n_left * cellSize + cellSize * i;
      m = bottom - n_bottom * cellSize + cellSize * j;

      if (n < right && m < bottom) {
        velocity.xy += calculateAdditionalVelocity(vec2(n / cellSize, m / cellSize) / levelTextureSize, pointPosition.xy);
      }
    }
  }

  gl_FragColor = velocity;
}`;
var Sv = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D levelFbo;
uniform sampler2D randomValues;

uniform float levelTextureSize;
uniform float repulsion;
uniform float alpha;

varying vec2 textureCoords;

vec2 calculateAdditionalVelocity (vec2 ij, vec2 pp) {
  vec2 add = vec2(0.0);
  vec4 centermass = texture2D(levelFbo, ij);
  if (centermass.r > 0.0 && centermass.g > 0.0 && centermass.b > 0.0) {
    vec2 centermassPosition = vec2(centermass.rg / centermass.b);
    vec2 distVector = pp - centermassPosition;
    float l = dot(distVector, distVector);
    float dist = sqrt(l);
    if (l > 0.0) {
      float angle = atan(distVector.y, distVector.x);
      float c = alpha * repulsion * centermass.b;

      float distanceMin2 = 1.0;
      if (l < distanceMin2) l = sqrt(distanceMin2 * l);
      float addV = c / sqrt(l);
      add = addV * vec2(cos(angle), sin(angle));
    }
  }
  return add;
}

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 random = texture2D(randomValues, textureCoords);

  vec4 velocity = vec4(0.0);

  
  velocity.xy += calculateAdditionalVelocity(pointPosition.xy / levelTextureSize, pointPosition.xy);
  
  velocity.xy += velocity.xy * random.rg;

  gl_FragColor = velocity;
}`;
var wv = class extends dn {
  constructor() {
    super(...arguments), this.levelsFbos = /* @__PURE__ */ new Map(), this.quadtreeLevels = 0;
  }
  create() {
    const { reglInstance: e, store: n } = this;
    if (!n.pointsTextureSize) return;
    this.quadtreeLevels = Math.log2(n.adjustedSpaceSize);
    for (let i = 0; i < this.quadtreeLevels; i += 1) {
      const o = Math.pow(2, i + 1);
      this.levelsFbos.has(`level[${i}]`) || this.levelsFbos.set(`level[${i}]`, e.framebuffer());
      const s = this.levelsFbos.get(`level[${i}]`);
      s && s({
        shape: [o, o],
        colorType: "float",
        depth: false,
        stencil: false
      });
    }
    const r = new Float32Array(n.pointsTextureSize * n.pointsTextureSize * 4);
    for (let i = 0; i < n.pointsTextureSize * n.pointsTextureSize; ++i)
      r[i * 4] = n.getRandomFloat(-1, 1) * 1e-5, r[i * 4 + 1] = n.getRandomFloat(-1, 1) * 1e-5;
    this.randomValuesTexture || (this.randomValuesTexture = e.texture()), this.randomValuesTexture({
      data: r,
      shape: [n.pointsTextureSize, n.pointsTextureSize, 4],
      type: "float"
    }), this.randomValuesFbo || (this.randomValuesFbo = e.framebuffer({
      color: this.randomValuesTexture,
      depth: false,
      stencil: false
    })), this.pointIndices || (this.pointIndices = e.buffer(0)), this.pointIndices(Bn(n.pointsTextureSize));
  }
  initPrograms() {
    const { reglInstance: e, config: n, store: r, data: i, points: o } = this;
    this.clearLevelsCommand || (this.clearLevelsCommand = e({
      frag: $n,
      vert: St,
      framebuffer: (s, f) => f.levelFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    })), this.calculateLevelsCommand || (this.calculateLevelsCommand = e({
      frag: Gu,
      vert: Bu,
      framebuffer: (s, f) => f.levelFbo,
      primitive: "points",
      count: () => i.pointsNumber ?? 0,
      attributes: {
        pointIndices: {
          buffer: this.pointIndices,
          size: 2
        }
      },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        pointsTextureSize: () => r.pointsTextureSize,
        levelTextureSize: (s, f) => f.levelTextureSize,
        cellSize: (s, f) => f.cellSize
      },
      blend: {
        enable: true,
        func: {
          src: "one",
          dst: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: { enable: false, mask: false },
      stencil: { enable: false }
    })), this.forceCommand || (this.forceCommand = e({
      frag: _v,
      vert: St,
      framebuffer: () => o == null ? void 0 : o.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        level: (s, f) => f.level,
        levels: this.quadtreeLevels,
        levelFbo: (s, f) => f.levelFbo,
        levelTextureSize: (s, f) => f.levelTextureSize,
        alpha: () => r.alpha,
        repulsion: () => n.simulationRepulsion,
        spaceSize: () => r.adjustedSpaceSize,
        theta: () => n.simulationRepulsionTheta
      },
      blend: {
        enable: true,
        func: {
          src: "one",
          dst: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: { enable: false, mask: false },
      stencil: { enable: false }
    })), this.forceFromItsOwnCentermassCommand || (this.forceFromItsOwnCentermassCommand = e({
      frag: Sv,
      vert: St,
      framebuffer: () => o == null ? void 0 : o.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        randomValues: () => this.randomValuesFbo,
        levelFbo: (s, f) => f.levelFbo,
        levelTextureSize: (s, f) => f.levelTextureSize,
        alpha: () => r.alpha,
        repulsion: () => n.simulationRepulsion,
        spaceSize: () => r.adjustedSpaceSize
      },
      blend: {
        enable: true,
        func: {
          src: "one",
          dst: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: { enable: false, mask: false },
      stencil: { enable: false }
    })), this.clearVelocityCommand || (this.clearVelocityCommand = e({
      frag: $n,
      vert: St,
      framebuffer: () => o == null ? void 0 : o.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    }));
  }
  run() {
    var n, r, i, o, s;
    const { store: e } = this;
    for (let f = 0; f < this.quadtreeLevels; f += 1) {
      (n = this.clearLevelsCommand) == null || n.call(this, { levelFbo: this.levelsFbos.get(`level[${f}]`) });
      const u = Math.pow(2, f + 1), l = e.adjustedSpaceSize / u;
      (r = this.calculateLevelsCommand) == null || r.call(this, {
        levelFbo: this.levelsFbos.get(`level[${f}]`),
        levelTextureSize: u,
        cellSize: l
      });
    }
    (i = this.clearVelocityCommand) == null || i.call(this);
    for (let f = 0; f < this.quadtreeLevels; f += 1) {
      const u = Math.pow(2, f + 1);
      (o = this.forceCommand) == null || o.call(this, {
        levelFbo: this.levelsFbos.get(`level[${f}]`),
        levelTextureSize: u,
        level: f
      }), f === this.quadtreeLevels - 1 && ((s = this.forceFromItsOwnCentermassCommand) == null || s.call(this, {
        levelFbo: this.levelsFbos.get(`level[${f}]`),
        levelTextureSize: u,
        level: f
      }));
    }
  }
};
function Tv(t3, e) {
  t3 = Math.min(t3, e);
  const n = e - t3, r = `
    float dist = sqrt(l);
    if (dist > 0.0) {
      float c = alpha * repulsion * centermass.b;
      addVelocity += calcAdd(vec2(x, y), l, c);
      addVelocity += addVelocity * random.rg;
    }
  `;
  function i(o) {
    if (o >= e)
      return r;
    {
      const s = Math.pow(2, o + 1), f = new Array(o + 1 - n).fill(0).map((l, p) => `pow(2.0, ${o - (p + n)}.0) * i${p + n}`).join("+"), u = new Array(o + 1 - n).fill(0).map((l, p) => `pow(2.0, ${o - (p + n)}.0) * j${p + n}`).join("+");
      return `
      for (float ij${o} = 0.0; ij${o} < 4.0; ij${o} += 1.0) {
        float i${o} = 0.0;
        float j${o} = 0.0;
        if (ij${o} == 1.0 || ij${o} == 3.0) i${o} = 1.0;
        if (ij${o} == 2.0 || ij${o} == 3.0) j${o} = 1.0;
        float i = pow(2.0, ${t3}.0) * n / width${o + 1} + ${f};
        float j = pow(2.0, ${t3}.0) * m / width${o + 1} + ${u};
        float groupPosX = (i + 0.5) / ${s}.0;
        float groupPosY = (j + 0.5) / ${s}.0;
        
        vec4 centermass = texture2D(level[${o}], vec2(groupPosX, groupPosY));
        if (centermass.r > 0.0 && centermass.g > 0.0 && centermass.b > 0.0) {
          float x = centermass.r / centermass.b - pointPosition.r;
          float y = centermass.g / centermass.b - pointPosition.g;
          float l = x * x + y * y;
          if ((width${o + 1} * width${o + 1}) / theta < l) {
            ${r}
          } else {
            ${i(o + 1)}
          }
        }
      }
      `;
    }
  }
  return `
#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D randomValues;
uniform float spaceSize;
uniform float repulsion;
uniform float theta;
uniform float alpha;
uniform sampler2D level[${e}];
varying vec2 textureCoords;

vec2 calcAdd(vec2 xy, float l, float c) {
  float distanceMin2 = 1.0;
  if (l < distanceMin2) l = sqrt(distanceMin2 * l);
  float add = c / l;
  return add * xy;
}

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 random = texture2D(randomValues, textureCoords);

  float width0 = spaceSize;

  vec2 velocity = vec2(0.0);
  vec2 addVelocity = vec2(0.0);

  ${new Array(e).fill(0).map((o, s) => `float width${s + 1} = width${s} / 2.0;`).join(`
`)}

  for (float n = 0.0; n < pow(2.0, ${n}.0); n += 1.0) {
    for (float m = 0.0; m < pow(2.0, ${n}.0); m += 1.0) {
      ${i(n)}
    }
  }

  velocity -= addVelocity;

  gl_FragColor = vec4(velocity, 0.0, 0.0);
}
`;
}
var Av = class extends dn {
  constructor() {
    super(...arguments), this.levelsTextures = /* @__PURE__ */ new Map(), this.levelsFbos = /* @__PURE__ */ new Map(), this.quadtreeLevels = 0;
  }
  create() {
    const { reglInstance: e, store: n } = this;
    if (!n.pointsTextureSize) return;
    this.quadtreeLevels = Math.log2(n.adjustedSpaceSize);
    for (let i = 0; i < this.quadtreeLevels; i += 1) {
      const o = Math.pow(2, i + 1), s = `level[${i}]`;
      this.levelsTextures.has(s) || this.levelsTextures.set(s, e.texture());
      const f = this.levelsTextures.get(s);
      f && f({
        data: new Float32Array(o * o * 4),
        shape: [o, o, 4],
        type: "float"
      }), this.levelsFbos.has(s) || this.levelsFbos.set(s, e.framebuffer({
        color: this.levelsTextures.get(s),
        depth: false,
        stencil: false
      }));
    }
    const r = new Float32Array(n.pointsTextureSize * n.pointsTextureSize * 4);
    for (let i = 0; i < n.pointsTextureSize * n.pointsTextureSize; ++i)
      r[i * 4] = n.getRandomFloat(-1, 1) * 1e-5, r[i * 4 + 1] = n.getRandomFloat(-1, 1) * 1e-5;
    this.randomValuesTexture || (this.randomValuesTexture = e.texture()), this.randomValuesTexture({
      data: r,
      shape: [n.pointsTextureSize, n.pointsTextureSize, 4],
      type: "float"
    }), this.randomValuesFbo || (this.randomValuesFbo = e.framebuffer({
      color: this.randomValuesTexture,
      depth: false,
      stencil: false
    })), this.pointIndices || (this.pointIndices = e.buffer(0)), this.pointIndices(Bn(n.pointsTextureSize));
  }
  initPrograms() {
    const { reglInstance: e, config: n, store: r, data: i, points: o } = this;
    this.clearLevelsCommand || (this.clearLevelsCommand = e({
      frag: $n,
      vert: St,
      framebuffer: (s, f) => f.levelFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    })), this.calculateLevelsCommand || (this.calculateLevelsCommand = e({
      frag: Gu,
      vert: Bu,
      framebuffer: (s, f) => f.levelFbo,
      primitive: "points",
      count: () => i.pointsNumber ?? 0,
      attributes: {
        pointIndices: {
          buffer: this.pointIndices,
          size: 2
        }
      },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        pointsTextureSize: () => r.pointsTextureSize,
        levelTextureSize: (s, f) => f.levelTextureSize,
        cellSize: (s, f) => f.cellSize
      },
      blend: {
        enable: true,
        func: {
          src: "one",
          dst: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: { enable: false, mask: false },
      stencil: { enable: false }
    })), this.quadtreeCommand = e({
      frag: Tv(n.simulationRepulsionQuadtreeLevels ?? this.quadtreeLevels, this.quadtreeLevels),
      vert: St,
      framebuffer: () => o == null ? void 0 : o.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => o == null ? void 0 : o.previousPositionFbo,
        randomValues: () => this.randomValuesFbo,
        spaceSize: () => r.adjustedSpaceSize,
        repulsion: () => n.simulationRepulsion,
        theta: () => n.simulationRepulsionTheta,
        alpha: () => r.alpha,
        ...Object.fromEntries(this.levelsFbos)
      }
    });
  }
  run() {
    var n, r, i;
    const { store: e } = this;
    for (let o = 0; o < this.quadtreeLevels; o += 1) {
      (n = this.clearLevelsCommand) == null || n.call(this, { levelFbo: this.levelsFbos.get(`level[${o}]`) });
      const s = Math.pow(2, o + 1), f = e.adjustedSpaceSize / s;
      (r = this.calculateLevelsCommand) == null || r.call(this, {
        levelFbo: this.levelsFbos.get(`level[${o}]`),
        levelTextureSize: s,
        cellSize: f
      });
    }
    (i = this.quadtreeCommand) == null || i.call(this);
  }
};
var Ev = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform float repulsion;
uniform vec2 mousePos;

varying vec2 textureCoords;

void main() {  
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 velocity = vec4(0.0);
  vec2 mouse = mousePos;
  
  vec2 distVector = mouse - pointPosition.rg;
  float dist = sqrt(dot(distVector, distVector));
  dist = max(dist, 10.0);
  float angle = atan(distVector.y, distVector.x);
  float addV = 100.0 * repulsion / (dist * dist);
  velocity.rg -= addV * vec2(cos(angle), sin(angle));

  gl_FragColor = velocity;
}`;
var Pv = class extends dn {
  initPrograms() {
    const { reglInstance: e, config: n, store: r, points: i } = this;
    this.runCommand || (this.runCommand = e({
      frag: Ev,
      vert: St,
      framebuffer: () => i == null ? void 0 : i.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => i == null ? void 0 : i.previousPositionFbo,
        mousePos: () => r.mousePosition,
        repulsion: () => n.simulationRepulsionFromMouse
      }
    }));
  }
  run() {
    var e;
    (e = this.runCommand) == null || e.call(this);
  }
};
var Cv = `#ifdef GL_ES
precision highp float;
#endif

varying vec4 rgba;

void main() {
  gl_FragColor = rgba;
}`;
var kv = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D clusterTexture;
uniform float pointsTextureSize;
uniform float clustersTextureSize;

attribute vec2 pointIndices;

varying vec4 rgba;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, pointIndices / pointsTextureSize);
  rgba = vec4(pointPosition.xy, 1.0, 0.0);

  vec4 pointClusterIndices = texture2D(clusterTexture, pointIndices / pointsTextureSize);
  vec2 xy = vec2(0.0);
  if (pointClusterIndices.x >= 0.0 && pointClusterIndices.y >= 0.0) {
    xy = 2.0 * (pointClusterIndices.xy + 0.5) / clustersTextureSize - 1.0;
  }
  
  gl_Position = vec4(xy, 0.0, 1.0);
  gl_PointSize = 1.0;
}`;
var Fv = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D centermassTexture;
uniform sampler2D clusterTexture;
uniform sampler2D clusterPositionsTexture;
uniform sampler2D clusterForceCoefficient;
uniform float alpha;
uniform float clustersTextureSize;
uniform float clusterCoefficient;

varying vec2 textureCoords;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 velocity = vec4(0.0);
  vec4 pointClusterIndices = texture2D(clusterTexture, textureCoords);
  
  if (pointClusterIndices.x >= 0.0 && pointClusterIndices.y >= 0.0) {
    
    vec2 clusterPositions = texture2D(clusterPositionsTexture, pointClusterIndices.xy / clustersTextureSize).xy;
    if (clusterPositions.x < 0.0 || clusterPositions.y < 0.0) {
      vec4 centermassValues = texture2D(centermassTexture, pointClusterIndices.xy / clustersTextureSize);
      clusterPositions = centermassValues.xy / centermassValues.b;
    }
    vec4 clusterCustomCoeff = texture2D(clusterForceCoefficient, textureCoords);
    vec2 distVector = clusterPositions.xy - pointPosition.xy;
    float dist = length(distVector);
    if (dist > 0.0) {
      float addV = alpha * dist * clusterCoefficient * clusterCustomCoeff.r;
      velocity.rg += addV * normalize(distVector);
    }
  }

  gl_FragColor = velocity;
}`;
var zv = class extends dn {
  create() {
    var l;
    const { reglInstance: e, store: n, data: r } = this, { pointsTextureSize: i } = n;
    if (r.pointsNumber === void 0 || !r.pointClusters && !r.clusterPositions) return;
    const o = (r.pointClusters ?? []).reduce((p, y) => y === void 0 || y < 0 ? p : Math.max(p, y), 0) + 1;
    this.clustersTextureSize = Math.ceil(Math.sqrt(o)), this.clusterTexture || (this.clusterTexture = e.texture()), this.clusterPositionsTexture || (this.clusterPositionsTexture = e.texture()), this.clusterForceCoefficientTexture || (this.clusterForceCoefficientTexture = e.texture());
    const s = new Float32Array(i * i * 4), f = new Float32Array(this.clustersTextureSize * this.clustersTextureSize * 4).fill(-1), u = new Float32Array(i * i * 4).fill(1);
    if (r.clusterPositions)
      for (let p = 0; p < o; ++p)
        f[p * 4 + 0] = r.clusterPositions[p * 2 + 0] ?? -1, f[p * 4 + 1] = r.clusterPositions[p * 2 + 1] ?? -1;
    for (let p = 0; p < r.pointsNumber; ++p) {
      const y = (l = r.pointClusters) == null ? void 0 : l[p];
      y === void 0 ? (s[p * 4 + 0] = -1, s[p * 4 + 1] = -1) : (s[p * 4 + 0] = y % this.clustersTextureSize, s[p * 4 + 1] = Math.floor(y / this.clustersTextureSize)), r.clusterStrength && (u[p * 4 + 0] = r.clusterStrength[p] ?? 1);
    }
    this.clusterTexture({
      data: s,
      shape: [i, i, 4],
      type: "float"
    }), this.clusterFbo || (this.clusterFbo = e.framebuffer({
      color: this.clusterTexture,
      depth: false,
      stencil: false
    })), this.clusterPositionsTexture({
      data: f,
      shape: [this.clustersTextureSize, this.clustersTextureSize, 4],
      type: "float"
    }), this.clusterPositionsFbo || (this.clusterPositionsFbo = e.framebuffer({
      color: this.clusterPositionsTexture,
      depth: false,
      stencil: false
    })), this.clusterForceCoefficientTexture({
      data: u,
      shape: [i, i, 4],
      type: "float"
    }), this.clusterForceCoefficientFbo || (this.clusterForceCoefficientFbo = e.framebuffer({
      color: this.clusterForceCoefficientTexture,
      depth: false,
      stencil: false
    })), this.centermassTexture || (this.centermassTexture = e.texture()), this.centermassTexture({
      data: new Float32Array(this.clustersTextureSize * this.clustersTextureSize * 4).fill(0),
      shape: [this.clustersTextureSize, this.clustersTextureSize, 4],
      type: "float"
    }), this.centermassFbo || (this.centermassFbo = e.framebuffer({
      color: this.centermassTexture,
      depth: false,
      stencil: false
    })), this.pointIndices || (this.pointIndices = e.buffer(0)), this.pointIndices(Bn(n.pointsTextureSize));
  }
  initPrograms() {
    const { reglInstance: e, store: n, data: r, points: i } = this;
    r.pointClusters !== void 0 && (this.clearCentermassCommand || (this.clearCentermassCommand = e({
      frag: $n,
      vert: St,
      framebuffer: () => this.centermassFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    })), this.calculateCentermassCommand || (this.calculateCentermassCommand = e({
      frag: Cv,
      vert: kv,
      framebuffer: () => this.centermassFbo,
      primitive: "points",
      count: () => r.pointsNumber ?? 0,
      attributes: {
        pointIndices: {
          buffer: this.pointIndices,
          size: 2
        }
      },
      uniforms: {
        positionsTexture: () => i == null ? void 0 : i.previousPositionFbo,
        pointsTextureSize: () => n.pointsTextureSize,
        clusterTexture: () => this.clusterFbo,
        clustersTextureSize: () => this.clustersTextureSize
      },
      blend: {
        enable: true,
        func: {
          src: "one",
          dst: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: { enable: false, mask: false },
      stencil: { enable: false }
    })), this.applyForcesCommand || (this.applyForcesCommand = e({
      frag: Fv,
      vert: St,
      framebuffer: () => i == null ? void 0 : i.velocityFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => i == null ? void 0 : i.previousPositionFbo,
        clusterTexture: () => this.clusterFbo,
        centermassTexture: () => this.centermassFbo,
        clusterPositionsTexture: () => this.clusterPositionsFbo,
        clusterForceCoefficient: () => this.clusterForceCoefficientFbo,
        alpha: () => n.alpha,
        clustersTextureSize: () => this.clustersTextureSize,
        clusterCoefficient: () => this.config.simulationCluster
      }
    })));
  }
  run() {
    var e, n, r;
    !this.data.pointClusters && !this.data.clusterPositions || ((e = this.clearCentermassCommand) == null || e.call(this), (n = this.calculateCentermassCommand) == null || n.call(this), (r = this.applyForcesCommand) == null || r.call(this));
  }
};
var Vu = { exports: {} };
(function(t3, e) {
  (function(n, r) {
    t3.exports = r();
  })(hn, function() {
    var n = `<div class="gl-box">
  <svg viewBox="0 0 55 60">
    <text x="27" y="56" class="gl-fps">00 FPS</text>
    <text x="28" y="8" class="gl-mem"></text>
    <rect x="0" y="14" rx="4" ry="4" width="55" height="32"></rect>
    <polyline class="gl-chart"></polyline>
  </svg>
  <svg viewBox="0 0 14 60" class="gl-cpu-svg">
    <line x1="7" y1="38" x2="7" y2="11" class="opacity"/>
    <line x1="7" y1="38" x2="7" y2="11" class="gl-cpu" stroke-dasharray="0 27"/>
    <path d="M5.35 43c-.464 0-.812.377-.812.812v1.16c-.783.1972-1.421.812-1.595 1.624h-1.16c-.435 0-.812.348-.812.812s.348.812.812.812h1.102v1.653H1.812c-.464 0-.812.377-.812.812 0 .464.377.812.812.812h1.131c.1943.783.812 1.392 1.595 1.595v1.131c0 .464.377.812.812.812.464 0 .812-.377.812-.812V53.15h1.653v1.073c0 .464.377.812.812.812.464 0 .812-.377.812-.812v-1.131c.783-.1943 1.392-.812 1.595-1.595h1.131c.464 0 .812-.377.812-.812 0-.464-.377-.812-.812-.812h-1.073V48.22h1.102c.435 0 .812-.348.812-.812s-.348-.812-.812-.812h-1.16c-.1885-.783-.812-1.421-1.595-1.624v-1.131c0-.464-.377-.812-.812-.812-.464 0-.812.377-.812.812v1.073H6.162v-1.073c0-.464-.377-.812-.812-.812zm.58 3.48h2.088c.754 0 1.363.609 1.363 1.363v2.088c0 .754-.609 1.363-1.363 1.363H5.93c-.754 0-1.363-.609-1.363-1.363v-2.088c0-.754.609-1.363 1.363-1.363z"/>
  </svg>
  <svg viewBox="0 0 14 60" class="gl-gpu-svg">
    <line x1="7" y1="38" x2="7" y2="11" class="opacity"/>
    <line x1="7" y1="38" x2="7" y2="11" class="gl-gpu" stroke-dasharray="0 27"/>
    <path d="M1.94775 43.3772a.736.736 0 10-.00416 1.472c.58535.00231.56465.1288.6348.3197.07015.18975.04933.43585.04933.43585l-.00653.05405v8.671a.736.736 0 101.472 0v-1.4145c.253.09522.52785.1495.81765.1495h5.267c1.2535 0 2.254-.9752 2.254-2.185v-3.105c0-1.2075-1.00625-2.185-2.254-2.185h-5.267c-.28865 0-.5635.05405-.8165.1495.01806-.16445.04209-.598-.1357-1.0787-.22425-.6072-.9499-1.2765-2.0125-1.2765zm2.9095 3.6455c.42435 0 .7659.36225.7659.8119v2.9785c0 .44965-.34155.8119-.7659.8119s-.7659-.36225-.7659-.8119v-2.9785c0-.44965.34155-.8119.7659-.8119zm4.117 0a2.3 2.3 0 012.3 2.3 2.3 2.3 0 01-2.3 2.3 2.3 2.3 0 01-2.3-2.3 2.3 2.3 0 012.3-2.3z"/>
  </svg>
</div>`, r = `#gl-bench {
  position:absolute;
  left:0;
  top:0;
  z-index:1000;
  -webkit-user-select: none;
  -moz-user-select: none;
  user-select: none;
}

#gl-bench div {
  position: relative;
  display: block;
  margin: 4px;
  padding: 0 7px 0 10px;
  background: #6c6;
  border-radius: 15px;
  cursor: pointer;
  opacity: 0.9;
}

#gl-bench svg {
  height: 60px;
  margin: 0 -1px;
}

#gl-bench text {
  font-size: 12px;
  font-family: Helvetica,Arial,sans-serif;
  font-weight: 700;
  dominant-baseline: middle;
  text-anchor: middle;
}

#gl-bench .gl-mem {
  font-size: 9px;
}

#gl-bench line {
  stroke-width: 5;
  stroke: #112211;
  stroke-linecap: round;
}

#gl-bench polyline {
  fill: none;
  stroke: #112211;
  stroke-linecap: round;
  stroke-linejoin: round;
  stroke-width: 3.5;
}

#gl-bench rect {
  fill: #448844;
}

#gl-bench .opacity {
  stroke: #448844;
}
`;
    class i {
      /** GLBench constructor
       * @param { WebGLRenderingContext | WebGL2RenderingContext } gl context
       * @param { Object | undefined } settings additional settings
       */
      constructor(s, f = {}) {
        this.css = r, this.svg = n, this.paramLogger = () => {
        }, this.chartLogger = () => {
        }, this.chartLen = 20, this.chartHz = 20, this.names = [], this.cpuAccums = [], this.gpuAccums = [], this.activeAccums = [], this.chart = new Array(this.chartLen), this.now = () => performance && performance.now ? performance.now() : Date.now(), this.updateUI = () => {
          [].forEach.call(this.nodes["gl-gpu-svg"], (S) => {
            S.style.display = this.trackGPU ? "inline" : "none";
          });
        }, Object.assign(this, f), this.detected = 0, this.finished = [], this.isFramebuffer = 0, this.frameId = 0;
        let u, l = 0, p, y = (S) => {
          ++l < 20 ? u = requestAnimationFrame(y) : (this.detected = Math.ceil(1e3 * l / (S - p) / 70), cancelAnimationFrame(u)), p || (p = S);
        };
        if (requestAnimationFrame(y), s) {
          const S = async (Z, we) => Promise.resolve(setTimeout(() => {
            s.getError();
            const Se = this.now() - Z;
            we.forEach((ae, Ne) => {
              ae && (this.gpuAccums[Ne] += Se);
            });
          }, 0)), D = (Z, we, Se) => function() {
            const ae = we.now();
            Z.apply(Se, arguments), we.trackGPU && we.finished.push(S(ae, we.activeAccums.slice(0)));
          };
          [
            "drawArrays",
            "drawElements",
            "drawArraysInstanced",
            "drawBuffers",
            "drawElementsInstanced",
            "drawRangeElements"
          ].forEach((Z) => {
            s[Z] && (s[Z] = D(s[Z], this, s));
          }), s.getExtension = /* @__PURE__ */ ((Z, we) => function() {
            let Se = Z.apply(s, arguments);
            return Se && ["drawElementsInstancedANGLE", "drawBuffersWEBGL"].forEach((ae) => {
              Se[ae] && (Se[ae] = D(Se[ae], we, Se));
            }), Se;
          })(s.getExtension, this);
        }
        if (!this.withoutUI) {
          this.dom || (this.dom = document.body);
          let S = document.createElement("div");
          S.id = "gl-bench", this.dom.appendChild(S), this.dom.insertAdjacentHTML("afterbegin", '<style id="gl-bench-style">' + this.css + "</style>"), this.dom = S, this.dom.addEventListener("click", () => {
            this.trackGPU = !this.trackGPU, this.updateUI();
          }), this.paramLogger = ((D, Z, we) => {
            const Se = ["gl-cpu", "gl-gpu", "gl-mem", "gl-fps", "gl-gpu-svg", "gl-chart"], ae = Object.assign({}, Se);
            return Se.forEach((Ne) => ae[Ne] = Z.getElementsByClassName(Ne)), this.nodes = ae, (Ne, Ue, q, he, Re, Ge, qe) => {
              ae["gl-cpu"][Ne].style.strokeDasharray = (Ue * 0.27).toFixed(0) + " 100", ae["gl-gpu"][Ne].style.strokeDasharray = (q * 0.27).toFixed(0) + " 100", ae["gl-mem"][Ne].innerHTML = we[Ne] ? we[Ne] : he ? "mem: " + he.toFixed(0) + "mb" : "", ae["gl-fps"][Ne].innerHTML = Re.toFixed(0) + " FPS", D(we[Ne], Ue, q, he, Re, Ge, qe);
            };
          })(this.paramLogger, this.dom, this.names), this.chartLogger = ((D, Z) => {
            let we = { "gl-chart": Z.getElementsByClassName("gl-chart") };
            return (Se, ae, Ne) => {
              let Ue = "", q = ae.length;
              for (let he = 0; he < q; he++) {
                let Re = (Ne + he + 1) % q;
                ae[Re] != null && (Ue = Ue + " " + (55 * he / (q - 1)).toFixed(1) + "," + (45 - ae[Re] * 22 / 60 / this.detected).toFixed(1));
              }
              we["gl-chart"][Se].setAttribute("points", Ue), D(this.names[Se], ae, Ne);
            };
          })(this.chartLogger, this.dom);
        }
      }
      /**
       * Explicit UI add
       * @param { string | undefined } name 
       */
      addUI(s) {
        this.names.indexOf(s) == -1 && (this.names.push(s), this.dom && (this.dom.insertAdjacentHTML("beforeend", this.svg), this.updateUI()), this.cpuAccums.push(0), this.gpuAccums.push(0), this.activeAccums.push(false));
      }
      /**
       * Increase frameID
       * @param { number | undefined } now
       */
      nextFrame(s) {
        this.frameId++;
        const f = s || this.now();
        if (this.frameId <= 1)
          this.paramFrame = this.frameId, this.paramTime = f;
        else {
          let u = f - this.paramTime;
          if (u >= 1e3) {
            const l = this.frameId - this.paramFrame, p = l / u * 1e3;
            for (let y = 0; y < this.names.length; y++) {
              const S = this.cpuAccums[y] / u * 100, D = this.gpuAccums[y] / u * 100, Z = performance && performance.memory ? performance.memory.usedJSHeapSize / (1 << 20) : 0;
              this.paramLogger(y, S, D, Z, p, u, l), this.cpuAccums[y] = 0, Promise.all(this.finished).then(() => {
                this.gpuAccums[y] = 0, this.finished = [];
              });
            }
            this.paramFrame = this.frameId, this.paramTime = f;
          }
        }
        if (!this.detected || !this.chartFrame)
          this.chartFrame = this.frameId, this.chartTime = f, this.circularId = 0;
        else {
          let u = f - this.chartTime, l = this.chartHz * u / 1e3;
          for (; --l > 0 && this.detected; ) {
            const y = (this.frameId - this.chartFrame) / u * 1e3;
            this.chart[this.circularId % this.chartLen] = y;
            for (let S = 0; S < this.names.length; S++)
              this.chartLogger(S, this.chart, this.circularId);
            this.circularId++, this.chartFrame = this.frameId, this.chartTime = f;
          }
        }
      }
      /**
       * Begin named measurement
       * @param { string | undefined } name
       */
      begin(s) {
        this.updateAccums(s);
      }
      /**
       * End named measure
       * @param { string | undefined } name
       */
      end(s) {
        this.updateAccums(s);
      }
      updateAccums(s) {
        let f = this.names.indexOf(s);
        f == -1 && (f = this.names.length, this.addUI(s));
        const u = this.now(), l = u - this.t0;
        for (let p = 0; p < f + 1; p++)
          this.activeAccums[p] && (this.cpuAccums[p] += l);
        this.activeAccums[f] = !this.activeAccums[f], this.t0 = u;
      }
    }
    return i;
  });
})(Vu);
var Lv = Vu.exports;
var Iv = ba(Lv);
var Rv = `
  #gl-bench {
    position:absolute;
    right:0;
    top:0;
    z-index:1000;
    -webkit-user-select: none;
    -moz-user-select: none;
    user-select: none;
  }
  #gl-bench div {
    position: relative;
    display: block;
    margin: 4px;
    padding: 0 7px 0 10px;
    background: #5f69de;
    border-radius: 15px;
    cursor: pointer;
    opacity: 0.9;
  }
  #gl-bench svg {
    height: 60px;
    margin: 0 -1px;
  }
  #gl-bench text {
    font-size: 12px;
    font-family: Helvetica,Arial,sans-serif;
    font-weight: 700;
    dominant-baseline: middle;
    text-anchor: middle;
  }
  #gl-bench .gl-mem {
    font-size: 9px;
  }
  #gl-bench line {
    stroke-width: 5;
    stroke: #112211;
    stroke-linecap: round;
  }
  #gl-bench polyline {
    fill: none;
    stroke: #112211;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 3.5;
  }
  #gl-bench rect {
    fill: #8288e4;
  }
  #gl-bench .opacity {
    stroke: #8288e4;
  }
`;
var Kf = class {
  constructor(e) {
    this.destroy();
    const n = e.getContext("webgl") || e.getContext("experimental-webgl");
    this.bench = new Iv(n, { css: Rv });
  }
  begin() {
    var e;
    (e = this.bench) == null || e.begin("frame");
  }
  end(e) {
    var n, r;
    (n = this.bench) == null || n.end("frame"), (r = this.bench) == null || r.nextFrame(e);
  }
  destroy() {
    this.bench = void 0, Pt("#gl-bench").remove();
  }
};
var Dv = class {
  constructor(e) {
    this._config = e;
  }
  get pointsNumber() {
    return this.pointPositions && this.pointPositions.length / 2;
  }
  get linksNumber() {
    return this.links && this.links.length / 2;
  }
  updatePoints() {
    this.pointPositions = this.inputPointPositions;
  }
  /**
   * Updates the point colors based on the input data or default config value.
   */
  updatePointColor() {
    if (this.pointsNumber === void 0) {
      this.pointColors = void 0;
      return;
    }
    const e = ur(this._config.pointColor);
    if (this.inputPointColors === void 0 || this.inputPointColors.length / 4 !== this.pointsNumber) {
      this.pointColors = new Float32Array(this.pointsNumber * 4);
      for (let n = 0; n < this.pointColors.length / 4; n++)
        this.pointColors[n * 4] = e[0], this.pointColors[n * 4 + 1] = e[1], this.pointColors[n * 4 + 2] = e[2], this.pointColors[n * 4 + 3] = e[3];
    } else {
      this.pointColors = this.inputPointColors;
      for (let n = 0; n < this.pointColors.length / 4; n++)
        en(this.pointColors[n * 4]) || (this.pointColors[n * 4] = e[0]), en(this.pointColors[n * 4 + 1]) || (this.pointColors[n * 4 + 1] = e[1]), en(this.pointColors[n * 4 + 2]) || (this.pointColors[n * 4 + 2] = e[2]), en(this.pointColors[n * 4 + 3]) || (this.pointColors[n * 4 + 3] = e[3]);
    }
  }
  /**
   * Updates the point sizes based on the input data or default config value.
   */
  updatePointSize() {
    if (this.pointsNumber === void 0) {
      this.pointSizes = void 0;
      return;
    }
    if (this.inputPointSizes === void 0 || this.inputPointSizes.length !== this.pointsNumber)
      this.pointSizes = new Float32Array(this.pointsNumber).fill(this._config.pointSize);
    else {
      this.pointSizes = this.inputPointSizes;
      for (let e = 0; e < this.pointSizes.length; e++)
        en(this.pointSizes[e]) || (this.pointSizes[e] = this._config.pointSize);
    }
  }
  updateLinks() {
    this.links = this.inputLinks;
  }
  /**
   * Updates the link colors based on the input data or default config value.
   */
  updateLinkColor() {
    if (this.linksNumber === void 0) {
      this.linkColors = void 0;
      return;
    }
    const e = ur(this._config.linkColor);
    if (this.inputLinkColors === void 0 || this.inputLinkColors.length / 4 !== this.linksNumber) {
      this.linkColors = new Float32Array(this.linksNumber * 4);
      for (let n = 0; n < this.linkColors.length / 4; n++)
        this.linkColors[n * 4] = e[0], this.linkColors[n * 4 + 1] = e[1], this.linkColors[n * 4 + 2] = e[2], this.linkColors[n * 4 + 3] = e[3];
    } else {
      this.linkColors = this.inputLinkColors;
      for (let n = 0; n < this.linkColors.length / 4; n++)
        en(this.linkColors[n * 4]) || (this.linkColors[n * 4] = e[0]), en(this.linkColors[n * 4 + 1]) || (this.linkColors[n * 4 + 1] = e[1]), en(this.linkColors[n * 4 + 2]) || (this.linkColors[n * 4 + 2] = e[2]), en(this.linkColors[n * 4 + 3]) || (this.linkColors[n * 4 + 3] = e[3]);
    }
  }
  /**
   * Updates the link width based on the input data or default config value.
   */
  updateLinkWidth() {
    if (this.linksNumber === void 0) {
      this.linkWidths = void 0;
      return;
    }
    if (this.inputLinkWidths === void 0 || this.inputLinkWidths.length !== this.linksNumber)
      this.linkWidths = new Float32Array(this.linksNumber).fill(this._config.linkWidth);
    else {
      this.linkWidths = this.inputLinkWidths;
      for (let e = 0; e < this.linkWidths.length; e++)
        en(this.linkWidths[e]) || (this.linkWidths[e] = this._config.linkWidth);
    }
  }
  /**
   * Updates the link arrows based on the input data or default config value.
   */
  updateArrows() {
    if (this.linksNumber === void 0) {
      this.linkArrows = void 0;
      return;
    }
    this.linkArrowsBoolean === void 0 || this.linkArrowsBoolean.length !== this.linksNumber ? this.linkArrows = new Array(this.linksNumber).fill(+this._config.linkArrows) : this.linkArrows = this.linkArrowsBoolean.map((e) => +e);
  }
  updateLinkStrength() {
    this.linksNumber === void 0 && (this.linkStrength = void 0), this.inputLinkStrength === void 0 || this.inputLinkStrength.length !== this.linksNumber ? this.linkStrength = void 0 : this.linkStrength = this.inputLinkStrength;
  }
  updateClusters() {
    if (this.pointsNumber === void 0) {
      this.pointClusters = void 0, this.clusterPositions = void 0;
      return;
    }
    this.inputPointClusters === void 0 || this.inputPointClusters.length !== this.pointsNumber ? this.pointClusters = void 0 : this.pointClusters = this.inputPointClusters, this.inputClusterPositions === void 0 ? this.clusterPositions = void 0 : this.clusterPositions = this.inputClusterPositions, this.inputClusterStrength === void 0 || this.inputClusterStrength.length !== this.pointsNumber ? this.clusterStrength = void 0 : this.clusterStrength = this.inputClusterStrength;
  }
  update() {
    this.updatePoints(), this.updatePointColor(), this.updatePointSize(), this.updateLinks(), this.updateLinkColor(), this.updateLinkWidth(), this.updateArrows(), this.updateLinkStrength(), this.updateClusters(), this._createAdjacencyLists(), this._calculateDegrees();
  }
  getAdjacentIndices(e) {
    var n, r, i, o;
    return [...((r = (n = this.sourceIndexToTargetIndices) == null ? void 0 : n[e]) == null ? void 0 : r.map((s) => s[0])) || [], ...((o = (i = this.targetIndexToSourceIndices) == null ? void 0 : i[e]) == null ? void 0 : o.map((s) => s[0])) || []];
  }
  _createAdjacencyLists() {
    var e, n;
    if (this.linksNumber === void 0 || this.links === void 0) {
      this.sourceIndexToTargetIndices = void 0, this.targetIndexToSourceIndices = void 0;
      return;
    }
    this.sourceIndexToTargetIndices = new Array(this.pointsNumber).fill(void 0), this.targetIndexToSourceIndices = new Array(this.pointsNumber).fill(void 0);
    for (let r = 0; r < this.linksNumber; r++) {
      const i = this.links[r * 2], o = this.links[r * 2 + 1];
      i !== void 0 && o !== void 0 && (this.sourceIndexToTargetIndices[i] === void 0 && (this.sourceIndexToTargetIndices[i] = []), (e = this.sourceIndexToTargetIndices[i]) == null || e.push([o, r]), this.targetIndexToSourceIndices[o] === void 0 && (this.targetIndexToSourceIndices[o] = []), (n = this.targetIndexToSourceIndices[o]) == null || n.push([i, r]));
    }
  }
  _calculateDegrees() {
    var e, n, r, i;
    if (this.pointsNumber === void 0) {
      this.degree = void 0, this.inDegree = void 0, this.outDegree = void 0;
      return;
    }
    this.degree = new Array(this.pointsNumber).fill(0), this.inDegree = new Array(this.pointsNumber).fill(0), this.outDegree = new Array(this.pointsNumber).fill(0);
    for (let o = 0; o < this.pointsNumber; o++)
      this.inDegree[o] = ((n = (e = this.targetIndexToSourceIndices) == null ? void 0 : e[o]) == null ? void 0 : n.length) ?? 0, this.outDegree[o] = ((i = (r = this.sourceIndexToTargetIndices) == null ? void 0 : r[o]) == null ? void 0 : i.length) ?? 0, this.degree[o] = (this.inDegree[o] ?? 0) + (this.outDegree[o] ?? 0);
  }
};
var Mv = `precision highp float;

varying vec4 rgbaColor;
varying vec2 pos;
varying float arrowLength;
varying float linkWidthArrowWidthRatio;
varying float smoothWidthRatio;
varying float useArrow;

float map(float value, float min1, float max1, float min2, float max2) {
  return min2 + (value - min1) * (max2 - min2) / (max1 - min1);
}

void main() {
  float opacity = 1.0;
  vec3 color = rgbaColor.rgb;
  float smoothDelta = smoothWidthRatio / 2.0;
  if (useArrow > 0.5) {
    float end_arrow = 0.5 + arrowLength / 2.0;
    float start_arrow = end_arrow - arrowLength;
    float arrowWidthDelta = linkWidthArrowWidthRatio / 2.0;
    float linkOpacity = rgbaColor.a * smoothstep(0.5 - arrowWidthDelta, 0.5 - arrowWidthDelta - smoothDelta, abs(pos.y));
    float arrowOpacity = 1.0;
    if (pos.x > start_arrow && pos.x < start_arrow + arrowLength) {
      float xmapped = map(pos.x, start_arrow, end_arrow, 0.0, 1.0);
      arrowOpacity = rgbaColor.a * smoothstep(xmapped - smoothDelta, xmapped, map(abs(pos.y), 0.5, 0.0, 0.0, 1.0));
      if (linkOpacity != arrowOpacity) {
        linkOpacity += arrowOpacity;
      }
    }
    opacity = linkOpacity;
  } else opacity = rgbaColor.a * smoothstep(0.5, 0.5 - smoothDelta, abs(pos.y));
  
  gl_FragColor = vec4(color, opacity);
}`;
var Ov = `precision highp float;
attribute vec2 position, pointA, pointB;
attribute vec4 color;
attribute float width;
attribute float arrow;
uniform sampler2D positionsTexture;
uniform sampler2D pointGreyoutStatus;
uniform mat3 transformationMatrix;
uniform float pointsTextureSize;
uniform float widthScale;
uniform float arrowSizeScale;
uniform float spaceSize;
uniform vec2 screenSize;
uniform float ratio;
uniform vec2 linkVisibilityDistanceRange;
uniform float linkVisibilityMinTransparency;
uniform float greyoutOpacity;
uniform float curvedWeight;
uniform float curvedLinkControlPointDistance;
uniform float curvedLinkSegments;

varying vec4 rgbaColor;
varying vec2 pos;
varying float arrowLength;
varying float linkWidthArrowWidthRatio;
varying float smoothWidthRatio;
varying float useArrow;

float map(float value, float min1, float max1, float min2, float max2) {
  return min2 + (value - min1) * (max2 - min2) / (max1 - min1);
}

vec2 conicParametricCurve(vec2 A, vec2 B, vec2 ControlPoint, float t, float w) {
  vec2 divident = (1.0 - t) * (1.0 - t) * A + 2.0 * (1.0 - t) * t * w * ControlPoint + t * t * B;
  float divisor = (1.0 - t) * (1.0 - t) + 2.0 * (1.0 - t) * t * w + t * t;
  return divident / divisor;
}

void main() {
  pos = position;

  vec2 pointTexturePosA = (pointA + 0.5) / pointsTextureSize;
  vec2 pointTexturePosB = (pointB + 0.5) / pointsTextureSize;
  
  vec4 greyoutStatusA = texture2D(pointGreyoutStatus, pointTexturePosA);
  vec4 greyoutStatusB = texture2D(pointGreyoutStatus, pointTexturePosB);
  
  vec4 pointPositionA = texture2D(positionsTexture, pointTexturePosA);
  vec4 pointPositionB = texture2D(positionsTexture, pointTexturePosB);
  vec2 a = pointPositionA.xy;
  vec2 b = pointPositionB.xy;
  vec2 xBasis = b - a;
  vec2 yBasis = normalize(vec2(-xBasis.y, xBasis.x));

  
  float linkDist = length(xBasis);
  float h = curvedLinkControlPointDistance;
  vec2 controlPoint = (a + b) / 2.0 + yBasis * linkDist * h;

  float linkDistPx = linkDist * transformationMatrix[0][0];
  
  float linkWidth = width * widthScale;
  float k = 2.0;
  float arrowWidth = max(5.0, linkWidth * k);
  arrowWidth *= arrowSizeScale;

  float arrowWidthPx = arrowWidth / transformationMatrix[0][0];
  arrowLength = min(0.3, (0.866 * arrowWidthPx * 2.0) / linkDist);

  float smoothWidth = 2.0;
  float arrowExtraWidth = arrowWidth - linkWidth;
  linkWidth += smoothWidth / 2.0;
  useArrow = arrow;
  if (useArrow > 0.5) {
    linkWidth += arrowExtraWidth;
  }
  smoothWidthRatio = smoothWidth / linkWidth;
  linkWidthArrowWidthRatio = arrowExtraWidth / linkWidth;

  float linkWidthPx = linkWidth / transformationMatrix[0][0];

  
  vec3 rgbColor = color.rgb;
  float opacity = color.a * max(linkVisibilityMinTransparency, map(linkDistPx, linkVisibilityDistanceRange.g, linkVisibilityDistanceRange.r, 0.0, 1.0));

  if (greyoutStatusA.r > 0.0 || greyoutStatusB.r > 0.0) {
    opacity *= greyoutOpacity;
  }

  rgbaColor = vec4(rgbColor, opacity);

  float t = position.x;
  float w = curvedWeight;
  float tPrev = t - 1.0 / curvedLinkSegments;
  float tNext = t + 1.0 / curvedLinkSegments;
  vec2 pointCurr = conicParametricCurve(a, b, controlPoint, t, w);
  vec2 pointPrev = conicParametricCurve(a, b, controlPoint, max(0.0, tPrev), w);
  vec2 pointNext = conicParametricCurve(a, b, controlPoint, min(tNext, 1.0), w);
  vec2 xBasisCurved = pointNext - pointPrev;
  vec2 yBasisCurved = normalize(vec2(-xBasisCurved.y, xBasisCurved.x));
  pointCurr += yBasisCurved * linkWidthPx * position.y;
  vec2 p = 2.0 * pointCurr / spaceSize - 1.0;
  p *= spaceSize / screenSize;
  vec3 final =  transformationMatrix * vec3(p, 1);
  gl_Position = vec4(final.rg, 0, 1);
}`;
function zi(t3, e) {
  return t3 == null || e == null ? NaN : t3 < e ? -1 : t3 > e ? 1 : t3 >= e ? 0 : NaN;
}
function Nv(t3, e) {
  return t3 == null || e == null ? NaN : e < t3 ? -1 : e > t3 ? 1 : e >= t3 ? 0 : NaN;
}
function $u(t3) {
  let e, n, r;
  t3.length !== 2 ? (e = zi, n = (f, u) => zi(t3(f), u), r = (f, u) => t3(f) - u) : (e = t3 === zi || t3 === Nv ? t3 : Gv, n = t3, r = t3);
  function i(f, u, l = 0, p = f.length) {
    if (l < p) {
      if (e(u, u) !== 0) return p;
      do {
        const y = l + p >>> 1;
        n(f[y], u) < 0 ? l = y + 1 : p = y;
      } while (l < p);
    }
    return l;
  }
  function o(f, u, l = 0, p = f.length) {
    if (l < p) {
      if (e(u, u) !== 0) return p;
      do {
        const y = l + p >>> 1;
        n(f[y], u) <= 0 ? l = y + 1 : p = y;
      } while (l < p);
    }
    return l;
  }
  function s(f, u, l = 0, p = f.length) {
    const y = i(f, u, l, p - 1);
    return y > l && r(f[y - 1], u) > -r(f[y], u) ? y - 1 : y;
  }
  return { left: i, center: s, right: o };
}
function Gv() {
  return 0;
}
function Bv(t3) {
  return t3 === null ? NaN : +t3;
}
var Vv = $u(zi);
var $v = Vv.right;
$u(Bv).center;
function Jf(t3, e) {
  let n, r;
  for (const i of t3)
    i != null && (n === void 0 ? i >= i && (n = r = i) : (n > i && (n = i), r < i && (r = i)));
  return [n, r];
}
var ra = Math.sqrt(50);
var ia = Math.sqrt(10);
var oa = Math.sqrt(2);
function jv(t3, e, n) {
  var r, i = -1, o, s, f;
  if (e = +e, t3 = +t3, n = +n, t3 === e && n > 0) return [t3];
  if ((r = e < t3) && (o = t3, t3 = e, e = o), (f = ju(t3, e, n)) === 0 || !isFinite(f)) return [];
  if (f > 0) {
    let u = Math.round(t3 / f), l = Math.round(e / f);
    for (u * f < t3 && ++u, l * f > e && --l, s = new Array(o = l - u + 1); ++i < o; ) s[i] = (u + i) * f;
  } else {
    f = -f;
    let u = Math.round(t3 * f), l = Math.round(e * f);
    for (u / f < t3 && ++u, l / f > e && --l, s = new Array(o = l - u + 1); ++i < o; ) s[i] = (u + i) / f;
  }
  return r && s.reverse(), s;
}
function ju(t3, e, n) {
  var r = (e - t3) / Math.max(0, n), i = Math.floor(Math.log(r) / Math.LN10), o = r / Math.pow(10, i);
  return i >= 0 ? (o >= ra ? 10 : o >= ia ? 5 : o >= oa ? 2 : 1) * Math.pow(10, i) : -Math.pow(10, -i) / (o >= ra ? 10 : o >= ia ? 5 : o >= oa ? 2 : 1);
}
function Uv(t3, e, n) {
  var r = Math.abs(e - t3) / Math.max(0, n), i = Math.pow(10, Math.floor(Math.log(r) / Math.LN10)), o = r / i;
  return o >= ra ? i *= 10 : o >= ia ? i *= 5 : o >= oa && (i *= 2), e < t3 ? -i : i;
}
function Xv(t3, e, n) {
  t3 = +t3, e = +e, n = (i = arguments.length) < 2 ? (e = t3, t3 = 0, 1) : i < 3 ? 1 : +n;
  for (var r = -1, i = Math.max(0, Math.ceil((e - t3) / n)) | 0, o = new Array(i); ++r < i; )
    o[r] = t3 + r * n;
  return o;
}
function Uu(t3, e) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(t3);
      break;
    default:
      this.range(e).domain(t3);
      break;
  }
  return this;
}
function Hv(t3) {
  return function() {
    return t3;
  };
}
function Wv(t3) {
  return +t3;
}
var eu = [0, 1];
function tn(t3) {
  return t3;
}
function aa(t3, e) {
  return (e -= t3 = +t3) ? function(n) {
    return (n - t3) / e;
  } : Hv(isNaN(e) ? NaN : 0.5);
}
function qv(t3, e) {
  var n;
  return t3 > e && (n = t3, t3 = e, e = n), function(r) {
    return Math.max(t3, Math.min(e, r));
  };
}
function Yv(t3, e, n) {
  var r = t3[0], i = t3[1], o = e[0], s = e[1];
  return i < r ? (r = aa(i, r), o = n(s, o)) : (r = aa(r, i), o = n(o, s)), function(f) {
    return o(r(f));
  };
}
function Zv(t3, e, n) {
  var r = Math.min(t3.length, e.length) - 1, i = new Array(r), o = new Array(r), s = -1;
  for (t3[r] < t3[0] && (t3 = t3.slice().reverse(), e = e.slice().reverse()); ++s < r; )
    i[s] = aa(t3[s], t3[s + 1]), o[s] = n(e[s], e[s + 1]);
  return function(f) {
    var u = $v(t3, f, 1, r) - 1;
    return o[u](i[u](f));
  };
}
function Xu(t3, e) {
  return e.domain(t3.domain()).range(t3.range()).interpolate(t3.interpolate()).clamp(t3.clamp()).unknown(t3.unknown());
}
function Hu() {
  var t3 = eu, e = eu, n = xa, r, i, o, s = tn, f, u, l;
  function p() {
    var S = Math.min(t3.length, e.length);
    return s !== tn && (s = qv(t3[0], t3[S - 1])), f = S > 2 ? Zv : Yv, u = l = null, y;
  }
  function y(S) {
    return S == null || isNaN(S = +S) ? o : (u || (u = f(t3.map(r), e, n)))(r(s(S)));
  }
  return y.invert = function(S) {
    return s(i((l || (l = f(e, t3.map(r), Ht)))(S)));
  }, y.domain = function(S) {
    return arguments.length ? (t3 = Array.from(S, Wv), p()) : t3.slice();
  }, y.range = function(S) {
    return arguments.length ? (e = Array.from(S), p()) : e.slice();
  }, y.rangeRound = function(S) {
    return e = Array.from(S), n = Rm, p();
  }, y.clamp = function(S) {
    return arguments.length ? (s = S ? true : tn, p()) : s !== tn;
  }, y.interpolate = function(S) {
    return arguments.length ? (n = S, p()) : n;
  }, y.unknown = function(S) {
    return arguments.length ? (o = S, y) : o;
  }, function(S, D) {
    return r = S, i = D, p();
  };
}
function Qv() {
  return Hu()(tn, tn);
}
function Kv(t3) {
  return Math.abs(t3 = Math.round(t3)) >= 1e21 ? t3.toLocaleString("en").replace(/,/g, "") : t3.toString(10);
}
function Gi(t3, e) {
  if ((n = (t3 = e ? t3.toExponential(e - 1) : t3.toExponential()).indexOf("e")) < 0) return null;
  var n, r = t3.slice(0, n);
  return [
    r.length > 1 ? r[0] + r.slice(2) : r,
    +t3.slice(n + 1)
  ];
}
function cr(t3) {
  return t3 = Gi(Math.abs(t3)), t3 ? t3[1] : NaN;
}
function Jv(t3, e) {
  return function(n, r) {
    for (var i = n.length, o = [], s = 0, f = t3[0], u = 0; i > 0 && f > 0 && (u + f + 1 > r && (f = Math.max(1, r - u)), o.push(n.substring(i -= f, i + f)), !((u += f + 1) > r)); )
      f = t3[s = (s + 1) % t3.length];
    return o.reverse().join(e);
  };
}
function eg(t3) {
  return function(e) {
    return e.replace(/[0-9]/g, function(n) {
      return t3[+n];
    });
  };
}
var tg = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function Bi(t3) {
  if (!(e = tg.exec(t3))) throw new Error("invalid format: " + t3);
  var e;
  return new _a({
    fill: e[1],
    align: e[2],
    sign: e[3],
    symbol: e[4],
    zero: e[5],
    width: e[6],
    comma: e[7],
    precision: e[8] && e[8].slice(1),
    trim: e[9],
    type: e[10]
  });
}
Bi.prototype = _a.prototype;
function _a(t3) {
  this.fill = t3.fill === void 0 ? " " : t3.fill + "", this.align = t3.align === void 0 ? ">" : t3.align + "", this.sign = t3.sign === void 0 ? "-" : t3.sign + "", this.symbol = t3.symbol === void 0 ? "" : t3.symbol + "", this.zero = !!t3.zero, this.width = t3.width === void 0 ? void 0 : +t3.width, this.comma = !!t3.comma, this.precision = t3.precision === void 0 ? void 0 : +t3.precision, this.trim = !!t3.trim, this.type = t3.type === void 0 ? "" : t3.type + "";
}
_a.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};
function ng(t3) {
  e: for (var e = t3.length, n = 1, r = -1, i; n < e; ++n)
    switch (t3[n]) {
      case ".":
        r = i = n;
        break;
      case "0":
        r === 0 && (r = n), i = n;
        break;
      default:
        if (!+t3[n]) break e;
        r > 0 && (r = 0);
        break;
    }
  return r > 0 ? t3.slice(0, r) + t3.slice(i + 1) : t3;
}
var Wu;
function rg(t3, e) {
  var n = Gi(t3, e);
  if (!n) return t3 + "";
  var r = n[0], i = n[1], o = i - (Wu = Math.max(-8, Math.min(8, Math.floor(i / 3))) * 3) + 1, s = r.length;
  return o === s ? r : o > s ? r + new Array(o - s + 1).join("0") : o > 0 ? r.slice(0, o) + "." + r.slice(o) : "0." + new Array(1 - o).join("0") + Gi(t3, Math.max(0, e + o - 1))[0];
}
function tu(t3, e) {
  var n = Gi(t3, e);
  if (!n) return t3 + "";
  var r = n[0], i = n[1];
  return i < 0 ? "0." + new Array(-i).join("0") + r : r.length > i + 1 ? r.slice(0, i + 1) + "." + r.slice(i + 1) : r + new Array(i - r.length + 2).join("0");
}
var nu = {
  "%": (t3, e) => (t3 * 100).toFixed(e),
  b: (t3) => Math.round(t3).toString(2),
  c: (t3) => t3 + "",
  d: Kv,
  e: (t3, e) => t3.toExponential(e),
  f: (t3, e) => t3.toFixed(e),
  g: (t3, e) => t3.toPrecision(e),
  o: (t3) => Math.round(t3).toString(8),
  p: (t3, e) => tu(t3 * 100, e),
  r: tu,
  s: rg,
  X: (t3) => Math.round(t3).toString(16).toUpperCase(),
  x: (t3) => Math.round(t3).toString(16)
};
function ru(t3) {
  return t3;
}
var iu = Array.prototype.map;
var ou = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function ig(t3) {
  var e = t3.grouping === void 0 || t3.thousands === void 0 ? ru : Jv(iu.call(t3.grouping, Number), t3.thousands + ""), n = t3.currency === void 0 ? "" : t3.currency[0] + "", r = t3.currency === void 0 ? "" : t3.currency[1] + "", i = t3.decimal === void 0 ? "." : t3.decimal + "", o = t3.numerals === void 0 ? ru : eg(iu.call(t3.numerals, String)), s = t3.percent === void 0 ? "%" : t3.percent + "", f = t3.minus === void 0 ? "−" : t3.minus + "", u = t3.nan === void 0 ? "NaN" : t3.nan + "";
  function l(y) {
    y = Bi(y);
    var S = y.fill, D = y.align, Z = y.sign, we = y.symbol, Se = y.zero, ae = y.width, Ne = y.comma, Ue = y.precision, q = y.trim, he = y.type;
    he === "n" ? (Ne = true, he = "g") : nu[he] || (Ue === void 0 && (Ue = 12), q = true, he = "g"), (Se || S === "0" && D === "=") && (Se = true, S = "0", D = "=");
    var Re = we === "$" ? n : we === "#" && /[boxX]/.test(he) ? "0" + he.toLowerCase() : "", Ge = we === "$" ? r : /[%p]/.test(he) ? s : "", qe = nu[he], et = /[defgprs%]/.test(he);
    Ue = Ue === void 0 ? 6 : /[gprs]/.test(he) ? Math.max(1, Math.min(21, Ue)) : Math.max(0, Math.min(20, Ue));
    function rt(Le) {
      var st = Re, tt = Ge, N, Ae, oe;
      if (he === "c")
        tt = qe(Le) + tt, Le = "";
      else {
        Le = +Le;
        var Ce = Le < 0 || 1 / Le < 0;
        if (Le = isNaN(Le) ? u : qe(Math.abs(Le), Ue), q && (Le = ng(Le)), Ce && +Le == 0 && Z !== "+" && (Ce = false), st = (Ce ? Z === "(" ? Z : f : Z === "-" || Z === "(" ? "" : Z) + st, tt = (he === "s" ? ou[8 + Wu / 3] : "") + tt + (Ce && Z === "(" ? ")" : ""), et) {
          for (N = -1, Ae = Le.length; ++N < Ae; )
            if (oe = Le.charCodeAt(N), 48 > oe || oe > 57) {
              tt = (oe === 46 ? i + Le.slice(N + 1) : Le.slice(N)) + tt, Le = Le.slice(0, N);
              break;
            }
        }
      }
      Ne && !Se && (Le = e(Le, 1 / 0));
      var $e = st.length + Le.length + tt.length, Be = $e < ae ? new Array(ae - $e + 1).join(S) : "";
      switch (Ne && Se && (Le = e(Be + Le, Be.length ? ae - tt.length : 1 / 0), Be = ""), D) {
        case "<":
          Le = st + Le + tt + Be;
          break;
        case "=":
          Le = st + Be + Le + tt;
          break;
        case "^":
          Le = Be.slice(0, $e = Be.length >> 1) + st + Le + tt + Be.slice($e);
          break;
        default:
          Le = Be + st + Le + tt;
          break;
      }
      return o(Le);
    }
    return rt.toString = function() {
      return y + "";
    }, rt;
  }
  function p(y, S) {
    var D = l((y = Bi(y), y.type = "f", y)), Z = Math.max(-8, Math.min(8, Math.floor(cr(S) / 3))) * 3, we = Math.pow(10, -Z), Se = ou[8 + Z / 3];
    return function(ae) {
      return D(we * ae) + Se;
    };
  }
  return {
    format: l,
    formatPrefix: p
  };
}
var Ti;
var qu;
var Yu;
og({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});
function og(t3) {
  return Ti = ig(t3), qu = Ti.format, Yu = Ti.formatPrefix, Ti;
}
function ag(t3) {
  return Math.max(0, -cr(Math.abs(t3)));
}
function sg(t3, e) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(cr(e) / 3))) * 3 - cr(Math.abs(t3)));
}
function fg(t3, e) {
  return t3 = Math.abs(t3), e = Math.abs(e) - t3, Math.max(0, cr(e) - cr(t3)) + 1;
}
function ug(t3, e, n, r) {
  var i = Uv(t3, e, n), o;
  switch (r = Bi(r ?? ",f"), r.type) {
    case "s": {
      var s = Math.max(Math.abs(t3), Math.abs(e));
      return r.precision == null && !isNaN(o = sg(i, s)) && (r.precision = o), Yu(r, s);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      r.precision == null && !isNaN(o = fg(i, Math.max(Math.abs(t3), Math.abs(e)))) && (r.precision = o - (r.type === "e"));
      break;
    }
    case "f":
    case "%": {
      r.precision == null && !isNaN(o = ag(i)) && (r.precision = o - (r.type === "%") * 2);
      break;
    }
  }
  return qu(r);
}
function Zu(t3) {
  var e = t3.domain;
  return t3.ticks = function(n) {
    var r = e();
    return jv(r[0], r[r.length - 1], n ?? 10);
  }, t3.tickFormat = function(n, r) {
    var i = e();
    return ug(i[0], i[i.length - 1], n ?? 10, r);
  }, t3.nice = function(n) {
    n == null && (n = 10);
    var r = e(), i = 0, o = r.length - 1, s = r[i], f = r[o], u, l, p = 10;
    for (f < s && (l = s, s = f, f = l, l = i, i = o, o = l); p-- > 0; ) {
      if (l = ju(s, f, n), l === u)
        return r[i] = s, r[o] = f, e(r);
      if (l > 0)
        s = Math.floor(s / l) * l, f = Math.ceil(f / l) * l;
      else if (l < 0)
        s = Math.ceil(s * l) / l, f = Math.floor(f * l) / l;
      else
        break;
      u = l;
    }
    return t3;
  }, t3;
}
function sa() {
  var t3 = Qv();
  return t3.copy = function() {
    return Xu(t3, sa());
  }, Uu.apply(t3, arguments), Zu(t3);
}
function au(t3) {
  return function(e) {
    return e < 0 ? -Math.pow(-e, t3) : Math.pow(e, t3);
  };
}
function cg(t3) {
  return t3 < 0 ? -Math.sqrt(-t3) : Math.sqrt(t3);
}
function lg(t3) {
  return t3 < 0 ? -t3 * t3 : t3 * t3;
}
function hg(t3) {
  var e = t3(tn, tn), n = 1;
  function r() {
    return n === 1 ? t3(tn, tn) : n === 0.5 ? t3(cg, lg) : t3(au(n), au(1 / n));
  }
  return e.exponent = function(i) {
    return arguments.length ? (n = +i, r()) : n;
  }, Zu(e);
}
function Qu() {
  var t3 = hg(Hu());
  return t3.copy = function() {
    return Xu(t3, Qu()).exponent(t3.exponent());
  }, Uu.apply(t3, arguments), t3;
}
var dg = (t3) => {
  const e = Qu().exponent(2).range([0, 1]).domain([-1, 1]), n = Xv(0, t3).map((i) => -0.5 + i / t3);
  n.push(0.5);
  const r = new Array(n.length * 2);
  return n.forEach((i, o) => {
    r[o * 2] = [e(i * 2), 0.5], r[o * 2 + 1] = [e(i * 2), -0.5];
  }), r;
};
var mg = class extends dn {
  initPrograms() {
    const { reglInstance: e, config: n, store: r } = this;
    this.drawCurveCommand || (this.drawCurveCommand = e({
      vert: Ov,
      frag: Mv,
      attributes: {
        position: {
          buffer: () => this.curveLineBuffer,
          divisor: 0
        },
        pointA: {
          buffer: () => this.pointsBuffer,
          divisor: 1,
          offset: Float32Array.BYTES_PER_ELEMENT * 0,
          stride: Float32Array.BYTES_PER_ELEMENT * 4
        },
        pointB: {
          buffer: () => this.pointsBuffer,
          divisor: 1,
          offset: Float32Array.BYTES_PER_ELEMENT * 2,
          stride: Float32Array.BYTES_PER_ELEMENT * 4
        },
        color: {
          buffer: () => this.colorBuffer,
          divisor: 1,
          offset: Float32Array.BYTES_PER_ELEMENT * 0,
          stride: Float32Array.BYTES_PER_ELEMENT * 4
        },
        width: {
          buffer: () => this.widthBuffer,
          divisor: 1,
          offset: Float32Array.BYTES_PER_ELEMENT * 0,
          stride: Float32Array.BYTES_PER_ELEMENT * 1
        },
        arrow: {
          buffer: () => this.arrowBuffer,
          divisor: 1,
          offset: Float32Array.BYTES_PER_ELEMENT * 0,
          stride: Float32Array.BYTES_PER_ELEMENT * 1
        }
      },
      uniforms: {
        positionsTexture: () => {
          var i;
          return (i = this.points) == null ? void 0 : i.currentPositionFbo;
        },
        pointGreyoutStatus: () => {
          var i;
          return (i = this.points) == null ? void 0 : i.greyoutStatusFbo;
        },
        transformationMatrix: () => r.transform,
        pointsTextureSize: () => r.pointsTextureSize,
        pointSizeScale: () => n.pointSizeScale,
        widthScale: () => n.linkWidthScale,
        arrowSizeScale: () => n.linkArrowsSizeScale,
        spaceSize: () => r.adjustedSpaceSize,
        screenSize: () => r.screenSize,
        ratio: () => n.pixelRatio,
        linkVisibilityDistanceRange: () => n.linkVisibilityDistanceRange,
        linkVisibilityMinTransparency: () => n.linkVisibilityMinTransparency,
        greyoutOpacity: () => n.linkGreyoutOpacity,
        scalePointsOnZoom: () => n.scalePointsOnZoom,
        curvedWeight: () => n.curvedLinkWeight,
        curvedLinkControlPointDistance: () => n.curvedLinkControlPointDistance,
        curvedLinkSegments: () => n.curvedLinks ? n.curvedLinkSegments ?? je.curvedLinkSegments : 1
      },
      cull: {
        enable: true,
        face: "back"
      },
      blend: {
        enable: true,
        func: {
          dstRGB: "one minus src alpha",
          srcRGB: "src alpha",
          dstAlpha: "one minus src alpha",
          srcAlpha: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: {
        enable: false,
        mask: false
      },
      count: () => {
        var i;
        return ((i = this.curveLineGeometry) == null ? void 0 : i.length) ?? 0;
      },
      instances: () => this.data.linksNumber ?? 0,
      primitive: "triangle strip"
    }));
  }
  draw() {
    var e;
    !this.pointsBuffer || !this.curveLineBuffer || (this.colorBuffer || this.updateColor(), this.widthBuffer || this.updateWidth(), this.arrowBuffer || this.updateArrow(), (e = this.drawCurveCommand) == null || e.call(this));
  }
  updatePointsBuffer() {
    const { reglInstance: e, data: n, store: r } = this;
    if (n.linksNumber === void 0 || n.links === void 0) return;
    const i = new Float32Array(n.linksNumber * 4);
    for (let o = 0; o < n.linksNumber; o++) {
      const s = n.links[o * 2], f = n.links[o * 2 + 1], u = s % r.pointsTextureSize, l = Math.floor(s / r.pointsTextureSize), p = f % r.pointsTextureSize, y = Math.floor(f / r.pointsTextureSize), S = o * 4;
      i[S] = u, i[S + 1] = l, i[S + 2] = p, i[S + 3] = y;
    }
    this.pointsBuffer || (this.pointsBuffer = e.buffer(0)), this.pointsBuffer(i);
  }
  updateColor() {
    const { reglInstance: e, data: n } = this;
    this.colorBuffer || (this.colorBuffer = e.buffer(0)), this.colorBuffer(n.linkColors ?? new Float32Array());
  }
  updateWidth() {
    const { reglInstance: e, data: n } = this;
    this.widthBuffer || (this.widthBuffer = e.buffer(0)), this.widthBuffer(n.linkWidths ?? new Float32Array());
  }
  updateArrow() {
    const { reglInstance: e, data: n } = this;
    this.arrowBuffer || (this.arrowBuffer = e.buffer(0)), this.arrowBuffer(n.linkArrows ?? new Float32Array());
  }
  updateCurveLineGeometry() {
    const { reglInstance: e, config: { curvedLinks: n, curvedLinkSegments: r } } = this;
    this.curveLineGeometry = dg(n ? r ?? je.curvedLinkSegments : 1), this.curveLineBuffer || (this.curveLineBuffer = e.buffer(0)), this.curveLineBuffer(this.curveLineGeometry);
  }
};
var pg = `#ifdef GL_ES
precision highp float;
#endif

varying vec3 rgbColor;
varying float alpha;

const float smoothing = 0.9;

void main() {
    
    if (alpha == 0.0) { discard; }

    
    vec2 pointCoord = 2.0 * gl_PointCoord - 1.0;
    
    float pointCenterDistance = dot(pointCoord, pointCoord);
    
    float opacity = alpha * (1.0 - smoothstep(smoothing, 1.0, pointCenterDistance));

    gl_FragColor = vec4(rgbColor, opacity);
}`;
var vg = `#ifdef GL_ES
precision highp float;
#endif

attribute vec2 pointIndices;
attribute float size;
attribute vec4 color;

uniform sampler2D positionsTexture;
uniform sampler2D pointGreyoutStatus;
uniform float ratio;
uniform mat3 transformationMatrix;
uniform float pointsTextureSize;
uniform float sizeScale;
uniform float spaceSize;
uniform vec2 screenSize;
uniform float greyoutOpacity;
uniform bool scalePointsOnZoom;
uniform float maxPointSize;

varying vec2 textureCoords;
varying vec3 rgbColor;
varying float alpha;

float calculatePointSize(float size) {
  float pSize;
  if (scalePointsOnZoom) { 
    pSize = size * ratio * transformationMatrix[0][0];
  } else {
    pSize = size * ratio * min(5.0, max(1.0, transformationMatrix[0][0] * 0.01));
  }

  return min(pSize, maxPointSize * ratio);
}

void main() {  
  textureCoords = pointIndices;
  
  vec4 pointPosition = texture2D(positionsTexture, (textureCoords + 0.5) / pointsTextureSize);
  vec2 point = pointPosition.rg;

  
  vec2 normalizedPosition = 2.0 * point / spaceSize - 1.0;
  normalizedPosition *= spaceSize / screenSize;
  vec3 finalPosition = transformationMatrix * vec3(normalizedPosition, 1);
  gl_Position = vec4(finalPosition.rg, 0, 1);

  gl_PointSize = calculatePointSize(size * sizeScale);

  rgbColor = color.rgb;
  alpha = color.a;

  
  vec4 greyoutStatus = texture2D(pointGreyoutStatus, (textureCoords + 0.5) / pointsTextureSize);
  if (greyoutStatus.r > 0.0) {
    alpha *= greyoutOpacity;
  }
}`;
var gg = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D pointSize;
uniform float sizeScale;
uniform float spaceSize;
uniform vec2 screenSize;
uniform float ratio;
uniform mat3 transformationMatrix;
uniform vec2 selection[2];
uniform bool scalePointsOnZoom;
uniform float maxPointSize;

varying vec2 textureCoords;

float pointSizeF(float size) {
  float pSize;
  if (scalePointsOnZoom) { 
    pSize = size * ratio * transformationMatrix[0][0];
  } else {
    pSize = size * ratio * min(5.0, max(1.0, transformationMatrix[0][0] * 0.01));
  }
  return min(pSize, maxPointSize * ratio);
}

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec2 p = 2.0 * pointPosition.rg / spaceSize - 1.0;
  p *= spaceSize / screenSize;
  vec3 final = transformationMatrix * vec3(p, 1);

  vec4 pSize = texture2D(pointSize, textureCoords);
  float size = pSize.r * sizeScale;

  float left = 2.0 * (selection[0].x - 0.5 * pointSizeF(size)) / screenSize.x - 1.0;
  float right = 2.0 * (selection[1].x + 0.5 * pointSizeF(size)) / screenSize.x - 1.0;
  float top =  2.0 * (selection[0].y - 0.5 * pointSizeF(size)) / screenSize.y - 1.0;
  float bottom =  2.0 * (selection[1].y + 0.5 * pointSizeF(size)) / screenSize.y - 1.0;

  gl_FragColor = vec4(0.0, 0.0, pointPosition.rg);
  if (final.x >= left && final.x <= right && final.y >= top && final.y <= bottom) {
    gl_FragColor.r = 1.0;
  }
}`;
var xg = `precision mediump float;

uniform vec4 color;
uniform float width;

varying vec2 vertexPosition;
varying float pointOpacity;

const float smoothing = 1.05;

void main () {
  float r = dot(vertexPosition, vertexPosition);
  float opacity = smoothstep(r, r * smoothing, 1.0);
  float stroke = smoothstep(width, width * smoothing, r);
  gl_FragColor = vec4(color.rgb, opacity * stroke * color.a * pointOpacity);
}`;
var yg = `precision mediump float;

attribute vec2 vertexCoord;

uniform sampler2D positionsTexture;
uniform sampler2D pointGreyoutStatusTexture;
uniform float size;
uniform mat3 transformationMatrix;
uniform float pointsTextureSize;
uniform float sizeScale;
uniform float spaceSize;
uniform vec2 screenSize;
uniform bool scalePointsOnZoom;
uniform float pointIndex;
uniform float maxPointSize;
uniform vec4 color;
uniform float greyoutOpacity;

varying vec2 vertexPosition;
varying float pointOpacity;

float calculatePointSize(float size) {
  float pSize;
  if (scalePointsOnZoom) { 
    pSize = size * transformationMatrix[0][0];
  } else {
    pSize = size * min(5.0, max(1.0, transformationMatrix[0][0] * 0.01));
  }
  return min(pSize, maxPointSize);
}

const float relativeRingRadius = 1.3;

void main () {
  vertexPosition = vertexCoord;

  vec2 textureCoordinates = vec2(mod(pointIndex, pointsTextureSize), floor(pointIndex / pointsTextureSize)) + 0.5;
  vec4 pointPosition = texture2D(positionsTexture, textureCoordinates / pointsTextureSize);

  
  pointOpacity = color.a;
  vec4 greyoutStatus = texture2D(pointGreyoutStatusTexture, textureCoordinates / pointsTextureSize);
  if (greyoutStatus.r > 0.0) {
    pointOpacity *= greyoutOpacity;
  }

  
  float pointSize = (calculatePointSize(size * sizeScale) * relativeRingRadius) / transformationMatrix[0][0];
  float radius = pointSize * 0.5;

  
  vec2 a = pointPosition.xy;
  vec2 b = pointPosition.xy + vec2(0.0, radius);
  vec2 xBasis = b - a;
  vec2 yBasis = normalize(vec2(-xBasis.y, xBasis.x));
  vec2 pointPositionInScreenSpace = a + xBasis * vertexCoord.x + yBasis * radius * vertexCoord.y;

  
  vec2 p = 2.0 * pointPositionInScreenSpace / spaceSize - 1.0;
  p *= spaceSize / screenSize;
  vec3 final =  transformationMatrix * vec3(p, 1);
  
  gl_Position = vec4(final.rg, 0, 1);
}`;
var bg = `#ifdef GL_ES
precision highp float;
#endif

varying vec4 rgba;

void main() {
  gl_FragColor = rgba;
}`;
var _g = `#ifdef GL_ES
precision highp float;
#endif

attribute float size;

uniform sampler2D positionsTexture;
uniform float pointsTextureSize;
uniform float sizeScale;
uniform float spaceSize;
uniform vec2 screenSize;
uniform float ratio;
uniform mat3 transformationMatrix;
uniform vec2 mousePosition;
uniform bool scalePointsOnZoom;
uniform float maxPointSize;

attribute vec2 pointIndices;

varying vec4 rgba;

float calculatePointSize(float size) {
  float pSize;
  if (scalePointsOnZoom) { 
    pSize = size * ratio * transformationMatrix[0][0];
  } else {
    pSize = size * ratio * min(5.0, max(1.0, transformationMatrix[0][0] * 0.01));
  }
  return min(pSize, maxPointSize * ratio);
}

float euclideanDistance (float x1, float x2, float y1, float y2) {
  return sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
}

void main() {
  vec4 pointPosition = texture2D(positionsTexture, (pointIndices + 0.5) / pointsTextureSize);

  
  vec2 normalizedPoint = 2.0 * pointPosition.rg / spaceSize - 1.0;
  normalizedPoint *= spaceSize / screenSize;
  vec3 finalPosition = transformationMatrix * vec3(normalizedPoint, 1);

  float pointRadius = 0.5 * calculatePointSize(size * sizeScale);

  vec2 pointScreenPosition = (finalPosition.xy + 1.0) * screenSize / 2.0;
  rgba = vec4(0.0);
  gl_Position = vec4(0.5, 0.5, 0.0, 1.0);
  
  if (euclideanDistance(pointScreenPosition.x, mousePosition.x, pointScreenPosition.y, mousePosition.y) < pointRadius / ratio) {
    float index = pointIndices.g * pointsTextureSize + pointIndices.r;
    rgba = vec4(index, size, pointPosition.xy);
    gl_Position = vec4(-0.5, -0.5, 0.0, 1.0);
  }

  gl_PointSize = 1.0;
}`;
var Sg = `#ifdef GL_ES
precision highp float;
#endif

varying vec4 rgba;

void main() {
  gl_FragColor = rgba;
}`;
var wg = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform float pointsTextureSize;
uniform float spaceSize;
uniform vec2 screenSize;
uniform mat3 transformationMatrix;

attribute vec2 pointIndices;

varying vec4 rgba;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, (pointIndices + 0.5) / pointsTextureSize);
  vec2 p = 2.0 * pointPosition.rg / spaceSize - 1.0;
  p *= spaceSize / screenSize;
  vec3 final = transformationMatrix * vec3(p, 1);

  vec2 pointScreenPosition = (final.xy + 1.0) * screenSize / 2.0;
  float index = pointIndices.g * pointsTextureSize + pointIndices.r;
  rgba = vec4(index, 1.0, pointPosition.xy);
  float i = (pointScreenPosition.x + 0.5) / screenSize.x;
  float j = (pointScreenPosition.y + 0.5) / screenSize.y;
  gl_Position = vec4(2.0 * vec2(i, j) - 1.0, 0.0, 1.0);

  gl_PointSize = 1.0;
}`;
var Tg = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D velocity;
uniform float friction;
uniform float spaceSize;

varying vec2 textureCoords;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);
  vec4 pointVelocity = texture2D(velocity, textureCoords);

  
  pointVelocity.rg *= friction;

  pointPosition.rg += pointVelocity.rg;

  pointPosition.r = clamp(pointPosition.r, 0.0, spaceSize);
  pointPosition.g = clamp(pointPosition.g, 0.0, spaceSize);
  
  gl_FragColor = pointPosition;
}`;
var Ag = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform sampler2D trackedIndices;
uniform float pointsTextureSize;

varying vec2 textureCoords;

void main() {
  vec4 trackedPointIndices = texture2D(trackedIndices, textureCoords);
  if (trackedPointIndices.r < 0.0) discard;
  vec4 pointPosition = texture2D(positionsTexture, (trackedPointIndices.rg + 0.5) / pointsTextureSize);

  gl_FragColor = vec4(pointPosition.rg, 1.0, 1.0);
}`;
var Eg = `#ifdef GL_ES
precision highp float;
#endif

uniform sampler2D positionsTexture;
uniform vec2 mousePos;
uniform float index;

varying vec2 textureCoords;

void main() {
  vec4 pointPosition = texture2D(positionsTexture, textureCoords);

  
  if (index >= 0.0 && index == pointPosition.b) {
    pointPosition.rg = mousePos.rg;
  }

  gl_FragColor = pointPosition;
}`;
var Pg = class extends dn {
  updatePositions() {
    const { reglInstance: e, store: n, data: r, config: { disableRescalePositions: i, disableSimulation: o } } = this, { pointsTextureSize: s } = n;
    if (!s || !r.pointPositions || r.pointsNumber === void 0) return;
    const f = new Float32Array(s * s * 4);
    this.scaleX = void 0, this.scaleY = void 0, (i === false || i === void 0 && o) && this.rescaleInitialNodePositions();
    for (let u = 0; u < r.pointsNumber; ++u)
      f[u * 4 + 0] = r.pointPositions[u * 2 + 0], f[u * 4 + 1] = r.pointPositions[u * 2 + 1], f[u * 4 + 2] = u;
    wi(this.currentPositionFbo), this.currentPositionFbo = e.framebuffer({
      color: e.texture({
        data: f,
        shape: [s, s, 4],
        type: "float"
      }),
      depth: false,
      stencil: false
    }), wi(this.previousPositionFbo), this.previousPositionFbo = e.framebuffer({
      color: e.texture({
        data: f,
        shape: [s, s, 4],
        type: "float"
      }),
      depth: false,
      stencil: false
    }), this.config.disableSimulation || (wi(this.velocityFbo), this.velocityFbo = e.framebuffer({
      color: e.texture({
        data: new Float32Array(s * s * 4).fill(0),
        shape: [s, s, 4],
        type: "float"
      }),
      depth: false,
      stencil: false
    })), this.selectedTexture || (this.selectedTexture = e.texture()), this.selectedTexture({
      data: f,
      shape: [s, s, 4],
      type: "float"
    }), this.selectedFbo || (this.selectedFbo = e.framebuffer({
      color: this.selectedTexture,
      depth: false,
      stencil: false
    })), this.hoveredFbo || (this.hoveredFbo = e.framebuffer({
      shape: [2, 2],
      colorType: "float",
      depth: false,
      stencil: false
    })), this.drawPointIndices || (this.drawPointIndices = e.buffer(0)), this.drawPointIndices(Bn(n.pointsTextureSize)), this.hoveredPointIndices || (this.hoveredPointIndices = e.buffer(0)), this.hoveredPointIndices(Bn(n.pointsTextureSize)), this.sampledPointIndices || (this.sampledPointIndices = e.buffer(0)), this.sampledPointIndices(Bn(n.pointsTextureSize)), this.updateGreyoutStatus(), this.updateSampledPointsGrid(), this.trackPointsByIndices();
  }
  initPrograms() {
    const { reglInstance: e, config: n, store: r, data: i } = this;
    n.disableSimulation || this.updatePositionCommand || (this.updatePositionCommand = e({
      frag: Tg,
      vert: St,
      framebuffer: () => this.currentPositionFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => this.previousPositionFbo,
        velocity: () => this.velocityFbo,
        friction: () => n.simulationFriction,
        spaceSize: () => r.adjustedSpaceSize
      }
    })), this.dragPointCommand || (this.dragPointCommand = e({
      frag: Eg,
      vert: St,
      framebuffer: () => this.currentPositionFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => this.previousPositionFbo,
        mousePos: () => r.mousePosition,
        index: () => {
          var o;
          return ((o = r.hoveredPoint) == null ? void 0 : o.index) ?? -1;
        }
      }
    })), this.drawCommand || (this.drawCommand = e({
      frag: pg,
      vert: vg,
      primitive: "points",
      count: () => i.pointsNumber ?? 0,
      attributes: {
        pointIndices: {
          buffer: this.drawPointIndices,
          size: 2
        },
        size: {
          buffer: () => this.sizeBuffer,
          size: 1
        },
        color: {
          buffer: () => this.colorBuffer,
          size: 4
        }
      },
      uniforms: {
        positionsTexture: () => this.currentPositionFbo,
        pointGreyoutStatus: () => this.greyoutStatusFbo,
        ratio: () => n.pixelRatio,
        sizeScale: () => n.pointSizeScale,
        pointsTextureSize: () => r.pointsTextureSize,
        transformationMatrix: () => r.transform,
        spaceSize: () => r.adjustedSpaceSize,
        screenSize: () => r.screenSize,
        greyoutOpacity: () => n.pointGreyoutOpacity,
        scalePointsOnZoom: () => n.scalePointsOnZoom,
        maxPointSize: () => r.maxPointSize
      },
      blend: {
        enable: true,
        func: {
          dstRGB: "one minus src alpha",
          srcRGB: "src alpha",
          dstAlpha: "one minus src alpha",
          srcAlpha: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: {
        enable: false,
        mask: false
      }
    })), this.findPointsOnAreaSelectionCommand || (this.findPointsOnAreaSelectionCommand = e({
      frag: gg,
      vert: St,
      framebuffer: () => this.selectedFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: {
        vertexCoord: yt(e)
      },
      uniforms: {
        positionsTexture: () => this.currentPositionFbo,
        pointSize: () => this.sizeFbo,
        spaceSize: () => r.adjustedSpaceSize,
        screenSize: () => r.screenSize,
        sizeScale: () => n.pointSizeScale,
        transformationMatrix: () => r.transform,
        ratio: () => n.pixelRatio,
        "selection[0]": () => r.selectedArea[0],
        "selection[1]": () => r.selectedArea[1],
        scalePointsOnZoom: () => n.scalePointsOnZoom,
        maxPointSize: () => r.maxPointSize
      }
    })), this.clearHoveredFboCommand || (this.clearHoveredFboCommand = e({
      frag: $n,
      vert: St,
      framebuffer: this.hoveredFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    })), this.findHoveredPointCommand || (this.findHoveredPointCommand = e({
      frag: bg,
      vert: _g,
      primitive: "points",
      count: () => i.pointsNumber ?? 0,
      framebuffer: () => this.hoveredFbo,
      attributes: {
        pointIndices: {
          buffer: this.hoveredPointIndices,
          size: 2
        },
        size: {
          buffer: () => this.sizeBuffer,
          size: 1
        }
      },
      uniforms: {
        positionsTexture: () => this.currentPositionFbo,
        ratio: () => n.pixelRatio,
        sizeScale: () => n.pointSizeScale,
        pointsTextureSize: () => r.pointsTextureSize,
        transformationMatrix: () => r.transform,
        spaceSize: () => r.adjustedSpaceSize,
        screenSize: () => r.screenSize,
        scalePointsOnZoom: () => n.scalePointsOnZoom,
        mousePosition: () => r.screenMousePosition,
        maxPointSize: () => r.maxPointSize
      },
      depth: {
        enable: false,
        mask: false
      }
    })), this.clearSampledPointsFboCommand || (this.clearSampledPointsFboCommand = e({
      frag: $n,
      vert: St,
      framebuffer: () => this.sampledPointsFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) }
    })), this.fillSampledPointsFboCommand || (this.fillSampledPointsFboCommand = e({
      frag: Sg,
      vert: wg,
      primitive: "points",
      count: () => i.pointsNumber ?? 0,
      framebuffer: () => this.sampledPointsFbo,
      attributes: {
        pointIndices: {
          buffer: this.sampledPointIndices,
          size: 2
        }
      },
      uniforms: {
        positionsTexture: () => this.currentPositionFbo,
        pointsTextureSize: () => r.pointsTextureSize,
        transformationMatrix: () => r.transform,
        spaceSize: () => r.adjustedSpaceSize,
        screenSize: () => r.screenSize
      },
      depth: {
        enable: false,
        mask: false
      }
    })), this.drawHighlightedCommand || (this.drawHighlightedCommand = e({
      frag: xg,
      vert: yg,
      attributes: { vertexCoord: yt(e) },
      primitive: "triangle strip",
      count: 4,
      uniforms: {
        color: e.prop("color"),
        width: e.prop("width"),
        pointIndex: e.prop("pointIndex"),
        size: e.prop("size"),
        positionsTexture: () => this.currentPositionFbo,
        sizeScale: () => n.pointSizeScale,
        pointsTextureSize: () => r.pointsTextureSize,
        transformationMatrix: () => r.transform,
        spaceSize: () => r.adjustedSpaceSize,
        screenSize: () => r.screenSize,
        scalePointsOnZoom: () => n.scalePointsOnZoom,
        maxPointSize: () => r.maxPointSize,
        pointGreyoutStatusTexture: () => this.greyoutStatusFbo,
        greyoutOpacity: () => n.pointGreyoutOpacity
      },
      blend: {
        enable: true,
        func: {
          dstRGB: "one minus src alpha",
          srcRGB: "src alpha",
          dstAlpha: "one minus src alpha",
          srcAlpha: "one"
        },
        equation: {
          rgb: "add",
          alpha: "add"
        }
      },
      depth: {
        enable: false,
        mask: false
      }
    })), this.trackPointsCommand || (this.trackPointsCommand = e({
      frag: Ag,
      vert: St,
      framebuffer: () => this.trackedPositionsFbo,
      primitive: "triangle strip",
      count: 4,
      attributes: { vertexCoord: yt(e) },
      uniforms: {
        positionsTexture: () => this.currentPositionFbo,
        trackedIndices: () => this.trackedIndicesFbo,
        pointsTextureSize: () => r.pointsTextureSize
      }
    }));
  }
  updateColor() {
    const { reglInstance: e, store: { pointsTextureSize: n }, data: r } = this;
    n && (this.colorBuffer || (this.colorBuffer = e.buffer(0)), this.colorBuffer(r.pointColors));
  }
  updateGreyoutStatus() {
    const { reglInstance: e, store: { selectedIndices: n, pointsTextureSize: r } } = this;
    if (!r) return;
    const i = new Float32Array(r * r * 4).fill(n ? 1 : 0);
    if (n)
      for (const o of n)
        i[o * 4] = 0;
    this.greyoutStatusTexture || (this.greyoutStatusTexture = e.texture()), this.greyoutStatusTexture({
      data: i,
      width: r,
      height: r,
      type: "float"
    }), this.greyoutStatusFbo || (this.greyoutStatusFbo = e.framebuffer({
      color: this.greyoutStatusTexture,
      depth: false,
      stencil: false
    }));
  }
  updateSize() {
    const { reglInstance: e, store: { pointsTextureSize: n }, data: r } = this;
    if (!n || r.pointsNumber === void 0 || r.pointSizes === void 0) return;
    this.sizeBuffer || (this.sizeBuffer = e.buffer(0)), this.sizeBuffer(r.pointSizes);
    const i = new Float32Array(n * n * 4);
    for (let o = 0; o < r.pointsNumber; o++)
      i[o * 4] = r.pointSizes[o];
    this.sizeTexture || (this.sizeTexture = e.texture()), this.sizeTexture({
      data: i,
      width: n,
      height: n,
      type: "float"
    }), this.sizeFbo || (this.sizeFbo = e.framebuffer({
      color: this.sizeTexture,
      depth: false,
      stencil: false
    }));
  }
  updateSampledPointsGrid() {
    const { store: { screenSize: e }, config: { pointSamplingDistance: n }, reglInstance: r } = this;
    let i = n ?? Math.min(...e) / 2;
    i === 0 && (i = je.pointSamplingDistance);
    const o = Math.ceil(e[0] / i), s = Math.ceil(e[1] / i);
    wi(this.sampledPointsFbo), this.sampledPointsFbo = r.framebuffer({
      shape: [o, s],
      depth: false,
      stencil: false,
      colorType: "float"
    });
  }
  trackPoints() {
    var e, n;
    (e = this.trackedIndices) != null && e.length && ((n = this.trackPointsCommand) == null || n.call(this));
  }
  draw() {
    var o, s, f, u, l;
    const { config: { renderHoveredPointRing: e, pointSize: n }, store: r, data: i } = this;
    this.colorBuffer || this.updateColor(), this.sizeBuffer || this.updateSize(), (o = this.drawCommand) == null || o.call(this), e && r.hoveredPoint && ((f = this.drawHighlightedCommand) == null || f.call(this, {
      width: 0.85,
      color: r.hoveredPointRingColor,
      pointIndex: r.hoveredPoint.index,
      size: ((s = i.pointSizes) == null ? void 0 : s[r.hoveredPoint.index]) ?? n
    })), r.focusedPoint && ((l = this.drawHighlightedCommand) == null || l.call(this, {
      width: 0.75,
      color: r.focusedPointRingColor,
      pointIndex: r.focusedPoint.index,
      size: ((u = i.pointSizes) == null ? void 0 : u[r.focusedPoint.index]) ?? n
    }));
  }
  updatePosition() {
    var e;
    (e = this.updatePositionCommand) == null || e.call(this), this.swapFbo();
  }
  drag() {
    var e;
    (e = this.dragPointCommand) == null || e.call(this), this.swapFbo();
  }
  findPointsOnAreaSelection() {
    var e;
    (e = this.findPointsOnAreaSelectionCommand) == null || e.call(this);
  }
  findHoveredPoint() {
    var e, n;
    (e = this.clearHoveredFboCommand) == null || e.call(this), (n = this.findHoveredPointCommand) == null || n.call(this);
  }
  trackPointsByIndices(e) {
    const { store: { pointsTextureSize: n }, reglInstance: r } = this;
    if (this.trackedIndices = e, !(e != null && e.length)) return;
    const i = Math.ceil(Math.sqrt(e.length)), o = new Float32Array(i * i * 4).fill(-1);
    for (const [s, f] of e.entries())
      f !== void 0 && (o[s * 4] = f % n, o[s * 4 + 1] = Math.floor(f / n), o[s * 4 + 2] = 0, o[s * 4 + 3] = 0);
    this.trackedIndicesTexture || (this.trackedIndicesTexture = r.texture()), this.trackedIndicesTexture({
      data: o,
      width: i,
      height: i,
      type: "float"
    }), this.trackedIndicesFbo || (this.trackedIndicesFbo = r.framebuffer({
      color: this.trackedIndicesTexture,
      depth: false,
      stencil: false
    })), this.trackedPositionsFbo || (this.trackedPositionsFbo = r.framebuffer()), this.trackedPositionsFbo({
      shape: [i, i],
      depth: false,
      stencil: false,
      colorType: "float"
    }), this.trackPoints();
  }
  getTrackedPositionsMap() {
    const e = /* @__PURE__ */ new Map();
    if (!this.trackedIndices) return e;
    const n = _n(this.reglInstance, this.trackedPositionsFbo);
    for (let r = 0; r < n.length / 4; r += 1) {
      const i = n[r * 4], o = n[r * 4 + 1], s = this.trackedIndices[r];
      i !== void 0 && o !== void 0 && s !== void 0 && e.set(s, [i, o]);
    }
    return e;
  }
  getSampledPointPositionsMap() {
    var r, i;
    const e = /* @__PURE__ */ new Map();
    if (!this.sampledPointsFbo) return e;
    (r = this.clearSampledPointsFboCommand) == null || r.call(this), (i = this.fillSampledPointsFboCommand) == null || i.call(this);
    const n = _n(this.reglInstance, this.sampledPointsFbo);
    for (let o = 0; o < n.length / 4; o++) {
      const s = n[o * 4], f = !!n[o * 4 + 1], u = n[o * 4 + 2], l = n[o * 4 + 3];
      f && s !== void 0 && u !== void 0 && l !== void 0 && e.set(s, [u, l]);
    }
    return e;
  }
  swapFbo() {
    const e = this.previousPositionFbo;
    this.previousPositionFbo = this.currentPositionFbo, this.currentPositionFbo = e;
  }
  rescaleInitialNodePositions() {
    const { config: { spaceSize: e } } = this;
    if (!this.data.pointPositions || !e) return;
    const n = this.data.pointPositions, r = n.length / 2;
    let i = 1 / 0, o = -1 / 0, s = 1 / 0, f = -1 / 0;
    for (let Se = 0; Se < n.length; Se += 2) {
      const ae = n[Se], Ne = n[Se + 1];
      i = Math.min(i, ae), o = Math.max(o, ae), s = Math.min(s, Ne), f = Math.max(f, Ne);
    }
    const u = o - i, l = f - s, p = Math.max(u, l);
    if (p > e) {
      this.scaleX = void 0, this.scaleY = void 0;
      return;
    }
    const y = e * e * 1e-3, D = (r > y ? e * Math.max(1.2, Math.sqrt(r) / e) : e * 0.1) / p, Z = (p - u) / 2 * D, we = (p - l) / 2 * D;
    this.scaleX = (Se) => (Se - i) * D + Z, this.scaleY = (Se) => (Se - s) * D + we;
    for (let Se = 0; Se < r; Se++)
      this.data.pointPositions[Se * 2] = this.scaleX(n[Se * 2]), this.data.pointPositions[Se * 2 + 1] = this.scaleY(n[Se * 2 + 1]);
  }
};
var su = typeof Float32Array < "u" ? Float32Array : Array;
Math.hypot || (Math.hypot = function() {
  for (var t3 = 0, e = arguments.length; e--; )
    t3 += arguments[e] * arguments[e];
  return Math.sqrt(t3);
});
function Cg() {
  var t3 = new su(9);
  return su != Float32Array && (t3[1] = 0, t3[2] = 0, t3[3] = 0, t3[5] = 0, t3[6] = 0, t3[7] = 0), t3[0] = 1, t3[4] = 1, t3[8] = 1, t3;
}
function fu(t3, e, n) {
  var r = e[0], i = e[1], o = e[2], s = e[3], f = e[4], u = e[5], l = e[6], p = e[7], y = e[8], S = n[0], D = n[1];
  return t3[0] = r, t3[1] = i, t3[2] = o, t3[3] = s, t3[4] = f, t3[5] = u, t3[6] = S * r + D * s + l, t3[7] = S * i + D * f + p, t3[8] = S * o + D * u + y, t3;
}
function Ho(t3, e, n) {
  var r = n[0], i = n[1];
  return t3[0] = r * e[0], t3[1] = r * e[1], t3[2] = r * e[2], t3[3] = i * e[3], t3[4] = i * e[4], t3[5] = i * e[5], t3[6] = e[6], t3[7] = e[7], t3[8] = e[8], t3;
}
function kg(t3, e, n) {
  return t3[0] = 2 / e, t3[1] = 0, t3[2] = 0, t3[3] = 0, t3[4] = -2 / n, t3[5] = 0, t3[6] = -1, t3[7] = 1, t3[8] = 1, t3;
}
var Sa = { exports: {} };
Sa.exports;
(function(t3) {
  (function(e, n, r) {
    function i(u) {
      var l = this, p = f();
      l.next = function() {
        var y = 2091639 * l.s0 + l.c * 23283064365386963e-26;
        return l.s0 = l.s1, l.s1 = l.s2, l.s2 = y - (l.c = y | 0);
      }, l.c = 1, l.s0 = p(" "), l.s1 = p(" "), l.s2 = p(" "), l.s0 -= p(u), l.s0 < 0 && (l.s0 += 1), l.s1 -= p(u), l.s1 < 0 && (l.s1 += 1), l.s2 -= p(u), l.s2 < 0 && (l.s2 += 1), p = null;
    }
    function o(u, l) {
      return l.c = u.c, l.s0 = u.s0, l.s1 = u.s1, l.s2 = u.s2, l;
    }
    function s(u, l) {
      var p = new i(u), y = l && l.state, S = p.next;
      return S.int32 = function() {
        return p.next() * 4294967296 | 0;
      }, S.double = function() {
        return S() + (S() * 2097152 | 0) * 11102230246251565e-32;
      }, S.quick = S, y && (typeof y == "object" && o(y, p), S.state = function() {
        return o(p, {});
      }), S;
    }
    function f() {
      var u = 4022871197, l = function(p) {
        p = String(p);
        for (var y = 0; y < p.length; y++) {
          u += p.charCodeAt(y);
          var S = 0.02519603282416938 * u;
          u = S >>> 0, S -= u, S *= u, u = S >>> 0, S -= u, u += S * 4294967296;
        }
        return (u >>> 0) * 23283064365386963e-26;
      };
      return l;
    }
    n && n.exports ? n.exports = s : this.alea = s;
  })(
    hn,
    t3
  );
})(Sa);
var Fg = Sa.exports;
var wa = { exports: {} };
wa.exports;
(function(t3) {
  (function(e, n, r) {
    function i(f) {
      var u = this, l = "";
      u.x = 0, u.y = 0, u.z = 0, u.w = 0, u.next = function() {
        var y = u.x ^ u.x << 11;
        return u.x = u.y, u.y = u.z, u.z = u.w, u.w ^= u.w >>> 19 ^ y ^ y >>> 8;
      }, f === (f | 0) ? u.x = f : l += f;
      for (var p = 0; p < l.length + 64; p++)
        u.x ^= l.charCodeAt(p) | 0, u.next();
    }
    function o(f, u) {
      return u.x = f.x, u.y = f.y, u.z = f.z, u.w = f.w, u;
    }
    function s(f, u) {
      var l = new i(f), p = u && u.state, y = function() {
        return (l.next() >>> 0) / 4294967296;
      };
      return y.double = function() {
        do
          var S = l.next() >>> 11, D = (l.next() >>> 0) / 4294967296, Z = (S + D) / (1 << 21);
        while (Z === 0);
        return Z;
      }, y.int32 = l.next, y.quick = y, p && (typeof p == "object" && o(p, l), y.state = function() {
        return o(l, {});
      }), y;
    }
    n && n.exports ? n.exports = s : this.xor128 = s;
  })(
    hn,
    t3
  );
})(wa);
var zg = wa.exports;
var Ta = { exports: {} };
Ta.exports;
(function(t3) {
  (function(e, n, r) {
    function i(f) {
      var u = this, l = "";
      u.next = function() {
        var y = u.x ^ u.x >>> 2;
        return u.x = u.y, u.y = u.z, u.z = u.w, u.w = u.v, (u.d = u.d + 362437 | 0) + (u.v = u.v ^ u.v << 4 ^ (y ^ y << 1)) | 0;
      }, u.x = 0, u.y = 0, u.z = 0, u.w = 0, u.v = 0, f === (f | 0) ? u.x = f : l += f;
      for (var p = 0; p < l.length + 64; p++)
        u.x ^= l.charCodeAt(p) | 0, p == l.length && (u.d = u.x << 10 ^ u.x >>> 4), u.next();
    }
    function o(f, u) {
      return u.x = f.x, u.y = f.y, u.z = f.z, u.w = f.w, u.v = f.v, u.d = f.d, u;
    }
    function s(f, u) {
      var l = new i(f), p = u && u.state, y = function() {
        return (l.next() >>> 0) / 4294967296;
      };
      return y.double = function() {
        do
          var S = l.next() >>> 11, D = (l.next() >>> 0) / 4294967296, Z = (S + D) / (1 << 21);
        while (Z === 0);
        return Z;
      }, y.int32 = l.next, y.quick = y, p && (typeof p == "object" && o(p, l), y.state = function() {
        return o(l, {});
      }), y;
    }
    n && n.exports ? n.exports = s : this.xorwow = s;
  })(
    hn,
    t3
  );
})(Ta);
var Lg = Ta.exports;
var Aa = { exports: {} };
Aa.exports;
(function(t3) {
  (function(e, n, r) {
    function i(f) {
      var u = this;
      u.next = function() {
        var p = u.x, y = u.i, S, D;
        return S = p[y], S ^= S >>> 7, D = S ^ S << 24, S = p[y + 1 & 7], D ^= S ^ S >>> 10, S = p[y + 3 & 7], D ^= S ^ S >>> 3, S = p[y + 4 & 7], D ^= S ^ S << 7, S = p[y + 7 & 7], S = S ^ S << 13, D ^= S ^ S << 9, p[y] = D, u.i = y + 1 & 7, D;
      };
      function l(p, y) {
        var S, D = [];
        if (y === (y | 0))
          D[0] = y;
        else
          for (y = "" + y, S = 0; S < y.length; ++S)
            D[S & 7] = D[S & 7] << 15 ^ y.charCodeAt(S) + D[S + 1 & 7] << 13;
        for (; D.length < 8; ) D.push(0);
        for (S = 0; S < 8 && D[S] === 0; ++S) ;
        for (S == 8 ? D[7] = -1 : D[S], p.x = D, p.i = 0, S = 256; S > 0; --S)
          p.next();
      }
      l(u, f);
    }
    function o(f, u) {
      return u.x = f.x.slice(), u.i = f.i, u;
    }
    function s(f, u) {
      f == null && (f = +/* @__PURE__ */ new Date());
      var l = new i(f), p = u && u.state, y = function() {
        return (l.next() >>> 0) / 4294967296;
      };
      return y.double = function() {
        do
          var S = l.next() >>> 11, D = (l.next() >>> 0) / 4294967296, Z = (S + D) / (1 << 21);
        while (Z === 0);
        return Z;
      }, y.int32 = l.next, y.quick = y, p && (p.x && o(p, l), y.state = function() {
        return o(l, {});
      }), y;
    }
    n && n.exports ? n.exports = s : this.xorshift7 = s;
  })(
    hn,
    t3
  );
})(Aa);
var Ig = Aa.exports;
var Ea = { exports: {} };
Ea.exports;
(function(t3) {
  (function(e, n, r) {
    function i(f) {
      var u = this;
      u.next = function() {
        var p = u.w, y = u.X, S = u.i, D, Z;
        return u.w = p = p + 1640531527 | 0, Z = y[S + 34 & 127], D = y[S = S + 1 & 127], Z ^= Z << 13, D ^= D << 17, Z ^= Z >>> 15, D ^= D >>> 12, Z = y[S] = Z ^ D, u.i = S, Z + (p ^ p >>> 16) | 0;
      };
      function l(p, y) {
        var S, D, Z, we, Se, ae = [], Ne = 128;
        for (y === (y | 0) ? (D = y, y = null) : (y = y + "\0", D = 0, Ne = Math.max(Ne, y.length)), Z = 0, we = -32; we < Ne; ++we)
          y && (D ^= y.charCodeAt((we + 32) % y.length)), we === 0 && (Se = D), D ^= D << 10, D ^= D >>> 15, D ^= D << 4, D ^= D >>> 13, we >= 0 && (Se = Se + 1640531527 | 0, S = ae[we & 127] ^= D + Se, Z = S == 0 ? Z + 1 : 0);
        for (Z >= 128 && (ae[(y && y.length || 0) & 127] = -1), Z = 127, we = 4 * 128; we > 0; --we)
          D = ae[Z + 34 & 127], S = ae[Z = Z + 1 & 127], D ^= D << 13, S ^= S << 17, D ^= D >>> 15, S ^= S >>> 12, ae[Z] = D ^ S;
        p.w = Se, p.X = ae, p.i = Z;
      }
      l(u, f);
    }
    function o(f, u) {
      return u.i = f.i, u.w = f.w, u.X = f.X.slice(), u;
    }
    function s(f, u) {
      f == null && (f = +/* @__PURE__ */ new Date());
      var l = new i(f), p = u && u.state, y = function() {
        return (l.next() >>> 0) / 4294967296;
      };
      return y.double = function() {
        do
          var S = l.next() >>> 11, D = (l.next() >>> 0) / 4294967296, Z = (S + D) / (1 << 21);
        while (Z === 0);
        return Z;
      }, y.int32 = l.next, y.quick = y, p && (p.X && o(p, l), y.state = function() {
        return o(l, {});
      }), y;
    }
    n && n.exports ? n.exports = s : this.xor4096 = s;
  })(
    hn,
    // window object or global
    t3
  );
})(Ea);
var Rg = Ea.exports;
var Pa = { exports: {} };
Pa.exports;
(function(t3) {
  (function(e, n, r) {
    function i(f) {
      var u = this, l = "";
      u.next = function() {
        var y = u.b, S = u.c, D = u.d, Z = u.a;
        return y = y << 25 ^ y >>> 7 ^ S, S = S - D | 0, D = D << 24 ^ D >>> 8 ^ Z, Z = Z - y | 0, u.b = y = y << 20 ^ y >>> 12 ^ S, u.c = S = S - D | 0, u.d = D << 16 ^ S >>> 16 ^ Z, u.a = Z - y | 0;
      }, u.a = 0, u.b = 0, u.c = -1640531527, u.d = 1367130551, f === Math.floor(f) ? (u.a = f / 4294967296 | 0, u.b = f | 0) : l += f;
      for (var p = 0; p < l.length + 20; p++)
        u.b ^= l.charCodeAt(p) | 0, u.next();
    }
    function o(f, u) {
      return u.a = f.a, u.b = f.b, u.c = f.c, u.d = f.d, u;
    }
    function s(f, u) {
      var l = new i(f), p = u && u.state, y = function() {
        return (l.next() >>> 0) / 4294967296;
      };
      return y.double = function() {
        do
          var S = l.next() >>> 11, D = (l.next() >>> 0) / 4294967296, Z = (S + D) / (1 << 21);
        while (Z === 0);
        return Z;
      }, y.int32 = l.next, y.quick = y, p && (typeof p == "object" && o(p, l), y.state = function() {
        return o(l, {});
      }), y;
    }
    n && n.exports ? n.exports = s : this.tychei = s;
  })(
    hn,
    t3
  );
})(Pa);
var Dg = Pa.exports;
var Ku = { exports: {} };
var Mg = {};
var Og = Object.freeze(Object.defineProperty({
  __proto__: null,
  default: Mg
}, Symbol.toStringTag, { value: "Module" }));
var Ng = Yp(Og);
(function(t3) {
  (function(e, n, r) {
    var i = 256, o = 6, s = 52, f = "random", u = r.pow(i, o), l = r.pow(2, s), p = l * 2, y = i - 1, S;
    function D(q, he, Re) {
      var Ge = [];
      he = he == true ? { entropy: true } : he || {};
      var qe = ae(Se(
        he.entropy ? [q, Ue(n)] : q ?? Ne(),
        3
      ), Ge), et = new Z(Ge), rt = function() {
        for (var Le = et.g(o), st = u, tt = 0; Le < l; )
          Le = (Le + tt) * i, st *= i, tt = et.g(1);
        for (; Le >= p; )
          Le /= 2, st /= 2, tt >>>= 1;
        return (Le + tt) / st;
      };
      return rt.int32 = function() {
        return et.g(4) | 0;
      }, rt.quick = function() {
        return et.g(4) / 4294967296;
      }, rt.double = rt, ae(Ue(et.S), n), (he.pass || Re || function(Le, st, tt, N) {
        return N && (N.S && we(N, et), Le.state = function() {
          return we(et, {});
        }), tt ? (r[f] = Le, st) : Le;
      })(
        rt,
        qe,
        "global" in he ? he.global : this == r,
        he.state
      );
    }
    function Z(q) {
      var he, Re = q.length, Ge = this, qe = 0, et = Ge.i = Ge.j = 0, rt = Ge.S = [];
      for (Re || (q = [Re++]); qe < i; )
        rt[qe] = qe++;
      for (qe = 0; qe < i; qe++)
        rt[qe] = rt[et = y & et + q[qe % Re] + (he = rt[qe])], rt[et] = he;
      (Ge.g = function(Le) {
        for (var st, tt = 0, N = Ge.i, Ae = Ge.j, oe = Ge.S; Le--; )
          st = oe[N = y & N + 1], tt = tt * i + oe[y & (oe[N] = oe[Ae = y & Ae + st]) + (oe[Ae] = st)];
        return Ge.i = N, Ge.j = Ae, tt;
      })(i);
    }
    function we(q, he) {
      return he.i = q.i, he.j = q.j, he.S = q.S.slice(), he;
    }
    function Se(q, he) {
      var Re = [], Ge = typeof q, qe;
      if (he && Ge == "object")
        for (qe in q)
          try {
            Re.push(Se(q[qe], he - 1));
          } catch {
          }
      return Re.length ? Re : Ge == "string" ? q : q + "\0";
    }
    function ae(q, he) {
      for (var Re = q + "", Ge, qe = 0; qe < Re.length; )
        he[y & qe] = y & (Ge ^= he[y & qe] * 19) + Re.charCodeAt(qe++);
      return Ue(he);
    }
    function Ne() {
      try {
        var q;
        return S && (q = S.randomBytes) ? q = q(i) : (q = new Uint8Array(i), (e.crypto || e.msCrypto).getRandomValues(q)), Ue(q);
      } catch {
        var he = e.navigator, Re = he && he.plugins;
        return [+/* @__PURE__ */ new Date(), e, Re, e.screen, Ue(n)];
      }
    }
    function Ue(q) {
      return String.fromCharCode.apply(0, q);
    }
    if (ae(r.random(), n), t3.exports) {
      t3.exports = D;
      try {
        S = Ng;
      } catch {
      }
    } else
      r["seed" + f] = D;
  })(
    // global: `self` in browsers (including strict mode and web workers),
    // otherwise `this` in Node and other environments
    typeof self < "u" ? self : hn,
    [],
    // pool: entropy pool starts empty
    Math
    // math: package containing random, pow, and seedrandom
  );
})(Ku);
var Gg = Ku.exports;
var Bg = Fg;
var Vg = zg;
var $g = Lg;
var jg = Ig;
var Ug = Rg;
var Xg = Dg;
var jn = Gg;
jn.alea = Bg;
jn.xor128 = Vg;
jn.xorwow = $g;
jn.xorshift7 = jg;
jn.xor4096 = Ug;
jn.tychei = Xg;
var Hg = jn;
var Wg = ba(Hg);
function qg(t3, e) {
  for (var n = 0; n < e.length; n++) {
    var r = e[n];
    r.enumerable = r.enumerable || false, r.configurable = true, "value" in r && (r.writable = true), Object.defineProperty(t3, Zg(r.key), r);
  }
}
function Ca(t3, e, n) {
  return e && qg(t3.prototype, e), Object.defineProperty(t3, "prototype", {
    writable: false
  }), t3;
}
function Ju(t3, e) {
  t3.prototype = Object.create(e.prototype), t3.prototype.constructor = t3, fa(t3, e);
}
function fa(t3, e) {
  return fa = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(r, i) {
    return r.__proto__ = i, r;
  }, fa(t3, e);
}
function Yg(t3, e) {
  if (typeof t3 != "object" || t3 === null) return t3;
  var n = t3[Symbol.toPrimitive];
  if (n !== void 0) {
    var r = n.call(t3, e);
    if (typeof r != "object") return r;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(t3);
}
function Zg(t3) {
  var e = Yg(t3, "string");
  return typeof e == "symbol" ? e : String(e);
}
var Xi = function() {
  function t3() {
  }
  var e = t3.prototype;
  return e._seed = function(r, i) {
    if (r === (r || 0))
      return r;
    for (var o = "" + r, s = 0, f = 0; f < o.length; ++f)
      s ^= o.charCodeAt(f) | 0;
    return s;
  }, t3;
}();
var uu = function(t3) {
  Ju(e, t3);
  function e(r, i) {
    var o;
    return o = t3.call(this) || this, o._rng = void 0, o.seed(r, i), o;
  }
  var n = e.prototype;
  return n.next = function() {
    return this._rng();
  }, n.seed = function(i, o) {
    this._rng = i;
  }, n.clone = function(i, o) {
    return new e(this._rng, o);
  }, Ca(e, [{
    key: "name",
    get: function() {
      return "function";
    }
  }]), e;
}(Xi);
var cu = function() {
  var t3 = [].slice.call(arguments), e = t3, n = e[0], r = n === void 0 ? "default" : n;
  switch (typeof r) {
    case "object":
      if (r instanceof Xi)
        return r;
      break;
    case "function":
      return new uu(r);
    case "number":
    case "string":
    default:
      return new uu(Wg.apply(void 0, t3));
  }
  throw new Error('invalid RNG "' + r + '"');
};
var Qg = function(t3, e, n) {
  return e === void 0 && (e = 0), n === void 0 && (n = 1), function() {
    return t3.next() * (n - e) + e;
  };
};
function qt(t3) {
  return new Kg(t3);
}
var Kg = function(e) {
  var n = this;
  this.n = void 0, this.isInt = function() {
    if (Number.isInteger(n.n))
      return n;
    throw new Error("Expected number to be an integer, got " + n.n);
  }, this.isPositive = function() {
    if (n.n > 0)
      return n;
    throw new Error("Expected number to be positive, got " + n.n);
  }, this.lessThan = function(r) {
    if (n.n < r)
      return n;
    throw new Error("Expected number to be less than " + r + ", got " + n.n);
  }, this.greaterThanOrEqual = function(r) {
    if (n.n >= r)
      return n;
    throw new Error("Expected number to be greater than or equal to " + r + ", got " + n.n);
  }, this.greaterThan = function(r) {
    if (n.n > r)
      return n;
    throw new Error("Expected number to be greater than " + r + ", got " + n.n);
  }, this.n = e;
};
var Jg = function(t3, e, n) {
  return e === void 0 && (e = 0), n === void 0 && (n = 1), n === void 0 && (n = e === void 0 ? 1 : e, e = 0), qt(e).isInt(), qt(n).isInt(), function() {
    return Math.floor(t3.next() * (n - e + 1) + e);
  };
};
var e1 = function(t3) {
  return function() {
    return t3.next() >= 0.5;
  };
};
var t1 = function(t3, e, n) {
  return e === void 0 && (e = 0), n === void 0 && (n = 1), function() {
    var r, i, o;
    do
      r = t3.next() * 2 - 1, i = t3.next() * 2 - 1, o = r * r + i * i;
    while (!o || o > 1);
    return e + n * i * Math.sqrt(-2 * Math.log(o) / o);
  };
};
var n1 = function(t3, e, n) {
  e === void 0 && (e = 0), n === void 0 && (n = 1);
  var r = t3.normal(e, n);
  return function() {
    return Math.exp(r());
  };
};
var r1 = function(t3, e) {
  return e === void 0 && (e = 0.5), qt(e).greaterThanOrEqual(0).lessThan(1), function() {
    return Math.floor(t3.next() + e);
  };
};
var i1 = function(t3, e, n) {
  return e === void 0 && (e = 1), n === void 0 && (n = 0.5), qt(e).isInt().isPositive(), qt(n).greaterThanOrEqual(0).lessThan(1), function() {
    for (var r = 0, i = 0; r++ < e; )
      t3.next() < n && i++;
    return i;
  };
};
var o1 = function(t3, e) {
  e === void 0 && (e = 0.5), qt(e).greaterThan(0).lessThan(1);
  var n = 1 / Math.log(1 - e);
  return function() {
    return Math.floor(1 + Math.log(t3.next()) * n);
  };
};
var a1 = [0, 0, 0.6931471805599453, 1.791759469228055, 3.1780538303479458, 4.787491742782046, 6.579251212010101, 8.525161361065415, 10.60460290274525, 12.801827480081469];
var s1 = function(e) {
  return a1[e];
};
var f1 = 0.9189385332046727;
var u1 = function(t3, e) {
  if (e === void 0 && (e = 1), qt(e).isPositive(), e < 10) {
    var n = Math.exp(-e);
    return function() {
      for (var u = n, l = 0, p = t3.next(); p > u; )
        p = p - u, u = e * u / ++l;
      return l;
    };
  } else {
    var r = Math.sqrt(e), i = 0.931 + 2.53 * r, o = -0.059 + 0.02483 * i, s = 1.1239 + 1.1328 / (i - 3.4), f = 0.9277 - 3.6224 / (i - 2);
    return function() {
      for (; ; ) {
        var u = void 0, l = t3.next();
        if (l <= 0.86 * f)
          return u = l / f - 0.43, Math.floor((2 * o / (0.5 - Math.abs(u)) + i) * u + e + 0.445);
        l >= f ? u = t3.next() - 0.5 : (u = l / f - 0.93, u = (u < 0 ? -0.5 : 0.5) - u, l = t3.next() * f);
        var p = 0.5 - Math.abs(u);
        if (!(p < 0.013 && l > p)) {
          var y = Math.floor((2 * o / p + i) * u + e + 0.445);
          if (l = l * s / (o / (p * p) + i), y >= 10) {
            var S = (y + 0.5) * Math.log(e / y) - e - f1 + y - (0.08333333333333333 - (0.002777777777777778 - 1 / (1260 * y * y)) / (y * y)) / y;
            if (Math.log(l * r) <= S)
              return y;
          } else if (y >= 0) {
            var D, Z = (D = s1(y)) != null ? D : 0;
            if (Math.log(l) <= y * Math.log(e) - e - Z)
              return y;
          }
        }
      }
    };
  }
};
var c1 = function(t3, e) {
  return e === void 0 && (e = 1), qt(e).isPositive(), function() {
    return -Math.log(1 - t3.next()) / e;
  };
};
var l1 = function(t3, e) {
  return e === void 0 && (e = 1), qt(e).isInt().greaterThanOrEqual(0), function() {
    for (var n = 0, r = 0; r < e; ++r)
      n += t3.next();
    return n;
  };
};
var h1 = function(t3, e) {
  e === void 0 && (e = 1), qt(e).isInt().isPositive();
  var n = t3.irwinHall(e);
  return function() {
    return n() / e;
  };
};
var d1 = function(t3, e) {
  e === void 0 && (e = 1), qt(e).greaterThanOrEqual(0);
  var n = 1 / e;
  return function() {
    return 1 / Math.pow(1 - t3.next(), n);
  };
};
var m1 = function(t3) {
  Ju(e, t3);
  function e() {
    return t3.apply(this, arguments) || this;
  }
  var n = e.prototype;
  return n.next = function() {
    return Math.random();
  }, n.seed = function(i, o) {
  }, n.clone = function() {
    return new e();
  }, Ca(e, [{
    key: "name",
    get: function() {
      return "default";
    }
  }]), e;
}(Xi);
var ec = function() {
  function t3(n) {
    var r = this;
    this._rng = void 0, this._patch = void 0, this._cache = {}, this.next = function() {
      return r._rng.next();
    }, this.float = function(i, o) {
      return r.uniform(i, o)();
    }, this.int = function(i, o) {
      return r.uniformInt(i, o)();
    }, this.integer = function(i, o) {
      return r.uniformInt(i, o)();
    }, this.bool = function() {
      return r.uniformBoolean()();
    }, this.boolean = function() {
      return r.uniformBoolean()();
    }, this.uniform = function(i, o) {
      return r._memoize("uniform", Qg, i, o);
    }, this.uniformInt = function(i, o) {
      return r._memoize("uniformInt", Jg, i, o);
    }, this.uniformBoolean = function() {
      return r._memoize("uniformBoolean", e1);
    }, this.normal = function(i, o) {
      return t1(r, i, o);
    }, this.logNormal = function(i, o) {
      return n1(r, i, o);
    }, this.bernoulli = function(i) {
      return r1(r, i);
    }, this.binomial = function(i, o) {
      return i1(r, i, o);
    }, this.geometric = function(i) {
      return o1(r, i);
    }, this.poisson = function(i) {
      return u1(r, i);
    }, this.exponential = function(i) {
      return c1(r, i);
    }, this.irwinHall = function(i) {
      return l1(r, i);
    }, this.bates = function(i) {
      return h1(r, i);
    }, this.pareto = function(i) {
      return d1(r, i);
    }, n && n instanceof Xi ? this.use(n) : this.use(new m1()), this._cache = {};
  }
  var e = t3.prototype;
  return e.clone = function() {
    var r = [].slice.call(arguments);
    return r.length ? new t3(cu.apply(void 0, r)) : new t3(this.rng.clone());
  }, e.use = function() {
    this._rng = cu.apply(void 0, [].slice.call(arguments));
  }, e.patch = function() {
    if (this._patch)
      throw new Error("Math.random already patched");
    this._patch = Math.random, Math.random = this.uniform();
  }, e.unpatch = function() {
    this._patch && (Math.random = this._patch, delete this._patch);
  }, e.choice = function(r) {
    if (!Array.isArray(r))
      throw new Error("Random.choice expected input to be an array, got " + typeof r);
    var i = r == null ? void 0 : r.length;
    if (i > 0) {
      var o = this.uniformInt(0, i - 1)();
      return r[o];
    } else
      return;
  }, e._memoize = function(r, i) {
    var o = [].slice.call(arguments, 2), s = "" + o.join(";"), f = this._cache[r];
    return (f === void 0 || f.key !== s) && (f = {
      key: s,
      distribution: i.apply(void 0, [this].concat(o))
    }, this._cache[r] = f), f.distribution;
  }, Ca(t3, [{
    key: "rng",
    get: function() {
      return this._rng;
    }
  }]), t3;
}();
new ec();
var ua = 1e-3;
var ca = 64;
var p1 = class {
  constructor() {
    this.pointsTextureSize = 0, this.linksTextureSize = 0, this.alpha = 1, this.transform = Cg(), this.screenSize = [0, 0], this.mousePosition = [0, 0], this.screenMousePosition = [0, 0], this.selectedArea = [[0, 0], [0, 0]], this.isSimulationRunning = false, this.simulationProgress = 0, this.selectedIndices = null, this.maxPointSize = ca, this.hoveredPoint = void 0, this.focusedPoint = void 0, this.draggingPointIndex = void 0, this.adjustedSpaceSize = je.spaceSize, this.isSpaceKeyPressed = false, this.hoveredPointRingColor = [1, 1, 1, ov], this.focusedPointRingColor = [1, 1, 1, av], this.alphaTarget = 0, this.scalePointX = sa(), this.scalePointY = sa(), this.random = new ec(), this._backgroundColor = [0, 0, 0, 0], this.alphaDecay = (e) => 1 - Math.pow(ua, 1 / e);
  }
  get backgroundColor() {
    return this._backgroundColor;
  }
  set backgroundColor(e) {
    this._backgroundColor = e;
    const n = lv(e[0], e[1], e[2]);
    document.documentElement.style.setProperty("--cosmos-attribution-color", n > 0.65 ? "black" : "white"), this.div && (this.div.style.backgroundColor = `rgba(${e[0] * 255}, ${e[1] * 255}, ${e[2] * 255}, ${e[3]})`);
  }
  addRandomSeed(e) {
    this.random = this.random.clone(e);
  }
  getRandomFloat(e, n) {
    return this.random.float(e, n);
  }
  /**
   * If the config parameter `spaceSize` exceeds the limits of WebGL,
   * it reduces the space size without changing the config parameter.
   */
  adjustSpaceSize(e, n) {
    e >= n ? (this.adjustedSpaceSize = n / 2, console.warn(`The \`spaceSize\` has been reduced to ${this.adjustedSpaceSize} due to WebGL limits`)) : this.adjustedSpaceSize = e;
  }
  updateScreenSize(e, n) {
    const { adjustedSpaceSize: r } = this;
    this.screenSize = [e, n], this.scalePointX.domain([0, r]).range([(e - r) / 2, (e + r) / 2]), this.scalePointY.domain([r, 0]).range([(n - r) / 2, (n + r) / 2]);
  }
  scaleX(e) {
    return this.scalePointX(e);
  }
  scaleY(e) {
    return this.scalePointY(e);
  }
  setHoveredPointRingColor(e) {
    const n = ur(e);
    this.hoveredPointRingColor[0] = n[0], this.hoveredPointRingColor[1] = n[1], this.hoveredPointRingColor[2] = n[2];
  }
  setFocusedPointRingColor(e) {
    const n = ur(e);
    this.focusedPointRingColor[0] = n[0], this.focusedPointRingColor[1] = n[1], this.focusedPointRingColor[2] = n[2];
  }
  setFocusedPoint(e) {
    e !== void 0 ? this.focusedPoint = { index: e } : this.focusedPoint = void 0;
  }
  addAlpha(e) {
    return (this.alphaTarget - this.alpha) * this.alphaDecay(e);
  }
};
var v1 = { passive: false };
var Nr = { capture: true, passive: false };
function Wo(t3) {
  t3.stopImmediatePropagation();
}
function ar(t3) {
  t3.preventDefault(), t3.stopImmediatePropagation();
}
function tc(t3) {
  var e = t3.document.documentElement, n = Pt(t3).on("dragstart.drag", ar, Nr);
  "onselectstart" in e ? n.on("selectstart.drag", ar, Nr) : (e.__noselect = e.style.MozUserSelect, e.style.MozUserSelect = "none");
}
function nc(t3, e) {
  var n = t3.document.documentElement, r = Pt(t3).on("dragstart.drag", null);
  e && (r.on("click.drag", ar, Nr), setTimeout(function() {
    r.on("click.drag", null);
  }, 0)), "onselectstart" in n ? r.on("selectstart.drag", null) : (n.style.MozUserSelect = n.__noselect, delete n.__noselect);
}
var Ai = (t3) => () => t3;
function la(t3, {
  sourceEvent: e,
  subject: n,
  target: r,
  identifier: i,
  active: o,
  x: s,
  y: f,
  dx: u,
  dy: l,
  dispatch: p
}) {
  Object.defineProperties(this, {
    type: { value: t3, enumerable: true, configurable: true },
    sourceEvent: { value: e, enumerable: true, configurable: true },
    subject: { value: n, enumerable: true, configurable: true },
    target: { value: r, enumerable: true, configurable: true },
    identifier: { value: i, enumerable: true, configurable: true },
    active: { value: o, enumerable: true, configurable: true },
    x: { value: s, enumerable: true, configurable: true },
    y: { value: f, enumerable: true, configurable: true },
    dx: { value: u, enumerable: true, configurable: true },
    dy: { value: l, enumerable: true, configurable: true },
    _: { value: p }
  });
}
la.prototype.on = function() {
  var t3 = this._.on.apply(this._, arguments);
  return t3 === this._ ? this : t3;
};
function g1(t3) {
  return !t3.ctrlKey && !t3.button;
}
function x1() {
  return this.parentNode;
}
function y1(t3, e) {
  return e ?? { x: t3.x, y: t3.y };
}
function b1() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function _1() {
  var t3 = g1, e = x1, n = y1, r = b1, i = {}, o = $i("start", "drag", "end"), s = 0, f, u, l, p, y = 0;
  function S(q) {
    q.on("mousedown.drag", D).filter(r).on("touchstart.drag", Se).on("touchmove.drag", ae, v1).on("touchend.drag touchcancel.drag", Ne).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  function D(q, he) {
    if (!(p || !t3.call(this, q, he))) {
      var Re = Ue(this, e.call(this, q, he), q, he, "mouse");
      Re && (Pt(q.view).on("mousemove.drag", Z, Nr).on("mouseup.drag", we, Nr), tc(q.view), Wo(q), l = false, f = q.clientX, u = q.clientY, Re("start", q));
    }
  }
  function Z(q) {
    if (ar(q), !l) {
      var he = q.clientX - f, Re = q.clientY - u;
      l = he * he + Re * Re > y;
    }
    i.mouse("drag", q);
  }
  function we(q) {
    Pt(q.view).on("mousemove.drag mouseup.drag", null), nc(q.view, l), ar(q), i.mouse("end", q);
  }
  function Se(q, he) {
    if (t3.call(this, q, he)) {
      var Re = q.changedTouches, Ge = e.call(this, q, he), qe = Re.length, et, rt;
      for (et = 0; et < qe; ++et)
        (rt = Ue(this, Ge, q, he, Re[et].identifier, Re[et])) && (Wo(q), rt("start", q, Re[et]));
    }
  }
  function ae(q) {
    var he = q.changedTouches, Re = he.length, Ge, qe;
    for (Ge = 0; Ge < Re; ++Ge)
      (qe = i[he[Ge].identifier]) && (ar(q), qe("drag", q, he[Ge]));
  }
  function Ne(q) {
    var he = q.changedTouches, Re = he.length, Ge, qe;
    for (p && clearTimeout(p), p = setTimeout(function() {
      p = null;
    }, 500), Ge = 0; Ge < Re; ++Ge)
      (qe = i[he[Ge].identifier]) && (Wo(q), qe("end", q, he[Ge]));
  }
  function Ue(q, he, Re, Ge, qe, et) {
    var rt = o.copy(), Le = un(et || Re, he), st, tt, N;
    if ((N = n.call(q, new la("beforestart", {
      sourceEvent: Re,
      target: S,
      identifier: qe,
      active: s,
      x: Le[0],
      y: Le[1],
      dx: 0,
      dy: 0,
      dispatch: rt
    }), Ge)) != null)
      return st = N.x - Le[0] || 0, tt = N.y - Le[1] || 0, function Ae(oe, Ce, $e) {
        var Be = Le, Ye;
        switch (oe) {
          case "start":
            i[qe] = Ae, Ye = s++;
            break;
          case "end":
            delete i[qe], --s;
          case "drag":
            Le = un($e || Ce, he), Ye = s;
            break;
        }
        rt.call(
          oe,
          q,
          new la(oe, {
            sourceEvent: Ce,
            subject: N,
            target: S,
            identifier: qe,
            active: Ye,
            x: Le[0] + st,
            y: Le[1] + tt,
            dx: Le[0] - Be[0],
            dy: Le[1] - Be[1],
            dispatch: rt
          }),
          Ge
        );
      };
  }
  return S.filter = function(q) {
    return arguments.length ? (t3 = typeof q == "function" ? q : Ai(!!q), S) : t3;
  }, S.container = function(q) {
    return arguments.length ? (e = typeof q == "function" ? q : Ai(q), S) : e;
  }, S.subject = function(q) {
    return arguments.length ? (n = typeof q == "function" ? q : Ai(q), S) : n;
  }, S.touchable = function(q) {
    return arguments.length ? (r = typeof q == "function" ? q : Ai(!!q), S) : r;
  }, S.on = function() {
    var q = o.on.apply(o, arguments);
    return q === o ? S : q;
  }, S.clickDistance = function(q) {
    return arguments.length ? (y = (q = +q) * q, S) : Math.sqrt(y);
  }, S;
}
var Ei = (t3) => () => t3;
function S1(t3, {
  sourceEvent: e,
  target: n,
  transform: r,
  dispatch: i
}) {
  Object.defineProperties(this, {
    type: { value: t3, enumerable: true, configurable: true },
    sourceEvent: { value: e, enumerable: true, configurable: true },
    target: { value: n, enumerable: true, configurable: true },
    transform: { value: r, enumerable: true, configurable: true },
    _: { value: i }
  });
}
function cn(t3, e, n) {
  this.k = t3, this.x = e, this.y = n;
}
cn.prototype = {
  constructor: cn,
  scale: function(t3) {
    return t3 === 1 ? this : new cn(this.k * t3, this.x, this.y);
  },
  translate: function(t3, e) {
    return t3 === 0 & e === 0 ? this : new cn(this.k, this.x + this.k * t3, this.y + this.k * e);
  },
  apply: function(t3) {
    return [t3[0] * this.k + this.x, t3[1] * this.k + this.y];
  },
  applyX: function(t3) {
    return t3 * this.k + this.x;
  },
  applyY: function(t3) {
    return t3 * this.k + this.y;
  },
  invert: function(t3) {
    return [(t3[0] - this.x) / this.k, (t3[1] - this.y) / this.k];
  },
  invertX: function(t3) {
    return (t3 - this.x) / this.k;
  },
  invertY: function(t3) {
    return (t3 - this.y) / this.k;
  },
  rescaleX: function(t3) {
    return t3.copy().domain(t3.range().map(this.invertX, this).map(t3.invert, t3));
  },
  rescaleY: function(t3) {
    return t3.copy().domain(t3.range().map(this.invertY, this).map(t3.invert, t3));
  },
  toString: function() {
    return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
  }
};
var Rr = new cn(1, 0, 0);
cn.prototype;
function qo(t3) {
  t3.stopImmediatePropagation();
}
function zr(t3) {
  t3.preventDefault(), t3.stopImmediatePropagation();
}
function w1(t3) {
  return (!t3.ctrlKey || t3.type === "wheel") && !t3.button;
}
function T1() {
  var t3 = this;
  return t3 instanceof SVGElement ? (t3 = t3.ownerSVGElement || t3, t3.hasAttribute("viewBox") ? (t3 = t3.viewBox.baseVal, [[t3.x, t3.y], [t3.x + t3.width, t3.y + t3.height]]) : [[0, 0], [t3.width.baseVal.value, t3.height.baseVal.value]]) : [[0, 0], [t3.clientWidth, t3.clientHeight]];
}
function lu() {
  return this.__zoom || Rr;
}
function A1(t3) {
  return -t3.deltaY * (t3.deltaMode === 1 ? 0.05 : t3.deltaMode ? 1 : 2e-3) * (t3.ctrlKey ? 10 : 1);
}
function E1() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function P1(t3, e, n) {
  var r = t3.invertX(e[0][0]) - n[0][0], i = t3.invertX(e[1][0]) - n[1][0], o = t3.invertY(e[0][1]) - n[0][1], s = t3.invertY(e[1][1]) - n[1][1];
  return t3.translate(
    i > r ? (r + i) / 2 : Math.min(0, r) || Math.max(0, i),
    s > o ? (o + s) / 2 : Math.min(0, o) || Math.max(0, s)
  );
}
function C1() {
  var t3 = w1, e = T1, n = P1, r = A1, i = E1, o = [0, 1 / 0], s = [[-1 / 0, -1 / 0], [1 / 0, 1 / 0]], f = 250, u = $m, l = $i("start", "zoom", "end"), p, y, S, D = 500, Z = 150, we = 0, Se = 10;
  function ae(N) {
    N.property("__zoom", lu).on("wheel.zoom", qe, { passive: false }).on("mousedown.zoom", et).on("dblclick.zoom", rt).filter(i).on("touchstart.zoom", Le).on("touchmove.zoom", st).on("touchend.zoom touchcancel.zoom", tt).style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  ae.transform = function(N, Ae, oe, Ce) {
    var $e = N.selection ? N.selection() : N;
    $e.property("__zoom", lu), N !== $e ? he(N, Ae, oe, Ce) : $e.interrupt().each(function() {
      Re(this, arguments).event(Ce).start().zoom(null, typeof Ae == "function" ? Ae.apply(this, arguments) : Ae).end();
    });
  }, ae.scaleBy = function(N, Ae, oe, Ce) {
    ae.scaleTo(N, function() {
      var $e = this.__zoom.k, Be = typeof Ae == "function" ? Ae.apply(this, arguments) : Ae;
      return $e * Be;
    }, oe, Ce);
  }, ae.scaleTo = function(N, Ae, oe, Ce) {
    ae.transform(N, function() {
      var $e = e.apply(this, arguments), Be = this.__zoom, Ye = oe == null ? q($e) : typeof oe == "function" ? oe.apply(this, arguments) : oe, nt = Be.invert(Ye), ut = typeof Ae == "function" ? Ae.apply(this, arguments) : Ae;
      return n(Ue(Ne(Be, ut), Ye, nt), $e, s);
    }, oe, Ce);
  }, ae.translateBy = function(N, Ae, oe, Ce) {
    ae.transform(N, function() {
      return n(this.__zoom.translate(
        typeof Ae == "function" ? Ae.apply(this, arguments) : Ae,
        typeof oe == "function" ? oe.apply(this, arguments) : oe
      ), e.apply(this, arguments), s);
    }, null, Ce);
  }, ae.translateTo = function(N, Ae, oe, Ce, $e) {
    ae.transform(N, function() {
      var Be = e.apply(this, arguments), Ye = this.__zoom, nt = Ce == null ? q(Be) : typeof Ce == "function" ? Ce.apply(this, arguments) : Ce;
      return n(Rr.translate(nt[0], nt[1]).scale(Ye.k).translate(
        typeof Ae == "function" ? -Ae.apply(this, arguments) : -Ae,
        typeof oe == "function" ? -oe.apply(this, arguments) : -oe
      ), Be, s);
    }, Ce, $e);
  };
  function Ne(N, Ae) {
    return Ae = Math.max(o[0], Math.min(o[1], Ae)), Ae === N.k ? N : new cn(Ae, N.x, N.y);
  }
  function Ue(N, Ae, oe) {
    var Ce = Ae[0] - oe[0] * N.k, $e = Ae[1] - oe[1] * N.k;
    return Ce === N.x && $e === N.y ? N : new cn(N.k, Ce, $e);
  }
  function q(N) {
    return [(+N[0][0] + +N[1][0]) / 2, (+N[0][1] + +N[1][1]) / 2];
  }
  function he(N, Ae, oe, Ce) {
    N.on("start.zoom", function() {
      Re(this, arguments).event(Ce).start();
    }).on("interrupt.zoom end.zoom", function() {
      Re(this, arguments).event(Ce).end();
    }).tween("zoom", function() {
      var $e = this, Be = arguments, Ye = Re($e, Be).event(Ce), nt = e.apply($e, Be), ut = oe == null ? q(nt) : typeof oe == "function" ? oe.apply($e, Be) : oe, Gt = Math.max(nt[1][0] - nt[0][0], nt[1][1] - nt[0][1]), bt = $e.__zoom, zt = typeof Ae == "function" ? Ae.apply($e, Be) : Ae, $t = u(bt.invert(ut).concat(Gt / bt.k), zt.invert(ut).concat(Gt / zt.k));
      return function(Lt) {
        if (Lt === 1) Lt = zt;
        else {
          var jt = $t(Lt), lr = Gt / jt[2];
          Lt = new cn(lr, ut[0] - jt[0] * lr, ut[1] - jt[1] * lr);
        }
        Ye.zoom(null, Lt);
      };
    });
  }
  function Re(N, Ae, oe) {
    return !oe && N.__zooming || new Ge(N, Ae);
  }
  function Ge(N, Ae) {
    this.that = N, this.args = Ae, this.active = 0, this.sourceEvent = null, this.extent = e.apply(N, Ae), this.taps = 0;
  }
  Ge.prototype = {
    event: function(N) {
      return N && (this.sourceEvent = N), this;
    },
    start: function() {
      return ++this.active === 1 && (this.that.__zooming = this, this.emit("start")), this;
    },
    zoom: function(N, Ae) {
      return this.mouse && N !== "mouse" && (this.mouse[1] = Ae.invert(this.mouse[0])), this.touch0 && N !== "touch" && (this.touch0[1] = Ae.invert(this.touch0[0])), this.touch1 && N !== "touch" && (this.touch1[1] = Ae.invert(this.touch1[0])), this.that.__zoom = Ae, this.emit("zoom"), this;
    },
    end: function() {
      return --this.active === 0 && (delete this.that.__zooming, this.emit("end")), this;
    },
    emit: function(N) {
      var Ae = Pt(this.that).datum();
      l.call(
        N,
        this.that,
        new S1(N, {
          sourceEvent: this.sourceEvent,
          target: ae,
          type: N,
          transform: this.that.__zoom,
          dispatch: l
        }),
        Ae
      );
    }
  };
  function qe(N, ...Ae) {
    if (!t3.apply(this, arguments)) return;
    var oe = Re(this, Ae).event(N), Ce = this.__zoom, $e = Math.max(o[0], Math.min(o[1], Ce.k * Math.pow(2, r.apply(this, arguments)))), Be = un(N);
    if (oe.wheel)
      (oe.mouse[0][0] !== Be[0] || oe.mouse[0][1] !== Be[1]) && (oe.mouse[1] = Ce.invert(oe.mouse[0] = Be)), clearTimeout(oe.wheel);
    else {
      if (Ce.k === $e) return;
      oe.mouse = [Be, Ce.invert(Be)], Fi(this), oe.start();
    }
    zr(N), oe.wheel = setTimeout(Ye, Z), oe.zoom("mouse", n(Ue(Ne(Ce, $e), oe.mouse[0], oe.mouse[1]), oe.extent, s));
    function Ye() {
      oe.wheel = null, oe.end();
    }
  }
  function et(N, ...Ae) {
    if (S || !t3.apply(this, arguments)) return;
    var oe = N.currentTarget, Ce = Re(this, Ae, true).event(N), $e = Pt(N.view).on("mousemove.zoom", ut, true).on("mouseup.zoom", Gt, true), Be = un(N, oe), Ye = N.clientX, nt = N.clientY;
    tc(N.view), qo(N), Ce.mouse = [Be, this.__zoom.invert(Be)], Fi(this), Ce.start();
    function ut(bt) {
      if (zr(bt), !Ce.moved) {
        var zt = bt.clientX - Ye, $t = bt.clientY - nt;
        Ce.moved = zt * zt + $t * $t > we;
      }
      Ce.event(bt).zoom("mouse", n(Ue(Ce.that.__zoom, Ce.mouse[0] = un(bt, oe), Ce.mouse[1]), Ce.extent, s));
    }
    function Gt(bt) {
      $e.on("mousemove.zoom mouseup.zoom", null), nc(bt.view, Ce.moved), zr(bt), Ce.event(bt).end();
    }
  }
  function rt(N, ...Ae) {
    if (t3.apply(this, arguments)) {
      var oe = this.__zoom, Ce = un(N.changedTouches ? N.changedTouches[0] : N, this), $e = oe.invert(Ce), Be = oe.k * (N.shiftKey ? 0.5 : 2), Ye = n(Ue(Ne(oe, Be), Ce, $e), e.apply(this, Ae), s);
      zr(N), f > 0 ? Pt(this).transition().duration(f).call(he, Ye, Ce, N) : Pt(this).call(ae.transform, Ye, Ce, N);
    }
  }
  function Le(N, ...Ae) {
    if (t3.apply(this, arguments)) {
      var oe = N.touches, Ce = oe.length, $e = Re(this, Ae, N.changedTouches.length === Ce).event(N), Be, Ye, nt, ut;
      for (qo(N), Ye = 0; Ye < Ce; ++Ye)
        nt = oe[Ye], ut = un(nt, this), ut = [ut, this.__zoom.invert(ut), nt.identifier], $e.touch0 ? !$e.touch1 && $e.touch0[2] !== ut[2] && ($e.touch1 = ut, $e.taps = 0) : ($e.touch0 = ut, Be = true, $e.taps = 1 + !!p);
      p && (p = clearTimeout(p)), Be && ($e.taps < 2 && (y = ut[0], p = setTimeout(function() {
        p = null;
      }, D)), Fi(this), $e.start());
    }
  }
  function st(N, ...Ae) {
    if (this.__zooming) {
      var oe = Re(this, Ae).event(N), Ce = N.changedTouches, $e = Ce.length, Be, Ye, nt, ut;
      for (zr(N), Be = 0; Be < $e; ++Be)
        Ye = Ce[Be], nt = un(Ye, this), oe.touch0 && oe.touch0[2] === Ye.identifier ? oe.touch0[0] = nt : oe.touch1 && oe.touch1[2] === Ye.identifier && (oe.touch1[0] = nt);
      if (Ye = oe.that.__zoom, oe.touch1) {
        var Gt = oe.touch0[0], bt = oe.touch0[1], zt = oe.touch1[0], $t = oe.touch1[1], Lt = (Lt = zt[0] - Gt[0]) * Lt + (Lt = zt[1] - Gt[1]) * Lt, jt = (jt = $t[0] - bt[0]) * jt + (jt = $t[1] - bt[1]) * jt;
        Ye = Ne(Ye, Math.sqrt(Lt / jt)), nt = [(Gt[0] + zt[0]) / 2, (Gt[1] + zt[1]) / 2], ut = [(bt[0] + $t[0]) / 2, (bt[1] + $t[1]) / 2];
      } else if (oe.touch0) nt = oe.touch0[0], ut = oe.touch0[1];
      else return;
      oe.zoom("touch", n(Ue(Ye, nt, ut), oe.extent, s));
    }
  }
  function tt(N, ...Ae) {
    if (this.__zooming) {
      var oe = Re(this, Ae).event(N), Ce = N.changedTouches, $e = Ce.length, Be, Ye;
      for (qo(N), S && clearTimeout(S), S = setTimeout(function() {
        S = null;
      }, D), Be = 0; Be < $e; ++Be)
        Ye = Ce[Be], oe.touch0 && oe.touch0[2] === Ye.identifier ? delete oe.touch0 : oe.touch1 && oe.touch1[2] === Ye.identifier && delete oe.touch1;
      if (oe.touch1 && !oe.touch0 && (oe.touch0 = oe.touch1, delete oe.touch1), oe.touch0) oe.touch0[1] = this.__zoom.invert(oe.touch0[0]);
      else if (oe.end(), oe.taps === 2 && (Ye = un(Ye, this), Math.hypot(y[0] - Ye[0], y[1] - Ye[1]) < Se)) {
        var nt = Pt(this).on("dblclick.zoom");
        nt && nt.apply(this, arguments);
      }
    }
  }
  return ae.wheelDelta = function(N) {
    return arguments.length ? (r = typeof N == "function" ? N : Ei(+N), ae) : r;
  }, ae.filter = function(N) {
    return arguments.length ? (t3 = typeof N == "function" ? N : Ei(!!N), ae) : t3;
  }, ae.touchable = function(N) {
    return arguments.length ? (i = typeof N == "function" ? N : Ei(!!N), ae) : i;
  }, ae.extent = function(N) {
    return arguments.length ? (e = typeof N == "function" ? N : Ei([[+N[0][0], +N[0][1]], [+N[1][0], +N[1][1]]]), ae) : e;
  }, ae.scaleExtent = function(N) {
    return arguments.length ? (o[0] = +N[0], o[1] = +N[1], ae) : [o[0], o[1]];
  }, ae.translateExtent = function(N) {
    return arguments.length ? (s[0][0] = +N[0][0], s[1][0] = +N[1][0], s[0][1] = +N[0][1], s[1][1] = +N[1][1], ae) : [[s[0][0], s[0][1]], [s[1][0], s[1][1]]];
  }, ae.constrain = function(N) {
    return arguments.length ? (n = N, ae) : n;
  }, ae.duration = function(N) {
    return arguments.length ? (f = +N, ae) : f;
  }, ae.interpolate = function(N) {
    return arguments.length ? (u = N, ae) : u;
  }, ae.on = function() {
    var N = l.on.apply(l, arguments);
    return N === l ? ae : N;
  }, ae.clickDistance = function(N) {
    return arguments.length ? (we = (N = +N) * N, ae) : Math.sqrt(we);
  }, ae.tapDistance = function(N) {
    return arguments.length ? (Se = +N, ae) : Se;
  }, ae;
}
var k1 = class {
  constructor(e, n) {
    this.eventTransform = Rr, this.behavior = C1().scaleExtent([1e-3, 1 / 0]).on("start", (r) => {
      var o, s;
      this.isRunning = true;
      const i = !!r.sourceEvent;
      (s = (o = this.config) == null ? void 0 : o.onZoomStart) == null || s.call(o, r, i);
    }).on("zoom", (r) => {
      var S, D;
      this.eventTransform = r.transform;
      const { eventTransform: { x: i, y: o, k: s }, store: { transform: f, screenSize: u } } = this, l = u[0], p = u[1];
      if (!l || !p) return;
      kg(f, l, p), fu(f, f, [i, o]), Ho(f, f, [s, s]), fu(f, f, [l / 2, p / 2]), Ho(f, f, [l / 2, p / 2]), Ho(f, f, [1, -1]);
      const y = !!r.sourceEvent;
      (D = (S = this.config) == null ? void 0 : S.onZoom) == null || D.call(S, r, y);
    }).on("end", (r) => {
      var o, s;
      this.isRunning = false;
      const i = !!r.sourceEvent;
      (s = (o = this.config) == null ? void 0 : o.onZoomEnd) == null || s.call(o, r, i);
    }), this.isRunning = false, this.store = e, this.config = n;
  }
  /**
   * Get the zoom transform that will fit the given point positions into the viewport
   *
   * @param positions An array of point positions in the form `[x, y]`
   * @param scale An optional scale factor to apply to the transform
   * @param padding Padding around the viewport in percentage
   */
  getTransform(e, n, r = 0.1) {
    if (e.length === 0) return this.eventTransform;
    const { store: { screenSize: i } } = this, o = i[0], s = i[1], f = Jf(e.map((ae) => ae[0])), u = Jf(e.map((ae) => ae[1]));
    f[0] = this.store.scaleX(f[0]), f[1] = this.store.scaleX(f[1]), u[0] = this.store.scaleY(u[0]), u[1] = this.store.scaleY(u[1]), f[0] === f[1] && (f[0] -= 0.5, f[1] += 0.5), u[0] === u[1] && (u[0] += 0.5, u[1] -= 0.5);
    const l = o * (1 - r * 2) / (f[1] - f[0]), p = s * (1 - r * 2) / (u[0] - u[1]), y = hv(n ?? Math.min(l, p), ...this.behavior.scaleExtent()), S = (f[1] + f[0]) / 2, D = (u[1] + u[0]) / 2, Z = o / 2 - S * y, we = s / 2 - D * y;
    return Rr.translate(Z, we).scale(y);
  }
  getDistanceToPoint(e) {
    const { x: n, y: r, k: i } = this.eventTransform, o = this.getTransform([e], i), s = n - o.x, f = r - o.y;
    return Math.sqrt(s * s + f * f);
  }
  getMiddlePointTransform(e) {
    const { store: { screenSize: n }, eventTransform: { x: r, y: i, k: o } } = this, s = n[0], f = n[1], u = (s / 2 - r) / o, l = (f / 2 - i) / o, p = this.store.scaleX(e[0]), y = this.store.scaleY(e[1]), S = (u + p) / 2, D = (l + y) / 2, Z = 1, we = s / 2 - S * Z, Se = f / 2 - D * Z;
    return Rr.translate(we, Se).scale(Z);
  }
  convertScreenToSpacePosition(e) {
    const { eventTransform: { x: n, y: r, k: i }, store: { screenSize: o } } = this, s = o[0], f = o[1], u = (e[0] - n) / i, l = (e[1] - r) / i, p = [u, f - l];
    return p[0] -= (s - this.store.adjustedSpaceSize) / 2, p[1] -= (f - this.store.adjustedSpaceSize) / 2, p;
  }
  convertSpaceToScreenPosition(e) {
    const n = this.eventTransform.applyX(this.store.scaleX(e[0])), r = this.eventTransform.applyY(this.store.scaleY(e[1]));
    return [n, r];
  }
  convertSpaceToScreenRadius(e) {
    const { config: { scalePointsOnZoom: n }, store: { maxPointSize: r }, eventTransform: { k: i } } = this;
    let o = e * 2;
    return n ? o *= i : o *= Math.min(5, Math.max(1, i * 0.01)), Math.min(o, r) / 2;
  }
};
var F1 = class {
  constructor(e, n) {
    this.isActive = false, this.behavior = _1().subject((r) => this.store.hoveredPoint && !this.store.isSpaceKeyPressed ? { x: r.x, y: r.y } : void 0).on("start", (r) => {
      var i, o;
      this.store.hoveredPoint && (this.store.draggingPointIndex = this.store.hoveredPoint.index, this.isActive = true, (o = (i = this.config) == null ? void 0 : i.onDragStart) == null || o.call(i, r));
    }).on("drag", (r) => {
      var i, o;
      (o = (i = this.config) == null ? void 0 : i.onDrag) == null || o.call(i, r);
    }).on("end", (r) => {
      var i, o;
      this.isActive = false, this.store.draggingPointIndex = void 0, (o = (i = this.config) == null ? void 0 : i.onDragEnd) == null || o.call(i, r);
    }), this.store = e, this.config = n;
  }
};
var z1 = '<svg clip-rule="evenodd" fill-rule="evenodd" stroke-linejoin="round" stroke-miterlimit="2" viewBox="0 0 151 11" xmlns="http://www.w3.org/2000/svg" xmlns:bx="https://boxy-svg.com" width="151px" height="11px"><defs/><clipPath id="a"><path clip-rule="evenodd" d="m0 0h163v11h-163z"/></clipPath><g transform="scale(.920956 1)"><path d="m0 0h163v11h-163z" fill="none"/><g clip-path="url(#a)"><g fill="currentColor" transform="matrix(1.08583 0 0 1 -14.1352 -16.2014)"><g fill-rule="nonzero"><path d="m.22 0h.096l.182-.486h-.064l-.16.44h-.012l-.16-.44h-.064z" transform="matrix(12 0 0 12 12.5618 24.8014)"/><path d="m.118-.57c.03 0 .052-.022.052-.052s-.022-.052-.052-.052-.052.022-.052.052.022.052.052.052zm-.03.57h.06v-.486h-.06z" transform="matrix(12 0 0 12 18.5138 24.8014)"/><path d="m.281.014c.118 0 .193-.052.193-.144s-.078-.124-.177-.141l-.046-.008c-.069-.012-.112-.032-.112-.086s.047-.083.121-.083.128.029.141.106l.056-.012c-.018-.095-.087-.146-.197-.146s-.181.051-.181.135.07.123.163.139l.046.008c.076.013.126.03.126.088s-.053.092-.133.092-.153-.034-.168-.14l-.059.011c.018.129.109.181.227.181z" transform="matrix(12 0 0 12 20.8658 24.8014)"/><path d="m.275.01c.104 0 .154-.048.178-.1h.012v.09h.058v-.486h-.06v.243c0 .133-.075.199-.175.199-.089 0-.147-.044-.147-.15v-.292h-.06v.296c0 .134.089.2.194.2z" transform="matrix(12 0 0 12 26.7218 24.8014)"/><path d="m.237.014c.1 0 .144-.045.17-.09h.01v.007c0 .043.026.069.067.069h.064v-.052h-.047c-.018 0-.028-.01-.028-.03v-.236c0-.114-.071-.182-.193-.182-.121 0-.187.068-.21.153l.058.017c.017-.07.066-.116.152-.116.087 0 .133.047.133.125v.039h-.178c-.101 0-.18.048-.18.145s.079.151.182.151zm.004-.054c-.074 0-.126-.035-.126-.098 0-.062.052-.092.121-.092h.177v.012c0 .113-.069.178-.172.178z" transform="matrix(12 0 0 12 33.5738 24.8014)"/><path d="m.088-.7h.06v.7h-.06z" transform="matrix(12 0 0 12 39.9818 24.8014)"/><path d="m.118-.57c.03 0 .052-.022.052-.052s-.022-.052-.052-.052-.052.022-.052.052.022.052.052.052zm-.03.57h.06v-.486h-.06z" transform="matrix(12 0 0 12 42.3338 24.8014)"/><path d="m.064 0h.396v-.054h-.336v-.01l.328-.34v-.082h-.378v.054h.317v.01l-.327.34z" transform="matrix(12 0 0 12 44.6858 24.8014)"/><path d="m.312.014c.129 0 .186-.071.217-.137l-.054-.024c-.027.06-.072.107-.163.107-.107 0-.184-.069-.188-.19h.414v-.032c0-.148-.097-.238-.233-.238-.14 0-.241.102-.241.251v.012c0 .149.098.251.248.251zm-.187-.296c.014-.105.083-.164.18-.164.098 0 .167.059.172.164z" transform="matrix(12 0 0 12 50.1818 24.8014)"/><path d="m.298.014c.107 0 .162-.05.19-.103h.012v.089h.058v-.7h-.06v.299h-.012c-.026-.049-.081-.099-.188-.099-.131 0-.234.097-.234.251v.012c0 .155.104.251.234.251zm.014-.054c-.11 0-.186-.075-.186-.199v-.008c0-.124.076-.199.186-.199s.186.075.186.199v.008c0 .124-.076.199-.186.199z" transform="matrix(12 0 0 12 56.8778 24.8014)"/><path d="m.168 0h.105l.125-.446h.012l.125.446h.105l.11-.486h-.062l-.096.446h-.012l-.125-.446h-.102l-.125.446h-.012l-.096-.446h-.062z" transform="matrix(12 0 0 12 66.7658 24.8014)"/><path d="m.118-.57c.03 0 .052-.022.052-.052s-.022-.052-.052-.052-.052.022-.052.052.022.052.052.052zm-.03.57h.06v-.486h-.06z" transform="matrix(12 0 0 12 75.9818 24.8014)"/><path d="m.264 0h.126v-.054h-.114c-.022 0-.03-.01-.03-.03v-.348h.17v-.054h-.17v-.182h-.06v.182h-.156v.054h.156v.356c0 .048.026.076.078.076z" transform="matrix(12 0 0 12 78.3338 24.8014)"/><path d="m.088 0h.06v-.243c0-.133.075-.198.175-.198.089 0 .147.043.147.149v.292h.06v-.296c0-.134-.089-.199-.194-.199-.104 0-.152.047-.176.094h-.012v-.299h-.06z" transform="matrix(12 0 0 12 83.3978 24.8014)"/><path d="m.334.014c.162 0 .251-.098.251-.255v-.013h-.064v.015c0 .115-.059.195-.187.195-.13 0-.206-.082-.206-.225v-.162c0-.143.076-.225.206-.225.128 0 .187.08.187.195v.007h.064v-.005c0-.157-.089-.255-.251-.255-.164 0-.27.098-.27.285v.158c0 .187.106.285.27.285z" transform="matrix(12 0 0 12 92.8778 24.8014)"/><path d="m.31.014c.148 0 .246-.101.246-.251v-.012c0-.15-.098-.251-.246-.251s-.246.101-.246.251v.012c0 .15.098.251.246.251zm0-.054c-.114 0-.186-.079-.186-.199v-.008c0-.12.072-.199.186-.199s.186.079.186.199v.008c0 .12-.072.199-.186.199z" transform="matrix(12 0 0 12 100.09 24.8014)"/><path d="m.281.014c.118 0 .193-.052.193-.144s-.078-.124-.177-.141l-.046-.008c-.069-.012-.112-.032-.112-.086s.047-.083.121-.083.128.029.141.106l.056-.012c-.018-.095-.087-.146-.197-.146s-.181.051-.181.135.07.123.163.139l.046.008c.076.013.126.03.126.088s-.053.092-.133.092-.153-.034-.168-.14l-.059.011c.018.129.109.181.227.181z" transform="matrix(12 0 0 12 107.05 24.8014)"/><path d="m.088 0h.06v-.295c0-.096.059-.147.136-.147.076 0 .12.046.12.115v.327h.06v-.295c0-.096.059-.147.136-.147.076 0 .12.046.12.115v.327h.06v-.333c0-.097-.067-.163-.166-.163-.087 0-.132.041-.154.092h-.012c-.022-.05-.07-.092-.148-.092-.082 0-.122.038-.142.074h-.012v-.064h-.058z" transform="matrix(12 0 0 12 112.906 24.8014)"/><path d="m.31.014c.148 0 .246-.101.246-.251v-.012c0-.15-.098-.251-.246-.251s-.246.101-.246.251v.012c0 .15.098.251.246.251zm0-.054c-.114 0-.186-.079-.186-.199v-.008c0-.12.072-.199.186-.199s.186.079.186.199v.008c0 .12-.072.199-.186.199z" transform="matrix(12 0 0 12 122.77 24.8014)"/><path d="m.064-.241c0 .152.103.247.234.247.107 0 .162-.05.188-.099h.012v.209c0 .02-.009.03-.031.03h-.32v.054h.332c.052 0 .077-.028.077-.076v-.61h-.056v.089h-.012c-.028-.053-.083-.103-.19-.103-.13 0-.234.094-.234.247zm.248.193c-.11 0-.186-.073-.186-.195v-.008c0-.122.076-.195.186-.195s.186.073.186.195v.008c0 .122-.076.195-.186.195z" transform="matrix(12 0 0 12 129.73 24.8014)"/><path d="m.088 0h.06v-.304c0-.088.047-.128.121-.128h.064v-.056h-.057c-.065 0-.1.019-.118.063h-.012v-.061h-.058z" transform="matrix(12 0 0 12 136.978 24.8014)"/><path d="m.237.014c.1 0 .144-.045.17-.09h.01v.007c0 .043.026.069.067.069h.064v-.052h-.047c-.018 0-.028-.01-.028-.03v-.236c0-.114-.071-.182-.193-.182-.121 0-.187.068-.21.153l.058.017c.017-.07.066-.116.152-.116.087 0 .133.047.133.125v.039h-.178c-.101 0-.18.048-.18.145s.079.151.182.151zm.004-.054c-.074 0-.126-.035-.126-.098 0-.062.052-.092.121-.092h.177v.012c0 .113-.069.178-.172.178z" transform="matrix(12 0 0 12 140.698 24.8014)"/><path d="m.088.2h.06v-.285h.012c.026.049.081.099.188.099.131 0 .234-.097.234-.251v-.012c0-.155-.104-.251-.234-.251-.107 0-.162.05-.19.103h-.012v-.089h-.058zm.246-.24c-.11 0-.186-.075-.186-.199v-.008c0-.124.076-.199.186-.199s.186.075.186.199v.008c0 .124-.076.199-.186.199z" transform="matrix(12 0 0 12 147.106 24.8014)"/><path d="m.088 0h.06v-.243c0-.133.075-.198.175-.198.089 0 .147.043.147.149v.292h.06v-.296c0-.134-.089-.199-.194-.199-.104 0-.152.047-.176.094h-.012v-.299h-.06z" transform="matrix(12 0 0 12 154.378 24.8014)"/></g><path d="m92.878 26.001h68.844v.6h-68.844z"/></g></g></g></svg>';
var L1 = class {
  constructor(e, n) {
    this.config = new dv(), this.graph = new Dv(this.config), this.requestAnimationFrameId = 0, this.isRightClickMouse = false, this.store = new p1(), this.zoomInstance = new k1(this.store, this.config), this.dragInstance = new F1(this.store, this.config), this._findHoveredPointExecutionCount = 0, this._isMouseOnCanvas = false, this._isFirstRenderAfterInit = true, this._hasPointPositionsChanged = false, this._hasPointColorsChanged = false, this._hasPointSizesChanged = false, this._hasLinksChanged = false, this._hasLinkColorsChanged = false, this._hasLinkWidthsChanged = false, this._hasLinkArrowsChanged = false, this._hasPointClustersChanged = false, this._hasClusterPositionsChanged = false, this._hasPointClusterForceChanged = false, n && this.config.init(n), this.store.div = e;
    const r = document.createElement("canvas");
    r.style.width = "100%", r.style.height = "100%", this.store.div.appendChild(r), this.addAttribution();
    const i = r.clientWidth, o = r.clientHeight;
    r.width = i * this.config.pixelRatio, r.height = o * this.config.pixelRatio, this.canvas = r, this.reglInstance = Qp({
      canvas: this.canvas,
      attributes: {
        antialias: false,
        preserveDrawingBuffer: true
      },
      extensions: ["OES_texture_float", "ANGLE_instanced_arrays"]
    }), this.store.adjustSpaceSize(this.config.spaceSize, this.reglInstance.limits.maxTextureSize), this.store.updateScreenSize(i, o), this.canvasD3Selection = Pt(this.canvas), this.canvasD3Selection.on("mouseenter.cosmos", () => {
      this._isMouseOnCanvas = true;
    }).on("mousemove.cosmos", () => {
      this._isMouseOnCanvas = true;
    }).on("mouseleave.cosmos", () => {
      this._isMouseOnCanvas = false;
    }), Pt(document).on("keydown.cosmos", (s) => {
      s.code === "Space" && (this.store.isSpaceKeyPressed = true);
    }).on("keyup.cosmos", (s) => {
      s.code === "Space" && (this.store.isSpaceKeyPressed = false);
    }), this.zoomInstance.behavior.on("start.detect", (s) => {
      this.currentEvent = s;
    }).on("zoom.detect", (s) => {
      !!s.sourceEvent && this.updateMousePosition(s.sourceEvent), this.currentEvent = s;
    }).on("end.detect", (s) => {
      this.currentEvent = s;
    }), this.dragInstance.behavior.on("start.detect", (s) => {
      this.currentEvent = s, this.updateCanvasCursor();
    }).on("drag.detect", (s) => {
      this.dragInstance.isActive && this.updateMousePosition(s), this.currentEvent = s;
    }).on("end.detect", (s) => {
      this.currentEvent = s, this.updateCanvasCursor();
    }), this.canvasD3Selection.call(this.dragInstance.behavior).call(this.zoomInstance.behavior).on("click", this.onClick.bind(this)).on("mousemove", this.onMouseMove.bind(this)).on("contextmenu", this.onRightClickMouse.bind(this)), (this.config.disableZoom || !this.config.enableDrag) && this.updateZoomDragBehaviors(), this.setZoomLevel(this.config.initialZoomLevel ?? 1), this.store.maxPointSize = (this.reglInstance.limits.pointSizeDims[1] ?? ca) / this.config.pixelRatio, this.points = new Pg(this.reglInstance, this.config, this.store, this.graph), this.lines = new mg(this.reglInstance, this.config, this.store, this.graph, this.points), this.config.disableSimulation || (this.forceGravity = new yv(this.reglInstance, this.config, this.store, this.graph, this.points), this.forceCenter = new gv(this.reglInstance, this.config, this.store, this.graph, this.points), this.forceManyBody = this.config.useQuadtree ? new Av(this.reglInstance, this.config, this.store, this.graph, this.points) : new wv(this.reglInstance, this.config, this.store, this.graph, this.points), this.forceLinkIncoming = new Qf(this.reglInstance, this.config, this.store, this.graph, this.points), this.forceLinkOutgoing = new Qf(this.reglInstance, this.config, this.store, this.graph, this.points), this.forceMouse = new Pv(this.reglInstance, this.config, this.store, this.graph, this.points), this.clusters = new zv(this.reglInstance, this.config, this.store, this.graph, this.points)), this.store.backgroundColor = ur(this.config.backgroundColor), this.config.hoveredPointRingColor && this.store.setHoveredPointRingColor(this.config.hoveredPointRingColor), this.config.focusedPointRingColor && this.store.setFocusedPointRingColor(this.config.focusedPointRingColor), this.config.focusedPointIndex !== void 0 && this.store.setFocusedPoint(this.config.focusedPointIndex), this.config.showFPSMonitor && (this.fpsMonitor = new Kf(this.canvas)), this.config.randomSeed !== void 0 && this.store.addRandomSeed(this.config.randomSeed);
  }
  /**
   * Returns the current simulation progress
   */
  get progress() {
    return this.store.simulationProgress;
  }
  /**
   * A value that gives information about the running simulation status.
   */
  get isSimulationRunning() {
    return this.store.isSimulationRunning;
  }
  /**
   * The maximum point size.
   * This value is the maximum size of the `gl.POINTS` primitive that WebGL can render on the user's hardware.
   */
  get maxPointSize() {
    return this.store.maxPointSize;
  }
  /**
   * Set or update Cosmos configuration. The changes will be applied in real time.
   * @param config Cosmos configuration object.
   */
  setConfig(e) {
    var r;
    const n = { ...this.config };
    this.config.init(e), n.pointColor !== this.config.pointColor && (this.graph.updatePointColor(), this.points.updateColor()), n.pointSize !== this.config.pointSize && (this.graph.updatePointSize(), this.points.updateSize()), n.linkColor !== this.config.linkColor && (this.graph.updateLinkColor(), this.lines.updateColor()), n.linkWidth !== this.config.linkWidth && (this.graph.updateLinkWidth(), this.lines.updateWidth()), n.linkArrows !== this.config.linkArrows && (this.graph.updateArrows(), this.lines.updateArrow()), (n.curvedLinkSegments !== this.config.curvedLinkSegments || n.curvedLinks !== this.config.curvedLinks) && this.lines.updateCurveLineGeometry(), n.backgroundColor !== this.config.backgroundColor && (this.store.backgroundColor = ur(this.config.backgroundColor)), n.hoveredPointRingColor !== this.config.hoveredPointRingColor && this.store.setHoveredPointRingColor(this.config.hoveredPointRingColor), n.focusedPointRingColor !== this.config.focusedPointRingColor && this.store.setFocusedPointRingColor(this.config.focusedPointRingColor), n.focusedPointIndex !== this.config.focusedPointIndex && this.store.setFocusedPoint(this.config.focusedPointIndex), (n.spaceSize !== this.config.spaceSize || n.simulationRepulsionQuadtreeLevels !== this.config.simulationRepulsionQuadtreeLevels) && (this.store.adjustSpaceSize(this.config.spaceSize, this.reglInstance.limits.maxTextureSize), this.resizeCanvas(true), this.update(this.store.isSimulationRunning ? this.store.alpha : 0)), n.showFPSMonitor !== this.config.showFPSMonitor && (this.config.showFPSMonitor ? this.fpsMonitor = new Kf(this.canvas) : ((r = this.fpsMonitor) == null || r.destroy(), this.fpsMonitor = void 0)), n.pixelRatio !== this.config.pixelRatio && (this.store.maxPointSize = (this.reglInstance.limits.pointSizeDims[1] ?? ca) / this.config.pixelRatio), (n.disableZoom !== this.config.disableZoom || n.enableDrag !== this.config.enableDrag) && this.updateZoomDragBehaviors();
  }
  /**
   * Sets the positions for the graph points.
   *
   * @param {Float32Array} pointPositions - A Float32Array representing the positions of points in the format [x1, y1, x2, y2, ..., xn, yn],
   * where `n` is the index of the point.
   * Example: `new Float32Array([1, 2, 3, 4, 5, 6])` sets the first point to (1, 2), the second point to (3, 4), and so on.
   */
  setPointPositions(e) {
    this.graph.inputPointPositions = e, this._hasPointPositionsChanged = true;
  }
  /**
   * Sets the colors for the graph points.
   *
   * @param {Float32Array} pointColors - A Float32Array representing the colors of points in the format [r1, g1, b1, a1, r2, g2, b2, a2, ..., rn, gn, bn, an],
   * where each color is represented in RGBA format.
   * Example: `new Float32Array([255, 0, 0, 1, 0, 255, 0, 1])` sets the first point to red and the second point to green.
  */
  setPointColors(e) {
    this.graph.inputPointColors = e, this._hasPointColorsChanged = true;
  }
  /**
   * Sets the sizes for the graph points.
   *
   * @param {Float32Array} pointSizes - A Float32Array representing the sizes of points in the format [size1, size2, ..., sizen],
   * where `n` is the index of the point.
   * Example: `new Float32Array([10, 20, 30])` sets the first point to size 10, the second point to size 20, and the third point to size 30.
   */
  setPointSizes(e) {
    this.graph.inputPointSizes = e, this._hasPointSizesChanged = true;
  }
  /**
   * Sets the links for the graph.
   *
   * @param {Float32Array} links - A Float32Array representing the links between points
   * in the format [source1, target1, source2, target2, ..., sourcen, targetn],
   * where `source` and `target` are the indices of the points being linked.
   * Example: `new Float32Array([0, 1, 1, 2])` creates a link from point 0 to point 1 and another link from point 1 to point 2.
   */
  setLinks(e) {
    this.graph.inputLinks = e, this._hasLinksChanged = true;
  }
  /**
   * Sets the colors for the graph links.
   *
   * @param {Float32Array} linkColors - A Float32Array representing the colors of links in the format [r1, g1, b1, a1, r2, g2, b2, a2, ..., rn, gn, bn, an],
   * where each color is in RGBA format.
   * Example: `new Float32Array([255, 0, 0, 1, 0, 255, 0, 1])` sets the first link to red and the second link to green.
   */
  setLinkColors(e) {
    this.graph.inputLinkColors = e, this._hasLinkColorsChanged = true;
  }
  /**
   * Sets the widths for the graph links.
   *
   * @param {Float32Array} linkWidths - A Float32Array representing the widths of links in the format [width1, width2, ..., widthn],
   * where `n` is the index of the link.
   * Example: `new Float32Array([1, 2, 3])` sets the first link to width 1, the second link to width 2, and the third link to width 3.
   */
  setLinkWidths(e) {
    this.graph.inputLinkWidths = e, this._hasLinkWidthsChanged = true;
  }
  /**
   * Sets the arrows for the graph links.
   *
   * @param {boolean[]} linkArrows - An array of booleans indicating whether each link should have an arrow,
   * in the format [arrow1, arrow2, ..., arrown], where `n` is the index of the link.
   * Example: `[true, false, true]` sets arrows on the first and third links, but not on the second link.
   */
  setLinkArrows(e) {
    this.graph.linkArrowsBoolean = e, this._hasLinkArrowsChanged = true;
  }
  /**
   * Sets the strength for the graph links.
   *
   * @param {Float32Array} linkStrength - A Float32Array representing the strength of each link in the format [strength1, strength2, ..., strengthn],
   * where `n` is the index of the link.
   * Example: `new Float32Array([1, 2, 3])` sets the first link to strength 1, the second link to strength 2, and the third link to strength 3.
   */
  setLinkStrength(e) {
    this.graph.inputLinkStrength = e;
  }
  /**
   * Sets the point clusters for the graph.
   *
   * @param {(number | undefined)[]} pointClusters - Array of cluster indices for each point in the graph.
   *   - Index: Each index corresponds to a point.
   *   - Values: Integers starting from 0; `undefined` indicates that a point does not belong to any cluster and will not be affected by cluster forces.
   * @example
   *   `[0, 1, 0, 2, undefined, 1]` maps points to clusters: point 0 and 2 to cluster 0, point 1 to cluster 1, and point 3 to cluster 2.
   * Points 4 is unclustered.
   * @note Clusters without specified positions via `setClusterPositions` will be positioned at their centermass by default.
   */
  setPointClusters(e) {
    this.graph.inputPointClusters = e, this._hasPointClustersChanged = true;
  }
  /**
   * Sets the positions of the point clusters for the graph.
   *
   * @param {(number | undefined)[]} clusterPositions - Array of cluster positions.
   *   - Every two elements represent the x and y coordinates for a cluster position.
   *   - `undefined` means the cluster's position is not defined and will use centermass positioning instead.
   * @example
   *   `[10, 20, 30, 40, undefined, undefined]` places the first cluster at (10, 20) and the second at (30, 40);
   * the third cluster will be positioned at its centermass automatically.
   */
  setClusterPositions(e) {
    this.graph.inputClusterPositions = e, this._hasClusterPositionsChanged = true;
  }
  /**
   * Sets the force strength coefficients for clustering points in the graph.
   *
   * This method allows you to customize the forces acting on individual points during the clustering process.
   * The force coefficients determine the strength of the forces applied to each point.
   *
   * @param {Float32Array} clusterStrength - A Float32Array of force strength coefficients for each point in the format [coeff1, coeff2, ..., coeffn],
   * where `n` is the index of the point.
   * Example: `new Float32Array([1, 0.4, 0.3])` sets the force coefficient for point 0 to 1, point 1 to 0.4, and point 2 to 0.3.
   */
  setPointClusterStrength(e) {
    this.graph.inputClusterStrength = e, this._hasPointClusterForceChanged = true;
  }
  /**
   * Renders the graph.
   *
   * @param {number} [simulationAlpha] - Optional value between 0 and 1
   * that controls the initial energy of the simulation.The higher the value,
   * the more initial energy the simulation will get. Zero value stops the simulation.
   */
  render(e) {
    this.graph.update();
    const { fitViewOnInit: n, fitViewDelay: r, fitViewPadding: i, fitViewDuration: o, fitViewByPointsInRect: s, initialZoomLevel: f } = this.config;
    if (!this.graph.pointsNumber && !this.graph.linksNumber) {
      this.stopFrames(), Pt(this.canvas).style("cursor", null), this.reglInstance.clear({
        color: this.store.backgroundColor,
        depth: 1,
        stencil: 0
      });
      return;
    }
    this._isFirstRenderAfterInit && n && f === void 0 && (this._fitViewOnInitTimeoutID = window.setTimeout(() => {
      s ? this.setZoomTransformByPointPositions(s, o, void 0, i) : this.fitView(o, i);
    }, r)), this._isFirstRenderAfterInit = false, this.update(e);
  }
  /**
   * Center the view on a point and zoom in, by point index.
   * @param index The index of the point in the array of points.
   * @param duration Duration of the animation transition in milliseconds (`700` by default).
   * @param scale Scale value to zoom in or out (`3` by default).
   * @param canZoomOut Set to `false` to prevent zooming out from the point (`true` by default).
   */
  zoomToPointByIndex(e, n = 700, r = sv, i = true) {
    const { store: { screenSize: o } } = this, s = _n(this.reglInstance, this.points.currentPositionFbo);
    if (e === void 0) return;
    const f = s[e * 4 + 0], u = s[e * 4 + 1];
    if (f === void 0 || u === void 0) return;
    const l = this.zoomInstance.getDistanceToPoint([f, u]), p = i ? r : Math.max(this.getZoomLevel(), r);
    if (l < Math.min(o[0], o[1]))
      this.setZoomTransformByPointPositions([f, u], n, p);
    else {
      const y = this.zoomInstance.getTransform([[f, u]], p), S = this.zoomInstance.getMiddlePointTransform([f, u]);
      this.canvasD3Selection.transition().ease($p).duration(n / 2).call(this.zoomInstance.behavior.transform, S).transition().ease(jp).duration(n / 2).call(this.zoomInstance.behavior.transform, y);
    }
  }
  /**
   * Zoom the view in or out to the specified zoom level.
   * @param value Zoom level
   * @param duration Duration of the zoom in/out transition.
   */
  zoom(e, n = 0) {
    this.setZoomLevel(e, n);
  }
  /**
   * Zoom the view in or out to the specified zoom level.
   * @param value Zoom level
   * @param duration Duration of the zoom in/out transition.
   */
  setZoomLevel(e, n = 0) {
    n === 0 ? this.canvasD3Selection.call(this.zoomInstance.behavior.scaleTo, e) : this.canvasD3Selection.transition().duration(n).call(this.zoomInstance.behavior.scaleTo, e);
  }
  /**
   * Get zoom level.
   * @returns Zoom level value of the view.
   */
  getZoomLevel() {
    return this.zoomInstance.eventTransform.k;
  }
  /**
   * Get current X and Y coordinates of the points.
   * @returns Array of point positions.
   */
  getPointPositions() {
    if (this.graph.pointsNumber === void 0) return [];
    const e = [], n = _n(this.reglInstance, this.points.currentPositionFbo);
    e.length = this.graph.pointsNumber * 2;
    for (let r = 0; r < this.graph.pointsNumber; r += 1) {
      const i = n[r * 4 + 0], o = n[r * 4 + 1];
      i !== void 0 && o !== void 0 && (e[r * 2] = i, e[r * 2 + 1] = o);
    }
    return e;
  }
  /**
   * Get current X and Y coordinates of the clusters.
   * @returns Array of point cluster.
   */
  getClusterPositions() {
    if (this.graph.pointClusters === void 0 || this.clusters === void 0) return [];
    const e = [], n = _n(this.reglInstance, this.clusters.centermassFbo);
    e.length = n.length / 2;
    for (let r = 0; r < e.length / 2; r += 1) {
      const i = n[r * 4 + 0], o = n[r * 4 + 1], s = n[r * 4 + 2];
      i !== void 0 && o !== void 0 && s !== void 0 && (e[r * 2] = i / s, e[r * 2 + 1] = o / s);
    }
    return e;
  }
  /**
   * Center and zoom in/out the view to fit all points in the scene.
   * @param duration Duration of the center and zoom in/out animation in milliseconds (`250` by default).
   * @param padding Padding around the viewport in percentage (`0.1` by default).
   */
  fitView(e = 250, n = 0.1) {
    this.setZoomTransformByPointPositions(this.getPointPositions(), e, void 0, n);
  }
  /**
   * Center and zoom in/out the view to fit points by their indices in the scene.
   * @param duration Duration of the center and zoom in/out animation in milliseconds (`250` by default).
   * @param padding Padding around the viewport in percentage
   */
  fitViewByPointIndices(e, n = 250, r = 0.1) {
    const i = this.getPointPositions(), o = new Array(e.length * 2);
    for (const [s, f] of e.entries())
      o[s * 2] = i[f * 2], o[s * 2 + 1] = i[f * 2 + 1];
    this.setZoomTransformByPointPositions(o, n, void 0, r);
  }
  /**
   * Center and zoom in/out the view to fit points by their positions in the scene.
   * @param duration Duration of the center and zoom in/out animation in milliseconds (`250` by default).
   * @param padding Padding around the viewport in percentage
   */
  fitViewByPointPositions(e, n = 250, r = 0.1) {
    this.setZoomTransformByPointPositions(e, n, void 0, r);
  }
  /**
   * Get points indices inside a rectangular area.
   * @param selection - Array of two corner points `[[left, top], [right, bottom]]`.
   * The `left` and `right` coordinates should be from 0 to the width of the canvas.
   * The `top` and `bottom` coordinates should be from 0 to the height of the canvas.
   * @returns A Float32Array containing the indices of points inside a rectangular area.
   */
  getPointsInRange(e) {
    const n = this.store.screenSize[1];
    return this.store.selectedArea = [[e[0][0], n - e[1][1]], [e[1][0], n - e[0][1]]], this.points.findPointsOnAreaSelection(), _n(this.reglInstance, this.points.selectedFbo).map((i, o) => o % 4 === 0 && i !== 0 ? o / 4 : -1).filter((i) => i !== -1);
  }
  /** Select points inside a rectangular area.
   * @param selection - Array of two corner points `[[left, top], [right, bottom]]`.
   * The `left` and `right` coordinates should be from 0 to the width of the canvas.
   * The `top` and `bottom` coordinates should be from 0 to the height of the canvas. */
  selectPointsInRange(e) {
    if (e) {
      const n = this.store.screenSize[1];
      this.store.selectedArea = [[e[0][0], n - e[1][1]], [e[1][0], n - e[0][1]]], this.points.findPointsOnAreaSelection();
      const r = _n(this.reglInstance, this.points.selectedFbo);
      this.store.selectedIndices = r.map((i, o) => o % 4 === 0 && i !== 0 ? o / 4 : -1).filter((i) => i !== -1);
    } else
      this.store.selectedIndices = null;
    this.points.updateGreyoutStatus();
  }
  /**
   * Select a point by index. If you want the adjacent points to get selected too, provide `true` as the second argument.
   * @param index The index of the point in the array of points.
   * @param selectAdjacentPoints When set to `true`, selects adjacent points (`false` by default).
   */
  selectPointByIndex(e, n = false) {
    if (n) {
      const r = this.graph.getAdjacentIndices(e) ?? [];
      this.selectPointsByIndices([e, ...r]);
    } else this.selectPointsByIndices([e]);
  }
  /**
   * Select multiples points by their indices.
   * @param indices Array of points indices.
   */
  selectPointsByIndices(e) {
    e ? e.length === 0 ? this.store.selectedIndices = new Float32Array() : this.store.selectedIndices = new Float32Array(e.filter((n) => n !== void 0)) : this.store.selectedIndices = null, this.points.updateGreyoutStatus();
  }
  /**
   * Unselect all points.
   */
  unselectPoints() {
    this.store.selectedIndices = null, this.points.updateGreyoutStatus();
  }
  /**
   * Get indices of points that are currently selected.
   * @returns Array of selected indices of points.
   */
  getSelectedIndices() {
    const { selectedIndices: e } = this.store;
    return e ? Array.from(e) : null;
  }
  /**
   * Get indices that are adjacent to a specific point by its index.
   * @param index Index of the point.
   * @returns Array of adjacent indices.
   */
  getAdjacentIndices(e) {
    return this.graph.getAdjacentIndices(e);
  }
  /**
   * Set focus on a point by index. A ring will be highlighted around the focused point.
   * If no index is specified, the focus will be reset.
   * If `focusedPointIndex` is specified in the config, this method will have no effect.
   * @param index The index of the point in the array of points.
   */
  setFocusedPointByIndex(e) {
    this.config.focusedPointIndex === void 0 && (e === void 0 ? this.store.setFocusedPoint() : this.store.setFocusedPoint(e));
  }
  /**
   * Converts the X and Y point coordinates from the space coordinate system to the screen coordinate system.
   * @param spacePosition Array of x and y coordinates in the space coordinate system.
   * @returns Array of x and y coordinates in the screen coordinate system.
   */
  spaceToScreenPosition(e) {
    return this.zoomInstance.convertSpaceToScreenPosition(e);
  }
  /**
   * Converts the X and Y point coordinates from the screen coordinate system to the space coordinate system.
   * @param screenPosition Array of x and y coordinates in the screen coordinate system.
   * @returns Array of x and y coordinates in the space coordinate system.
   */
  screenToSpacePosition(e) {
    return this.zoomInstance.convertScreenToSpacePosition(e);
  }
  /**
   * Converts the point radius value from the space coordinate system to the screen coordinate system.
   * @param spaceRadius Radius of point in the space coordinate system.
   * @returns Radius of point in the screen coordinate system.
   */
  spaceToScreenRadius(e) {
    return this.zoomInstance.convertSpaceToScreenRadius(e);
  }
  /**
   * Get point radius by its index.
   * @param index Index of the point.
   * @returns Radius of the point.
   */
  getPointRadiusByIndex(e) {
    var n;
    return (n = this.graph.pointSizes) == null ? void 0 : n[e];
  }
  /**
   * Track multiple point positions by their indices on each Cosmos tick.
   * @param indices Array of points indices.
   */
  trackPointPositionsByIndices(e) {
    this.points.trackPointsByIndices(e);
  }
  /**
   * Get current X and Y coordinates of the tracked points.
   * @returns A Map object where keys are the indices of the points and values are their corresponding X and Y coordinates in the [number, number] format.
   */
  getTrackedPointPositionsMap() {
    return this.points.getTrackedPositionsMap();
  }
  /**
   * For the points that are currently visible on the screen, get a sample of point indices with their coordinates.
   * The resulting number of points will depend on the `pointSamplingDistance` configuration property,
   * and the sampled points will be evenly distributed.
   * @returns A Map object where keys are the index of the points and values are their corresponding X and Y coordinates in the [number, number] format.
   */
  getSampledPointPositionsMap() {
    return this.points.getSampledPointPositionsMap();
  }
  /**
   * Gets the X-axis of rescaling function.
   *
   * This scale is automatically created when position rescaling is enabled.
   */
  getScaleX() {
    return this.points.scaleX;
  }
  /**
   * Gets the Y-axis of rescaling function.
   *
   * This scale is automatically created when position rescaling is enabled.
   */
  getScaleY() {
    return this.points.scaleY;
  }
  /**
   * Start the simulation.
   * @param alpha Value from 0 to 1. The higher the value, the more initial energy the simulation will get.
   */
  start(e = 1) {
    var n, r;
    this.graph.pointsNumber && (this.store.isSimulationRunning = true, this.store.alpha = e, this.store.simulationProgress = 0, (r = (n = this.config).onSimulationStart) == null || r.call(n), this.stopFrames(), this.frame());
  }
  /**
   * Pause the simulation.
   */
  pause() {
    var e, n;
    this.store.isSimulationRunning = false, (n = (e = this.config).onSimulationPause) == null || n.call(e);
  }
  /**
   * Restart the simulation.
   */
  restart() {
    var e, n;
    this.store.isSimulationRunning = true, (n = (e = this.config).onSimulationRestart) == null || n.call(e);
  }
  /**
   * Render only one frame of the simulation (stops the simulation if it was running).
   */
  step() {
    this.store.isSimulationRunning = false, this.stopFrames(), this.frame();
  }
  /**
   * Destroy this Cosmos instance.
   */
  destroy() {
    var e, n;
    window.clearTimeout(this._fitViewOnInitTimeoutID), this.stopFrames(), this.reglInstance.destroy(), this.reglInstance.clear({
      color: this.store.backgroundColor,
      depth: 1,
      stencil: 0
    }), Pt(this.canvas).style("cursor", null), (e = this.fpsMonitor) == null || e.destroy(), (n = document.getElementById("gl-bench-style")) == null || n.remove();
  }
  /**
   * Create new Cosmos instance.
   */
  create() {
    var e, n, r, i, o;
    this._hasPointPositionsChanged && this.points.updatePositions(), this._hasPointColorsChanged && this.points.updateColor(), this._hasPointSizesChanged && this.points.updateSize(), (this._hasLinksChanged || this._hasPointPositionsChanged) && this.lines.updatePointsBuffer(), this._hasLinkColorsChanged && this.lines.updateColor(), this._hasLinkWidthsChanged && this.lines.updateWidth(), this._hasLinkArrowsChanged && this.lines.updateArrow(), this.lines.updateCurveLineGeometry(), (e = this.forceManyBody) == null || e.create(), (n = this.forceLinkIncoming) == null || n.create(na.INCOMING), (r = this.forceLinkOutgoing) == null || r.create(na.OUTGOING), (i = this.forceCenter) == null || i.create(), (this._hasPointClustersChanged || this._hasClusterPositionsChanged || this._hasPointClusterForceChanged) && ((o = this.clusters) == null || o.create()), this._hasPointPositionsChanged = false, this._hasPointColorsChanged = false, this._hasPointSizesChanged = false, this._hasLinksChanged = false, this._hasLinkColorsChanged = false, this._hasLinkWidthsChanged = false, this._hasLinkArrowsChanged = false, this._hasPointClustersChanged = false, this._hasClusterPositionsChanged = false, this._hasPointClusterForceChanged = false;
  }
  /**
   * Converts an array of tuple positions to a single array containing all coordinates sequentially
   * @param pointPositions An array of tuple positions
   * @returns A flatten array of coordinates
   */
  flatten(e) {
    return e.flat();
  }
  /**
   * Converts a flat array of point positions to a tuple pairs representing coordinates
   * @param pointPositions A flattened array of coordinates
   * @returns An array of tuple positions
   */
  pair(e) {
    const n = new Array(e.length / 2);
    for (let r = 0; r < e.length / 2; r++)
      n[r] = [e[r * 2], e[r * 2 + 1]];
    return n;
  }
  update(e = this.store.alpha) {
    const { graph: n } = this;
    this.store.pointsTextureSize = Math.ceil(Math.sqrt(n.pointsNumber ?? 0)), this.store.linksTextureSize = Math.ceil(Math.sqrt((n.linksNumber ?? 0) * 2)), this.create(), this.initPrograms(), this.store.setFocusedPoint(this.config.focusedPointIndex), this.store.hoveredPoint = void 0, this.start(e);
  }
  initPrograms() {
    var e, n, r, i, o, s, f;
    this.points.initPrograms(), this.lines.initPrograms(), (e = this.forceGravity) == null || e.initPrograms(), (n = this.forceLinkIncoming) == null || n.initPrograms(), (r = this.forceLinkOutgoing) == null || r.initPrograms(), (i = this.forceMouse) == null || i.initPrograms(), (o = this.forceManyBody) == null || o.initPrograms(), (s = this.forceCenter) == null || s.initPrograms(), (f = this.clusters) == null || f.initPrograms();
  }
  frame() {
    const { config: { simulationGravity: e, simulationCenter: n, renderLinks: r, disableSimulation: i }, store: { alpha: o, isSimulationRunning: s } } = this;
    o < ua && s && this.end(), this.store.pointsTextureSize && (this.requestAnimationFrameId = window.requestAnimationFrame((f) => {
      var u, l, p, y, S, D, Z, we, Se, ae, Ne, Ue, q;
      (u = this.fpsMonitor) == null || u.begin(), this.resizeCanvas(), this.dragInstance.isActive || this.findHoveredPoint(), i || (this.isRightClickMouse && (s || this.start(0.1), (l = this.forceMouse) == null || l.run(), this.points.updatePosition()), s && !(this.zoomInstance.isRunning && !this.config.enableSimulationDuringZoom) && (e && ((p = this.forceGravity) == null || p.run(), this.points.updatePosition()), n && ((y = this.forceCenter) == null || y.run(), this.points.updatePosition()), (S = this.forceManyBody) == null || S.run(), this.points.updatePosition(), this.store.linksTextureSize && ((D = this.forceLinkIncoming) == null || D.run(), this.points.updatePosition(), (Z = this.forceLinkOutgoing) == null || Z.run(), this.points.updatePosition()), (this.graph.pointClusters || this.graph.clusterPositions) && ((we = this.clusters) == null || we.run(), this.points.updatePosition()), this.store.alpha += this.store.addAlpha(this.config.simulationDecay ?? je.simulation.decay), this.isRightClickMouse && (this.store.alpha = Math.max(this.store.alpha, 0.1)), this.store.simulationProgress = Math.sqrt(Math.min(1, ua / this.store.alpha)), (Ue = (Ne = this.config).onSimulationTick) == null || Ue.call(
        Ne,
        this.store.alpha,
        (Se = this.store.hoveredPoint) == null ? void 0 : Se.index,
        (ae = this.store.hoveredPoint) == null ? void 0 : ae.position
      )), this.points.trackPoints()), this.reglInstance.clear({
        color: this.store.backgroundColor,
        depth: 1,
        stencil: 0
      }), r && this.store.linksTextureSize && this.lines.draw(), this.points.draw(), this.dragInstance.isActive && (this.points.drag(), this.points.drag()), (q = this.fpsMonitor) == null || q.end(f), this.currentEvent = void 0, this.frame();
    }));
  }
  stopFrames() {
    this.requestAnimationFrameId && window.cancelAnimationFrame(this.requestAnimationFrameId);
  }
  end() {
    var e, n;
    this.store.isSimulationRunning = false, this.store.simulationProgress = 1, (n = (e = this.config).onSimulationEnd) == null || n.call(e);
  }
  onClick(e) {
    var n, r, i, o;
    (o = (i = this.config).onClick) == null || o.call(
      i,
      (n = this.store.hoveredPoint) == null ? void 0 : n.index,
      (r = this.store.hoveredPoint) == null ? void 0 : r.position,
      e
    );
  }
  updateMousePosition(e) {
    if (!e) return;
    const n = e.offsetX ?? e.x, r = e.offsetY ?? e.y;
    n === void 0 || r === void 0 || (this.store.mousePosition = this.zoomInstance.convertScreenToSpacePosition([n, r]), this.store.screenMousePosition = [n, this.store.screenSize[1] - r]);
  }
  onMouseMove(e) {
    var n, r, i, o;
    this.currentEvent = e, this.updateMousePosition(e), this.isRightClickMouse = e.which === 3, (o = (i = this.config).onMouseMove) == null || o.call(
      i,
      (n = this.store.hoveredPoint) == null ? void 0 : n.index,
      (r = this.store.hoveredPoint) == null ? void 0 : r.position,
      this.currentEvent
    );
  }
  onRightClickMouse(e) {
    e.preventDefault();
  }
  resizeCanvas(e = false) {
    const n = this.canvas.width, r = this.canvas.height, i = this.canvas.clientWidth, o = this.canvas.clientHeight;
    if (e || n !== i * this.config.pixelRatio || r !== o * this.config.pixelRatio) {
      const [s, f] = this.store.screenSize, { k: u } = this.zoomInstance.eventTransform, l = this.zoomInstance.convertScreenToSpacePosition([s / 2, f / 2]);
      this.store.updateScreenSize(i, o), this.canvas.width = i * this.config.pixelRatio, this.canvas.height = o * this.config.pixelRatio, this.reglInstance.poll(), this.canvasD3Selection.call(this.zoomInstance.behavior.transform, this.zoomInstance.getTransform([l], u)), this.points.updateSampledPointsGrid();
    }
  }
  setZoomTransformByPointPositions(e, n = 250, r, i) {
    this.resizeCanvas();
    const o = this.zoomInstance.getTransform(this.pair(e), r, i);
    this.canvasD3Selection.transition().ease(Up).duration(n).call(this.zoomInstance.behavior.transform, o);
  }
  updateZoomDragBehaviors() {
    this.config.enableDrag ? this.canvasD3Selection.call(this.dragInstance.behavior) : this.canvasD3Selection.call(this.dragInstance.behavior).on(".drag", null), this.config.disableZoom ? this.canvasD3Selection.call(this.zoomInstance.behavior).on("wheel.zoom", null) : this.canvasD3Selection.call(this.zoomInstance.behavior);
  }
  findHoveredPoint() {
    var o, s, f, u, l;
    if (!this._isMouseOnCanvas) return;
    if (this._findHoveredPointExecutionCount < 2) {
      this._findHoveredPointExecutionCount += 1;
      return;
    }
    this._findHoveredPointExecutionCount = 0, this.points.findHoveredPoint();
    let e = false, n = false;
    const r = _n(this.reglInstance, this.points.hoveredFbo);
    if (r[1]) {
      const p = r[0];
      ((o = this.store.hoveredPoint) == null ? void 0 : o.index) !== p && (e = true);
      const y = r[2], S = r[3];
      this.store.hoveredPoint = {
        index: p,
        position: [y, S]
      };
    } else
      this.store.hoveredPoint && (n = true), this.store.hoveredPoint = void 0;
    e && this.store.hoveredPoint && ((f = (s = this.config).onPointMouseOver) == null || f.call(
      s,
      this.store.hoveredPoint.index,
      this.store.hoveredPoint.position,
      this.currentEvent
    )), n && ((l = (u = this.config).onPointMouseOut) == null || l.call(u, this.currentEvent)), this.updateCanvasCursor();
  }
  updateCanvasCursor() {
    const { hoveredPointCursor: e } = this.config;
    this.dragInstance.isActive ? Pt(this.canvas).style("cursor", "grabbing") : this.store.hoveredPoint ? !this.config.enableDrag || this.store.isSpaceKeyPressed ? Pt(this.canvas).style("cursor", e) : Pt(this.canvas).style("cursor", "grab") : Pt(this.canvas).style("cursor", null);
  }
  addAttribution() {
    var r;
    if (this.config.disableAttribution) return;
    this.attributionDivElement = document.createElement("div"), this.attributionDivElement.style.cssText = `
      cursor: pointer;
      user-select: none;
      position: absolute;
      margin: 0 0.6rem 0.6rem 0;
      line-height: 0;
      bottom: 0;
      right: 0;
      color: var(--cosmos-attribution-color);
    `, this.attributionDivElement.onclick = () => {
      var i;
      return (i = window.open("https://cosmograph.app/", "_blank")) == null ? void 0 : i.focus();
    };
    const n = new DOMParser().parseFromString(z1, "image/svg+xml").firstChild;
    this.attributionDivElement.appendChild(n), (r = this.store.div) == null || r.appendChild(this.attributionDivElement);
  }
};
export {
  L1 as Graph,
  hv as clamp,
  ur as getRgbaColor,
  cv as isAClassInstance,
  Nu as isArray,
  fv as isFunction,
  en as isNumber,
  uv as isObject,
  Zf as isPlainObject,
  _n as readPixels,
  lv as rgbToBrightness
};
//# sourceMappingURL=@cosmograph_cosmos.js.map
