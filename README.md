# Project Setup
-------------

Run with Docker:
After cloning the repository, simply run the following command from the root directory:

`docker compose up`

Access the Application:
The app will be exposed on port 30306.
Open your browser and go to:

`http://localhost:30306/`
